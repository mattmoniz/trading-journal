# Known Issues / Technical Debt

Living tracker. Originally compiled 2026-06-07 (see archived `docs/ARCHITECTURE_2026-06-07_superseded.md`), re-verified 2026-06-30 against current code. **Convention changed 2026-07-14**: when you fix an item, strike it through (`~~like this~~`) and add a short "Fixed <date>" note rather than deleting it — keep it in place as a record, don't move it to a separate section. The original "delete when fixed" rule (present since this file's creation) turned out to be an AI-invented default from whichever session first built this file, not an explicit user preference — confirmed by checking git history, which showed no user instruction behind it.

0. ~~Wrong $/pt ($5 instead of real $2 MNQ) used throughout every `resolution_method='BACKFILL'` `active_setups` row~~ — **Fixed 2026-07-14.** `scripts/archive/backfill_level_fades.js` used `PT=5, COMM=5`, propagated into every `scripts/repair_*.mjs` script built earlier the same day — but live `server/routes/acd.js` (~line 155) is explicit: `const PNL_PER_POINT = 2; // MNQ = $2/point`, the real contract this journal trades. $5/pt matched neither MNQ ($2) nor standard NQ ($20) — an uncaught error from the start, confirmed with the user before fixing ("not sure why you picked $5"). Overstated every `BACKFILL`-sourced setup_type's dollar-EV by ~2.5x (sign unaffected, magnitude wrong) — found while investigating why `update_optimal_stops.mjs`'s `size_multiplier`/DPP mining showed a clean bimodal $5-vs-$2 split instead of the expected single value. Fixed via `scripts/repair_dollars_per_point.mjs` (backup: `active_setups_pnl_rescale_backup_20260714`) — rescaled `actual_pnl` directly from each row's existing point distances, no re-detection needed. All 6 `repair_*.mjs` scripts corrected at the source too. Recalibration produced 14 more real `SETUP_STATUS` flips on top of the 32 already documented — full account in docs/OPEN_THREADS.md's correction notice at the top of "Pending decisions."

1. **`App.jsx` monolith** — now ~23,200 lines (grew from ~19,000 on 2026-06-07). Single-file React component holding most dashboard/panel logic inline rather than in `src/components/`. Makes the file hard to navigate and impossible to unit test in isolation.

2. **Backup files committed to the tree**: `server/index.js.bak` and `server/index.js.backup` sit alongside the live `server/index.js` — clutter, and a real risk someone edits the wrong one. Safe to delete (git history preserves prior versions) unless there's a reason they're kept outside git tracking.

3. **Three day-type classifiers exist independently** and can disagree:
   - `caseEngine.js` → `classifyDayType` (the one tracked for accuracy via `daytype_accuracy_log`)
   - `auctionRead.js` → `dayTypeDeveloping` (separate intraday heuristic)
   - `acd.js` → an implicit OR-range-based inference embedded in route logic
   Only the `caseEngine.js` version is currently measured for accuracy. If you touch day-type logic, check which of the three you're actually changing.

4. **DTC prototype not integrated**: `scripts/dtc_phase0_test.cjs` demonstrates a direct DTC-protocol connection to Sierra Chart but is a standalone test script, not wired into the running ingestion pipeline.

5. **Dormant feature tables** (route/service code exists, table holds 0 rows because the feature has never been exercised — see [ARCHITECTURE.md](../ARCHITECTURE.md)): `phase_change_alerts`, `wyckoff_levels`, `trade_screenshots`, `trading_sessions`, `premarket_walkthroughs`. Decide to either use or remove; don't let new code assume they're populated.

6. **`caseEngine.js`'s overnight-range query (`onQ`, ~line 886) may leak lookahead when run for historical/past dates.** It filters `ts::date = $1 AND (hour>=18 OR hour<9)` — a *single*-date equality, not `$1::date - 1` for the evening leg like the equivalent (correct) 24hr-VWAP query in `morningBrief.js` does. In live/intraday use this accidentally "works" because tonight's evening bars for `$1` don't exist yet when the case is built each morning. But if `computeCase` is ever called for a past date with a full day of bars already in the DB (replay, backfill, or historical case re-derivation), `hour>=18` would pull tonight's (i.e. *after* tradeDate's own session close) bars into "overnight," not last night's. Found 2026-06-30 while wiring `OVERNIGHT_HIGH/LOW` into `phaseChangeDetector.js` (which uses the correct `priorDate`-qualified version) — not fixed yet because it needs confirmation of whether `computeCase` is ever actually invoked in a historical-replay context before changing live-session behavior.

## Re-verified fixed / no longer applicable (removed from list 2026-06-30)
- "Empty/unused tables" list — `auction_reads` is now actively populated (395 rows, used by 11 files) — no longer dormant. The remaining dormant tables are listed in item 6 above.

8. **CORRECTION 2026-08-09: "SierraWatcher dead code, never `.start()`'d" was WRONGLY marked
   fixed above on 2026-06-30 — it is still genuinely dead code today.** The 2026-06-30 note
   only verified that `sierraWatcher` is instantiated (`server/index.js` ~line 302, `const
   sierraWatcher = new SierraWatcher(SIERRA_DATA_DIR);`) and passed into
   `createSierraRouter(io, sierraWatcher)` — a superficial "appears wired" check, not a check
   that `.start()` is actually called anywhere. It is not: grepped `server/index.js` and
   `server/routes/sierra.js` in full, zero matches for `sierraWatcher.start(` or any call to
   `.start()` on this instance. `createSierraRouter` only uses the injected instance for a
   read-only `/api/sierra/watcher/status`-style `.getStatus()` call — it never starts the
   watcher either. Found while investigating a 2026-08-09 process-health incident (the
   managed server was down for 4 days undetected, see `docs/DECISIONS_LOG.md`), checking
   whether this gap put the trade-import pipeline at risk. **It doesn't, practically** — Trade
   Activity Log import runs via a separate, real, currently-working mechanism: the
   `AUTO_IMPORT_4PM` cron (`server/index.js`, `cron.schedule('0 16 * * 1-5', ...)`, comment:
   "replaces setInterval below") calls `manualImportFromFile()` →
   `tradeImportService.js`'s `importSierraTrades()` once daily at 4PM ET — the exact same
   function `SierraWatcher._scan()` would have called continuously, had it ever started. So
   the `trades` table (personal broker activity log) updates once/day via cron, not
   continuously via a live file watcher, but it does update — this is a real, narrower gap
   (no near-real-time trade visibility intraday) than "trade import is broken," and not
   related to `active_setups`/live setup-detection at all. **Separately confirmed market-data
   (price bar) ingestion is NOT affected by this gap and has no DTC dependency** — it runs via
   two independent `setInterval` file-scanners (`server/index.js`'s unconditional 60s poll,
   `server/routes/priceBars.js`'s RTH-gated 60s poll), both calling
   `priceBarService.js`'s `scanAndIngestNewBarFiles()` against Sierra Chart's exported
   `*-BarData.txt` files — confirmed running normally via a live `./start.sh` test the same
   session ("📊 Ingesting price bars: NQU6.CME-BarData.txt" in the real startup log). There is
   no live DTC-protocol connection anywhere in the running app — `scripts/archive/dtc_phase0_test.cjs`
   remains exactly what item 4 above already says: a standalone prototype, never integrated.
   **General lesson, worth restating even though this exact lesson was already learned once
   this session in a different context**: "instantiated and passed to a router" is not the
   same claim as "started" — a note marking something "re-verified fixed" needs to show the
   actual call that proves it, not just that the object exists somewhere in scope.
- 6 confirmed-dead tables (`price_bars_old`, `trades_backup_tz_fix`, `calibration_snapshots`, `session_volume_summary`, `sot_signals`, `intraday_snapshots`) were dropped 2026-06-30 — see [ARCHITECTURE.md](../ARCHITECTURE.md).

7. **`ACDSessionState` in `App.jsx` (~line 10875) is dead code** — defined with props `{ todayData, nl, pivot }` but never rendered anywhere in the app. The correct vehicle for ACD-related pre-session UI is `SessionForecastPanel.jsx` and the morning prep view. Do not edit `ACDSessionState` expecting users to see the result.


8. **Root cause of duplicate sub-minute `price_bars` ingestion not found yet.** Fixed 2026-07-13 at the read layer (`price_bars_primary` now `GROUP BY`s to guaranteed one-row-per-minute — see [ARCHITECTURE.md](../ARCHITECTURE.md)), but the underlying `price_bars` table itself still has ~14.6% of RTH minutes with 2+ sub-minute-resolution rows (up to 25 in one minute, worst cluster Jun–Aug 2025) — looks like a Sierra Chart export/import mixing in a different (possibly volume-based) bar type under the same symbol/contract on certain days, not yet root-caused. The view fix compensates correctly for all current and future reads, but new imports could keep adding more sub-minute rows to the raw table indefinitely unless the ingestion path (`priceBarService.js`) is fixed or a `CHECK`/trigger rejects sub-minute writes. **2026-07-14: found this also silently contaminated the historical `active_setups` corpus** — `scripts/archive/backfill_level_fades.js` (the source of ~3,764 `resolution_method='BACKFILL'` rows across 72 setup_types, spanning 2023-11-16 to present) queried raw `price_bars` directly, not `price_bars_primary`, so it was written *before* the view fix existed and never re-run after. Repaired via `scripts/repair_backfill_duplicate_bars.mjs` (backs up to `active_setups_backfill_backup_20260714`, re-simulates against clean data) — see docs/OPEN_THREADS.md for the before/after numbers and the 13 live ACTIVE→SUPPRESS recommendation flips this produced.

9. ~~Historical level-fade backfill's detection window (10:30am–noon ET) doesn't match live firing behavior~~ — **Resolved 2026-07-14, all 34 base level families.** `scripts/archive/backfill_level_fades.js` only scanned for a level touch in a narrow 10:30–noon window, but the live level-fade engine (`server/routes/acd.js` ~line 4757, `nearLevels`) fires on the first RTH touch, whenever that happens (with the correct per-level formation gate — not blanket 9:30, that would be lookahead for same-day-forming levels). Fixed in 5 waves (`scripts/repair_cam_r4_s3_window_mismatch.mjs`, `repair_top8_window_mismatch.mjs`, `repair_remaining_window_mismatch.mjs`, `repair_ib_dependent_window_mismatch.mjs`, `repair_weekly_vwap_window_mismatch.mjs`) covering every prior-period-derived level family, OR_HIGH/OR_LOW (gate 9:35 ET), IB_HIGH/IB_LOW/IB_MID/OR_MID (gate 10:30 ET, after fixing the IB definitional bug below), and WEEKLY_VWAP (gate 9:30 ET, after fixing its own lookahead bug — see item 10). Produced 32 real `performance_audit` recommendation flips session-wide — full list in docs/OPEN_THREADS.md. **`CAM_R4_FADE_SHORT` flipped from a claimed +$84.56 EV top-4 setup to a confirmed -$34.87 EV structural loser**; `IB_HIGH_FADE_LONG` collapsed from N=81 to N=10 once the window-definition artifact was removed.

  **Also found and fixed mid-fix: `IB_HIGH`/`IB_LOW` had a *definitional* mismatch, not just a window one** — live `acd.js` computed a 60-minute Initial Balance (bars 570-629, gated `etMinNow>=630`), but `scripts/compute_levels.js` (source of `level_prices`, what historical backfills read) computed a 30-minute one (bars 570-599) — genuinely different price levels. Git history showed the 60-minute definition in `acd.js` predates `compute_levels.js` by a full month (2026-06-01 vs 2026-07-01) and matches this codebase's documented day-type-classifier convention ("Initial Balance closes" at 10:30 ET, `docs/daytype_classifier_v2_candidate.md`) — strong evidence the 30-minute version was the bug. User confirmed standardizing on 60-minute before it was applied. Fixed at the source (`scripts/compute_levels.js`'s `IB_HIGH`/`IB_LOW`/`IB_MID` and `PD_IB_HIGH`/`PD_IB_LOW`/`PD_IB_MID` blocks now both use bars 570-629), full historical `level_prices` re-backfilled (`node scripts/compute_levels.js --backfill`, backed up first to `level_prices_ib_backup_20260714`).

  **Found mid-fix: `node scripts/compute_levels.js --backfill` silently skips dates under concurrent DB load, with no error surfaced in the truncated console output.** Both full-backfill runs today (IB fix, WEEKLY_VWAP fix) left ~40 dates untouched — a per-date `try/catch` in the backfill loop swallows the failure and logs to `console.error`, which never appeared in the piped/tailed output used to monitor the run. Discovered by spot-checking a supposedly-fixed value against a raw SQL recomputation and finding it still matched the *old* buggy formula. Diagnosed via `level_prices.computed_at` staleness (any row not touched on run day was suspect), fixed by re-running `compute_levels.js <date>` individually for each stale date (all succeeded standalone — the failures were concurrency-related, not a real per-date bug). **Lesson for future backfills**: never trust a truncated/piped console summary as proof of completeness — verify with a `computed_at` freshness check (or equivalent) across the full expected date range before treating a `--backfill` run as done, especially when other processes (the live server's 60s poller, concurrent scripts) are hitting the same DB.

10. ~~`WEEKLY_VWAP`'s stored `level_prices` value has a lookahead bug in its own formula~~ — **Fixed 2026-07-14.** `scripts/compute_levels.js` used `WHERE ts::date BETWEEN $1 AND $2` with `$2 = wb.fri` (that week's Friday) instead of the date being computed — so `level_prices.WEEKLY_VWAP` for e.g. a Monday reflected volume-weighted price data through that week's Friday close, which hadn't happened yet relative to Monday. Independently corroborated by Gemini (`scratch/antigravity_response.md`). Fixed by changing the query's end bound to `date` (now a legitimate week-to-date VWAP as of each date); full historical `level_prices` re-backfilled (backed up to `level_prices_weeklyvwap_backup_20260714`). Then re-ran the window-mismatch fix (item 9) for `WEEKLY_VWAP_FADE_LONG`/`SHORT` via `scripts/repair_weekly_vwap_window_mismatch.mjs` — both correctly resolve to `SUPPRESS` (N=152/137, EV=-$38.69/-$21.34).

  **Found and self-healed along the way, cause not fully identified: all `WEEKLY_VWAP_FADE_LONG`/`SHORT` rows (98 total) had silently vanished from `active_setups` sometime between the earlier duplicate-bar fix (item 8, which produced them with N=55/43) and this fix — confirmed isolated to just this one setup_type (spot-checked 4 other recently-suppressed setup_types, all had their expected row counts intact) and confirmed NOT caused by `expireStaleSetups()`'s SHADOW-row cleanup (that only deletes `status='SHADOW'` rows; every backfill-sourced row is inserted already-`RESOLVED`/`EXPIRED`, never `SHADOW`).** Self-healed by this fix's own full backup+delete+reinsert cycle (which regenerates from clean source data regardless of prior state), so no data was permanently lost, but the root cause of the disappearance is unresolved — worth a quick check if any *other* setup_type's row count looks unexpectedly low in a future session.

11. ~~`AlphaEngineOverview.jsx`'s `suppressed` array (~line 465) is a hand-maintained, hardcoded list of WR%/N/$ narrative strings~~ — **Fixed 2026-07-15.** Was not backed by live `performance_audit` data — e.g. claimed `IB_HIGH_FADE_SHORT` suppressed at "55.7% WR" (it's `ACTIVE` now) and `PD_POC_FADE_SHORT` suppressed at "52.9% WR" (it's `PROMOTE` now), among ~10 other drifted individual-setup entries. Fixed same pattern as the 2026-07-13 `describeLevel()` fix for `acd.js`'s `levelMap.bestCtx`: removed every hand-typed per-setup_type WR/N/$ literal and replaced with a live render of `setupStatus.suppressed` (`/api/acd/setup-status`, already fetched by the component — no new endpoint needed). Kept only the handful of entries that describe a suppression *mechanism* rather than a specific setup_type's stat (pipeline description, WPP conditional-variant mechanism, MOMENTUM_60m_60m family methodology) — none of those hand-type a drifting number. Also de-duplicated the "Auto-Suppression Engine" tool card, which was independently rendering the same live list a second time on the same page — now just a compact summary line pointing at the one live list.

12. **Recurring ~61-day NQ data gaps at every quarterly contract rollover — confirmed a genuine raw-data source limitation, not a code bug.** `data_sanity_audit.mjs` flags 3 gaps (2024-09-20→2024-11-20, 2024-12-20→2025-02-19, 2025-03-21→2025-05-21), each with *zero* bars in `price_bars` for any contract. Root-caused 2026-07-27: `price_bar_ingests` shows every quarterly-contract-specific Sierra Chart export file (`NQZ4`, `NQH5`, `NQM5`, `NQZ5`, `NQM6`, and others going back to `NQZ3`/`NQM4`) recorded `bars_inserted=0` — these source files were essentially empty when provided. The wider continuous-range files (`NQU4.dly`, `NQU4.scid`, etc.) only cover through whenever that particular export happened to be run, not through the next rollover. Confirmed `price_bars_contract_calendar` (computed dynamically from actual bar counts per date, `priceBarService.js`'s ingest-time upsert) has **zero rows at all** — for either the outgoing or incoming contract — during each gap window, proving the calendar isn't miscomputing anything; it correctly has nothing to compute from. **Not fixable in code** — would need the user to locate and provide proper historical NQ export files covering these specific ~2-month windows from whichever Sierra Chart source they use. Any trend/move/regime detector that scans across one of these windows will silently bridge the void and misread it as continuous price action — worth remembering when a finding spans one of these 3 date ranges. See `OPEN_DECISION` `price_bars_recurring_2month_data_gaps_pattern` (resolved).

13. **`price_bars_primary` (NQ) has only ONE bar per calendar day — not real intraday density — from 2022-12-14 through 2023-11-14, immediately before the already-documented ES-symbol-contamination window (item 12's neighbor, 2023-11-15 to 2023-12-15).** Found 2026-08-11 auditing an ATR-compression-breakout pilot: a suspicious identical sample bar appeared at the same timestamp across two differently-aggregated bar widths (15-min vs 30-min), which shouldn't happen — traced to a direct per-day `COUNT(*)` showing avg 1.0 bars/day every month Jan–Oct 2023, jumping to ~630 bars/day starting Nov 2023. Real usable NQ intraday history is therefore ~2.5 years (starting 2023-12-16, right after the ES-contamination window), not the ~3.7 years the raw `MIN(ts)`/`MAX(ts)` range implies — any script that queries `price_bars_primary` from an early-2023-or-before start date without checking density will silently treat single daily snapshots as sequential intraday bars, corrupting any rolling-window computation (ATR/TR, rolling range, etc.) that spans the transition, and skewing where any bar-count-based warmup/lookback period actually lands in real time. Confirmed this actually happened: an ATR(20)/ATR(30) warmup and the first ~230 bars of a 60-min backtest were built on this degenerate stretch, materially changing results once excluded (`scripts/pilot_atr_compression_breakout.mjs`/`_mtf.mjs`, `atr_compression_breakout_60m` `RESEARCH_CLAIM`, corrected same session). **Not fixed at the source** (no code bug — this reflects a real gap in the underlying Sierra Chart export data for that period, same category as item 12's rollover gaps) — the fix is per-consumer: any new script reading `price_bars_primary` with an early start date should check `GROUP BY ts::date, COUNT(*)` for the intended range first, or default to `ts >= '2023-12-16'` unless it specifically needs to reason about the sparse pre-Nov-2023 daily-only period. `data_sanity_audit.mjs`'s standing checks do not currently catch this (they check for outlier bar VALUES, not density/gaps in bar coverage) — worth extending in a future session, not done here.

## Not re-verified (carried over unconfirmed — check before trusting)
- "Duplicate pattern-memory trigger" (cron + parallel `setInterval` both calling `runNightlyUpdate`)
- "Silent error handling" (`catch (e) {}` blocks swallowing errors without logging) throughout `index.js`
