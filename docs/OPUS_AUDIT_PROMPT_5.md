# AUTONOMOUS — OPUS STRATEGIC AUDIT (AUDIT #5): does this system's overall architecture actually serve its stated goal?

You are Claude Opus 4.8. Audits 1-4 each answered a narrow question (code/bug forensics, sizeMultiplier/objective alignment, closed-loop learning, one order-flow-vs-price-action research pattern). **This audit is broader and more foundational, triggered by a direct user question after a long research session left them feeling lost**: "Is the system designed properly to get what it's trying to achieve overall?" Don't answer this in the abstract — answer it against the system's OWN stated goal, using real current-state numbers, not assumptions carried over from prior audits (a lot has changed since Audit #2, three weeks ago).

Deliverable: `scratch/opus_audit_5_results.md` — structured findings + a small number (2-4) of concrete, prioritized recommendations for what to actually do next. Same format as prior audits: no code for immediate paste, action items specific enough Sonnet can execute cold with exact file/line references.

## The system's actual stated goal — read this first, hold everything else against it

This is NOT "build the smartest possible trading signal." Per the user's own repeatedly-stated framing (see `docs/` project memory and `CLAUDE.md`'s collaboration section): the point of this whole system is **unemotional, disciplined execution** — the user already has real edge from waiting for setups they genuinely trust; the system's job is to make "strong" vs. "weak" setups trustworthy enough that the user can act on that judgment without hesitation, and the success metric is **asymmetric payoff** (contained losses, occasional large winners), not win rate, not the number of signals built, not research elegance. "Just wait, no clever filter fires" is explicitly framed as a WIN for this system, not a shortcoming. Hold every finding below against this: does it make the user's real-time decisions MORE trustworthy and actionable, or does it add research volume without changing what the user actually sees and does at the moment of decision?

## What triggered this audit — read this, it's the concrete texture the abstract question needs

A single session today (2026-07-28) ran three sequential investigations that each surfaced something real, but by the third one the user said, verbatim: *"Im at a loss with this daytype idea now. You just went pff jnto several directions and im loss."* In order:

1. **`SETUP_STATUS_DOW` rigor audit**: the per-day-of-week suppression pass had never had a chronological-stability/day-clustering check applied to it (unlike its sibling main-suppression check in the same file) — of 39 then-live DOW suppressions, only 10 (26%) were actually statistically real; 29 were noise from ~180 `(setup_type × weekday)` cells tested weekly with zero multiple-comparison correction. Fixed live, narrowed to 10.
2. **Re-fire gate testing** (`IB_BEARISH` fires unlimited times/day with no cooldown — 16 real fires in one session): tested volume-z-score gating vs. a blind time-cooldown control across `IB_BEARISH`/`IB_BULLISH` then 21 level-fade setup_types. Two REAL BUGS caught mid-thread (an uncorrected `EXPIRED`-position mark-to-market, then a Map-key type-mismatch that silently disabled the entire volume-gate mechanism for 21 setup_types without erroring) — both reversed the reported conclusion once fixed. Final honest result: no universal mechanism, effect is setup-specific, only a handful of setup_types show a real, validated improvement.
3. **Rolling-trade-count dynamic health gate** (user's own idea: shadow a setup when a rolling win-rate/EV window "cools off," reactivate when it "warms back up"): tested with real hysteresis (data-derived percentile thresholds, not a flat cutoff) and a proper chronological calibration/test split. 5 of 21 setup_types showed a clean improvement — but their "cooling" periods overlapped heavily in calendar time with an elevated `TURBULENT` day-type rate, suggesting the mechanism was rediscovering an already-known, already-handled market-regime effect rather than detecting genuine per-setup fatigue. Not validated, not built.

Then, stepping back to the actual live state (found by direct query, not assumption): **of 182 tracked `setup_types` in `SETUP_STATUS`, only 5 are currently `ACTIVE`/`DAY_TYPE_MANAGED`** (`IB_BEARISH` +$4.17 N=144, `IB_BULLISH` -$25.50 N=119, `MOMENTUM_60m_60m_BALANCE_FADE` -$4.00 N=2053, `MOMENTUM_60m_60m_TREND` +$12.57 N=713, `WPP_FADE_SHORT_GAP_UP` +$11.21 N=28). The user's own observation was that live-firing trades feel "repetitive, same few." **A live, apparently-still-open bug was found in the same conversation**: `IB_BULLISH` stays live specifically because its `TREND`-day bucket shows +$16.69 EV (N=32) — but every one of those 32 trades is `origin_status='UNKNOWN'` (unrecoverable pre-tracking data, zero confirmed-real, most recent one 3 weeks stale) with a confirmed rigor-stable **-$25.50 blended EV** otherwise. This is the exact "day-type-conditional branch never got the real-N floor the main check got" gap already flagged in `CLAUDE.md` on 2026-07-20 and left explicitly unfixed — confirm whether it's still unfixed as of when you run this (check git log / the live code), since it may get fixed in this same session before or during your audit.

## Questions this audit must answer

### 1. Is the "5 of 182 live" ratio a sign of a properly conservative, well-calibrated pipeline, or a sign that the pipeline's inputs (backfill contamination, thin real-N, day-type bucket carve-outs bypassing real-N floors) are systematically starving legitimate setups of the chance to prove themselves?

Query `active_setups` directly: what fraction of ALL rows (not just `IB_BULLISH`) are `origin_status IN ('BACKFILL','UNKNOWN')` vs `('ACTIVE','SHADOW')`? Given real forward data is this thin (302 total real resolved trades across the WHOLE table per a direct count this session), is N≥20-on-real-data even an achievable bar for most of these 182 setup_types on any realistic timeline, or does the system's own design guarantee most setups sit in `THIN_N`/`SUPPRESS` limbo indefinitely regardless of true underlying quality? If so, is that a real problem, or the correct conservative behavior given how little live data actually exists? Give a real verdict, not both-sides-ism.

### 2. Audit the FULL day-type-conditional real-N gap, not just `IB_BULLISH`'s one bucket

The `DAY_TYPE_MANAGED`/`DAY_TYPE_CONDITIONAL` mechanism (`IB_BULLISH`/`IB_BEARISH`, `scripts/backtest_setup_status.mjs`'s per-day-type bucket logic) computes buckets with NO real-N floor at all, unlike the main blended-EV check (fixed 2026-07-20 via `PROMOTE_MIN_REAL_N`). Check `IB_BEARISH`'s own day-type buckets the same way — is IT also being kept alive by a low-real-N or `UNKNOWN`-origin bucket, or is its live status genuinely well-supported? This is a real, scoped, fixable gap — say concretely whether it should be fixed now (adding a real-N floor to the day-type-bucket path, mirroring the existing `PROMOTE_MIN_REAL_N` pattern) and what the actual before/after live-roster impact would be if you can determine it from the data.

### 3. The informational-signal family — genuinely useful, or accumulating dashboard clutter?

This codebase has built a growing family of live, informational-only signals that never gate/size anything: `STACK_VOL_BREAK_LIVE`, `BIGMOVE_LIVE_SIGNAL`, `SIGMA_CONTINUATION_LIVE`, `bar6_checkpoint`/`bar6_exit_recommended`, `exhaustion_signal_at_detection`, `touch_quality`. Each was individually validated at build time. But per the system's stated goal (make decisions more trustworthy/actionable, not just interesting), audit: is there ANY evidence (annotation data, `RESEARCH_CLAIM` follow-ups, explicit user feedback in `docs/OPEN_THREADS.md`) that any of these have actually changed a real decision, or are they accumulating as passively-displayed badges nobody has confirmed are being used? Cross-reference `project_the_coach_parked.md`/`project_daily_coaching_redesign_parked.md`-style memory notes if relevant (these describe related, deliberately-parked ideas about annotation-driven feedback). Give a concrete recommendation: is it time to build the annotation/outcome-tracking loop that would tell us whether these signals are earning their keep, before adding a 7th one?

### 4. Is the sizeMultiplier stack still mostly degenerate?

`unify_sizemultiplier_into_validated_score` (2026-07-27) found and fixed 3 real `origin_status`-scoping bugs causing `active_setups.size_multiplier` to sit at a binary 0.500/1.000 on 56/57 real rows checked, but explicitly did NOT build the full validated joint model (structural + data-volume reasons, tracked as `sizemultiplier_needs_per_factor_instrumentation`). Check the CURRENT real `size_multiplier` distribution on real (`ACTIVE`/`SHADOW`-origin) rows since that fix — did fixing the 3 scoping bugs actually restore real variety to the ~20-factor stack's output, or is it still effectively binary for a different reason? This is directly checkable from live data, not a judgment call — do it.

### 5. Is today's research energy (DOW gate → re-fire gate → rolling-window health gate) a coherent trajectory, or reactive scatter — and what SHOULD the next thread be, if any?

Given the answers to 1-4, and the full `RESEARCH_CLAIM` ledger (`node scripts/record_claim.mjs --list` — 166 entries as of this session) and `OPEN_DECISION` backlog (`node scripts/flag_decision.mjs --list` — 25 pending as of this session, oldest-first), is the backlog being worked down or growing faster than it resolves? Identify any `OPEN_DECISION`s that are now stale/superseded and should be resolved/closed rather than carried forward. Then give a real prioritization: of everything currently open (day-type real-N floor, informational-signal validation loop, sizeMultiplier joint model, the 3 validated re-fire-gate setups not yet wired, the rolling-window health gate's unresolved regime-confound), what are the top 2-3 highest-leverage things to actually do next, ranked by how directly they serve "make the user's real-time decisions more trustworthy," not by research interestingness?

## Hard rules (same as prior audits)

1. N≥20 before citing any stat as meaningful (or explicitly say "N=X, thin" and don't lean on it). Below that in this audit specifically covers a LOT of near-zero-real-N buckets — say so plainly wherever you find one, that's often the finding itself.
2. Query the live DB directly for every number in this report — do not trust this prompt's own citations without re-verifying them (they were correct as of when this was written, but re-derive rather than copy).
3. Check `docs/OPEN_THREADS.md`'s 2026-07-28 entries (three, in order: DOW-gate audit, re-fire-gate volZ-bug-corrected, rolling-window-health-gate) before re-deriving anything already found today — read them fully, they contain the exact bugs/fixes/caveats already established.
4. Do not implement anything. Do not generate code for immediate paste.
5. Per this codebase's own standing rule: audit any surprising or too-clean-looking number yourself before including it — this exact discipline caught two real bugs earlier today alone.
6. Be willing to conclude "the system is fine, stop adding signals and go do X instead" if that's genuinely what the data shows — this audit is explicitly allowed to recommend LESS building, not just more.

## Read first (in this order)

1. `CLAUDE.md` in full — especially the "Hard rules" section (day-type real-N precedent, no-dead-ends rule) and the "Collaboration" section (the actual stated goal — asymmetric payoff, not win rate; unemotional discipline)
2. `docs/OPEN_THREADS.md`'s three 2026-07-28 entries (search "SETUP_STATUS_DOW", "re-fire", "Rolling-trade-count") for the full account of today's three threads
3. `ARCHITECTURE.md` — service/route table, especially the `active_setups`, `sizeMultiplier`, `SETUP_STATUS`/`SETUP_STATUS_DOW` entries
4. `scripts/backtest_setup_status.mjs` — the main suppression/promotion/day-type-bucket logic, and its real-N floor (`PROMOTE_MIN_REAL_N`) applied to the blended check but not the day-type-bucket path
5. `node scripts/record_claim.mjs --list` and `node scripts/flag_decision.mjs --list` — full current ledgers, primary data sources for question 5
6. Direct DB queries for questions 1, 2, 4 — do not estimate, query
