# CLAUDE.md

Trading journal: React frontend (Vite, port 3000) + Express backend (port 3002) + PostgreSQL (`trading_journal` db). See [ARCHITECTURE.md](ARCHITECTURE.md) for full route/service/table inventory — this file is conventions and rules, not structure.

**Start of session / after a context clear: read [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md) first.** It tracks unfinished work, unconfirmed proposals, and stale-stats findings from prior sessions — the goal is to never lose a thread to a context reset.

## Dev workflow

- Start: `./start.sh` (kills stale processes, starts server + client, self-heals the `pre-commit` hook symlink) — Stop: `./stop.sh` or `fuser -k 3002/tcp`
- `git commit` runs a **pre-commit hook** (`scripts/git-hooks/pre-commit`, symlinked from `.git/hooks/pre-commit` — `.git/hooks/` isn't tracked, `./start.sh` re-creates the symlink if missing) that blocks new hardcoded trading-threshold anti-patterns in `acd.js`/`caseEngine.js`, plus 3 more broadly-scoped patterns across `server/`/`src/`/`scripts/` (wrong $/pt constants, `SUM(pnl)`-style CumPL-diff violations, the JS-`toISOString()`-for-a-trading-day-date bug) — 7 patterns total, defined once in `scripts/hardcoded-threshold-patterns.sh` and shared with the Stop hook's advisory version. Deliberate exception: `ALLOW_HARDCODED=1 git commit ...`.
- Server only: `npm run server` (nodemon, port from `.env` `PORT=3002`) — Client only: `npm run client`
- DB: `server/schema.sql` is a full `pg_dump --schema-only` snapshot (122 tables/views, regenerated 2026-06-30 after a dead-table cleanup) — there's still no tracked migration history, so it drifts again the moment a table is added/altered live without regenerating. See the regen command in [ARCHITECTURE.md](ARCHITECTURE.md#schema-source-of-truth). When in doubt, query `information_schema.tables`/`information_schema.columns` against the live DB rather than trusting the file is current.

## Hard rules

- **Anything that drives or anticipates a decision — a data point, a computed signal, a classifier, a backtest finding, not just a "script" — must be built so the system keeps learning from it and can revisit it. No dead ends.** Added 2026-07-18 per explicit user directive ("its not just scripts it[s] data or datapoints anything that drives or anticipates in decision making must be help the system keep learning and growing. No dead ends") after a session spent the day building exactly this pattern piecemeal (persisting `VOL_REGIME_HIST`, then `GARCH_VOL_SCALE`, recording `RESEARCH_CLAIM` rows for every tested finding) without it ever being named as a governing rule new work is held to. Concretely, before considering anything of this kind done, check all four:
  1. **Persisted somewhere queryable** (`performance_audit` or a real table) — not trapped in a script-local variable, a one-off `scratch/*.json` file, or a hardcoded literal in the serving code. A hardcoded snapshot of a once-real number is the textbook failure case here — see the hand-typed-stat rule below, 7 confirmed instances, most recently `keyLevels.js`'s `tierStats` object (2026-07-18): a real calibration got computed once, dropped into a literal, and then just sat there while the real number underneath it drifted 3-6x away, including a full sign flip, with nothing to ever notice.
  2. **Has a recheck/recalibration path** — a scheduled script (`run_weekly_backtests.sh`), a `RESEARCH_CLAIM` 30-day cycle, or at minimum a documented manual recheck trigger. Not compute-once-and-forget.
  3. **Actually wired to a consumer, or explicitly flagged if not yet wired.** A signal nobody reads is a dead end whether or not it's persisted — either it feeds a live decision (sizing, suppression, a displayed card) or it's tracked as a `flag_decision.mjs` `OPEN_DECISION` so someone has to actively decide to wire it in or drop it, never just silently sitting there unreferenced.
  4. **Discoverable** — has an entry in `ARCHITECTURE.md`'s inventory or this file's "Where to look" section, not an orphan only findable by grepping around and hoping.
  This names the connective principle behind several mechanisms that already exist as separate conventions — `RESEARCH_CLAIM` (item 2), `OPEN_DECISION` (item 3), "export the real function, never reimplement" (item 1, applied to code), the no-static-thresholds rule below (item 1, applied to a threshold specifically) — treat this as the reason those exist, not a fifth new mechanism to build. When starting something new that computes or anticipates anything, ask which of the four it's missing before calling it done.
- **Any new setup, signal, or calibration finding must be evaluated for BOTH the RTH window (9:30am-4pm ET) and the Globex/overnight window (6PM-8:30AM ET) before being considered complete — never default to RTH-only and call it done.** User directive 2026-07-22, given directly after the confluence-pair proximity calibration was built and backtested RTH-only (`scripts/backtest_confluence.js`'s `getRTHBars()`) without the question of overnight applicability ever being raised. This codebase already has two parallel, structurally different detection engines — the full RTH level-fade engine (`acd.js` ~3830-4430, `sizeMultiplier` IIFE, `keepLevelsAll`, confluence-aware) and the much simpler `detectGlobexSetup()` (~line 594, single-level touches only, no `sizeMultiplier` stack, no confluence tracking at all as of 2026-07-22) — and it is easy for a new finding built/tested against one to simply never get asked about the other. Concretely, before calling any new setup-discovery or calibration thread done: (1) test it against Globex/overnight data too, not just RTH, even if the result is negative or thin — a tested-and-recorded "doesn't apply overnight" is fine, an untested gap is not; (2) if the live wiring mechanisms differ between the two engines (e.g. RTH has a `sizeMultiplier` stack to hook a bonus into, Globex currently doesn't), wire into whichever supports it and explicitly flag (`OPEN_DECISION`) whichever doesn't rather than silently leaving it RTH-only with no record of the gap. See `confluence_pair_globex_backtest` `RESEARCH_CLAIM` for the first applied instance of this rule.
- **No static thresholds.** Every threshold in this codebase must be derived from a rolling distribution (σ from a rolling mean/std), never a hardcoded number. This applies to entries, stops, targets, signal triggers — everything. If you're about to write a literal number as a cutoff, stop and compute it from historical data instead.
- **P&L must use the CumPL diff method**, not `SUM(pnl)` or `SUM(FlatToFlat)` — both overcount. See the SQL pattern in [ARCHITECTURE.md](ARCHITECTURE.md#pnl-calculation-cumpl-diff--critical-dont-regress-this). `/api/daily-logs` and `/api/stats/daily` both implement this — keep them in sync if you touch either. **A raw, account-type-unscoped CumPL-diff is a DIFFERENT and WORSE bug than the `SUM(pnl)` it replaces — always scope to `custom_fields->>'account' LIKE '%-PRO%'`, matching `stats.js`'s `/stats/capture-ratio` (the established correct reference).** Found 2026-07-19 fixing `server/services/patternMemoryUpdate.js`'s `session_pnl` (`OPEN_DECISION` `pattern_memory_update_session_pnl_needs_cumpl_audit`): a naive `LAG(cum_pl) OVER (PARTITION BY account ORDER BY log_date)` interprets a practice/sim account's periodic balance reset as a single-day loss in the tens of thousands — confirmed live, `PRACTICEDec108855775`'s `cum_pl` dropped from $13,000.00 (2024-12-16 close) to resetting near zero the next day, which an unscoped diff reads as a **-$12,155 loss that never happened**. Caught only because the "fix" was audited against real numbers before trusting it — Gemini's first pass reported the CumPL-diff and `SUM(pnl)` "matching almost perfectly," which was wrong (its query used a flawed `COALESCE(LAG(...), cum_pl)` fallback that silently zeroed out exactly the rows that would have exposed the reset); a from-scratch re-implementation of the canonical pattern surfaced the $13K-scale discrepancy immediately. `-PRO%` accounts don't get balance-reset mid-history the way practice ones do — this is why `stats.js` already scoped this way; `patternMemoryUpdate.js` didn't, until this fix. When there's no PRO-account CumPL data for a given day (only ~48 of 358 historical trading days have any), fall back to raw `SUM(pnl)` for that day only — imprecise but not corrupted, same documented-exception convention as `dll.js`'s own fallback.
- **This journal trades MNQ (Micro E-mini Nasdaq): $2/point, $1 round-trip commission** — matches `server/routes/acd.js`'s live resolution path (`const PNL_PER_POINT = 2; // MNQ = $2/point`, `const COMMISSION = 1`), the ground truth for what's actually traded. **Never copy a `$/pt` or commission constant from another script without checking it matches this.** Found 2026-07-14: `scripts/archive/backfill_level_fades.js` used `PT=5, COMM=5` (matching neither MNQ's real $2 nor standard NQ's real $20) — an uncaught error present since that script's creation, silently copied into 6 new `repair_*.mjs` scripts written earlier the same session before being caught. Overstated every affected setup_type's dollar-EV by ~2.5x (correct sign, wrong magnitude) until fixed via `scripts/repair_dollars_per_point.mjs`. If you're about to write a `$/pt` or commission constant in a new script, check it against `acd.js`'s live values first — don't trust an existing script's constant just because it's already there.
- **~80% of `active_setups` is synthetic backfill data, not live-fired trades — check `origin_status` before treating any figure derived from this table as realized/historical performance.** Found 2026-07-17 (Opus Audit #3, `docs/OPEN_THREADS.md`): `resolution_method` breakdown is `BACKFILL` 6,375 + `EARLY_TOUCH_BACKFILL` 4 (inserted retroactively by old backfill/repair scripts, **never fired through the live system at all**) vs. `PRICE_CLEAN` 231 (genuinely live-resolved) out of 7,988 total rows. `active_setups.origin_status` (immutable at insert, never touched by any resolution UPDATE — unlike `status`, which gets overwritten to `RESOLVED`/`EXPIRED`) now distinguishes `ACTIVE` (real, live-visible)/`SHADOW` (suppressed, background-only)/`BACKFILL` (synthetic)/`UNKNOWN` (pre-2026-07-09, unrecoverable — no `SETUP_STATUS` snapshot history reaches back that far). **A `SUM(actual_pnl)` or backtest built from `active_setups` without filtering by `origin_status` is a strategy simulation, not an account record** — this bit an entire session's worth of walk-forward-backtest/DLL-sweep/weekly-P&L work before the gap was found. Before presenting any `active_setups`-derived dollar figure as "what happened," check what fraction of its population is `BACKFILL`.
- **Never fabricate a stat.** Any win-rate/hit-rate claim needs N≥20 in the sample before it's reported as decisive (see `engineReadHitRates.js` convention). Below that, say so explicitly rather than rounding to a confident-sounding number.
- **Never hand-type a WR%/N/$ literal into a live-rendered card, label, or description string.** Found 2026-07-13: 6 separate instances across `mine_session_bias.mjs`, `antigravityEdges.js` (IB re-test cards, gap-fill card, V-pattern card, `SETUP_CONTEXT` adjustment table), and `acd.js` (`levelMap.bestCtx`) — all presented a specific hand-typed number as if freshly computed, several silently drifted to the point of being *directionally wrong* (e.g. `acd.js` claimed IB_LOW was an "88% WR" edge; the real, live number was 66% WR at **negative** EV). **7th instance found 2026-07-18**: `server/routes/keyLevels.js`'s `/api/confluence/today-zones` `tierStats` object — hand-typed despite its own comment claiming it was "latest weekly rigor-checked calibration," found while consolidating an unrelated GARCH backtest and comparing its touch-detection constants against the live confluence system. Every tier was off 3-6x vs. the real latest `performance_audit` `CONFLUENCE_AUDIT` row, `SINGLE` wrong-signed (+$6.91 hardcoded vs. real **-$3.02**). Fixed the same way as the other 6: query `performance_audit` live (12h cache, `server/lib/cache.js`). Full writeup: [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md). Either derive the text from a `row` field already fetched from `performance_audit` in the same function, or write a dedicated `scripts/backtest_<name>.mjs` that populates one — never hand-type the number, even as a placeholder "for now." **This pattern keeps recurring (7 instances now) — when touching ANY file that renders a WR%/N/$ figure for any reason, even in passing, check whether that figure is a live query result or a literal before moving on, don't wait to stumble into it.**
- **No lookahead in backtests/replays.** Case engine, day-type reassessment, and all backtest scripts must only use information that would have been available at that point in time. This is a frequent source of subtle bugs — when writing a new backtest script, explicitly check that no future bar/level data leaks into a decision made earlier in the session.
- **Volume-profile computations (POC/VAH/VAL) must spread each bar's volume across its own high-low tick range, never bucket 100% of it at one price.** `server/services/developingValueService.js`'s `computeProfile(bars)` is the single correct reference implementation (spreads volume evenly across each bar's H-L range, then extends VAH/VAL alternately toward whichever side has more volume next until 70% total is enclosed) — for any period-range VA (week/month/quarter/2-day), call its exported `computeVolumeProfileForRange(queryFn, { startDate, endDate, ... })` wrapper rather than writing a new SQL aggregation. Found 2026-07-17: a `ROUND(low/0.25)*0.25 ... GROUP BY` bucket-by-low pattern (plus a non-standard fixed 35%-up/35%-down value-area split instead of the correct alternating-to-70% method) had been hand-duplicated across `scripts/compute_levels.js` (PW/PM/3M_VAH/VAL/POC — the values written to `level_prices`, feeding every backtest/live level-fade candidate for those levels), `server/routes/acd.js` (6 occurrences: the live 2-day-composite POC, 2 prior-month VA computations, 2 prior-day VA fallbacks), and `server/routes/weekly.js` (3 endpoints, all confirmed dead/no frontend caller). Because a bar that trades through a 5-point range in one minute got 100% of its volume counted at the bottom tick, every affected VAH/VAL/POC was dragged toward the low of the session/period — drift confirmed real and often large (many periods showed 100+ point differences once corrected) once independently spot-checked directly (not just via Gemini's own estimate, which had at least one confirmed replication error against the actual live SQL — audited per the standing Gemini-output-audit rule). Fixed by centralizing on `computeProfile`/`computeVolumeProfileForRange`, backing up and recomputing the full `level_prices` history for the affected level_names, and re-tagging every trade in `trades.level_proximity` (whose nearest-level ranking could have been skewed by a wrong VA value even when the final tag wasn't itself a VA level) against the corrected data. Full writeup and backup-table names: docs/OPEN_THREADS.md / docs/DB_BACKUP_CATALOG.md.
- **Export the real function, never reimplement live-derived classification logic inline in a backtest script.** If a backtest/mining script needs a value that a live service already computes (a regime, a day-type, a level, any derived classifier — not a raw DB column), it must `import`/reuse that service's real exported function, or read a persisted canonical table populated by it, never re-code the math by hand. Found 2026-07-16: two independent Gemini attempts at a Markov-vs-volatility-regime comparison each had to hand-reimplement `classifyRegime()` from scratch (`volatilityRegimeService.js` had no persisted history to check against), and the two reimplementations disagreed with each other and with the live tool's own prior validation — a real, silent drift bug, not a hypothetical. Fixed at the root by exporting `fiveMinBars`/`stdevLogReturns`/`getPercentile`/`classifyRegime`/`getMorningVolBaseline` from `volatilityRegimeService.js` and building `scripts/backfill_volatility_regime_history.mjs`, which imports them directly and persists one row/day to `performance_audit` (`signal_type='VOL_REGIME_HIST'`) as the now-canonical source every future comparison should join against. When adding a new derived/live classifier, export its core function(s) at creation time rather than waiting for a second script to need it and reimplement instead.
- **Every tested research/exploratory claim — not just setup-calibration ones — gets recorded via `scripts/record_claim.mjs`, positive or negative.** A hypothesis that gets tested, produces a real N/WR/EV number, and only lives as prose in `docs/OPEN_THREADS.md` or a `scratch/*.json` file has no lifecycle: nothing re-checks it, and the next session re-derives it from scratch (sometimes differently — see the rule above). `recordClaim({slug, claimText, sourceFile, sourceDate, sampleSize, winRate, evPerTrade, rigorStatus, status})` writes/updates a `signal_type='RESEARCH_CLAIM'` row in `performance_audit` with a 30-day `next_recheck_due`; `node scripts/record_claim.mjs --list` shows every claim and flags overdue ones. `status` is `CONFIRMED` (independently re-verified), `PROVISIONAL` (real but thin/unstable/unchecked-for-rigor), or `STALE` (needs a fresh recheck, generally by age). This is separate from the existing setup-calibration tables (`SETUP_STATUS`/`OPTIMAL_STOP`/`DAY_TYPE_ALPHA`/etc.) — those already have their own lifecycle; `RESEARCH_CLAIM` is for everything else. Built 2026-07-16 per an Opus consultation on making this codebase's research findings durable instead of evaporating each session; seeded with 7 audited claims from this session (one caught a real Gemini-fabricated set of numbers before it entered the ledger — see docs/OPEN_THREADS.md). **Found 2026-07-20 (new session): two scripts (`test_extension_vs_garch_vol.mjs`, `test_extension_vs_next_level_distance.mjs`) hand-rolled their own `performance_audit` INSERT instead of calling `recordClaim()` — and set `next_recheck_due` to the SAME DAY the claim was written, not +30 days, so the session-start hook flagged both as overdue the very next day.** Fixed both to call the canonical helper. **When writing any new script that persists a `RESEARCH_CLAIM` row, always import and call `recordClaim()` — never hand-write the INSERT, even if it looks like a one-off** — this is the same "shared module, don't reimplement" principle applied to a persistence helper instead of a computation function.
- **Resolve every `isolation: worktree` Agent dispatch before the spawning session ends — never leave one for a future session to discover by accident.** The harness only auto-deletes an isolated worktree if the agent made *no* changes; if it made any, the path/branch are handed back and a human/session must explicitly commit+merge it or deliberately discard it (`git worktree remove` + delete the branch). Found 2026-07-16: a 2026-07-01 `isolation: worktree` dispatch (24hr Globex VWAP feature) left ~3,100 uncommitted lines across 31 files sitting untouched for two weeks — nothing in this repo's workflow surfaces an orphaned worktree, so it was only found by accident during an unrelated dead-code grep. `.claude/hooks/session-start.sh` now flags any worktree besides the main checkout at the start of every session (see its `STRAY_WORKTREES` check) as a backstop, but the real fix is not leaving one dangling in the first place.
- **Do not guess on third-party tool behavior** (especially Sierra Chart specifics) — fetch documentation or ask before asserting how an external tool behaves.
- **Never write a file >400 lines in a single Write call.** Use Write for the first section, then `cat >>` via Bash to append the rest. This avoids the 32k output token limit that silently truncates large writes.
- **Use Gemini for mining/analysis, Claude for implementation.** When a task is combinatorial exploration or pattern-mining over `performance_audit`/trade data — "which pairs are best under condition X," "what does the data show about Y across N setup types," multi-scenario simulations — dispatch it to Gemini via the `scratch/claude_request.md` protocol rather than grinding through it directly. Gemini has read-only DB access and doesn't burn Claude's context on large query output; only the findings should come back into context, not raw rows. Claude's job is building the systems (scripts, endpoints, hooks) and interpreting/applying what Gemini finds — not re-deriving the mining work itself. Reserve doing it directly for small, deterministic, single-query aggregations (e.g. one bucketed backtest over one table) where dispatching would cost more round-trip time than it saves, and for anything Gemini has already gotten wrong twice on this task (see the 2-strikes rule below). This does not relax **Audit all Gemini output before acting on it** below — mining work still gets verified before anything is promoted to live.
- **Gemini gets 2 correction attempts, then Claude takes over.** When Gemini's output is wrong (wrong numbers, wrong methodology, SQL error loops — not a partial/incomplete response), send a correction prompt and re-invoke. If the 2nd attempt is also wrong, stop correcting and do the task directly instead of retrying a 3rd time. User rule: "after 2 incorrect attempts by Gemini, you can step in."
- **Audit all Gemini output before acting on it.** Gemini produces plausible-looking but sometimes wrong analysis. After reading `scratch/antigravity_response.md`, verify 3–5 key claims by re-querying the DB directly before promoting any finding to `performance_audit` or changing live parameters. Specific checks: (1) 100%/0% WR with N≥10 → always check `COUNT(DISTINCT log_date)` for single-session clustering; (2) grid output → compare "current params" cell to existing `performance_audit` baseline; (3) N counts → verify with a direct `COUNT(*)`; (4) any TRADE recommendation → re-run the WR/EV query yourself. User delegated this explicitly: "I want Claude to audit anything and everything Gemini does. It lies sometimes."
- **Every pending product/architecture decision — "wire this in or delete it," "merge this branch," "change this cadence" — gets recorded via `scripts/flag_decision.mjs`, not left as prose in `docs/OPEN_THREADS.md`.** Deliberately a sibling mechanism to `RESEARCH_CLAIM` above, not the same rows with a different label: a decision has no statistical content and doesn't go "stale" the way a research finding does — it just sits `PENDING` until a human actually decides, then `RESOLVED`. `flagDecision({slug, decisionText, sourceFile, sourceDate})` writes a `signal_type='OPEN_DECISION'` row in `performance_audit`; `decisionText` must include real context — what's being decided, why it matters, what resolving it looks like (a bare fact like "5 cards aren't rendered" isn't enough; write the why and what's-at-stake alongside it, the same way this file's own entries do). `node scripts/flag_decision.mjs --list` shows every pending decision; `--resolve <slug> '<resolution text>'` closes one out. **Every pending decision prints unconditionally at the start of every session** (`.claude/hooks/session-start.sh`'s `OPEN_DECISIONS` section, sorted oldest-first with age since first flagged) — this is the actual fix, not the table: nothing here depends on a future session remembering to re-read prose in `OPEN_THREADS.md`. Built 2026-07-17 per explicit user request ("anything that needs to be reevaluated should [be] flagged with something and actively monitored. Nothing can be buried") after the session-start hook was found to have zero mechanism for resurfacing this category — `RESEARCH_CLAIM`'s 30-day recheck only covers statistical findings, not decisions; seeded with 10 real pending items pulled from this session and the prior day's SSOT/dead-end audit.
- **A code comment claiming a number was "verified" or "cross-checked" is not itself verification — this is a distinct, more dangerous risk than a one-off wrong Gemini response, because it sits trusted in committed production code instead of a scratch file someone might still double-check.** Found 2026-07-17: `scripts/update_optimal_stops.mjs` hardcoded `DEFAULT_DPP=5` (a dollars-per-point fallback), defended by a comment claiming "real $/pt is cleanly bimodal (~$5 for the level-fade family)... independently verified via Gemini mining pass, cross-checked by Claude against direct SQL before trusting." That claim was false — every setup_type with N≥20 STOP_HIT trades (54 checked) resolves to $2.01-$2.06/pt, no exceptions, no bimodal split (the 4th occurrence of the wrong-$/pt-constant bug class documented in `server/config/instruments.js`'s header). 25 live setup_types had `OPTIMAL_STOP` rows calibrated with the wrong 2.5x-inflated fallback before this was caught — not by anyone re-reading the comment skeptically, but by someone happening to re-run the check from scratch months later. Treat "verified" language in an existing comment as a claim to re-check when you're about to depend on the value it's attached to, not as a substitute for checking it yourself.
- **When a script both computes a derived value (a sweep, a classifier, an optimizer) and separately stores/prints a number labeled as describing it, that pairing needs a standing check — a feature added on top of an existing field can silently break the field's own meaning without anyone touching the field's line.** Found 2026-07-17: `scripts/update_optimal_stops.mjs`'s `ev_per_trade` column was `ROUND(AVG(actual_pnl))` — a raw historical average using whatever stop/target each trade actually fired with — from the file's original commit (`a04b2fd`, 2026-07-05), back when `optimal_stop`/`optimal_target` were themselves just plain percentiles (`p75_mae`/`p50_mfe`), so the raw average was a defensible, honest, standalone stat at the time. The real EV-optimizing sweep (`sweepOptimalTarget`) was added 4 days later (`de3e407`, "EV-sweep targets") to make `optimal_stop`/`optimal_target` genuinely EV-optimal — but `ev_per_trade`'s write path (`console.log`, the INSERT param) was never rewired to report the sweep's own `swept.ev`. From that commit on, `ev_per_trade` silently stopped describing the stop/target it sat next to, for every setup_type, every run — reproduced live: `CAM_R1_FADE_LONG` showed `stop=23/target=40/EV=-$14`, but re-running the exact formula at that exact stop/target gave **+$13.92**, a sign-flipping $28 discrepancy. Survived 4 more commits touching the same file (including one earlier the same day this was caught) because none of them had a reason to look at that specific line — a normal code review or diff read catches "does this line look right," not "does this field's original meaning still hold after a later feature was bolted on three commits ago." **Fixed** by writing `swept.ev` (falling back to the raw average only in the documented thin-tail-gate case), and — the actual prevention, not just the one-off patch — `scripts/test_invariants.mjs` check `[5]` now re-derives every `OPTIMAL_STOP` row's expected stop/target/EV via the real, imported `sweepOptimalStopAndTarget()` (not a hand-copied formula — a second copy would itself be the same bug class) and fails if the stored row doesn't match either the fresh sweep result or the documented fallback exactly. Verified the check actually catches this specific bug (not just passes trivially) by deliberately reintroducing the old raw-average line, confirming 65 failures, then restoring the fix and confirming a clean pass. **General lesson: when you add a smarter computation on top of a value a field already claims to describe, either rewire that field in the same commit or explicitly rename/re-scope it — and if the value is trusted enough to sort/display/gate anything, add an invariant check that re-derives it via the real function, not a plausibility check that a bug could trivially satisfy** (the first version of check `[5]` had exactly this flaw — it accepted "matches the raw average" as always-valid, which a bug that *always* writes the raw average would pass 100% of the time; fixed by requiring an exact match against the real sweep function's actual output). Full writeup: `docs/OPEN_THREADS.md`.
- **Auditing a backtest's simulation logic is not the same as auditing where its baseline comes from — a comparison is only as trustworthy as its least-audited side.** Found 2026-07-19: `scripts/backtest_scaleout_runner.mjs` (Gemini-authored, Claude-audited) had its chronological bar-by-bar simulation logic checked carefully and found sound — but its "baseline EV" was read directly from the live `OPTIMAL_STOP.ev_per_trade` column instead of being resimulated the same way as everything else it compared against. That stored value is computed by the OLD order-blind `sweepOptimalTarget()` against `mae_points`/`mfe_points` captured under whatever stop/target was live when each trade historically fired — for `VALUE_AREA_RESPONSIVE_SHORT`, historical stop distances actually used ranged 15-28pt+ while the CURRENT live stop is 10pt, so the stored baseline (-$1.99) was silently comparing today's tight stop against MAE data that was never bounded to it. A properly self-consistent chronological resimulation of the identical 100%-at-T1 baseline gave +$15.22/trade — not a loser at all — which **reversed** the finding: the scale-out config that looked like a clear winner (+$17.76 vs the wrong -$1.99 baseline) actually loses out-of-sample against the true baseline. Already promoted to an `OPEN_DECISION` and a `RESEARCH_CLAIM` before being caught — both had to be retracted (with a full account, not silently deleted) once a sibling script (`backtest_target_sweep_v2.mjs`, built the same day, which computed its baseline via the same resimulation as its candidates from the start) exposed the discrepancy on a side-by-side check of the same setup_type. **Fixed** by always resimulating the baseline in the same pass as everything else being compared (never reading a stored EV column as ground truth for a fresh comparison), and tightening the promotion gate to require beating baseline both full-sample AND out-of-sample. The real replacement finding (`CAM_R4_FADE_SHORT`) is actually stronger than the retracted one. **When building or auditing any script that claims "config X beats baseline Y," verify baseline Y was computed by the exact same method as X — a stored/precomputed number read from elsewhere, even one that looks authoritative because it's "the live value," needs the same scrutiny as the thing being tested against it.**
- **A backtest comparing two conditions against a shared, unchanging exit needs to rule out a structural/algebraic advantage before crediting the "smarter" condition with the win.** Found 2026-07-21: a test of whether a candle-pattern-confirmed re-entry (after price overshoots a level fade past its own historical median adverse excursion) beats blind first-touch entry, both walked to the SAME original stop/target, came back from a Gemini corpus-wide run looking dramatic — pooled paired EV improved from -$85.77 to -$5.67, in 62 of 64 setup_types with zero exceptions. That zero-exception uniformity was the tell (same "too clean" signal as every other incident in this section) — a dedicated control script (`scripts/pilot_overshoot_control_check.mjs`) proved nearly all of the improvement was mechanical: entering LATER against a FIXED stop/target is algebraically favorable regardless of any pattern (a winner pays more from a cheaper entry, a loser costs less from a closer one) — confirmed directly, the win/loss OUTCOME matched between a pattern-free "enter blind at the overshoot point" control and the pattern-confirmed entry in 92-100% of trades per setup_type. Corrected: `ORIG` EV=-$82.56 → blind-overshoot-entry EV=-$8.99 → pattern-confirmed EV=-$2.96 — the pattern's real marginal contribution was ~$6/trade, not the ~$77/trade the uncontrolled comparison implied. **When a new backtest's two arms differ in WHEN they enter but share the same exit levels, add an entry-price-only control arm (no "smart" filter, just the later/different entry) before trusting that a filter/pattern/signal explains an EV gap — the gap may be entirely explained by the entry price itself.** Same family as the baseline-mismatch rule above, applied to entry timing instead of a stored baseline column. Full account: docs/OPEN_THREADS.md.
- **When a backtest script's overfitting guardrails get tightened and re-run, anything that no longer survives needs its OLD `performance_audit` row explicitly deleted, not just left unrefreshed by the new run.** Hit twice in one day, 2026-07-19: `backtest_scaleout_runner.mjs`'s first run persisted 3 survivors; the corrected rewrite (fixed promotion gate) only found the same 3 slots worth updating naturally, but a broader case bit `backtest_target_sweep_v2.mjs` harder — its first (broken, spike-picking) run persisted a row for all 103 tested setup_types unconditionally, then the corrected rewrite only inserted/updated the 18 that survived the new guardrails, leaving the other 85 setups' original spike-picked rows (e.g. `PD_LOW_FADE_SHORT`'s absurd 719.8pt pick off a single trade) sitting in the table with the SAME `run_date` as the fresh ones — a naive `DISTINCT ON (signal_name) ORDER BY run_date DESC` query can't tell them apart, since "most recent run" doesn't distinguish "recomputed this run" from "untouched leftover from a run that happened on the same day." Caught both times only by noticing a downstream consumer's own count looked wrong (a walk-forward script logging "103 setup_types" when 18 was expected) — not by anything in either backtest script itself. **When re-running a script with stricter/different guardrails, explicitly query which prior rows for that `signal_type` are NOT in this run's fresh output and delete them** (or diff on a marker field the new version writes that the old one didn't, like `rigor`/`oosEv` in this case) — don't assume `ON CONFLICT DO UPDATE` alone keeps the table consistent, since it only touches rows the new run actually produces.
- **A backtest candidate grid needs a floor derived from the underlying DATA'S OWN RESOLUTION, not just statistical overfitting guardrails (thin-tail/plateau/OOS/rigor) — those catch overfitting to a few trades, not overfitting to a precision the data cannot actually resolve.** Found 2026-07-19: widening `backtest_scaleout_runner.mjs`'s trailing-stop candidate grid (for thoroughness, per user request) surfaced 32/96 setup "survivors" — but 31 of them picked a trailing-stop width of just 3-6.8pt, passing every existing guardrail cleanly. Checked directly: the median 1-minute NQ bar range is 6.25pt, the 25th percentile is 3.25pt — a trail that tight sits *within normal single-bar noise*. OHLC bar data only records a bar's high/low, not the intra-bar price path, so the backtest literally cannot determine whether such a tight trail would have actually triggered in real execution — the near-universal "survival" was a data-resolution artifact, not a real edge, and every one of this codebase's existing guardrails (all statistical, checking sample size/stability/out-of-sample performance) passed it cleanly because none of them check whether the candidate itself is finer than the data can support. **Fixed** by adding a minimum trail-width floor derived from the same live data (median 1-min NQ bar range, queried fresh — not a hardcoded number, matching the no-static-thresholds rule) — survivors dropped from 32 to 18, and even those cluster suspiciously close to the floor, suggesting a genuinely different (smaller, execution-risk-heavier) phenomenon than the original, wider-trail finding it was meant to generalize. **General lesson: before trusting ANY backtest whose candidates are themselves a distance/time/price granularity (a trail width, a tick-level threshold, a sub-bar timing window), check that granularity against the data's own real resolution — a candidate finer than what the underlying data can distinguish will pass statistical guardrails by construction, since the "noise" it's exploiting is measurement noise, not market behavior.**
- **Check for frontend render errors before calling any frontend change done.** Run `npm run lint:frontend` (ESLint `no-undef` over `src/**/*.jsx`/`*.js` with browser+JSX globals in `eslint.config.js` — catches missing-import/undefined-variable `ReferenceError`s before they hit the browser) and `npm run build` (catches broken import/export paths and esbuild-level errors like duplicate object keys that lint won't). Also check `scratch/gemini_alerts.txt` / `scratch/server_errors.jsonl` for `[CLIENT_ERROR]` entries — `trading-journal-watcher.service` (`scratch/gemini_error_watcher.mjs`) logs live browser errors there, and a real one can sit unnoticed for days if nothing checks it. Added 2026-07-13 after a code-split refactor left 4 components used-but-unimported (`LivePlaybookCard`, `ApproachingLevelBanner`, plus 5 more in `BacktestView.jsx`) and a recurring `obj.fmtP(field)` bug (should be `fmtP(obj.field)` — `fmtP` is an imported function, not a method) sitting live undetected. If real browser/Playwright tooling isn't available in the environment, say so explicitly rather than claiming an interactive check was done — static checks (lint/build/dev-server-transform) confirm nothing throws on load, not that a specific interaction (hover, click) behaves correctly. **This rule's fast half is now hook-enforced (2026-07-18):** `.claude/hooks/post-edit-lint.sh` (`PostToolUse`, `Write|Edit`) runs single-file `eslint --format json` (not the default formatter — confirmed by direct testing that it crashes in this environment with `TypeError: util.styleText is not a function`, a Node/ESLint version mismatch, which is evidently why `package.json`'s `lint:frontend` script already avoided it) on every touched `server/`/`scripts/`/`src/` file and injects real findings straight back into context. Caught a real ~6-week-old bug in its first test run: `server/index.js`'s intraday auto-import handler referenced `target.name` in its `io.emit(...)` call, a variable that stopped existing in that scope after a 2026-06-01 refactor (`ebfb653`) correctly updated the 4PM handler's equivalent line but missed this one — fixed to the same `TradeActivityLogExport_${todayStr}.txt` literal the 4PM handler already used. Does NOT replace the full `npm run build` + browser-check gate above (single-file lint can't see cross-file import/export breaks or interaction bugs) — it's the fast, real-time layer underneath, not a substitute.
- **Whenever a change touches data-fetching — a new component with its own `fetch`/poll, a new or modified API endpoint, or edits to an existing one on a hot path (Morning Prep, Trades/Calendar) — verify actual loading performance before calling it done, not just that it renders without errors.** "No console errors" and "actually fast" are different claims; this codebase has repeatedly shipped the former while being badly wrong on the latter (a 51MB unbounded `/api/trades` response, an N+1 firing ~18 redundant queries per setup, a shared-poll hook that silently multiplied requests 16x instead of deduping them — all invisible to lint/build/error-log checks, all found only by measuring). Concretely, using Playwright (already available in this environment — see the `/tmp` node_modules-symlink trick used 2026-07-15 if `playwright` isn't a direct project dependency):
  1. **Count and list actual network requests** a real page load/interaction fires (`page.on('request', ...)`, filter to `/api/`) — group by endpoint and flag anything firing more than once per load. A component fetching the same URL another component already polls is almost always a bug, not a coincidence (`grep` the exact URL string across `src/` before adding a new fetch of something that sounds "already available elsewhere").
  2. **Time the actual settle point**, not just first paint — watch for whatever loading indicator the page uses (this codebase's convention: `page.locator('text=Loading…').locator('visible=true').count()` polled every ~500ms-1s) until it hits zero, not just `domcontentloaded`.
  3. **For any new/modified endpoint, `curl` it standalone** (`-w "%{time_total}s size=%{size_download}\n"`) and compare against its cost *inside a real concurrent page load* — a large gap between the two means connection contention or a pile-up, not a slow query, and points to request-coalescing/caching (see below) rather than query optimization.
  4. **If a response is unexpectedly large, check the query is actually bounded** — this codebase has repeatedly found genuinely unbounded historical queries (`SELECT ... FROM trades` with no date/limit, `price_bars_primary` lookbacks missing an upper or lower bound defeating partition pruning) hiding behind code that looked bounded at a glance.
  Don't run this full checklist for every one-line change — but any change that adds a fetch, adds a component that will live on Morning Prep or Trades, or touches an endpoint already known to be hot (`setup-detection`, `confluence/today`, `live-session-context`, `edges-context`, `trades`) gets it. Added 2026-07-15 after a multi-pass session where "components take forever to load" kept resurfacing despite each individual fix passing lint/build/no-errors — the actual causes were only found by measuring request counts and response timing directly, and each one clean-checked (no error, no warning) right up until it was.
- **Before proposing or building anything new, `grep ARCHITECTURE.md` for the relevant capability first — the doc is more complete than it gets credit for, and re-deriving via `grep`/exploration when the answer is already written down wastes real time.** Found 2026-07-17: a session spent real effort rediscovering `server/services/touchQuality.js` and `scripts/calibrate_touch_quality.mjs` (the live volume-spike/touch-quality classification system) via code exploration before realizing `ARCHITECTURE.md` already had a full, accurate writeup of exactly that system (added 2026-07-15, the same day it was built) — the gap wasn't missing documentation, it was skipping the cheap check of the canonical inventory doc before diving into `grep`. Same discipline as the existing "check git history before changing weird-looking code" rule, just aimed at "does this already exist" instead of "why does this exist." Concretely: when a task starts with "can we build X" or "is Y already tracked somewhere," `grep -i` the relevant keyword against `ARCHITECTURE.md` before writing a single line of new code or running an exploratory grep across `server/`/`scripts/`/`src/`.
- **Verifying a statistical split is real is not the same as verifying the classifier that produced the split is valid.** A backtest that conditions setup EV on some newly-invented regime/classification (not an existing, already-computed field — a fresh z-score/percentile scheme built for the occasion) can pass every rigor check this codebase uses (N≥20, chi-square-ish significance, chronological stability, day-clustering) while the classifier itself is still an arbitrary construction: window lengths and cutoffs picked semi-informally rather than derived from a principled search, never sensitivity-tested (does the finding survive a 25-day window instead of 30?), never checked for whether the label predicts anything on its own independent of whichever setup it's being used to condition, and never adjusted for the multiple-comparisons surface of testing several regime schemes against dozens of setup_types at once. A plausible-sounding economic narrative for why a split makes sense ("don't fade a fresh trend") is not validation either — that kind of story can be told for almost any split after the fact. Found 2026-07-18: three regime-conditioning metrics built and rigor-checked in one session (directional-trend z-score, price-stretch z-score, trend-persistence tercile) had every downstream EV split verified, but the classifiers themselves never were — flagged by the user, recorded as `OPEN_DECISION` `regime_detection_methodology_needs_validation`. Before trusting a *newly-built* classifier's conditioning effects (as opposed to conditioning on an existing field like `day_type` or `prior_day_profile`, which only needs the split itself checked), validate the classifier's own parameter robustness and independent predictive power first. **Follow-up, same day: the validation was actually run, and this specific classifier failed it.** A non-overlapping-window re-test, a placebo/permutation test, and an independent PELT-changepoint ground truth on raw price data (not the classifier's own input) all came back negative — full account in `docs/REGIME_DETECTION_SPEC.md` §7.1/§8.1. This is the concrete, worked example of why this rule exists: the EV splits all passed rigor checks; the classifiers themselves were still arbitrary.
- **A `PENDING` `OPEN_DECISION` describes the state of the codebase *as of whenever it was last checked*, not necessarily now — verify its underlying claim before relying on it if you're doing related work, especially anything touching a file it names.** The mechanism guarantees a decision won't be silently forgotten; it does not guarantee the decision's factual claim is still accurate, and nothing currently re-checks a decision's claim automatically. Found 2026-07-18: `unrendered_dashboard_cards_5` was flagged pending on 2026-07-17, describing 5 specific dashboard components as still-unrendered dead code — they had actually already been deleted a day earlier (2026-07-16, commit `9fb677b`), almost certainly because the flagging check ran against stale `main` instead of the active feature branch (the exact risk `main_branch_55_commits_behind` had already warned about in the abstract — this is the confirmed real instance of it). Only caught because a routine main-merge diligence pass happened to touch the same files. Concretely: before citing a pending decision's claim as current fact (not just before resolving it), a cheap `grep`/direct check costs little and prevents compounding a stale finding into new work.
- **A prior "confirmed empirically" claim — including Claude's own, including from earlier in the same session — is not evidence once it has crossed a context-compaction boundary; the actual verification script/query is gone and only the conclusion survives.** This is the same trust-boundary problem the standing Gemini-output-audit rule solves for Gemini, but it applies just as much to a summarized version of Claude's own prior work: a compaction summary preserves *what was concluded*, not *how it was checked*, and a wrong methodology (e.g. a verification query that didn't scope to the right `signal_type`/table/column) survives the summary just as confidently-worded as a correct one. Found 2026-07-18: `keepLevels_ls_base_key_mismatch_selection_bug` was flagged PENDING with language claiming a standalone script had "confirmed empirically" that `ls()` always returned null in production — carried across a context-compaction boundary as settled fact, then acted on (a code fix was written and committed) before its own `GUNNING FOR` text ("re-verify... live afterward, not just code review") was actually carried out. When that follow-up verification was finally done from scratch — querying `UNIFIED_BACKTEST` directly and checking `backtest_unified.js`'s own naming convention via `git log -S` — it flatly contradicted the original claim: the table's signal names had never had the shape the original diagnosis assumed, for any of the 64 real candidate levels checked, since the file was first written on 2026-07-01. The one-line "fix" already committed was a harmless no-op against real data, not a fix of anything real; both the code comment and an `ARCHITECTURE.md` entry that had already picked up the false claim needed correcting in a follow-up commit, and the `OPEN_DECISION` had to be resolved with a corrected account instead of a triumphant one. **General lesson**: before resolving an `OPEN_DECISION`, or otherwise treating any pre-compaction "confirmed via a standalone script" claim as load-bearing, redo the actual check against current live data rather than trusting the conclusion — a session summary is not a re-runnable artifact, and skepticism that's already standard practice for Gemini's output needs to apply to a compacted version of Claude's own prior claims too.

## Conventions

- **Confound checklist for any new comparison-style backtest (two arms compared against each other).** Built 2026-07-21 per Opus Audit #4 after the same failure shape recurred 5+ times across ~5 sessions, always looking like a real win first, twice reaching `RESEARCH_CLAIM`/`OPEN_DECISION` before being retracted (most recently the same day: `volume_confirmed_candle_pattern_low_vol_trap`, selected the 6 largest-effect setup_types from a 48-setup sweep, reversed under held-out replication). Before recording or promoting any "config/condition A beats B" finding, check all four:
  1. **Do the two compared arms differ ONLY in the hypothesis variable**, or does one arm have a structural/algebraic edge (a later entry against the same fixed exit, a smaller remaining risk distance, anything mechanical)? If unsure, ask whether the "smarter" arm would still win with the intelligence stripped out and only the structural difference left.
  2. **Is the baseline computed by the exact same method/window as the thing being compared** — resimulated in the same pass, never read from a stored column computed under different historical conditions (the `backtest_scaleout_runner.mjs` baseline-mismatch incident, 2026-07-19).
  3. **Does the signal select a sub-population** (overshot / made-a-new-extreme / pattern-present / any filter)? If so, is there a same-selection-minus-the-signal-condition control bucket — the 3-way template in `scripts/pilot_cvd_divergence.mjs` (`SIGNAL` / `SAME_SELECTION_NO_SIGNAL` / `NEVER_SELECTED`) is the pattern to reuse, not reinvent.
  4. **Was this the largest of K effects pulled from a sweep** (top-N setup_types, best candidate in a grid, etc.)? If so, run `computeReplication()` (`server/services/rigorDiagnostics.js`) — a held-out/permutation check that pools the selected subset against everything NOT selected and reports whether the direction and magnitude survive. This is the one sub-family that's cheaply automatable; item 1-3 are domain-specific each time and need manual reasoning, not a generic detector (deliberately not built — would create false confidence).
  Items 1-3 are a documented checklist, not code — the actual bugs they catch are different every time. Item 4 has a real, reusable function now; use it before recording any `RESEARCH_CLAIM` that came from a "pick the biggest movers, then check them" process.
- **Benchmarking bundle size across commits (git worktrees):** never symlink `node_modules` between a worktree and the main repo — Vite's dependency-optimizer cache (`node_modules/.vite`) is shared through the symlink and can silently contaminate build output from a different commit's module graph, even after deleting `node_modules/.vite` between runs. Give each worktree its own real `npm install`. Also build each commit in its own fully separate Bash/tool call — chaining `cd worktreeA && npm run build` followed by a second build command in the *same* call risks the shell's cwd silently carrying over to the second build too. Found 2026-07-13: a self-audit of a lazy-loading change reported a "52% bundle regression" that was entirely a benchmarking artifact from these two bugs stacked together; an independent review with a clean `node_modules` install got the correct (and much better) numbers.
- Backtest scripts live in `scripts/`, named `backtest_<hypothesis>.js`, run manually via `node`, and typically write findings to the `performance_audit` table. They are not imported by the running app.
- **Frontend API calls fetch the backend directly (`:3002`), never the relative `/api` path (2026-07-15).** `src/constants/api.js` exports the one `API_URL` constant every view/component imports — never redeclare `const API_URL = '/api'` locally. Reason: routing API traffic through Vite's `/api` proxy put it on the same origin (`:3000`) as the app's own module-serving, and Chrome's HTTP/1.1 6-connections-per-origin cap meant Morning Prep's own background polling could starve a newly-navigated-to view's module fetch indefinitely (root-caused via a `curl` to the stuck URL succeeding instantly from a separate process — the server was never hung, the browser's per-origin connection queue was). Splitting API traffic onto its own origin (`:3002`) gives it a separate connection budget. Backend already has permissive CORS and no cookie/session auth, so this needed no backend change. Full writeup: docs/OPEN_THREADS.md.
- **Day-stable backend queries (bounded strictly `< date`, or otherwise fixed for a whole trading day) should be cached on `server/lib/cache.js`'s `cacheGet`/`cacheSet`, not recomputed on every poll.** Already used by `acd.js`/`auctionRead.js`/`keyLevels.js`/`weekly.js`/`stats.js`/`morningBrief.js`/`sessionForecastService.js` — check this file before hand-rolling a new module-level cache object; `cacheSet` returns its `val`, so `return cacheSet(key, result, ttl)` works inline. Use a shorter TTL (minutes, not hours) for anything that reads the *target date's own same-day* data (e.g. today's still-accumulating premarket bars) — see `sessionForecastService.js`'s `getSessionForecast` (5min) vs. `morningBrief.js`'s strictly-prior-day `getTrailing*` helpers (12h) for the two conventions side by side.
- **`useSharedPollData` (`src/utils/useSharedPollData.js`) dedupes the recurring poll interval automatically, but NOT independently-added consumers of a new URL, and NOT (before 2026-07-15) the very first load.** Before wiring a new component to an existing polled endpoint, `grep` for the exact URL string first — it's cheap, and this hook has already silently gained duplicate non-shared fetchers of the same endpoint multiple times (edges-context, live-session-context, morning-brief/forecast all had 2-6 independent fetchers found in one pass 2026-07-15, including ones that predated the hook's own creation). The hook itself also had a real bug fixed the same day: any subscriber mounting before the first fetch resolved saw `entry.data` still `null` and called `load()` again itself — with N real subscribers × React StrictMode's 2x dev-mode effect invocation, that was measured as 16 requests for one URL on a single page load. Fixed with an in-flight guard inside `load()`; no call-site changes were needed, so no other consumer should need touching for this specific bug again.
- **A genuinely expensive endpoint that's also polled frequently or fetched by several components on mount needs a request-coalescing lock, not just a data cache.** If a response takes longer than its own poll interval (or several components independently fetch it at once on page load), concurrent/overlapping requests pile up and compete for the same DB connections, making every one of them progressively slower — a self-inflicted DoS against your own database. Found 2026-07-15: `/api/acd/setup-detection` (polled every 15s, ~8-10s per call) and `/api/confluence/today` (fetched by 3+ components on mount, ~5-6s per call) were both measuring 13-24s+ under normal use for exactly this reason — the per-call cost alone never explained the total. Pattern (see `acd.js`'s `setupDetectionInFlight` / `confluence.js`'s `confluenceTodayInFlight`): a module-level in-flight promise; the first caller runs the real handler against a minimal fake `res` (capturing just `.status()`/`.json()` — verify no other `res` methods are used in the handler first), concurrent callers `await` that same promise and get the identical result. This doesn't make the underlying query faster — it only stops concurrent callers from multiplying the cost. Don't add this reflexively to every route; only where polling frequency or multi-component fan-out can realistically overlap with the endpoint's own response time.
- Sierra Chart TAL data is stored as JSONB under `custom_fields->'sierra_data'` rather than typed columns — this lets new TAL columns appear without a migration and lets old Activity Log format rows coexist.
- Account filter state is lifted to `App.jsx` and shared between Calendar and Dashboard — don't duplicate it locally in a component.
- **Views stay mounted across tab switches (2026-07-15) — never add a new top-level view back to the old conditional-unmount pattern.** `App.jsx` tracks a `visitedViews` Set and CSS-hides (`display:none`) inactive views instead of removing them from the tree, so switching tabs doesn't re-run every view's fetches from zero. Consequence: any `setInterval`/polling effect in a view now keeps running while hidden unless it's gated. New polling code inside a view must read `useViewActive()` (`src/utils/useViewActive.js`) and add it to the effect's dependency array with an early `if (!isViewActive) return;` — see `ACDView.jsx`/`MarketPulseBar.jsx`/`PermSlipAndStackBar.jsx`/`LivePlaybookCard.jsx` for the pattern. Bounded, user-triggered job-status polls (e.g. `ACDBacktestRunner`, `PhaseChangeBacktestPanel`) are the deliberate exception — those should keep running while the user is on another tab. Full writeup, including a separate pre-existing Chrome-connection-limit bug found while shipping this: docs/OPEN_THREADS.md.
- **Don't keep adding to `App.jsx`.** It was 20K+ lines before the 2026-07-13 code-split (`git log` commit `7c9b22a`) moved most view-layer code out into `src/views/`; it's now ~1.8K lines, but the split isn't an excuse to reverse it by piling new code back in. When a change needs new UI, extract it into its own file (`src/components/dashboard/` or `src/views/`) instead of inlining it. If a constant or helper needs to be shared across view files, put it in `src/constants/` or `src/utils/` — don't leave it as an unexported local `const`/`function` in App.jsx. That exact mistake (`SETUP_DISPLAY_LABELS`, `SETUP_RESOLUTION_TEXT`, `CAL_SETUP_SHORT_LABELS`, `LR_SLATE`, `getSetupConviction` all defined locally in App.jsx pre-split) caused several `ReferenceError`s in `ACDView.jsx`/`CalendarView.jsx` after the split, since they referenced App.jsx-local bindings across a file boundary. User directive, restated 2026-07-13.
- **Unnecessary monolith files get an automatic, non-blocking nudge — not a fixed line-count limit.** User request 2026-07-18: "I want the system to decide when the file is too big... unless they need to be." `.claude/hooks/post-edit-filesize.sh` (`PostToolUse`, `Write|Edit`) compares a touched file's line count against its own cohort (sibling files in the same directory, same extension) using median + MAD (median absolute deviation, robust to an already-known legitimate outlier like `acd.js` — confirmed by direct measurement: `server/routes/`'s cohort median is ~255 lines, and `acd.js` at ~7980 would blow out a plain mean/stdev threshold for every other file in the directory). Fires only when the file is BOTH a statistical outlier for its cohort (modified z-score > 3.5, the standard Iglewicz–Hoaglin robust-outlier cutoff) AND grew by at least a cohort-median's-worth of lines since the last commit — this is what keeps it quiet on routine small edits to `acd.js` itself, only firing on genuinely new sprawl. `acd.js`'s size is a known, deliberate exception (its own "largest route file" note in this doc); the hook doesn't special-case it by name, the growth-gate does that naturally since small edits never trip it. Advisory only (`additionalContext`, informs the judgment call, doesn't block) — splitting isn't always right, cohesion sometimes matters more than line count.
- **Server-autonomous detection:** `server/index.js` runs a `setInterval` every 15s calling `GET /api/acd/setup-detection`, across the full CME Globex week (Sun 6PM–Fri 5PM ET, with the daily 5–6PM ET maintenance gap excluded) — not just RTH. This is intentional — it makes level fade detection independent of browser clients. Do not remove it. The INSERT in `acd.js` is `ON CONFLICT DO NOTHING` so concurrent client polls don't double-write. **This entry was stale from 2026-06-01 until 2026-07-19** (described a `GET /api/acd/today`, 60s, RTH-only poller that no longer exists under that name/schedule — the endpoint was consolidated and the schedule extended to full Globex hours on 2026-07-16, but this bullet was never updated to match). The same staleness had propagated into two frontend display strings (`AlphaEngineOverview.jsx`'s "Server Poll Cadence" card, `PlaybookView.jsx`'s scanning FAQ) — both said "60s, 9:30–4:00 PM ET" and both were fixed the same session, found while investigating a user report that several dashboard cards showed "Market Closed" during an actually-open Sunday-evening Globex session (`DayOfWeekPlaybookCard.jsx`'s weekend branch checked `getDay()` with no time-of-day check at all, and `App.jsx`'s `inGlobex`/`inRTH` sidebar logic had no day-of-week boundary, so it misread Saturday RTH-hours as "open"). **General lesson: a hard rule describing a poller's schedule/endpoint is exactly the kind of fact that silently drifts when the poller itself changes — if you touch the setup-detection poller's schedule or endpoint again, grep for "9:30" / "RTH" / "market hours" across `CLAUDE.md`, `src/components/dashboard/`, and `src/views/` in the same session, not just the code.**
- **Level fade detection gate:** `acd.js` line ~3838 checks `allRthBarsRow.rows.length >= 3` (not 60). IB-specific levels (`ibHighToday`/`ibLowToday`, `IB_MID_SCALP_FADE`, `OR_MID_AFTER_IB_FADE`) self-gate via `etMinNow >= 630` — do not restore the 60-bar gate.
- **sizeMultiplier is an IIFE** inside the `levelScalpSetup` object in `acd.js`. All sizing adjustments live there. When adding a new factor: (a) research backs it (N≥20), (b) add to the IIFE, (c) update `AlphaEngineOverview.jsx`, (d) document in OPEN_THREADS.
- **DAY_TYPE_MANAGED types need their bucket breakdown re-read, not just their headline recommendation.** A `DAY_TYPE_CONDITIONAL` type (currently `IB_BULLISH`/`IB_BEARISH`) staying off `SUPPRESS` can mean "genuinely strong on its good day-type" or "one thin/borderline bucket happens to clear the -$5 bar this week" — the recommendation string alone doesn't distinguish these, and the IB_BULLISH regression (found 2026-07-16, docs/OPEN_THREADS.md) happened because nobody re-checked the bucket numbers for two days after a fix. `.claude/hooks/session-start.sh`'s `DAY_TYPE_MANAGED WATCH` section now prints every such type's full bucket breakdown at the start of every session and flags (⚠️) any bucket that's the *sole* reason a type isn't `SUPPRESS`ed while sitting within $10 of the bar or under N=50 — read it, don't skip past it as boilerplate.
- **`active.sizeMultiplier` overwrite footgun:** the level-fade IIFE above sets a real, ~20-factor `sizeMultiplier` (0.10-1.5x) on `levelScalpSetup`. Later in the same `/api/acd/setup-detection` handler, the "Build full trade brief" block (`acd.js` ~line 5855, shared by every candidate in the `candidates` array — both `levelScalpSetup` and `ibSetup`) sets `active.sizeMultiplier` again for a completely different reason (post-loss "Death Sequence" protection). Found 2026-07-16: this second assignment used to unconditionally *replace* the first, silently discarding the entire elaborate factor stack down to a crude binary 0.5/1.0 — confirmed live via 100% of persisted `active_setups.size_multiplier` rows being exactly 0.500/1.000, zero variety. Fixed to apply the post-loss rule as an additional ceiling (`Math.min(existing, 0.5)` when `hasLossToday`) instead of a replacement. **Any future code that sets `active.sizeMultiplier` (or any other field a candidate object already sets in its own construction) after candidate selection must merge/cap, never blindly overwrite** — check what the candidate already computed first.
- **New setup type checklist (anti-hardcode gate) — also the checklist for auditing *existing* live setups, not just gating new ones.** Items 5-10 below were each found by retroactively checking an *already-live* setup, not by reviewing a new one before launch — if you're doing a broad audit pass over existing setup_types (see docs/OPEN_THREADS.md's "broader audit of all setup types" thread), these are exactly the categories of bug to check every one of them against: real stop/target simulation (5), half-wired integration (6), standalone-poller promotion gaps (7), Unified Signal Table status-mapping (8), fire-gate-vs-data-availability timing (9), and backfill-detection-window-vs-live-firing-window mismatch (10). Before any new `setup_type` goes live in `acd.js`, ALL of the following must be satisfied:
  1. Run `node scripts/backtest_setup_status.mjs` → generates/updates `SETUP_STATUS` row in `performance_audit`
  2. Run `node scripts/update_optimal_stops.mjs` → generates/updates `OPTIMAL_STOP` row with data-derived stop/target
  3. If N<20 resolved trades, do NOT fire live — insert as `status='SHADOW'` only until N≥20
  4. Never write a stop, target, or WR claim as a literal number in `acd.js` — always read from `liveStats._opt[type]` or `performance_audit`
  5. **Simulate the real stop/target as an actual bar-by-bar trade before ever calling a signal "validated."** A statistically stable *sign* on a raw point value (e.g. "price at bar+N") is necessary but not sufficient — it has no drawdown tolerance and is not a tradeable strategy. Found 2026-07-14: 3 of 4 minute-bar scanner signals passed day-clustering, time-of-day, and 3-way chronological stability checks, then turned negative EV (-$15.75, -$18.49, -$3.20/trade) the moment a real p75-MAE-stop/p50-MFE-target was simulated instead of the raw exit value. Only after this step should a signal be treated as a live candidate.
  6. **Verify integration, don't assume it.** New setup types are easy to half-wire: check `dropToTimeline()` is called from the actual insert path (not just the pattern copied from elsewhere), check `SETUP_DISPLAY_LABELS` (`src/constants/setupDisplay.js`) has an entry, check `isLongSetup()`/`inferDirection()` resolve correctly for the exact string being inserted. Found 2026-07-14: a new setup's live poller skipped `dropToTimeline()` entirely and had no display label — both silent, both would only surface once the setup was visible enough to matter.
  7. **If the setup lives outside the level-fade candidates array (its own poller — e.g. a bar-window statistical signal, not a level touch), it must replicate the dynamic SHADOW→ACTIVE promotion check manually.** The candidates-array insert path already does this correctly: `liveStats._suppressedSetups` is rebuilt from the latest `SETUP_STATUS` rows every trading day and checked fresh on every single fire (`server/routes/acd.js` ~line 4564). A standalone poller has no such built-in mechanism — if you hardcode `status='SHADOW'` at insert time with no live re-check, the setup sits in SHADOW **forever**, even after clearing N≥20 live trades with good EV, because nothing else in the pipeline will ever flip it. Found 2026-07-14 in `minuteBarSignalDetector.js`'s first version. Fixed with a `getLiveStatus()` function — queries real resolved `active_setups` trades for the type (N≥20 & EV>-$5 → `ACTIVE`, else `SHADOW`), called fresh on every fire — use it as the template for any future standalone poller.
  8. **If the calibration script writes a new `recommendation` value, register it in `/api/performance-audit/unified`'s status-mapping** (`server/routes/acd.js` ~line 6976) or the setup type is silently invisible in the Unified Signal Table (`BacktestView.jsx`'s "Unified Signal Table"). Found 2026-07-14: `SETUP_STATUS`'s actual vocabulary (`SUPPRESS`/`THIN_N`/`PROMOTE`/`DAY_TYPE_MANAGED`) matched none of the recognized strings there, hiding ~61 of 113 setup_types from the table.
  9. **If a setup's fire gate is earlier than the data it depends on for suppression, its suppression logic is a silent no-op.** Any setup exempted from the standard blended-EV `SUPPRESS` check (a `DAY_TYPE_CONDITIONAL`-style carve-out in `backtest_setup_status.mjs`, or a DOW/day-type check inline in `acd.js`) must have its fire-gate timing verified against when that dependency actually becomes available — check the actual gate condition (`etMin >= ...`), not just that a suppression check exists in the code. Found 2026-07-14: `IB_BULLISH`/`IB_BEARISH` fired off a deliberate 30-min IB window (`etMin>=600`, 10:00 ET) but their day-type suppression checks depend on `acd_daily_log.day_type`, which isn't classified until IB close at 10:30 ET — so the checks were guaranteed no-ops every time they fired, and `IB_BULLISH` ran live at -$27.81/trade EV (N=106) with zero effective guardrail. Fixed by moving the fire gate to `etMin>=630` (not the 30-min level *definition*, just when the alert is allowed to fire) and by giving `DAY_TYPE_CONDITIONAL` setups a real per-day-type floor in `backtest_setup_status.mjs` (SUPPRESS if every bucket with N≥20 is below the standard `-$5` bar) instead of unconditionally trusting the day-type carve-out. Full writeup: docs/OPEN_THREADS.md.
  10. **A backtest/backfill's detection population must match what the live poller actually fires on — window, touch-definition, and all.** If a historical calibration script restricts detection to a narrower window or stricter touch condition than the live code path uses, the resulting WR/EV describes a different, more selective strategy than what's actually live. Found 2026-07-14: `scripts/archive/backfill_level_fades.js` (source of every `resolution_method='BACKFILL'` row in `active_setups`, 72 setup_types) only scans for a level touch in a 10:30am-noon window, but live `acd.js` (`nearLevels`, ~line 4757) fires on the first RTH touch any time from ~9:34 AM — re-simulating `CAM_R4_FADE_SHORT` under live-matching "first touch anywhere in RTH" flipped it from +$51.72 EV (window-restricted) to **-$34.87 EV** (N=114), a real winner-to-loser flip, not noise. Fixed for all 34 base level families the same day, in 5 waves (`scripts/repair_cam_r4_s3_window_mismatch.mjs`, `repair_top8_window_mismatch.mjs`, `repair_remaining_window_mismatch.mjs`, `repair_ib_dependent_window_mismatch.mjs`, `repair_weekly_vwap_window_mismatch.mjs` — 32 total `performance_audit` recommendation flips), each using the *correct per-level formation gate* (verified against `scripts/compute_levels.js` directly, cross-checked against an independent Gemini classification pass) rather than a blind blanket re-test — same-day-forming levels (Opening Range, Initial Balance) need their own gate or the re-test itself introduces lookahead. **Two deeper bugs surfaced mid-fix, both now fixed**: (a) `IB_HIGH`/`IB_LOW` had a genuine *definitional* mismatch, not just a window one — live `acd.js` computed a 60-min IB, `compute_levels.js` (source of `level_prices`) computed a 30-min one, different price levels entirely. Git history showed the 60-min version predates the 30-min one by a month and matches this codebase's own day-type-classifier convention — strong circumstantial evidence, but not certainty against original design intent, so this was flagged to the user for explicit confirmation before fixing (user confirmed 60-min) rather than assumed unilaterally. (b) `WEEKLY_VWAP`'s own formula spanned through that week's Friday regardless of query date, a genuine lookahead bug — fixed by bounding the query to the date being computed. Both fixed at the source (`compute_levels.js`) plus full `level_prices` re-backfills (docs/KNOWN_ISSUES.md item 9 has the full account). **New lesson from this pass**: `compute_levels.js --backfill` silently skips dates under concurrent DB load — a per-date `try/catch` swallows the failure to `console.error`, invisible when output is piped through `tail`. Both re-backfills this session left ~40 dates stale despite reporting success. Never trust a truncated console summary as proof a `--backfill` run is complete — verify with `level_prices.computed_at` freshness (or equivalent) across the full expected date range before treating it as done, especially with other DB load happening concurrently (the live server's 60s poller, other scripts). When building or re-verifying any backfill/calibration script, diff its detection window/conditions against the live poller's actual firing logic — don't assume they match just because they share level names, and don't assume two functions computing "the same" derived level (like IB) actually use the same window without checking both directly. When a fix requires choosing between two long-standing, differently-implemented conventions and the "correct" one isn't 100% certain from code alone, surface the evidence and ask before rewriting shared historical data — don't decide unilaterally just because the evidence leans one way.
  11. **A level with zero real touches ever is not automatically SHADOW-safe — the generic auto-discovery pipeline can't produce a row for it.** `backtest_setup_status.mjs` (the only source of `_suppressedSetups`) groups `active_setups` rows by `setup_type` — a type with literally 0 rows never appears in its result set, so it can never get even a `THIN_N` row from the standard pipeline no matter how many weekly runs pass. Since the live candidates-array insert gate treats "absent from `_suppressedSetups`" as "not suppressed," a rare-touch level wired into `keepLevelsAll` with zero history sits fully exposed — if it's ever touched, it fires as a full unsuppressed `ACTIVE` candidate, sized off whatever `UNIFIED_BACKTEST`/`ls()` stats happen to exist (which, per item 10's sibling incidents, can be stale/wrong methodology). Found 2026-07-19 via a systematic sweep (136 signal_names checked directly, one query, not sampled): 6 already known (`PD2_VAH`/`PD2_VAL`/`2D_POC` — the trigger for the sweep) plus 3 more never-checked (`3M_VAL_FADE_LONG/SHORT`, `PY_POC_FADE_SHORT`, `PY_VAL_FADE_LONG/SHORT`) — all genuinely rare, wide, month/year-scale magnets, matching the already-known "these mostly don't resolve within one RTH session" finding for `MONTHLY_VWAP`/`PY_*` (see "Where to look"). Fixed by manually seeding a `THIN_N` placeholder row for each (documented in `notes` as a manual seed, not a real calibration) — `backtest_setup_status.mjs` self-heals to a real row the moment N≥1 real touch occurs, same as everything else. **When wiring any new level into `keepLevelsAll` (or auditing an existing one), check `SELECT 1 FROM performance_audit WHERE signal_type='SETUP_STATUS' AND signal_name=<type>` before considering it "covered" — an absent row is not the same as a `THIN_N` row, and only one of them is actually safe.**
  - The `Stop` hook (`.claude/hooks/check-docs-drift.sh`) scans for hardcoded anti-patterns and will warn if any of these are violated. The `SessionStart` hook checks for setup_types missing a fresh SETUP_STATUS row.
- **`active_setups`' `existingSetup` dedup check only prevents two *simultaneously open* rows — it does NOT stop the same exact touch instant from being inserted twice once the first one resolves.** Found 2026-07-17: `IB_HIGH_FADE_SHORT` duplicated on every single fire that day (10:39, 14:05, 14:30, 14:54, 14:55, 14:58, all pairs with identical `fired_at`), traced to a `sessionEndET` hard cap (`acd.js`, "Hard cap" comment near `computeExpiry`) hardcoded to **1:00 PM ET since the file's original 2026-06-01 commit** — never updated as detection extended to full RTH (4 PM) and then to full Globex hours (2026-07-16). Any setup detected after 1 PM got an `expires_at` already in the past at the moment of INSERT, so the next 15s poll instantly expired it, and because the dedup check (`existingSetup`, `WHERE status IN ('ACTIVE','SHADOW')`) only looks at still-open rows, the poll after that re-inserted a fresh duplicate for the exact same touch. Fixed the cap to 4:00 PM ET, rolled forward a day if already past (so it can never land in the past even for an overnight Globex fire), **and** added a second, unconditional unique index `idx_as_unique_touch_instant` on `(trade_date, setup_type, fired_at)` (alongside the existing partial `idx_as_unique_setup` on ACTIVE/SHADOW only) so the exact same touch instant can never produce two rows regardless of status — the real structural fix, not just the one root cause. **All `ON CONFLICT` clauses against `active_setups` (7 total: 6 in `acd.js`, 1 in `minuteBarSignalDetector.js`) are now bare `ON CONFLICT DO NOTHING`** (no explicit target) so they catch a violation against *either* unique index — a targeted `ON CONFLICT (...)` clause only suppresses a violation against the index it names; adding a new index without updating every existing targeted clause would have reintroduced the exact "duplicate key value violates unique constraint" crash-spam bug from the 2026-07-16 fix. Repaired 13 pre-existing duplicate pairs across all history (backup: `active_setups_duplicate_touch_backup_20260717` / `trade_timeline_events_duplicate_backup_20260717`, see `docs/DB_BACKUP_CATALOG.md`) — removing one changed `FLOOR_S1_FADE_LONG`'s trade population enough to require an `update_optimal_stops.mjs` re-run (expected, not a bug: a genuine data correction shifts the sweep). **When adding any future time-based cap/expiry in this file, verify it can never evaluate to a timestamp in the past relative to "now" at ANY hour the app can legitimately be running** — now that detection runs across full Globex hours, a same-day-only hardcoded hour is never safe without a rollover check.
- **`scripts/backtest_unified.js`'s `detectLevelFades()` had the exact window-mismatch bug already fixed for the CAM_R4/CAM_S3 family back on 2026-07-14, just never fixed here — a blanket `IB_CLOSE=630`-to-`AM_CUT=720` (10:30 AM-noon) scan window applied to ALL ~65 level families it computes, plus zero walk-forward stability checking on this pipeline at all (the sole one of CLAUDE.md's standing "all 4" rigor-diagnostic pipelines missing it).** Found 2026-07-20 answering a direct user question ("what's the 10:30-12 thing, should it go") — the live system fires on the first RTH touch anytime from ~9:34 AM to 4:00 PM, so this backtest was silently discarding every real touch outside a 90-minute window, for every level, regardless of whether that level actually needs to wait for anything. **Fixed with care, not a blind removal**: verified each of the ~65 level names in `fadeLevels` against its own computation function (`buildIbMids`/`buildRollingVAs`/`buildTwoDayPOC`/etc. all window strictly *before* the current date) to confirm only 6 are genuinely same-day-forming (`OR_HIGH`/`OR_LOW` need gate 575 — bars 570-574 to complete; `IB_HIGH`/`IB_LOW`/`IB_MID_SCALP`/`OR_MID_AFTER_IB` need gate 630 — IB to close) — reused the exact per-level gate values already established and verified across the 2026-07-14 repair-script waves (`repair_cam_r4_s3_window_mismatch.mjs` etc.) rather than re-deriving them, since re-deriving from scratch is exactly what caused the original bug. The other ~59 levels now scan from RTH open (570) through close (960), matching live firing behavior. **A comparison against the CAM_R4 repair script's own numbers initially looked like a discrepancy (this fix: N=52 EV=+$42 vs. that script's N=114 EV=-$34.87) and triggered a full stop-and-recheck before trusting anything** — turned out to be an invalid comparison, not a bug: `backtest_unified.js` and the dedicated repair script are separate implementations with different stop/target methodology (blanket 90pt/40pt here vs. CAM_R4-specific 85.44pt/40.38pt there) that were never meant to produce matching numbers; the correct comparison is this script against its own pre-fix output (`CAM_R4_SHORT`: N=37 EV=-$4 → N=52 EV=+$42), which is a normal, expected shift from capturing more real touches. **Also added `computeRigor()` chronological-stability checking to this pipeline** (previously the only one of the "all 4" standing pipelines without it), directly answering a user "can we walk this forward" request rather than trusting the corrected numbers at face value: of 99 `ACTIVE`/`PROMOTE` recommendations the corrected backtest produces, only 23 (23%) are chronologically stable — 76% fail the same 3-way sign-stability check used everywhere else in this codebase, matching the exact pattern already found in `SETUP_STATUS` the same night. **Practical live impact is bounded**: `_suppressedSetups` (what gates live firing) reads `SETUP_STATUS`, never `UNIFIED_BACKTEST` — this fix corrects the fallback sizing/tier/filter data (`lv.ev` in `acd.js`'s `keepLevelsAll`) used for whatever's currently firing in `SHADOW` mode, but doesn't change what fires as a full live alert today. **Not done this session**: individually classifying the 76 unstable recommendations into degrading/improving/noisy (same as `backtest_setup_status.mjs`'s own `classifyTrend()`) — the rigor flag is now visible and persisted, but nothing reads/acts on it yet for this pipeline specifically.
- **`resolveSetupsByPrice()`'s general (non-trail) branch only ever produced `TARGET_HIT`/`STOP_HIT` — a setup that ran out of bars without hitting either fell through with `resolution=null`, relying entirely on `expireStaleSetups()` (a separate function, called right after in the same poll cycle) to force-close it, which set `resolution='TIME_EXPIRED'` and never touched `actual_pnl` at all — a permanent null, not a stale one.** Found 2026-07-20 while investigating why `TRT_LONG`/`TRT_SHORT` (and 16 other setup_types — `IB_BULLISH`/`BEARISH`, `BRACKET_BREAKOUT_*`, `OPEN_TEST_DRIVE_*`, `C_STANDALONE_*`, `VALUE_AREA_RESPONSIVE_*`, `OPEN_DRIVE_*`, `FAILED_AUCTION_*`, `TRT_MAH_*`) had a large population of `resolution='EXPIRED', actual_pnl=NULL` rows. The original 341-row batch shared one bulk-insert `created_at` (2026-06-15) and its writer script no longer exists in git history (exhaustive search across all commits/patches found nothing — concluded uncommitted one-off) — but confirming the bug was *only* historical would have been wrong: 46 more null rows existed with `created_at` spread across 2026-06-24 through 2026-07-17 (as recent as 3 days before being caught), proving `expireStaleSetups()`'s blunt no-pnl force-close was live and ongoing, not a closed incident. **Fixed at the root**, not just backfilled: `resolveSetupsByPrice()` now fetches `expires_at` and a shared `nowEt` (naive ET text, same convention as `fired_at` — see the timezone rule below) up front, and after its per-bar loop exhausts with no stop/target hit, marks `TIME_EXPIRED` via the last available bar's close (`method='MARK_TO_MARKET'`) whenever `expires_at` has passed and at least one real bar exists — reusing the exact same UPDATE/dropToTimeline/io.emit path TARGET_HIT/STOP_HIT already use, so this is a pure branch addition, not new plumbing. `expireStaleSetups()` itself was also fixed as the genuine backstop (rows with no entry/stop/t1 to walk, or truly zero bars since `fired_at`): it now looks up the single most recent known close and mark-to-markets against that (`method='MARK_TO_MARKET'`) instead of leaving `actual_pnl` null by default — only rows with no entry price recorded, or where price data has never arrived at all, stay genuinely null (`method='NO_PRICE_DATA'`). Both new method strings kept ≤20 chars per the `VARCHAR(20)` lesson immediately below. Backfilled both batches (341 + 46 rows, backup tables `active_setups_null_pnl_recovery_backup_20260720` / `_batch2`, script `scripts/repair_null_pnl_shadow_setups_20260720.mjs --batch2`) via the identical bar-walk logic now living in production code — 21 rows remain genuinely un-scoreable (no price data ever arrived in their window, including one row with a corrupted `expires_at < fired_at` predating this session). **A parallel, narrower gap was found and deliberately left open, not silently fixed**: `INVALIDATED`/`SESSION_CLOSED` resolutions also never get `actual_pnl` computed anywhere — but unlike this bug, `backtest_setup_status.mjs` never reads their pnl in the first place (`WHERE resolution IN ('TARGET_HIT','STOP_HIT')`), so they aren't silently corrupting live calibration today. Flagged as `OPEN_DECISION` `invalidated_session_closed_setups_never_get_actual_pnl` (MEDIUM) rather than fixed reflexively, since `INVALIDATED` means "the premise broke," not "time ran out," and deserves its own design call on whether `POST_ENTRY`-timed invalidations should get a real mark-to-market. **General lesson, matching the TIME_EXPIRED-mark-to-market convention established the same week for `FLOOR_R1_FADE_SHORT_TRAIL`**: any code path that can terminally close an `active_setups` row must compute a real `actual_pnl` in the same transaction — a "we'll catch it in a later cleanup pass" resolution path is exactly how a structural bug hides as a one-off for weeks. **RESOLVED same week (2026-07-20, new session, user-confirmed)**: `structurallyInvalidateSetups()` now computes a real mark-to-market `actual_pnl` whenever `invalidationTiming==='POST_ENTRY'` (same `isLongSetup`/`LIVE_INSTRUMENT` pattern as everywhere else), leaving `PRE_ENTRY` null on purpose (no real entry occurred). Backfilled the 11 pre-existing historical rows (`scripts/repair_invalidated_post_entry_pnl_20260720.mjs`, backup `active_setups_invalidated_pnl_backup_20260720`). `SESSION_CLOSED` remains deliberately untouched — a separate, still-open question the user did not approve fixing this pass.
- **Any string literal written into a short `VARCHAR(N)` column (`resolution_method`/`resolution`/`actual_outcome`/`replay_resolution` are all `VARCHAR(20)`; `status` is `VARCHAR(10)`; `origin_status` is `VARCHAR(12)`; `performance_audit.signal_type`/`.recommendation` are `VARCHAR(20)`) throws a raw, uncaught `value too long for type character varying(N)` Postgres error, not a graceful truncation — and if that column is written from inside a shared per-row loop (like `resolveSetupsByPrice()`'s single loop over every active/shadow setup), ONE bad literal crashes resolution for every OTHER setup in that same poll too, not just the one row.** Found 2026-07-20: `scripts/repair_null_pnl_shadow_setups_20260720.mjs`'s own `'RECOVERY_MARK_TO_MARKET'` (23 chars) hit this on its first real (non-dry-run) write, which prompted checking whether the same class of bug existed anywhere else — it did, already committed and pushed: `resolveSetupsByPrice()`'s breakeven-trail branch used `'SAME_BAR_ARM_AND_STOP'` (21 chars) and `'BREAKEVEN_TIME_EXPIRED'` (22 chars), both over the `resolution_method` `VARCHAR(20)` limit, sitting live and untriggered only because neither code path had fired yet in production. Fixed both (`SAME_BAR_ARM_STOP`/`TRAIL_TIME_EXPIRED`, ≤20 chars) and swept the whole project: grepped every `≤20`-char `VARCHAR` column in `active_setups`/`performance_audit` against every literal/derived value actually written to it in live code (`server/routes/acd.js`, every non-archived backfill/repair script) — no other overflow found, but did find a related near-miss: `scripts/flag_decision.mjs`/`scripts/record_claim.mjs` write an arbitrary-length human-authored `slug` straight into `performance_audit.signal_name` (`VARCHAR(60)`) with zero length check, and one live `OPEN_DECISION` slug was already sitting at exactly 60/60 chars — the next long slug would have crashed with the same error class. Added a fail-fast length guard to both (throws a clear message over 60 chars — deliberately not silent truncation, since two distinct 61+ char slugs could collide on the same truncated prefix). **When adding any new hardcoded status/method/label string anywhere in this codebase, check it against its target column's actual declared width in `server/schema.sql` before assuming it fits — a string that reads as "short enough" by eye is not the same as verifying the number.**
- **`SHADOW_SETUP_TYPES`-style hardcoded classification lists are the same anti-pattern as a hardcoded suppression list, even when they're "just" a display filter.** Found 2026-07-17: `/api/setups/history`'s hardcoded `SHADOW_SETUP_TYPES` array (used to hide non-live-traded setup_types from the default Setup Log view) had gone stale — 3 of its ~27 entries (`OPEN_DRIVE_SHORT`, `VALUE_AREA_RESPONSIVE_LONG`, `VALUE_AREA_RESPONSIVE_SHORT`) were hardcoded as permanently hidden while the live SETUP_STATUS pipeline had already promoted all three to `ACTIVE` — real, tradeable setups were being permanently hidden from the user's own trade log by a list nothing ever re-synced. Fixed by replacing it with `getShadowSetupTypes()`, which reads `performance_audit` SETUP_STATUS fresh on every call (`recommendation IN ('SUPPRESS','THIN_N')` — identical logic to the live candidates array's `liveStats._suppressedSetups`), falling back to a much shorter hardcoded `UNCALIBRATED_SHADOW_TYPES` list only for setup_types with no SETUP_STATUS row at all yet. Same lesson as the existing "unified suppression pipeline" convention below — apply it to any hardcoded list that gates what the user sees, not just ones that gate what fires live. **Follow-up, same day**: the "much shorter" fallback list itself had *already* quietly reproduced the exact bug it replaced — 22 of its 27 entries had picked up real `SETUP_STATUS` rows (all correctly `SUPPRESS`/`THIN_N` that day, so no visible symptom yet), and several were thin-N-but-positive-EV types that would've stayed hidden forever past a future `PROMOTE`. Pruned to the 5 genuinely-uncalibrated entries, moved it to `server/config/setupTypes.js` (`UNCALIBRATED_SHADOW_TYPES`, importable rather than re-typed), and added `test_invariants.mjs` check `[6]` to re-verify every entry against live `SETUP_STATUS` on each run so it can't drift stale again unnoticed. **General lesson**: a hardcoded fallback added to patch a gap in a dynamic query needs the *same* standing staleness check as the thing it was patching — "much shorter" is not the same as "self-maintaining."
- **Trading-day dates: always derive from SQL `CURRENT_DATE`, never JS `new Date().toISOString()`.** The DB server runs in `America/New_York`; JS's `toISOString()` is UTC, and the two disagree once past 8PM ET — a full calendar day earlier in JS-UTC-land than the DB's own ET-based "today." This caused 3 separate same-day bugs on 2026-07-14 (`backtest_minute_bar_scan.mjs`, and pre-existing `backtest_setup_status.mjs`) where a script's own `run_date` silently drifted a day from its siblings', breaking every `MAX(run_date)`-based "latest run" join `performance_audit` consumers rely on (including `/api/performance-audit/unified`). Fetch it with `SELECT CURRENT_DATE::text as today` in every new backtest script — never `new Date().toISOString().slice(0,10)`.
- **Never parse a naive (no-timezone) timestamp string with `new Date(str).toISOString()` — it silently depends on the ambient process/system timezone, not on any timezone you actually intend.** Found 2026-07-16 in `server/parsers/sierraParser.js`'s `parseDateTime()`: Sierra Chart export strings carry no timezone marker but are always America/New_York wall-clock time. `new Date("2026-07-14 10:48:58").toISOString()` gives a *different* answer depending on whether the process happens to be running with ambient timezone UTC or America/New_York — and this system's ambient timezone silently changed at some point, so **every trade imported from 2026-06-09 onward was stored 4-5 hours off** (4h in EDT, 5h in EST — a flat offset is wrong, the correct shift depends on whether the date being parsed falls in EDT or EST) while imports before that date (which happened to run under an environment resolving to UTC) were stored correctly. The frontend (`AllTradesView.jsx`/`CalendarView.jsx`) formats `entry_time` by treating the stored value as a genuine UTC instant and converting to `timeZone: 'America/New_York'` for display — so the post-06-09 bug's output *happened* to display correctly (two wrongs cancelling out) while the pre-06-09 *correct* storage displayed 4-5 hours **early**, the exact opposite of what either the user or Claude initially assumed from "recent trades look right." Also silently corrupted `log_date` (computed via `entryDateTime.split('T')[0]` on the already-wrong shifted string) for any trade whose real ET time was within ~4-5 hours of midnight, since the shift could push it across a calendar-day boundary. **Root cause of the timezone flip itself never fully identified** — likely a one-off manual script/backfill run from a different shell environment during a period with no corresponding git commits (2026-06-03 → 2026-06-12 gap) — but the fix doesn't depend on knowing that: `parseDateTime()` now explicitly computes the naive string's real UTC-equivalent instant via `Intl.DateTimeFormat`-based ET anchoring (`etNaiveStringToUtcIso()`, same file), verified byte-identical regardless of ambient `TZ`. **The pattern to reuse for any future naive-local-timestamp parsing**: guess a UTC instant from the naive components, ask `Intl.DateTimeFormat` what that guess actually looks like in the target zone, then correct by the wall-clock difference — this correctly handles both EDT and EST per-date, unlike a hardcoded `+4`/`+5`.
  - **Same bug produced two full-history duplicate-import events, both cleaned up 2026-07-16** (backup: `trades_backup_20260716`, full 40,453-row pre-repair snapshot): a 2026-06-09 mass re-import of already-existing pre-06-09 trades through the by-then-broken parser (4,100 rows, `scripts/repair_trades_dedupe_20260609_batch.mjs`) and a separate, unrelated 2026-04-22 re-import of the original 2026-03-11 mass historical load (1,168 rows, no time-shift involved, `scripts/repair_trades_dedupe_20260422_batch.mjs`, root cause not investigated). A third pass (`scripts/repair_trades_dedupe_remaining.mjs`, 3,769 rows) found a structural Sierra Chart TAL quirk unrelated to either import event: the same completed flat-to-flat position is logged on **multiple separate lines** (one tagged with a trailing ` BP`, one with ` EP`, sometimes a third with neither), and the importer inserted one `trades` row per line instead of recognizing them as one position — confirmed directly (`custom_fields->'sierra_data'` raw strings identical except for the BP/EP suffix) and independently audited by Gemini before deletion. All three passes verified zero `trade_screenshots` (the only FK referencing `trades.id`) impact before deleting. `trades` total: 40,453 → 31,416.
  - **Process lesson**: don't trust "recent data looks right" as proof a timestamp pipeline is healthy — this bug's two halves (wrong storage, compensating wrong display formatting) each looked fine in isolation depending on which period you happened to check. Verify via the *raw* `custom_fields->'sierra_data'` string against the stored column directly, not via the rendered UI, when auditing a timestamp pipeline.
- **Rigor diagnostics are standing, not one-off.** Day-clustering (top-5-dates % of N) + 3-way chronological sign-stability run on every weekly mining/calibration pass now, across **all 4** statistical pipelines in this codebase (`backtest_setup_status.mjs`, `mine_minutebar_conditions.mjs`, `patternScannerService.js`'s `mineLevelFades()`, `mine_tod_patterns.mjs`). These are informational — they do NOT feed SUPPRESS/PROMOTE or ACTIVE/DEGRADED logic — because "unstable" doesn't mean "losing," it can mean genuinely improving. Don't blanket-suppress on these flags; classify each one (degrading vs. improving vs. noisy-but-stable vs. too-thin-to-tell) before acting, the same way the 2026-07-14 audit did. Real result from the first full pass across all 4: level-fade patterns were the worst offender (**0 of 13 currently-`ACTIVE` patterns are clean** — all clustered, because that miner only uses a 90-day window and level touches aren't independent day-to-day), `mine_tod_patterns.mjs` was the best (6 of 7 clean) since its unit of analysis is already one-observation-per-day. **This was already fully wired end-to-end (backend computed + exposed via `/performance-audit/unified`'s `stabilityTrend`/`stabilityStable` fields, frontend shown as a "Stability" column in `BacktestView.jsx`'s Unified Signal Table) but had zero aggregate/summary view anywhere — found 2026-07-20 while investigating a user question about whether the live setup roster's backtested edge holds up: rediscovered the entire mechanism via manual `grep`/DB query because nothing in `ARCHITECTURE.md`/this file ever documented it existed.** Full current snapshot at that check: of 73 live (non-`SUPPRESS`/`THIN_N`) setup_types, 18 `STABLE`, 17 `IMPROVING`, 13 `NOISY_BUT_STABLE`, 9 `AMBIGUOUS`, 8 `THIN`, only **5 `DEGRADING`** (`DAILY_OPEN_FADE_SHORT`, `IB_MID_SCALP_FADE_LONG`, `FLOOR_R1_FADE_SHORT`, `IB_BEARISH`, `MONTHLY_OPEN_FADE_SHORT`), 3 `NO_DATA` — added the missing aggregate view as `GET /api/acd/rigor-stability-coverage` (`server/routes/acd.js`) + `RigorStabilityPanel` in `AlphaEngineOverview.jsx` so this doesn't require a manual query again. **General lesson, matching the "check ARCHITECTURE.md before re-deriving" rule below**: a mechanism can be fully built, wired, and even displayed in one place, and still be practically undiscoverable if nothing in the canonical docs says so — "is this already surfaced somewhere" needs its own grep of `CLAUDE.md`/`ARCHITECTURE.md`, not just of the code.
- **Share modules when the same logic would otherwise be reimplemented.** The rigor diagnostic above was independently hand-written **three separate times** on 2026-07-14 (one per script) before being centralized into `server/services/rigorDiagnostics.js` (`computeRigor()`) — the exact inconsistency this principle exists to prevent. Any script computing the same kind of check (day-clustering, chronological stability, or similar) must import the shared version, not write a 4th copy. `computeRigor` takes a generic `pnlFn` so callers without a real dollar EV (e.g. `mine_tod_patterns.mjs`'s pure hit-rate claims) can pass a +1/-1 hit/miss proxy instead — the stability math is identical either way. **Same precedent repeated 2026-07-21**: the held-out replication check (see the confound checklist above) had already been hand-written twice ad hoc (`regime_c_persistence_debunked_placebo_test`, `globex_large_moves_start_near_pit_safe_levels`) before a third real instance (`volume_confirmed_candle_pattern_low_vol_trap`'s retraction) prompted centralizing it as `computeReplication()` in the same file — import it, don't write a 4th copy either.
- **`minePatterns()` (`patternScannerService.js`) is a known, unaddressed gap** — the 6 hand-coded "next-day tendency" hypotheses (session-type → next-day bias, gap-fill rate, etc.) have no persistence, no ACTIVE/DEGRADED lifecycle, and recompute fresh on every call, unlike the other 4 pipelines. Structurally different enough (no discovery table, no rigor check applicable in its current form) that it needs its own design pass rather than a bolt-on fix — not done as of 2026-07-14, flagged for a future session.
- **Unified suppression pipeline (the only suppression source):** All suppression of level fade setups flows through `scripts/backtest_setup_status.mjs` → `performance_audit` SETUP_STATUS/SETUP_STATUS_DOW → `liveStats._suppressedSetups` / `liveStats._dowSuppressToday`. Never add a hardcoded suppression list or day-of-week skip list to `acd.js` — the pipeline handles it automatically and re-evaluates weekly.
- **The `PROMOTE` recovery-detection check (`backtest_setup_status.mjs`'s `wasSuppressed && rec90.n>=15 && rec90.wr>=0.52 && rec90.ev>0`) never checked whether the "recovery" trades were real, and 15 was already below this codebase's own N≥20 significance floor.** Found 2026-07-20: `FLOOR_S1_FADE_LONG` (all-time N=115, EV=-$9.68, rigor-confirmed negative in all 3 chronological thirds — never once positive, -$1,113 total, a genuine stable loser) was live via `PROMOTE`, triggered entirely by 15 `BACKFILL`-origin (synthetic) trades averaging +$8.33 — zero real trades behind the override. Checked the other 6 currently-`PROMOTE` setups the same way: 5 more (`CAM_R1_FADE_SHORT`, `CAM_S1_FADE_LONG`, `OPEN_DRIVE_LONG`, `PD_POC_FADE_SHORT`, `PD_VAL_FADE_LONG`) had `real_n` of 0-4 in their recovery window (3 of them exactly 0 — 100% synthetic), only `IB_LOW_FADE_LONG` (real_n=6) had genuine backing. **Fixed** by adding `PROMOTE_MIN_REAL_N = 5` (reusing `classifyTrend()`'s own existing N<5 "too thin" cutoff, not a new arbitrary number) requiring at least that many `ACTIVE`/`SHADOW`-origin trades within the same 90-day recovery window, alongside the existing raw-N/WR/EV bars. Re-ran `backtest_setup_status.mjs`: all 6 fake recoveries correctly reverted to `SUPPRESS`, `IB_LOW_FADE_LONG` correctly stayed live. This is a narrow, concrete instance of the broader `OPEN_DECISION` `setup_status_calibration_ignores_origin_status_backfill`, **fully resolved the same night**: `backtest_setup_status.mjs`'s main `allTimeQ` now also counts `real_n` (ACTIVE/SHADOW-origin only), and any setup_type whose blended `n` clears `SUPPRESS_MIN_N=20` but whose `real_n` doesn't now gets `THIN_N` (the same SHADOW-firing, auto-clearing-when-real-N-reaches-20 treatment already used for low-blended-N types — zero new mechanism, just the existing N≥20 floor applied to the count it always claimed to be checking). Effect, run live 2026-07-20: 64 more setup_types flipped `ACTIVE`→`THIN_N` (full list and real_n for each in the commit), leaving only 5 genuinely "live" (non-`SUPPRESS`/`THIN_N`) setup_types system-wide: `IB_BULLISH`/`IB_BEARISH` (`DAY_TYPE_MANAGED`, deliberately exempted — see below), `MOMENTUM_60m_60m_BALANCE_FADE`/`MOMENTUM_60m_60m_TREND` (already-documented orphaned-calibration rows, zero `active_setups` history under that name, untouched by this fix since the loop only iterates rows that exist), `WPP_FADE_SHORT_GAP_UP` (written by its own dedicated `backtest_wpp_short_gap.mjs` per the Conditional Variant convention, not this script). **`IB_BULLISH`/`IB_BEARISH` deliberately NOT fixed the same way** — their `DAY_TYPE_CONDITIONAL` branch computes per-day-type buckets (a fundamentally different mechanism from the single ACTIVE/THIN_N toggle, consumed differently downstream via day-type-specific sizing, not `_suppressedSetups`), and applying a real-N floor there needs its own design pass, not a same-night bolt-on — real EVs for both were already independently checked and are thin-and-negative (`IB_BULLISH` -$159 N=2, `IB_BEARISH` -$101 N=1), so the exposure is known even though unfixed. Every flip is honest, not punitive: `THIN_N` still fires `SHADOW`, still resolves, still accumulates real data, and self-clears the moment `real_n` reaches 20 on a future weekly run — nothing here is a dead end.
- **Conditional variant setup checklist** — when a single level name generates two setup types based on entry condition (e.g., `WPP_FADE_SHORT` vs `WPP_FADE_SHORT_GAP_UP`):
  1. Add the override to `resolveSetupType()` in `acd.js` (defined just above the `nearLevels` filter). This is the **single source of truth** — both the live path and early-touch backfill call it.
  2. Write a dedicated backtest script (`scripts/backtest_<variant>.mjs`) that persists SETUP_STATUS + OPTIMAL_STOP rows for the new type. Add it to `run_weekly_backtests.sh` under "Conditional-variant backtests".
  3. Add an entry to `CONDITIONAL_VARIANTS` in `server/config/setupTypes.js` with `baseType`, `direction`, `condition`, `backtestScript`. If the type name doesn't encode direction via a standard suffix, also add it to `CONTEXTUAL_DIRECTION_TYPES`.
  4. Update `AlphaEngineOverview.jsx` `suppressed` array — add entries for both the suppressed base type and the active variant, noting the condition.
  5. Add a note to `docs/OPEN_THREADS.md` to monitor variant at N=50.
  6. **Run `node scripts/test_invariants.mjs`** — this catches everything else automatically: OPTIMAL_STOP coverage, direction inference correctness, backtest script on disk, base type suppressed, and (added 2026-07-17) that every `OPTIMAL_STOP` row's stored `ev_per_trade` actually matches a fresh re-derivation of its own `optimal_stop`/`optimal_target`. If it passes, the pipeline is complete.
- **`antigravityEdges.js` Promise.all ordering footgun:** `getLiveEdgesContext()` fires all DB queries in a single `Promise.all` wave (20 entries). The destructured variable names are **positionally bound** — adding or removing a query shifts every entry below it to a wrong variable with no runtime error. Always update the destructuring list and the array body together, and check the index comments (0–19) when inserting. Cache-miss-only entries (indices 0–3) use `isCached ? Promise.resolve(null) : query(...)` — do not move them to a later position without updating the cache skip logic.

- **`acd.js` block-scoping footgun:** `liveStats` is declared with `let` inside the level-fade block (`if (currentPrice && allRthBarsRow.rows.length >= 3)` at line ~4065, inner `if (last5.length >= 3...)` at ~4108). Anything outside those blocks — including `const candidates`, the `for (const cand of candidates)` loop, `shadowCandidates`, and the INSERT body — is outside `liveStats`'s scope and will throw a ReferenceError if it references `liveStats` directly. The fix pattern is `getCached(todayET, 'levelFadeStats')` at the outer scope (already done at line ~5064). **When adding any new reference to `liveStats` in `acd.js`, check whether the call site is inside or outside the level-fade block.** Similarly, `ls()` and `lsMon()` are only valid inside the inner block — do not call them from the `candidates` array or below.**

## Collaboration

- **During a multi-threaded investigative session — several items in flight, one branching into another — proactively state what's currently pending on the user, don't wait to be asked.** User stated explicitly 2026-07-19: "You need to persist some rule that during these rabbit holes, if your waiting for me and your pursuing multiple items. You need to let me know what you're currently waiting for me on." Said right after a session that pursued four sequential threads (a target-sweep overfitting fix, a baseline bug found and fixed in the scale-out script, a commission-handling fix, a walk-forward comparison) and correctly flagged four real `OPEN_DECISION`s along the way via `scripts/flag_decision.mjs` — but never once stated a running status, so the user had to explicitly ask "are we still waiting on something?" to find out four items were sitting open. Recording a decision correctly is necessary but not sufficient: the `OPEN_DECISION` mechanism guarantees nothing gets buried *forever* (it resurfaces every session start), but does nothing to keep the user informed *in the moment* while several things are still in flight. Whenever a new `OPEN_DECISION` gets flagged mid-session, or multiple threads are open at once and one becomes genuinely blocked on user input, say so explicitly in the next reply — a short, direct status line — not just a properly-recorded database row the user would have to run `flag_decision.mjs --list` to discover. Full account: memory `feedback-proactive-pending-status`.
- **Don't give up on a thread without vetting it thoroughly — persist to a real, root-caused answer, not the first plausible-looking stopping point.** User stated explicitly 2026-07-18: "You're taking the lead. I can't have you giving up on something without vetting it thoroughly. Persist that." Said right after a GARCH-scaled-stop investigation where the first result (fitted GARCH alpha reading ~0 every single day) could have been accepted at face value as "this whole approach is fake" — instead, root-causing it surfaced a real ES-symbol-contamination bug driving that reading, then a second numerical-degeneracy bug once that was fixed, then a third, unrelated stale-calibration bug in the consuming script. The real answer (a genuine but modest effect) was only reachable by refusing to stop at any of the earlier "looks debunked" checkpoints. **A result that looks like a dead end is exactly as suspect as a result that looks too good** — the audit discipline already applied to suspiciously positive numbers (Gemini output, "verified" comments) applies equally to suspiciously conclusive negative ones. When something comes back surprising, too clean (identical value across every observation), or flatly debunking, treat it as a lead to root-cause mechanically, not a stopping point — only stop once the result is stable under continued scrutiny, not merely once you've found *an* explanation that sounds plausible. Full account: `docs/OPEN_THREADS.md`, memory `feedback-session-style`.
- **"Self check" (user says "have you done a self check" or similar) means running this full list, not a vibe-based "yes, looks good":**
  1. `bash .claude/hooks/check-docs-drift.sh` — the persistence self-check (CLAUDE.md/ARCHITECTURE.md drift vs structural code changes) and the hardcoded-threshold-pattern scan. Report anything it flags; distinguish pre-existing hits (check via `git blame`/`git log` whether the flagged line predates this session) from genuinely new ones.
  2. `node scripts/test_invariants.mjs` — must be reviewed line by line, not just checked for a nonzero exit code; note any WARNING even if unrelated to this session's work, and confirm via `git stash` (or equivalent) whether it's pre-existing.
  3. `npm run lint:frontend`, `npm run lint`, `npm run build` — all three, not a subset, even if only one layer (frontend/backend) was touched.
  4. Tail `scratch/server_errors.jsonl` and `scratch/gemini_alerts.txt` for any `CLIENT_ERROR`/`SERVER_ERROR`/`SERVER_DOWN` entries newer than session start.
  5. `node scripts/flag_decision.mjs --list` — confirm nothing new needs flagging, and that nothing being relied on this session is a stale `PENDING` claim (see the `OPEN_DECISION`-staleness hard rule above).
  6. `git status` — confirm the working tree is clean and every change is committed with a real message, not left dangling.
  7. Per the pre-compaction-claims hard rule above: if anything resolved or acted on this session originated from a summarized/pre-compaction claim ("confirmed empirically," "verified earlier"), confirm it was actually re-derived from live data this session, not just re-asserted.
  8. `node scripts/audit_pipeline_freshness.mjs` — cross-references every `signal_type` in `performance_audit` against whether a writer is still scheduled and whether anything live reads it back. Added 2026-07-19 after the user asked how they'd track the health of new systems this session built (GARCH_VOL_SCALE, CVD_DAILY, GLOBEX_LEVEL_PROX, TARGET_SWEEP_V2, SCALEOUT_RUNNER_TEST, etc.) — this tool already answers that generically (it queries every distinct `signal_type`, no hardcoded list to maintain) but wasn't part of the standing self-check sequence, so it was easy to forget to actually run. Report anything newly `⚠️ FLAGGED` since the session started, same as the other checks.
  Report results honestly, including anything that DOESN'T pass — a self check that always comes back clean isn't being run for real. This was named and scoped 2026-07-18 after a session ran exactly this sequence by hand to close out a bug-diagnosis correction; the user asked for it to have a name so it can be invoked as a single request going forward.
- **Standing permission to run commands in this repo without asking first.** This covers all normal tool use — Bash commands, file edits/writes, running scripts/backtests, git commits, schema/doc regeneration, dropping confirmed-dead tables after backup, etc. Don't pause to confirm routine actions; just do the work. This was restated explicitly on 2026-06-30 because re-asking was burning the user's time/limits unnecessarily.
- The one carve-out: genuinely high-blast-radius or hard-to-reverse actions outside normal project workflow still warrant a heads-up — force-push, `git reset --hard`, dropping data without a backup, anything touching production secrets, or actions visible outside this repo (sending messages, posting externally). This carve-out is narrow on purpose; don't expand it to cover ordinary commands.
- **Gemini timeout/fallback:** After invoking Gemini and waiting: (1) 2 min with no `scratch/antigravity_response.md` update → nudge by re-invoking with the same task; (2) another 2 min still nothing (4 min total) → Gemini's context ran out, Claude does the task directly. Don't block on Gemini indefinitely. Check elapsed time with `stat --format='%Y' scratch/antigravity_response.md` vs invocation time. (Shortened from 5min/10min to 2min/4min 2026-07-14 — user feedback, no reason to wait longer once it's clear nothing productive is happening.)
- **Gemini rabbit-holing / going off-task:** If Gemini's step-by-step trace shows it chasing something well outside the task (process/PID tracing, hunting for log files, systemd/OS-level forensics) instead of running the requested queries, check `/home/mmoniz/.gemini/antigravity-cli/settings.json`'s `"model"` field, but don't assume a recent change explains it — checked 2026-07-14 across every session transcript back to 2026-06-15 (the earliest available), and `Gemini 3.5 Flash (Medium)` has been the default starting model for essentially every new session that whole time, with one brief 2026-06-18 experiment (GPT-OSS 120B, Gemini 3.1 Pro, Claude Sonnet 4.6) that reverted right back. So a Flash-tier model improvising tangential shell commands instead of stopping/reporting per `docs/ANTIGRAVITY_CONSTRAINTS.md` rule #7 is a standing property of the default config, not a "something changed lately" regression — don't repeat the mistake of diagnosing it as a recent switch without checking the full history first. If a stronger default model is wanted, that's a deliberate upgrade decision (ask the user — it's a global setting), not a fix for a regression that didn't happen. **2026-07-14: switched to `Gemini 3.1 Pro (Low)`** in `/home/mmoniz/.gemini/antigravity-cli/settings.json` per explicit user confirmation, as a deliberate quality upgrade — not a regression fix. If Gemini's default model in a future session doesn't match this, something changed it again; check the transcript for a `USER_SETTINGS_CHANGE` event.
- **Gemini quota exhaustion:** If `invoke_gemini.sh` exits in under 10 seconds with 0 lines written, diagnose immediately: `agy --log-file /tmp/agy_diag.log --print "test" 2>&1; grep -i "RESOURCE_EXHAUSTED\|quota" /tmp/agy_diag.log`. If it shows `RESOURCE_EXHAUSTED (429)`, quota is exhausted — **do not wait, do not pause work.** Claude does the task directly right now (read the files, run the queries, write the findings). Note the reset time from the log for future Gemini use, but keep moving immediately.
- **Never combine a trailing `&` with the Bash tool's own `run_in_background: true`** when calling `invoke_gemini.sh` (or any long-running script). Stacking both loses harness tracking of the real process — the wrapper returns instantly after backgrounding the job with `&`, so a "completed" notification fires seconds later while `agy` is still working, and reading `antigravity_response.md` at that point shows partial/misleading output that looks like a stall or failure. Just call `./scripts/invoke_gemini.sh <timeout>` directly with `run_in_background: true` and no `&` — let the harness track the actual duration. (Discovered 2026-07-13: this mistake also let two `agy` processes run concurrently against the same output file, corrupting it with interleaved writes — `invoke_gemini.sh` now takes a `flock` on `scratch/.invoke_gemini.lock` and refuses to start a second run.)
- **`invoke_gemini.sh` now `cd`s into the repo and passes `--add-dir` before invoking `agy`.** Without this, `agy`'s working directory isn't pinned to the project, so a prompt that says "read docs/X.md" makes it fall back to a full `find /` filesystem scan (~10-15s wasted) to locate the file. If you ever see Gemini's output full of "I am listing the contents of... I am running find to locate..." lines instead of actual task progress, this is why — check that `invoke_gemini.sh` still has the `cd "$REPO"` / `--add-dir "$REPO"` lines before re-diagnosing from scratch.
- **A background `./start.sh` Bash call left running when a Claude Code session ends becomes an orphaned dev-server supervisor, invisible to `systemctl status` and capable of silently taking the app down hours later.** Found 2026-07-18: a prior session ran `./start.sh` (which launches its own `nodemon`+`vite` tree and, since 2026-07-13, deliberately `systemctl stop`s the managed `trading-journal-server.service` on takeover — see the comment in `start.sh`) as a background Bash call and ended without running `./stop.sh`. The orphaned `nodemon` process kept running for hours after the session ended, watching `server/**/*.{js,mjs,json}` and auto-restarting its own child on *any* file save (not just a commit) — explaining "the server bounced overnight with zero commits happening," previously misdiagnosed as a mysterious crash/watchdog issue. Once that orphan's own child eventually died, nothing brought the app back: systemd had been told to `stop`, not left to crash, so `Restart=on-failure` never fired, and the next session found the app fully dark with no obvious cause in `journalctl`. **Fixed at the root**: `start.sh` now has a `trap ... EXIT INT TERM` that hands port 3002 back to `trading-journal-server.service` whenever the dev session ends by *any* means — Ctrl+C, a clean exit, or the process just being killed — not only via an explicit `./stop.sh` call. Verified live (launched `start.sh`, sent it `SIGTERM` to simulate an abandoned session, confirmed systemd auto-recovered within ~15s). **The trap is a strong mitigation, not a guarantee** — a `SIGKILL` on the whole process tree (or the underlying VM itself dying) still bypasses it. If you run `./start.sh` as a background Bash call, either run `./stop.sh` before the turn/session ends or verify the trap fired (`systemctl --user is-active trading-journal-server.service`) — don't just let a background dev session dangle and assume it's harmless.

## Where to look

- Conviction/session read logic: `server/services/caseEngine.js`
- Opening range / day-type: `server/services/acdService.js` + `server/routes/acd.js`
- **Level fade alpha engine** (size multiplier stack, suppression logic, keepLevels, sizeMultiplier IIFE): `server/routes/acd.js` lines ~3830–4430. Gate is 3 bars (~9:34 AM). Server polls every 60s via `setInterval` in `server/index.js`.
- **Confluence persistence + live pair-bonus lookup, with per-pair calibrated proximity (2026-07-22)**: `active_setups.confluence_score_at_detection`/`confluence_levels_at_detection` (TEXT[]) are now written on every live INSERT in `acd.js` (both the real candidate path and the suppressed-near-level SHADOW-audit path) — previously computed per-candidate but never persisted, so there was no way to test "does a suppressed level perform better near specific other levels." The old hardcoded 5-pair `_PAIR_BONUS_MAP` (never validated against real data) was replaced with a live lookup (`liveStats._pairBonus`) against `backtest_confluence.js`'s real `PAIR:X+Y` rows, which now trust that script's single `recommendation='VALIDATED_PAIR'` field as the complete "safe to use live" decision. First pass (180-day window, flat 15pt proximity) found NO pair had enough distinct convergence days to trust (best case 7 days) — same-day resolution: **widened to the full ~420-day/~1.7yr `level_prices` history AND replaced the flat proximity with a per-pair CALIBRATED threshold** (`loadPairProximityThresholds()`, `min(2×15pt, that pair's own historical p25 |priceA−priceB|)` from real `level_prices` data — some pairs like `PD_HIGH`/`PD_IB_HIGH` sit within 30pt of each other on almost every day by construction, so the old flat cap couldn't tell genuine convergence from routine near-duplication). Result: **71 pairs now clear `distinctDays>=20` + real calibration + chronological stability + positive EV** (N 301-1291, EV $4-$20/trade) — a real, wired result, not the placeholder zero-bonus state from the first pass. Pairs involving a *developing* level (session `VWAP`, `DEV_POC` — computed per-bar, not in `level_prices`, so uncalibratable and a confirmed confound: they track price all session, so "VWAP near static level X" mostly just means "price lingered near X") are excluded from calibration and therefore can never reach `VALIDATED_PAIR` — `WEEKLY_VWAP`/`MONTHLY_VWAP` are NOT developing in this sense (real once-daily static levels in `level_prices`) and calibrate normally. Also found and fixed mid-build: `getRTHBars()`/`getTradingDays()` in this script had no `symbol='NQ'` filter, so the documented ES-contamination window (2023-11-15 to 2023-12-15) produced multi-thousand-point spurious MAE/MFE — see the `data_sanity_audit.mjs` entry above. See `confluence_pair_proximity_2yr_backtest` `RESEARCH_CLAIM` for the full write-up; `confluence_proximity_should_be_per_pair_calibrated` `OPEN_DECISION` resolved.
- **Overnight/Globex level-fade detector** (6PM–8:30AM ET window, separate from the RTH-only engine above): `detectGlobexSetup()`, `server/routes/acd.js` ~line 582. Originally PD_VAH/PD_VAL/PD_POC only (fire straight to ACTIVE, no SHADOW gate). Extended 2026-07-20 with 4 more prior-period levels (`3M_VAL/3M_POC/WS1/PM_POC` FADE SHORT), and again 2026-07-22 with 4 more (`MPP_FADE_SHORT`, `MONTHLY_OPEN_FADE_LONG`, `10D_IB_MID_FADE_SHORT`, `WR1_FADE_SHORT`) scoped from a broader 51-setup `THIN_N` candidate pass — see `docs/OPEN_THREADS.md` 2026-07-22 entry for the full scoping/verification writeup, including a real bug caught in Gemini's first independent-reimplementation attempt (a stray direction filter that inflated touch counts 1.5-3x, fixed directly before trusting the "0/19 agree" conclusion it produced). All 8 are distinctly-named `_OVERNIGHT` setup_types (`WIDER_WINDOW_OVERNIGHT_LEVELS` const, same file, now tracks `dir` per-entry since the second batch includes a LONG-direction level — do not assume the whole array is SHORT-only). Kept separate from their RTH-only siblings' calibration on purpose. All 8 are dynamically SHADOW-gated via `getOvernightLevelLiveStatus()` (mirrors `minuteBarSignalDetector.js`'s N≥20/EV≥-$5 promotion check) until real forward data clears the bar — do not assume every candidate in this function fires live; check `widerWindowNew` in the code or the setup_type suffix. **"Constantly assessed for suitability" is already a property of the existing pipeline, not something built specially for these**: `backtest_setup_status.mjs`'s population query is a generic `GROUP BY setup_type` scan of all of `active_setups` (no hardcoded list), run daily (20:20 ET Mon-Fri, `run_daily_calibration.sh`) and weekly — any setup_type, including these 8, gets picked up and reassessed automatically the moment it has real resolved trades.
- **Alpha Engine overview page**: Edge → Alpha Engine tab (`src/components/dashboard/AlphaEngineOverview.jsx`) — human-readable summary of every system component, size multiplier factor, suppressed setup, and pending road map.
- **Opus audit prompts**: `docs/OPUS_AUDIT_PROMPT.md` (Audit 1 — code/bugs/behavioral forensics; all 9 findings implemented 2026-07-07) · `docs/OPUS_AUDIT_PROMPT_2.md` (Audit 2 — sizeMultiplier discrimination, objective alignment, edge attribution, coaching efficacy, untapped data) · `docs/OPUS_AUDIT_PROMPT_3.md` (Audit 3 — closed-loop learning / lost-state audit, 2026-07-17; found `active_setups` had no way to validate a suppression decision against its own forward outcome — fixed via `origin_status`, see `docs/OPEN_THREADS.md`) · `docs/OPUS_AUDIT_PROMPT_4.md` (Audit 4, drafted 2026-07-21, not yet run — does the "order-flow signals generalize, price-action-shape signals don't" pattern from that session's candle/CVD-divergence thread hold up across this codebase's full research history, and what's the highest-value untried order-flow idea). Run via Claude Opus 4.8 — not invoked automatically, triggered manually when strategic review is needed.
- **Database migration protocol**: [docs/DB_MIGRATION_PROTOCOL.md](docs/DB_MIGRATION_PROTOCOL.md) — standing checklist for any schema/data change (backup-before-destructive, dry-run-first, timezone/type safety, exhaustive INSERT-site wiring, post-migration verification). There is no tracked migration-file system in this codebase; this protocol is what replaces one. Read before writing any `ALTER TABLE`, bulk `UPDATE`, or backfill script.
- **Database backup table catalog**: [docs/DB_BACKUP_CATALOG.md](docs/DB_BACKUP_CATALOG.md) — every `*_backup_*` table currently in the live DB, what it's a snapshot of, why, and how to use it to spot-check a suspected anomaly against prior state. 13 backup tables existed with zero index anywhere before 2026-07-17. **Any script that creates a backup table must add an entry here in the same commit** — an unindexed backup is a future landmine, not a safety net.
- **Standing data-sanity audit**: `node scripts/data_sanity_audit.mjs` (wired into `run_weekly_backtests.sh`) — catches the class of bug found manually 2026-07-17 (impossible MAE/MFE values, a non-uniform `$/pt` constant) automatically: MAE/MFE outliers (self-calibrating, 10x p95), `$/pt` uniformity across setup_types vs. `LIVE_INSTRUMENT`, unexpected symbols in `price_bars_primary`, impossible `win_rate`/`sample_size` bounds in `performance_audit`. Exit code 1 is currently expected (one standing flag: `ES` symbol data still present in `price_bars_primary`, 2023-11-15 to 2023-12-15 — the source data was left in place, only the queries that read it were fixed to filter by symbol) — read the output, don't treat non-zero exit alone as a failure. **`scripts/backtest_confluence.js` was found 2026-07-22 to be a previously-unaudited consumer still missing this filter** — `getRTHBars()`/`getTradingDays()` queried `price_bars_primary` with no `symbol='NQ'` clause, so any touch/pair falling in the contamination window silently mixed ES bars (~$4,500 in that period) into the same day's NQ bar sequence, producing multi-thousand-point spurious MAE/MFE swings (caught directly: two pairs showed `MFE P50 > 11,000pt`, impossible for NQ in a 30-bar window). Fixed by adding `symbol='NQ'` to both queries. **General lesson: `data_sanity_audit.mjs`'s existing ES-symbol check only flags rows in `price_bars_primary` itself — it does not, and structurally cannot, verify that every ad hoc script querying that table has its own `symbol='NQ'` filter.** When writing or auditing any new script that reads `price_bars_primary` directly (not through an existing service function), check for this filter explicitly — don't assume the standing audit would have caught its absence.
- Shared query helpers (NL30/NL10, gap drift, prior-week range): `server/services/queries.js` — check here before writing a new one
- Point-in-time regime classification (directional-trend/price-stretch/persistence z-scores, post-rotation-day flag, vol-regime lookup, conditioned sizing multiplier — used by the 2026-07-18 regime-conditioning research thread): `server/services/regimeClassificationService.js`, added 2026-07-18 after being hand-copied 3x in one night. **Deduplicates the code only — the methodology was tested the same day and FAILED** (see below). Do not build live sizing/suppression logic on Regime A/B/C as currently constructed.
- **Regime detection/anticipation research spec + validation framework — `regime_detection_methodology_needs_validation` RESOLVED (negative)**: `docs/REGIME_DETECTION_SPEC.md` (2026-07-18) — inventories every regime-adjacent component in this codebase with its real validation status, surveys the broader methodology space (changepoint detection, formal Markov-switching, GARCH-as-regime, intraday Hurst, order-flow/CVD, calendar effects — none built), and built + ran a 5-stage validation framework against Regime A/B/C same day. Result: a first pass (§7) found a differentiated result, but a same-day placebo test and non-overlapping-window re-test (§7.1) overturned the most promising part of it, and an independent PELT-changepoint ground truth on raw NQ price data (§8/§8.1 — not the classifier's own `daily_score` input, a genuinely external check) found Regime A's label transitions align with real structural breaks *worse than chance* on the best-tested configuration. **All three of Regime A/B/C now have a real, tested negative result, not just an absence of validation** — read §8.1 first, it's the decisive one. Doesn't rule out regime detection generally (§3 surveys untried methods); rules out today's specific z-score/tercile construction on this ~410-day history.
- **Candlestick & order-flow entry-confirmation research (2026-07-21)**: `docs/CANDLE_ORDERFLOW_RESEARCH_SPEC.md` — consolidated index of every test in this thread (candle shape at first touch, at overshoot, with volume confirmation, intrabar CVD divergence, the Opus Audit #4 strategic review) with `RESEARCH_CLAIM` slugs, outcomes, and what's still open. Headline: every candle-pattern variant failed once properly checked, including one (`volume_confirmed_candle_pattern_low_vol_trap`) that looked real and was retracted the same day after failing a held-out replication test — see `server/services/candlePatternQuality.js` and `computeReplication()` in `rigorDiagnostics.js` (built specifically from this thread) before starting any related work. Read this spec before re-deriving any of it; `docs/OPEN_THREADS.md`'s 2026-07-21 entries have the full narrative if needed.
- Risk guardrails (DLL, profit-lock, cooldown): `server/routes/dll.js`, `profitLock.js`, `cooldown.js`
- Optimal stops/targets (data-derived, weekly recompute): `performance_audit` rows with `signal_type='OPTIMAL_STOP'`; computed by `scripts/update_optimal_stops.mjs`; read in acd.js as `liveStats._opt[setup_type]`. **Always read the LATEST row per `signal_name` via `DISTINCT ON (signal_name) ... ORDER BY signal_name, run_date DESC`** — an unordered `SELECT` against this table returns every historical weekly run, and JS object-key overwriting on an unordered result set is not guaranteed to land on the latest one. Found 2026-07-18: exactly this bug in a new backtest script silently used a stale calibration for 35/108 setup_types (32%), some off by huge margins (IB_BULLISH stale=19pt vs actual latest=79pt). **A second, worse variant of the same bug class found 2026-07-20 in `/api/performance-audit/unified`**: its shared query joined on `MAX(run_date) GROUP BY signal_type` (not `signal_name`) — correct for signal_types where every signal_name is rewritten in lockstep every run, but `OPTIMAL_STOP` isn't one of those (`update_optimal_stops.mjs`'s own population query can skip a signal_name on a given run — thin data, direction-inference failure, etc). A signal_name lagging even one run behind its signal_type's global max wasn't just stale, it was **completely absent** from the result set — confirmed live, `IB_HIGH_FADE_LONG` (real `optimal_stop=53`, `optimal_target=35`) silently vanished from the endpoint entirely, not just shown outdated. **The `DISTINCT ON (signal_name)` pattern is the only safe one for any signal_type whose signal_names don't all update together — a `GROUP BY signal_type` join is a strictly weaker guarantee that can silently exclude, not just stale-serve, individual rows.** Fixed with a dedicated `DISTINCT ON (signal_name)` query for `OPTIMAL_STOP` specifically, not by trying to fix the shared multi-type join (broader blast radius, not audited). **Target selection now has two paths (2026-07-19)**: the original EV-sweep (fixed point grid, capped at `p75_mfe`, order-blind) is still the default, but `server/services/targetCalibrationService.js`'s `computeCorrectedTarget()` (chronological bar-walk, thin-tail/plateau/OOS/rigor-guardrailed) is attempted for every setup_type first and OVERRIDES the target when it clears every guardrail — check a row's `notes` field for `"method":"corrected-resim"` to see which path produced it; `notes.oldTarget` (added 2026-07-20) lets you see exactly how much wider the correction validated riding the trade. 18/110 setup_types use the corrected path as of 2026-07-20 (every one of them widened, none narrowed, +10% to +297%) — visible in `AlphaEngineOverview.jsx`'s "Target Calibration Coverage" panel and as a "✓ CAL" badge on the `t1` column in the Unified Signal Table (`BacktestView.jsx`). See `docs/TARGET_CALIBRATION_SPEC.md` for the full derivation and `docs/OPEN_THREADS.md` for the wiring writeup. **The Unified Signal Table's stop/t1/t2 columns now read this real calibration directly** (fixed 2026-07-20) — they used to come from a 14-entry hand-typed `overrides` literal (removed; 13 of 14 entries were already fully dead, redundant with real data; the last, `TRT_LONG`, now correctly shows `--` instead of a stale guess) or an MAE/MFE percentile fallback disconnected from what's actually live.
- **Setup Reference page** (Edge → Setup Reference, added 2026-07-20): what each setup_type IS — criteria, detection window (RTH-only / same-day-forming valid-until-superseded / Globex-capable / day-type-conditional), family — joined against live N/WR/EV/status/rigor. Static definitions in `server/config/setupDefinitions.js` (`WINDOW_RULES` categories + `LEVEL_FADE_DEFINITIONS`/`OTHER_SETUP_DEFINITIONS`), served by `GET /api/setups/reference` (`server/routes/setups.js`), frontend `src/components/dashboard/SetupReferenceView.jsx`. 16 setup_types remain genuinely undocumented (shown, flagged, not hidden) — mostly thin-N pattern types (`TRT_MAH`, `STOP_SWEEP`, `C_PAIRED`, `GAP_FILL`, `ZONE_EDGE_FADE`, `VWAP_MAGNET`) where the exact entry mechanics weren't verified against code before writing criteria text — add real definitions there before claiming full coverage, don't guess.
- **Overnight research (past-year walkthroughs, stop/target recalibration, taper-timing, open-ended pattern discovery, big-move diagnostics — count/MAE-MFE/target-truncation/level-respect/inception-timing — cross-referenced against the RTH large-movement calibration playbook)**: `docs/OVERNIGHT_RESEARCH_SPEC.md` — read this before starting any new overnight-specific analysis; it has a line-by-line mapping of every RTH lesson (sequence-tracking, efficiency-ratio framing, why a static wider target failed, the scale-out/breakeven-trail mechanism, the 4-part guardrail stack, data-resolution floors, same-method baselining, low-survival-rate-as-a-good-sign) to its overnight analog, so this doesn't have to be re-learned from scratch. Two open decisions: `overnight_bigmove_target_truncation_needs_sequence_fix` (HIGH) and `overnight_bigmove_trailing_mechanism_test` (MEDIUM).
- Wider-window (24hr / valid-until-superseded) level-fade backtest findings: `docs/WIDER_WINDOW_BACKTEST_20260720.md` — real N/WR/EV comparison for RTH-only vs wider firing windows across all level-fade families, `OPEN_DECISION` `wider_window_level_fade_backtest_findings_20260720` (not implemented live yet — would require changing the RTH gate in `server/routes/acd.js`'s `keepLevelsAll`, a bigger change than the backtest-only work already done). **UPDATE 2026-07-20 (new session): the 12-row same-day-forming-levels half of this table (`OR_HIGH`/`OR_LOW`/`IB_HIGH`/`IB_LOW`/`IB_MID_SCALP`/`OR_MID_AFTER_IB`) is now confirmed unreliable, not just unverified** — an independent reimplementation of `IB_HIGH_SHORT`/`IB_LOW_LONG` (`scripts/verify_ib_wider_window_20260720.mjs`, real `resolve()` imported, RTH-only side byte-exact-matched against the committed script) disagrees with Gemini's original numbers by 3x on N and flips sign on one row's EV. Root cause: `detectLevelFades()` hardcodes an RTH-only bar filter internally, so no same-day-forming-level wider-window test can call the real function — any such test requires a reimplementation, which is exactly the "two independent reimplementations disagreeing" risk already documented for `classifyRegime()`. Do not use any of these 12 rows for a live decision. The separate 59-level prior-period-levels table is unaffected by this finding (different mechanism, no same-day-formation dependency) but remains only spot-checked on its RTH-only side.
- Monthly VWAP + Prior-Year value area (`MONTHLY_VWAP`, `PY_VAH`/`PY_VAL`/`PY_POC`) — added 2026-07-19 after auditing which VWAP/POC magnet variants were actually tested (RTH VWAP magnet is live but N=3, thin; weekly VWAP fade confirmed no edge; monthly VWAP and yearly POC had simply never been built). `MONTHLY_VWAP` = month-to-date VWAP, same live-accumulating convention as `WEEKLY_VWAP`. `PY_VAH`/`PY_VAL`/`PY_POC` = prior COMPLETE calendar year's value area (deliberately the safe prior-period convention like PW/PM, not 3M's date-inclusive rolling window — see the `3m_val_poc_same_day_lookahead_risk` `OPEN_DECISION` for why that distinction matters). Both computed by `scripts/compute_levels.js`, backfilled historically by `scripts/backfill_monthly_vwap_yearly_poc.mjs` (modeled on `repair_weekly_vwap_window_mismatch.mjs`, using a wide 300pt placeholder box — widened from an initial 100pt after directly checking the resulting `mae_points`/`mfe_points` distributions and finding real truncation at 100pt, a live instance of the same bug class documented elsewhere in this file). Wired into `acd.js`'s `keepLevelsAll` candidates array — all 4 are `THIN_N` as calibrated (`SETUP_STATUS`), so they fire `SHADOW`-only via the standard gate, never a live alert. Real finding, not a bug: most touches of a month/year-scale magnet don't resolve (stop or target) within a single RTH session — `PY_VAL` has 0 touches and `PY_POC` has 1 over ~2.3 years of history (a full year's value area is wide; price rarely revisits its exact center). `MONTHLY_VWAP` has a real touch sample (80 SHORT/96 LONG) but only 12-17 same-day-*decided* outcomes, below the N≥20 floor for `OPTIMAL_STOP`. Continues accumulating real SHADOW data going forward via the already-scheduled `backtest_setup_status.mjs`/`update_optimal_stops.mjs` — no new cron entry needed, they're generic and auto-discover new setup_types.
- Breakeven-then-trail single-position exit mechanism: `performance_audit` rows with `signal_type='BREAKEVEN_TRAIL_TEST'`, computed by `scripts/backtest_breakeven_trail.mjs` (manual-only, no cron entry yet). Mechanism: stay on the normal stop until the setup's calibrated target is reached, then snap the stop to breakeven and trail it forward (never loosening) instead of taking a fixed second target — designed specifically to be 1-contract-compatible (no fractional position split), after the fractional 10%/90% scale-out version was found not executable (user trades 1 MNQ micro max, live). 6 survivors as of 2026-07-19: `FLOOR_R1_FADE_SHORT` (N=77, strongest by sample size), `DAILY_OPEN_FADE_LONG`, `FLOOR_S1_FADE_LONG`, `CAM_S2_FADE_LONG`, `PD_POC_FADE_LONG`, `PW_HIGH_FADE_LONG` (N=16, thin). All modest, real incremental improvements (+$1.60 to +$4/trade OOS) on top of already-positive baseline EV, not dramatic flips. **All 6 now wired live (SHADOW-only) as of 2026-07-21** — `FLOOR_R1_FADE_SHORT_TRAIL` (2026-07-19), the other 5 (`PW_HIGH_FADE_LONG_TRAIL`/`PD_POC_FADE_LONG_TRAIL`/`FLOOR_S1_FADE_LONG_TRAIL`/`DAILY_OPEN_FADE_LONG_TRAIL`/`CAM_S2_FADE_LONG_TRAIL`) added 2026-07-21 via the `CONDITIONAL_VARIANTS` pattern, `test_invariants.mjs` clean for all 6. **Caveat found before wiring the other 5, worth tracking**: `FLOOR_R1_FADE_SHORT_TRAIL` had zero real (non-suppressed) live touches in its first ~2 days — the code's own documented gating condition ("prove itself at live N≥20 before adding more") wasn't actually met. Not a bug (the one existing row is a correctly-minimal suppressed-touch audit entry, `suppression_reason='S2_DOUBLE_COUNTER'`) — just means none of the 6 has real forward validation yet, only the backtest. Full build spec: `docs/SCALEOUT_RUNNER_SPEC.md`. Recheck: `RESEARCH_CLAIM` `breakeven_then_trail_single_position_2026_07_19` (30-day cycle). **Found and fixed mid-audit 2026-07-19**: the first backtest pass wrongly excluded `TIME_EXPIRED` trades (~12% of the population) — same bug class as the `OPTIMAL_STOP` staleness note above — which was enough to flip the original flagship candidate (`CAM_R4_FADE_SHORT`) from survivor to reject once corrected. Verify any future re-run's population query is `status IN ('RESOLVED','EXPIRED')`, not `status='RESOLVED'` alone.
- GARCH(1,1) walk-forward daily volatility scale (canonical, persisted, do not refit inline): `performance_audit` rows with `signal_type='GARCH_VOL_SCALE'`, one row/trading day, `notes` JSON has `{forecast_vol, unc_vol, scale, alpha, beta, degenerate_fallback}`; computed by `scripts/backfill_garch_vol_scale_history.py` (`scratch/.venv`, requires `symbol='NQ'` filter on its bar query — see below). Tested against every level-fade `setup_type`'s real `OPTIMAL_STOP` via `scripts/backtest_garch_scaled_stop_all_setups.mjs`: real but modest, minority effect (28/103 setup_types improve, only 2/103 improve AND reduce regime-to-regime EV spread) — **not wired into live `sizeMultiplier`.** This line item exists because getting here took 4 compounding bugs, each worth remembering: (1) the scale denominator must be each day's own self-consistent unconditional variance, not a frozen historical anchor — a frozen one inflated an earlier confluence-zone-only result by capturing multi-month vol drift, not real signal; (2) the daily bar query fee­ding the GARCH fit **must filter `symbol='NQ'`** — `price_bars_primary` has documented ES contamination 2023-11-16 to 2023-12-14 that, left unfiltered, sits inside the walk-forward warmup window and gets carried forward by every subsequent day's expanding window forever; (3) the self-consistent scale is numerically degenerate on days where `alpha+beta` is very close to 1 (near-unit-root) — the unconditional-variance formula blows up (observed 0.13x-284x swings on ~44/317 days) — needs a persistence-floor fallback (carry forward the last valid day's value); (4) see the `OPTIMAL_STOP` staleness bug just above, caught in the consuming script, not this one.
- Day-type per-setup edge (SIZE_UP/SUPPRESS flags): `performance_audit` rows with `signal_type='DAY_TYPE_ALPHA'`; computed by `scripts/backtest_day_type_alpha.js` Sunday 9:10 PM; read in acd.js as `liveStats._dta[setup_type-day_type]`
- **Live-roster backtest stability/trend** (does the currently-firing setup roster's backtested edge hold up chronologically, not just on average): computed weekly by `backtest_setup_status.mjs`'s `computeRigor()`/`classifyTrend()` (`server/services/rigorDiagnostics.js`), stored per-setup in `SETUP_STATUS`'s `notes.rigor`. Per-row: `stabilityTrend`/`stabilityStable` fields on `/api/performance-audit/unified`, shown as the "Stability" column in `BacktestView.jsx`'s Unified Signal Table. Aggregate: `GET /api/acd/rigor-stability-coverage` + `RigorStabilityPanel` in `AlphaEngineOverview.jsx` (Edge → Alpha Engine).
- Nightly latency audit results: `performance_audit` rows with `signal_type='LATENCY_AUDIT'`; alerts in `scratch/gemini_alerts.txt`
- Daily order-flow (CVD — cumulative volume delta, `ask_volume - bid_volume`, overnight Globex + RTH split): `performance_audit` rows with `signal_type='CVD_DAILY'`; computed by `scripts/backfill_cvd_daily_history.mjs` (`symbol='NQ'` filtered). First real signal built here 2026-07-18 (order-flow was previously an untested gap, see `docs/REGIME_DETECTION_SPEC.md` §3.7) — overnight CVD extremity has no directional predictive power for the next RTH session, but a weak, clean, real range-elevation effect exists (~+5-6%, much smaller than the already-live volatility regime classifier's spread). Not wired into anything live. Intraday CVD-vs-price divergence (the other §3.7 angle) not yet tested.
- Globex/overnight large-move-vs-known-level proximity: `performance_audit` rows with `signal_type='GLOBEX_LEVEL_PROX'` (also `RESEARCH_CLAIM` `globex_large_moves_start_near_pit_safe_levels`); computed by `scripts/backtest_globex_move_levels.mjs`. Dispatched to Gemini 2026-07-18, whose script was correct on threshold/inflection-point/PIT-safe-level-scoping but whose placebo comparison used a single unseeded random draw per day (not significant on its own, z=1.73/p=.085) — replaced with a 5,000-draw permutation test, which found the effect is real and had actually been *understated* by the single draw (null mean 65.6% vs real 76.3%, z=2.22, empirical p=0.018, N=80). Modest effect, not wired into anything live (setup roster is RTH-only). **Read this entry before re-doing this kind of placebo test**: a single random-draw placebo is not enough to accept OR reject a "near a level" claim — always permutation-test it.
- **Pipeline freshness/consumption report**: `node scripts/audit_pipeline_freshness.mjs` — cross-references every `signal_type` in `performance_audit` against whether a writer script is still scheduled (checks both scheduling layers: `server/index.js`'s node-cron blocks AND the system `crontab -l` → `scripts/run_*.sh` → `node scripts/X` chain) and whether any live route/service reads it back out. Built 2026-07-17 so "is this signal_type still read/written" is one command instead of the grep+git-log archaeology done by hand at least twice in one week for overlapping signal_types. Static-grep based (misses parameterized `signal_type = $1` reads) — treat a flag as "worth a look," not proof.
- Known bugs / tech debt: [docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md)
- Pending decisions, unconfirmed proposals, stale-stats findings, unfinished multi-session work: [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md) — **check this at the start of any session**, especially after a context clear.

## Documentation maintenance — keep this perpetually current

This file and [ARCHITECTURE.md](ARCHITECTURE.md) are the handoff point for the next session (human or Claude) — treat letting them go stale as a real cost, the same as introducing a bug. A `Stop` hook (`.claude/settings.json`) checks at the end of every session whether structural files changed without a matching doc update and prints a reminder — if you see that reminder, act on it before ending the turn rather than dismissing it.

Concretely, whenever a session does any of the following, update the relevant doc **in that same session**, not as a follow-up:
- Add/remove a route file, service file, or DB table → update [ARCHITECTURE.md](ARCHITECTURE.md)'s route/service/table tables, and if a table was added/dropped/altered, regenerate `server/schema.sql` (command is in ARCHITECTURE.md)
- Add/remove a dashboard component → update the frontend structure table in [ARCHITECTURE.md](ARCHITECTURE.md)
- Establish a new hard rule or convention (the user corrects an approach, or confirms a non-obvious one) → add it to the **Hard rules** or **Conventions** section above
- Fix something listed in [docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md) → remove that entry; discover a new one → add it
- The user confirms a fix/decision but it isn't fully implemented in the same session, proposes a follow-up that isn't confirmed yet, or you find a stat/display that's stale or disconnected from its source of truth → add it to [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md) **before the session ends** — don't rely on conversation memory alone to carry pending work forward. This is on you to maintain proactively; the user has delegated it rather than asking for it each time.
- `docs/` also holds point-in-time specs (`MONTE_CARLO_SPEC*.md`, `BACKTESTING_PLAYBOOK.md`) and parked design docs (`*_parked.md`) — those describe proposals, not live state, and aren't required to track the running app. Don't confuse them with ARCHITECTURE.md.
- **Any change that supersedes existing code, data, or documentation must clean up what it replaces in the same session** — don't leave stale duplicate descriptions, dead constants/variables, orphaned scratch/temp files, or superseded DB rows sitting alongside the replacement. Concretely: rewriting a script's methodology → grep for every other doc that describes the old methodology (not just the primary one) and update all of them, and remove now-unused constants/params from the code itself; reverting a bad live change → delete the temp files used to make and revert it; a finding that invalidates an old proposal → replace/remove the old note, don't just append a new one next to it (OPEN_THREADS.md especially — resolved items get struck through with a "Resolved <date>" note per its own convention, not left dangling as if still open; see that file's own header — the older "delete when fixed" wording was changed 2026-07-14). Restated 2026-07-13 after a session left stale/duplicate descriptions in ARCHITECTURE.md and HARDCODED_CONSTANTS.md that had to be caught separately after the code change was already done.

There is exactly one canonical architecture doc — `/ARCHITECTURE.md` at the repo root. If you're ever tempted to write a second one (e.g. inside `docs/`), update the existing one instead; a prior session already made that mistake once (`docs/ARCHITECTURE_2026-06-07_superseded.md`) and it silently went stale because nothing pointed back to it.
