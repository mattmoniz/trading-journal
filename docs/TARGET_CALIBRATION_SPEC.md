# Target Calibration & Runner Design — Dialogue Log

Started 2026-07-19. This is a running log of an in-progress design conversation, not just a findings summary — per user request ("keep documenting these questions... and dialogue context, not questions"), the reasoning trail matters as much as the conclusions, because the next session (human or Claude) needs to see *why* a design choice was made, not just what it landed on. Update this file as the conversation continues; don't just append a terse Q&A list.

Related: [OPEN_THREADS.md](OPEN_THREADS.md) has the day-by-day chronological findings (the `optimal_target_blind_to_post_resolution_continuation` `OPEN_DECISION`, the two negative Layer-2 trigger tests). This doc is the design narrative that thread grew out of. Also related: `project_scaleout_optimization_parked.md` (memory) — a scale-out/runner brief parked 39+ days ago, directly relevant to the "how do you avoid capping the upside" question below; this conversation revives that thread with actual data behind it now.

## How this started

Session began by auditing a Gemini-dispatched finding (does a large Globex move start near a known level — yes, real but modest, r=0.018 empirical p-value after fixing Gemini's weak single-draw placebo into a proper permutation test). While reviewing the wider-target EV test from the prior day (7 hand-picked setups, all positive), the user asked the obvious next question: **why only 7, could more setups use it?**

Expanding the test to all 90 qualifying setups (not a hand-picked subset — fixed the script to pull its roster dynamically from live data instead of a hardcoded list, since a hardcoded list is exactly the anti-pattern this codebase already has a standing rule against) found **88/90 setups (97.8%) show positive EV at a 1.5x wider target**. That result was too broad to accept as 88 independent discoveries. Root-caused it: `sweepOptimalTarget()` — the function that sets the *live* target — picks its target using `mfe_points`, and `mfe_points` is written by a bar-walk that terminates the instant the original target is hit. Direct spot-check on 6 real `PD_SESSION_MID_FADE_SHORT` trades confirmed it: target distance ~48.5pt, and `mfe_points` was barely past it every single time (50.25, 57.5, 56, 67, 57.5, 65.5pt). **The live target calibration has never seen what happens after price reaches target.** Flagged as `OPEN_DECISION` `optimal_target_blind_to_post_resolution_continuation` (HIGH priority) rather than promoting the arbitrary 1.5x multiplier live.

## "Are we calibrating off the std dev of large extensions?"

User's direct question. Answer, after reading the actual code: **no, not for targets.** Stops ARE already distribution-calibrated — `sweepOptimalStopAndTarget` sweeps candidate stops off that setup_type's own p25/p40/p50/p60/p75 `mae_points` percentiles, genuinely following this codebase's no-static-threshold convention. Targets are not: `sweepOptimalTarget` picks from a fixed point grid (`TARGET_SWEEP = [10,15,20,25,30,35,40,50,60,70,80,90,100,120,150]`), capped at `p75_mfe` — which is exactly the truncated distribution above. The user's instinct (calibrate off σ of the real large-extension distribution) is the right fix and is the same fix the `OPEN_DECISION` already names.

## "Can we monitor for large moves? What's the trigger to recalibrate?"

Split this into two genuinely different problems, since conflating them was the risk:
- **Layer 1**: batch recalibration — fix what the *static* target should be, using the real (untruncated) extension distribution. No live monitoring needed, this is a periodic recompute like the existing `OPTIMAL_STOP` pipeline.
- **Layer 2**: a *live* signal that says THIS specific in-progress trade looks like it's going to be one of the big ones, so hold past the normal target for that one instance.

Tested two cheap, already-available Layer 2 candidates (both zero-new-infrastructure, pure joins against existing persisted data):
- **`GARCH_VOL_SCALE`** (already live) vs. extension size: **negative**, Pearson r=0.0507 (N=4,710). Bucket means barely move (LOW-vol 135.8pt / NORMAL 135.8pt / HIGH-vol 147.7pt).
- **Distance to next known level beyond target** ("room to run", via `level_prices`) vs. extension size: **negative**, r=0.0476 (N=6,092). Small monotonic trend across quartiles (112.2pt tightest-room → 140.0pt most-room) but too weak/noisy to act on. Side-finding: a quarter of all target-hit trades already have a tracked level within 1-3pt of target — the 65-level universe packs densely, the same density this session's Globex placebo test had to control for.

Both rigor-clean (no day-clustering, stable across chronological thirds) — real, honest negatives, not thin non-results. Untested Layer 2 candidates: time-of-day (the taper-timing data already hints at a real morning/afternoon asymmetry), live momentum/velocity into target (most genuinely "real-time," also most novel/expensive — deprioritized until cheaper candidates were exhausted). **Conclusion at this point: no validated Layer 2 trigger exists yet. Layer 1 is the sure win.**

## "Yes but how'd you determine what the target should be if it's dynamic?"

Sharp follow-up — pressed on whether Layer 1 is actually well-specified, not just "use the corrected distribution" hand-waved.

**Why a naive percentile/σ-multiple of the corrected MFE would be wrong**: favorable-extension distributions are right-skewed — a handful of huge continuation days pull the mean way up, but the median trade doesn't run nearly that far. Set the target off an inflated statistic and most trades never reach it; win rate collapses.

**Why simply feeding the corrected MFE into the EXISTING sweep (`computeEvAtStopTarget`) is also wrong**: that function is order-blind — it checks "did MAE exceed stop" and "did MFE reach target" as two independent facts with no notion of which happened first in time. This is a known, already-documented issue in this codebase (the reason the stop-sweep cap is p75 and not p90 — a comment in `update_optimal_stops.mjs` explains p90 let some IB_BEARISH trades get "rescued" into fake wins by the sim, because it couldn't tell that the real stop would have triggered before the sim's chosen target). Widening the sweep's target range using corrected MFE without a correspondingly corrected, chronologically-aware MAE for that SAME wider window would systematically overstate EV, because holding a trade open longer to chase a bigger target also exposes it to bigger real adverse risk before getting there — risk the old, still-truncated `mae_points` field doesn't capture.

**The actual fix**: reuse the methodology already built and validated in `backtest_wider_target_ev.mjs` — real bar-by-bar chronological resimulation from entry, checking on each bar whether the candidate target or the ORIGINAL stop is hit first, in true time order. That's what made the 88/90 finding trustworthy in the first place (as opposed to the order-blind two-percentile shortcut). So the corrected Layer 1 sweep is:
1. Per setup_type, generate candidate target distances from percentiles of the real corrected-MFE distribution (data-derived candidates, not a hardcoded list).
2. For each candidate, chronologically resimulate every trade from entry — same walk as the wider-target test, generalized beyond just 1.5x/2x to a genuine sweep.
3. Pick whichever candidate maximizes real dollar EV — same "EV-optimal search" philosophy already used for stops today, just made order-aware instead of order-blind.

User approved: "Yes build it and test." — implementation below.

## "If you enter a trade and it turns out to be a big one, how do you prevent [exiting at] the initial target and missing out on the larger move?"

This is a different question from Layer 2 above — not "predict in advance whether this will be a big one" (which the two negative tests already showed is hard), but "structure the exit so you don't need to predict in advance." That reframing matters: instead of a predictive trigger, this is a *reactive/adaptive* exit — once price is already at or past target, don't force a binary exit-or-hold decision; let the position tell you.

This directly revives a parked brief (`project_scaleout_optimization_parked.md`, 39+ days old, never executed) that asked almost this exact question for the discretionary MNQ position: scale out partially at T1, run a remainder with a trailing/structural exit rather than a fixed second target. The parked brief's own guardrail is worth repeating here since it applies just as much to the setup-calibration version of this question: *"Rushing optimization = curve-fit TP traded with real money... 'Best' config must be a plateau, not a lucky spike."* This is genuinely its own focused piece of work, not a quick patch to the sweep above.

## "Yes build it and test" — the fixed-target sweep, and why it failed its own sanity check

Built `scripts/backtest_target_sweep_v2.mjs`: per setup_type, generate candidate target distances from percentiles (40th-95th) of the true (untruncated) MFE distribution, chronologically resimulate every trade from entry for each candidate (real bar-by-bar first-touch-of-target-vs-original-stop, not the order-blind independent-percentile shortcut), pick whichever candidate maximizes real dollar EV. Ran it against 103 setup_types.

**It failed its own sanity check before being reported as a finding.** Auditing the winning picks (the same discipline applied to every other result this session): `PD_LOW_FADE_SHORT` picked T=719.8pt with EV=$14.57 — but `targetHits=1` out of N=38. A single trade's dollar value flipped the average from negative (every smaller, more-often-hit candidate was -$2.83 to -$12.50) to positive. `FLOOR_S1_FADE_SHORT` picked T=657 the same way, also `targetHits=1`. `CAM_S3_FADE_SHORT`'s EV bounced non-monotonically across candidates (+3.77, -3.02, -7.73, then +11.47, +12.32, +17.92, +1.29, -18.22) with hit counts down to 0-2 — noise, not a real signal. This is precisely what the parked scale-out brief already named 39+ days ago: *"'Best' config must be a plateau, not a lucky spike — a spike is curve-fit, reject it."* The sweep as built has no thin-tail gate and a candidate grid that jumps straight past the already-validated sweet spot (the earlier hand-tested 1.5x on `PD_SESSION_MID_FADE_SHORT` was ~75pt; this sweep's smallest tested candidate for that setup was 88.5pt, so it never even re-tested the known-good region).

**Conclusion, not just a bug to patch**: a single static wider target is structurally the wrong tool for this problem. One number can't simultaneously be (a) reliably reached often enough to matter and (b) wide enough to capture a rare huge continuation — those two properties pull in opposite directions, and the spike-picking behavior above is exactly what happens when a sweep is forced to pick one number to do both jobs. `TARGET_SWEEP_V2` rows are persisted (backtest-only, not live) but should NOT be treated as a trustworthy finding as they stand.

## "What do you think about that question i posed" (the runner one) — recommendation

Given the sweep's own failure mode, the answer to "how do you avoid missing the bigger move" isn't a better fixed target — it's **not using a fixed second target at all**. Recommended design, converging independently with the parked `project_scaleout_optimization_parked.md` brief from 39+ days ago (partial at T1, breakeven stop, trailing runner) — two different angles landing on the same shape is a real signal it's structurally right, not a coincidence:

1. Take partial profit at the real, well-calibrated T1 — preserves the existing target's proven reliability.
2. Let the remainder ride past T1 with a TRAILING stop instead of a second fixed target — sidesteps needing to predict in advance whether a trade is "a big one" (which is exactly what both Layer 2 candidates, GARCH vol scale and next-level distance, already failed to do, r≈0.05 each).
3. Trail width itself must be data-derived (no static thresholds) — the natural source is the "how far does price pull back before setting a new favorable extreme during a real continuation" distribution, buildable from the same bar-walking machinery already used throughout this thread.

User confirmed: prioritize this over patching the fixed-target sweep, and dispatch the combinatorial exploration (scale fraction × trail width, per setup_type) to Gemini per the standing "Gemini for mining, Claude for implementation" convention. Dispatched 2026-07-19 (`scratch/claude_request.md`) with the sweep's own overfitting failure written into the brief as a required guardrail (thin-tail gate, plateau check against neighboring configs, chronological out-of-sample split) so the same mistake doesn't get repeated.

## Gemini's result, audited — a narrow real finding, not a broad one, and that's a good sign

Gemini wrote `scripts/backtest_scaleout_runner.mjs`. The chat response was the usual near-empty self-report (known recurring truncation pattern), so as always the actual script and persisted `performance_audit` rows were audited directly, not the chat text.

**Code audit**: the core simulation logic holds up. Chronological order is preserved throughout (trades sorted by `fired_at`, `events` array inherits that order, so the in-sample/out-of-sample split via array slicing is genuinely chronological — not a random shuffle dressed up as OOS). Best-in-sample selection correctly uses ONLY the first-2/3 EV to pick a config, then evaluates the held-out last-1/3 separately — the right way to do an honest OOS test, no leakage. Pullback-distribution candidates for trail width are genuinely data-derived (walks the real favorable-extreme path from entry, records real retracement depths, takes percentiles) — not hardcoded. One real bug found and fixed: `new Date().toISOString().split('T')[0]` for `run_date` — the exact forbidden pattern this codebase's own hard rule warns about (JS UTC vs the DB's ET trading day). Fixed to `SELECT CURRENT_DATE::text`. Also added a `funnel` object to make the guardrail rejection funnel visible (Gemini's version enforced the guardrails correctly but reported zero visibility into how many setups got rejected at each stage) — this is a transparency addition, not a logic change, and re-running afterward reproduced byte-identical EV numbers, confirming the fix didn't alter the simulation.

**Result: of 96 setup_types tested, only 3 (3.1%) survived every guardrail** (18 rejected for a too-thin T1-reach count, 22 failed the plateau check, 53 failed the OOS-positive-and-beats-baseline requirement, 0 failed the rigor-clean check). That's a believable shape for a real result — contrast with the earlier fixed-target sweep, where 88/90 (97.8%) "improved," which was the tell that something was structurally wrong (see above). A narrow survival rate after strict guardrails is what a genuine, non-overfit finding is supposed to look like.

**Auditing the 3 survivors individually** (never trust an aggregate pass/fail count without reading the actual numbers):
- **`VALUE_AREA_RESPONSIVE_SHORT` — genuinely strong.** Baseline (100% at T1) EV was actually NEGATIVE (-$1.99/trade). Scale-out config (50% at T1 / 50% runner, 16pt trail) blends to +$17.76 full-sample, +$19.24 in-sample, **+$14.81 out-of-sample** — real improvement holds up out of sample, not just in-sample curve-fit. N=28 T1-reaches, rigor-clean, stable across chronological thirds (11.57 / 26.90 / 14.81 — no sign flip, no collapse). This is the one worth taking seriously.
- **`OPEN_DRIVE_SHORT` — fragile, don't trust yet.** In-sample EV $33.67 collapses to OOS $2.40 (thirds: 33.06 / 34.29 / **2.40** — a real late-period degradation, not noise). Technically passes the ">0" OOS gate but by the thinnest possible margin, and N=18 T1-reaches is right at this script's own thin-tail floor. Passing a guardrail by a hair is not the same as a robust finding.
- **`CAM_R4_FADE_SHORT` — real but marginal.** Baseline $13.66 → blended $14.27, a $0.61/trade improvement. Technically survived every check (N=39, rigor-clean, OOS $10.10 positive) but the edge over doing nothing is small enough that transaction/slippage noise not modeled here could plausibly erase it.

**Honest bottom line**: the scale-out + trailing-runner design is validated as a real, viable mechanism (unlike the fixed-target sweep, which wasn't) — but it is NOT a broad fix applicable across the setup roster. It's a genuine, setup-specific opportunity for `VALUE_AREA_RESPONSIVE_SHORT` specifically, with `OPEN_DRIVE_SHORT` and `CAM_R4_FADE_SHORT` too thin/marginal to act on without more data. Nothing wired live — `SCALEOUT_RUNNER_TEST` rows are backtest-only, same standing caveat as everything else in this thread.

## "What's the decision, explain 90→3, and once fixed can we compare old vs new walk-forward" — clarifying what actually happened

User asked three sharp follow-ups that surfaced a real risk of the two experiments blurring together in memory.

**Clarified the 90→3 framing is wrong — these are two different questions, not one pool narrowing.** The 88/90 number (wider FIXED target, no overfitting guardrails at all) was never trustworthy — it just hadn't been stress-tested yet, and `backtest_target_sweep_v2.mjs` proved that the moment it tried to formalize the same idea properly. The 3/96 number (partial scale-out + trailing runner, WITH full guardrails: thin-tail, plateau, OOS, rigor) is a different mechanism, tested rigorously for the first time. **Explicitly confirmed: this does NOT mean "we've now calibrated large targets for 3 setups."** The scale-out finding validates an alternative EXIT STRUCTURE for exactly one setup (`VALUE_AREA_RESPONSIVE_SHORT`). The Layer 1 problem — the live target calibration itself being blind to genuine continuation — remains fully broken and unfixed for the other ~90-107 setups on the roster. That's still open, separate work.

**Decision presented and resolved (2026-07-19)**: build the live trailing-stop mechanism now for `VALUE_AREA_RESPONSIVE_SHORT` (N=28, real, OOS-validated), or wait for more data. **User chose: wait for more N.** This is the first-of-its-kind live execution mechanism in the codebase (no trailing-stop state machine exists anywhere else to build on) and N=28 is thin for that kind of change even though the statistics are real. `OPEN_DECISION` updated to reflect this as a deliberate wait-and-recheck (not urgently pending, not closed) — revisit once the T1-reach count grows meaningfully, re-running `backtest_scaleout_runner.mjs` to confirm the config still survives all four guardrails before building anything live.

**Walk-forward comparison, confirmed as the right next step, gated correctly**: user wants a before/after comparison (targets, stops, P&L, MAE, MFE) between the existing 2yr walk-forward (`scratch/backtest_prop_2yr_walkforward_RESULTS.md`, from earlier this session) and a new one run against corrected calibration — but only once there's actually something corrected to compare against. That means Layer 1 needs to be genuinely fixed first (repair `backtest_target_sweep_v2.mjs`'s overfitting — add a thin-tail gate, fill in the candidate grid between the current live target and the wide percentiles it jumped past), not just diagnosed as broken. Not yet started.

## Layer 1 fix, and a much bigger correction found while cross-checking it

User confirmed: fix `backtest_target_sweep_v2.mjs`'s overfitting problem, and "do what you think is best." Rewrote it applying the same guardrail methodology validated in the scale-out script: a thin-tail gate (`MIN_TARGET_HITS=15`), a candidate grid ANCHORED to the current live target (1.0x/1.1x/1.25x/1.5x/1.75x/2.0x, unioned with true-MFE percentiles — so it can no longer skip past the already-validated region), a genuine chronological out-of-sample split, and a plateau check against numeric neighbors. Re-ran: **18 of 103 setup_types (17.5%) survived** — a believable, non-suspicious rate (contrast the original 88/90).

**While building this, an important design choice paid off immediately**: this rewrite computes its baseline (the CURRENT live target's EV) via the SAME chronological resimulation as every candidate, found by looking up the old target inside its own `candidateResults` array — never by reading the stored live `OPTIMAL_STOP.ev_per_trade` column. That choice turned out to matter enormously, because auditing `VALUE_AREA_RESPONSIVE_SHORT`'s numbers side by side across both scripts surfaced a real bug in `backtest_scaleout_runner.mjs`.

**The bug**: `backtest_scaleout_runner.mjs` sourced its "baseline EV" directly from the live `OPTIMAL_STOP.ev_per_trade` column (-$1.99 for `VALUE_AREA_RESPONSIVE_SHORT`) instead of resimulating it. That stored value is computed by the OLD, order-blind `sweepOptimalTarget()` against `mae_points`/`mfe_points` — and those columns were captured under whatever stop/target was live when each individual trade historically fired, NOT today's current live values. Directly verified: `VALUE_AREA_RESPONSIVE_SHORT`'s historical stop distances actually used across its trade history ranged 15-28pt+, while its CURRENT live stop is 10pt — a much tighter boundary. Testing today's 10pt stop against MAE data that was never bounded to 10pt in the first place (and checking it order-blind, with no notion of whether the tight stop or the target would have been hit first) silently produces a wrong, often falsely-negative number. A properly self-consistent chronological resimulation of the exact same 100%-at-T1 baseline (same stop=10/target=40, walked bar-by-bar from entry) gives **+$15.22/trade full-sample, +$18.65 out-of-sample** — not a loser at all.

**This reverses the finding.** Against the corrected baseline, the originally-reported scale-out config ($17.76 full / $14.81 OOS) actually LOSES out-of-sample (-$3.84/trade vs. the true baseline's $18.65). `VALUE_AREA_RESPONSIVE_SHORT` no longer survives the scale-out test at all once fairly baselined.

**Fixed the script properly** (not patched around it): `backtest_scaleout_runner.mjs` now always resimulates its own baseline in the same loop that computes Portion A's pnl (which was already the correct chronological 100%-at-T1 computation — it just wasn't being kept/compared against). The promotion gate now requires the scale-out config beat baseline BOTH full-sample AND out-of-sample (previously only OOS>0 was required, an easier bar). Re-ran clean: **4 of 96 setups now survive** (up from the original, bug-tainted 3):

| Setup | Baseline (full/OOS) | Scale-out (full/OOS) | N (T1 reached) |
|---|---|---|---|
| `CAM_R4_FADE_SHORT` | $6.32 / $0.42 | **$14.27 / $10.10** | 39 |
| `PW_HIGH_FADE_LONG` | $19.51 / $5.71 | $21.18 / $8.45 | 16 (thin) |
| `FLOOR_S1_FADE_LONG` | $4.83 / $6.46 | $5.05 / $7.24 | 43 (marginal) |
| `CAM_R2_FADE_LONG` | $1.43 / $0.32 | $1.69 / $2.65 | 36 (marginal, baseline itself near-breakeven) |

`CAM_R4_FADE_SHORT` is now the real, strong finding — a wide, OOS-durable improvement over an underlying setup that's barely breaking even at its current 100%-at-T1 target. The `OPEN_DECISION`/`RESEARCH_CLAIM` for `VALUE_AREA_RESPONSIVE_SHORT` were retracted (not silently deleted — the record shows what was wrong and why); new ones recorded for `CAM_R4_FADE_SHORT`. The user's earlier "wait for more N" decision was specifically about `VALUE_AREA_RESPONSIVE_SHORT`, which no longer applies to a setup that no longer exists as a finding — a fresh decision is needed for `CAM_R4_FADE_SHORT` when it comes up.

**Independently spot-checked `target_sweep_v2`'s own baseline computation** before trusting its 18 survivors, given today's own lesson that a structurally-fine-looking script can still have a baseline bug: `DAILY_OPEN_FADE_LONG`'s reported baseline ($13.67) matches a from-scratch independent resimulation ($13.75, difference explained by per-setup vs. flat dpp rounding) — confirmed clean, no equivalent bug.

**General lesson, worth stating plainly**: auditing a backtest's core simulation LOGIC is not the same as auditing where its BASELINE comes from. This session's earlier audit of `backtest_scaleout_runner.mjs` checked the Portion A/B chronological simulation carefully and found it sound — but didn't independently verify that the *baseline it was being compared against* was computed the same way. A comparison is only as trustworthy as its least-audited side.

## Walk-forward comparison, finally run — one more stale-data catch first

Before running the comparison, a second data-hygiene bug from the SAME family surfaced: `target_sweep_v2`'s FIRST (broken, spike-picking) run had persisted a `TARGET_SWEEP_V2` row for all 103 tested setup_types unconditionally; the corrected rewrite only inserted/updated the 18 that survived its guardrails, leaving the other 85 setups' original spike-picked rows (e.g. `PD_LOW_FADE_SHORT`'s 719.8pt pick off a single trade) sitting in the table with the same `run_date`, indistinguishable from the 18 legitimate ones by a naive `DISTINCT ON` query. Caught immediately when the walk-forward script logged "103 setup_types have a validated corrected target" instead of the expected 18 — verified by checking which persisted rows had the new fields (`rigor`, `oosEv`) the corrected script writes vs. which didn't, deleted the 85 stale rows. Same root cause, same fix pattern as the earlier `SCALEOUT_RUNNER_TEST` stale-row cleanup — worth naming as its own recurring risk: **whenever a script's guardrails get tightened and re-run, anything that no longer survives needs its OLD row deleted, not just left unrefreshed.**

**Result** (`scratch/backtest_prop_2yr_walkforward_CORRECTED_TARGETS.mjs`, companion to the existing `scratch/backtest_prop_2yr_walkforward_FILTERED.mjs` — identical DLL/Regime-A day-loop logic, the only change is the 18 corrected setups' trades resimulated under their new target/existing stop instead of their original `actual_pnl`):

| Metric | OLD (live calibration) | NEW (18 corrected targets) | Δ |
|---|---|---|---|
| Total P&L (Regime A, 2yr) | $21,152.01 | $29,095.64 | **+$7,943.63 (+37.6%)** |
| Trade count | 3,462 | 3,863 | +401 |
| Win rate | 61.7% | 53.9% | -7.8pp |
| Max drawdown | $5,299.12 | $5,792.68 | +$493.56 |

(Numbers here are the FINAL, commission-corrected figures — see the commission-audit section below. An earlier pass reported +$9,372/+44% using a flat $/pt approximation; the exact formula brought it down to +$7,944/+37.6%, still a real, strong result.)

Win rate dropping is expected, not a red flag — corrected targets are mostly wider, so fewer trades reach target but each win is bigger (the whole point of an EV-optimal search rather than a win-rate-optimal one). Trade count rising is a real second-order effect, DIRECTLY VERIFIED (not just asserted) after the user pushed back on "3800 trades seems like a lot if we're letting trades run": 335 of the ~400 additional trades (84%) are concentrated in exactly the 18 corrected setups, and every single one of them shows MORE trades under the new calibration, several dramatically (`PD_LOW_FADE_LONG`: 11→74 trades, 6.7x). Mechanism confirmed: these setups were being suppressed by the day-loop's rolling-EV eligibility filter (`n>=20 && ev>=-$5`) because their OLD, understated target dragged their historical rolling EV below the eligibility bar on many days — correcting the target raises that rolling EV, making them eligible on far more days. The remaining ~65 trades are a smaller second-order cascade through the DLL lockout's daily timing (changing a day's cumulative P&L path can shift when the -$400 lockout triggers, affecting how many of that SAME day's OTHER trades, from any setup, get included). 17 of 18 corrected setups show a real positive P&L delta; `PD_OR_MID_FADE_SHORT` is a small loser once retested at Regime-A eligibility level despite passing its own isolated OOS check — reasonable noise, not concerning. Full per-setup breakdown: `scratch/backtest_prop_2yr_walkforward_CORRECTED_TARGETS_RESULTS.md`.

## "Are you including commissions in PnL?" — no, an approximation of them, now fixed

Sharp catch. Every resimulation script in this thread (`backtest_target_sweep_v2.mjs`, `backtest_scaleout_runner.mjs`, the walk-forward comparison) computed pnl as `points * dpp` using a flat, empirically-derived dollars-per-point multiplier (`DEFAULT_DPP`, or a flat `2.02` in the walk-forward script) instead of the exact live formula. Verified directly: `server/routes/acd.js`'s real resolution path always uses `pnl = points * PNL_PER_POINT - COMMISSION` (exactly $2/pt, exactly $1 flat round-trip commission, from `server/config/instruments.js` — whose own header comment confirms MNQ's $1 commission was independently verified against a real broker statement, not guessed, unlike NQ's provisional $2). A flat dpp multiplier is only an approximation of that exact formula, and drifts further from it the more a candidate target/stop distance differs from whatever distance the dpp was empirically calibrated against — worst for setups with unusually tight stops (a few points), where the flat $1 commission is a much larger fraction of the trade's total point value.

**Fixed all three scripts** to use the exact formula (`signed_points * PNL_PER_POINT - COMMISSION`), importing `LIVE_INSTRUMENT` from `server/config/instruments.js` directly rather than any approximation. Re-ran everything: the same 18 setups survive `target_sweep_v2` (EVs shifted down slightly but the qualitative finding is robust), the scale-out survivor count dropped from 4 to 3 (`CAM_R2_FADE_LONG` no longer clears the corrected promotion bar — real, not a bug, since its baseline was already only $1.43/trade, thin enough that a uniform -$1/trade commission correction was enough to tip it over), and the walk-forward improvement came down from the earlier imprecise +44% to the correct +37.6%.

**Caught a third instance of the stale-row pattern while re-running the scale-out script**: since `DEFAULT_DPP` had been exactly 2 (not commission-adjusted), the fix shifts every trade's pnl by exactly -$1.00 — a clean, verifiable signature. Three of the four scale-out survivors shifted by exactly -$1.00 on both full and OOS EV as expected; `CAM_R2_FADE_LONG`'s numbers were byte-identical to the pre-fix run, proving its row hadn't actually been refreshed (it no longer survives the corrected guardrails). Deleted it. **Fixed the root cause this time instead of just cleaning up again**: both `backtest_scaleout_runner.mjs` and `backtest_target_sweep_v2.mjs` now explicitly `DELETE ... WHERE NOT (signal_name = ANY(survivors))` after every run, so a setup that stops surviving can never again leave a silently-stale row behind.

**Explicit caveat**: this is a retrospective "what if every trade in the 2-year history had used the corrected target" comparison, not a claim this P&L would have been captured live in real time — calibration itself would have evolved differently along the way in reality. Same standing disclosure as every other walk-forward this session: most of `active_setups` is `origin_status='BACKFILL'`, a strategy simulation over reconstructed signals, not a record of real account history.

**Status at this point**: Layer 1 fix is real, audited (including a from-scratch baseline spot-check and a second stale-row cleanup), and quantified end-to-end. Nothing wired live yet — the 18 corrected targets are backtest-only findings pending a deliberate promotion decision.

## "Wire the 18. Why just the 18? What was excluded and why?" — full context, then a durable live fix

User asked for the complete picture before wiring anything, then to proceed with the best approach. Sent the full per-setup exclusion log as a file. Summary of the 85 exclusions: **22 thin-tail** (no candidate had ≥15 trades actually reach it — not evidence of "no edge," just not enough data yet), **38 failed the plateau check** (the best-looking candidate was an isolated spike, neighbors too thin or negative — the exact overfitting failure mode the guardrails exist to catch), **22 failed OOS-or-baseline** (looked good in-sample, lost money out-of-sample, or didn't actually beat the current live target once fairly compared), **3 not rigor-clean** (day-clustered or chronologically unstable). Only the 22 OOS failures are closer to "tested and didn't hold up" — the other 63 simply lack sufficient evidence either way.

**Decided: wire it durably, not as a one-time patch.** The original `OPEN_DECISION` explicitly flagged that patching 18 `OPTIMAL_STOP` rows directly would get silently reverted by the next scheduled `update_optimal_stops.mjs` run (weekly Sunday + daily 8:20 PM ET). Instead:

1. **Extracted the corrected methodology into `server/services/targetCalibrationService.js`** (`computeCorrectedTarget()`) — a shared, importable function, not a second hand-copied version. Refactored `backtest_target_sweep_v2.mjs` to use it; verified byte-identical to the pre-refactor 18 survivors before trusting it.
2. **Wired it into `update_optimal_stops.mjs` itself** — after computing each setup's stop via the existing EV-sweep (unchanged), the corrected methodology is attempted using that stop as a fixed anchor. If it clears every guardrail, it overrides the target (tagged `notes.method='corrected-resim'`); otherwise the existing EV-sweep/`p50_mfe` fallback is used unchanged — zero regression risk for anything that doesn't have enough data yet. Every future scheduled run re-evaluates every setup_type automatically — no more manually-maintained list, satisfying CLAUDE.md's no-dead-ends rule (a real recheck/recalibration path, not a compute-once-and-forget patch).
3. **Updated `test_invariants.mjs` check `[5]`** to re-derive `corrected-resim` rows via the real `computeCorrectedTarget()` function (not a hand-copied comparison) before trusting them — verified clean against the OLD data first (baseline, before any live rows used the new path), then again after.
4. **Ran it live for real**: first pass found only **17** setups (not 18) survived — a real, investigated discrepancy, not hand-waved away. Root cause: with an *identical* (stop, anchor-target) pair, `CAM_S2_FADE_LONG` flipped from surviving (oosEv=+$0.23, N=157) to failing (oosEv=-$13.78, N=154) purely from the live pipeline's pre-existing exclusion of `TIME_EXPIRED` trades from its trade population. That exclusion is itself a second instance of the exact truncation problem this whole fix targets — a `TIME_EXPIRED` trade timed out under a *shorter* historical resolution window, but `computeCorrectedTarget`'s own 390-bar walk from entry can correctly re-evaluate it. Fixed by giving the corrected-target call its own wider, `TIME_EXPIRED`-inclusive trade population (matching `backtest_target_sweep_v2.mjs`'s own convention) while leaving the old EV-sweep's population untouched (out of scope, pre-existing, unrelated). Re-ran: **19/103 setup_types** now use the corrected path — all 18 original backtest survivors plus `IB_BULLISH` (which qualifies using the live pipeline's own freshly-computed stop/target anchor rather than a stale previously-stored one).
5. **Full self-check after the live write**: `test_invariants.mjs` — all 103 `OPTIMAL_STOP` rows (19 corrected-resim + 84 EV-sweep/fallback) match a fresh re-derivation via the real production functions, 0 mismatches. Lint (backend scripts + `targetCalibrationService.js`) clean. Build clean. No new client/server errors. `check-docs-drift.sh`'s hardcoded-pattern flags all pre-date this session (verified via `git blame`). Git status clean, everything committed.
6. **`ARCHITECTURE.md` and `CLAUDE.md` updated** with the new service and the two-path target-selection behavior. `OPEN_DECISION` `promote_18_layer1_corrected_targets` resolved.

**What's still open, deliberately not addressed today**: (a) the old EV-sweep's own dpp approximation (still present for the ~84 setups that don't clear the corrected guardrails) — left untouched as out-of-scope for this specific wiring task; (b) `OPEN_DECISION` `live_setup_status_pipeline_likely_has_same_ev_bugs` — the separate live *suppression* pipeline (`backtest_setup_status.mjs`, different from `update_optimal_stops.mjs`) has not been checked for the same class of bugs, and remains the biggest unexamined risk from this whole thread.

## Live suppression pipeline checked (resolved) → surfaced the real headline-vs-realistic split

Read `backtest_setup_status.mjs` directly. Clean — computes EV straight from `AVG(actual_pnl)` (already commission-exact), no `mae_points`/`mfe_points` sweep at all, neither of today's two bugs apply. But it surfaced a real consequence instead: `actual_pnl` reflects whatever target was live *when each trade fired*, so a setup's SUPPRESS/PROMOTE decision is computed against pre-correction history. Confirmed: `PD_LOW_FADE_LONG` is currently SUPPRESSED despite its corrected target being one of the biggest walk-forward winners; 5 more are borderline. Verified this self-heals (`liveStats._opt` — now holding the corrected target — is read for both ACTIVE and SHADOW candidates), just not instantly. Added `CORRECTED-TARGET-BUT-SUPPRESSED WATCH` to session-start. `OPEN_DECISION` resolved.

## "Should we walk-forward again?" → no, split the existing headline number instead

User asked whether to re-run the comparison now that 19 (not 18) setups are corrected live. Recommended against it — the delta from adding 1 more setup (`IB_BULLISH`, `DAY_TYPE_MANAGED`, handled specially) wouldn't move the number meaningfully. Instead, given the suppression finding above, did something more valuable: **decomposed the existing $7,943.63 headline** into what's realistically available now vs. what depends on suppression recovery.

Matched every setup_type between the OLD and NEW walk-forward's per-setup Regime-A `CUM` tables (`scratch/backtest_prop_2yr_walkforward_FILTERED_RESULTS.md` vs `scratch/backtest_prop_2yr_walkforward_CORRECTED_TARGETS_RESULTS.md`) and computed the true eligibility-filtered delta per setup — verified the sum across ALL setups equals $7,943.63 exactly before trusting the breakdown. Result:

- The 18 corrected setups gain **+$13,026.57** in isolation.
- Correcting them shifts daily DLL-lockout timing enough to drag **other, untouched setups down -$5,082.94** — a real, unavoidable side effect (bigger wins/losses shift when the day's -$400 lockout triggers, changing how much of other setups' same-day trades get included). Net: $13,026.57 − $5,082.94 = $7,943.63 ✓.
- Of the 18's own gain, **$5,646.07 (43%) comes from exactly the 6 at-risk setups** flagged in `CORRECTED-TARGET-BUT-SUPPRESSED WATCH` — **71% of the entire headline number**.

**Honest split**: realistic near-term improvement (safe setups + the DLL drag, both already live) ≈ **+$2,298**, not +$7,944. The remaining ≈**+$5,646** is real but pending — contingent on `PD_LOW_FADE_LONG` and the 5 borderline setups clearing suppression via the existing N≥15/90-day recovery mechanism. Recorded as `RESEARCH_CLAIM` `walkforward_headline_split_realistic_vs_pending` — the original `+37.6%` headline should never be cited again without this caveat.

(Design + any preliminary data pulled in this session continues below as the conversation progresses.)
