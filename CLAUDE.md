# CLAUDE.md

Trading journal: React frontend (Vite, port 3000) + Express backend (port 3002) + PostgreSQL (`trading_journal` db). See [ARCHITECTURE.md](ARCHITECTURE.md) for full route/service/table inventory — this file is conventions and rules, not structure.

**Start of session / after a context clear: read [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md) first.** It tracks unfinished work, unconfirmed proposals, and stale-stats findings from prior sessions — the goal is to never lose a thread to a context reset.

## Dev workflow

- Start: `./start.sh` (kills stale processes, starts server + client, self-heals the `pre-commit` hook symlink) — Stop: `./stop.sh` or `fuser -k 3002/tcp` — Restart: `./restart.sh` (thin wrapper around `start.sh`, not a separate implementation)
- **Single-instance server takeover lives in ONE place: `scripts/lib/server-lifecycle.sh`** (`cleanup_ports`/`stop_systemd_server`/`handback_on_exit`/`start_health_monitor`), sourced by `start.sh`, `restart.sh`, `stop.sh`, and `scripts/dev-server-guard.sh` (what `npm run server` now actually runs). Found 2026-08-18: `restart.sh` had silently duplicated `start.sh`'s cleanup logic by hand and drifted out of sync (missing the systemd-stop-first step and the EXIT-trap handback), and bare `npm run server` had none of this at all — together these let 5 simultaneous `nodemon` supervisors pile up from separate sessions across one day, fighting over port 3002 and each other until `trading-journal-server.service` tripped its failure-burst limit and went permanently `failed` mid-RTH, with zero live setups firing `ACTIVE` all day as a result. **Never reimplement any piece of this by hand in a new entry point — source the shared lib.** `.claude/hooks/session-start.sh` also has a standing backstop that loudly flags >1 simultaneous `nodemon server/index.js` supervisor if something still bypasses all of the above.
- `git commit` runs a **pre-commit hook** (`scripts/git-hooks/pre-commit`, symlinked from `.git/hooks/pre-commit` — `.git/hooks/` isn't tracked, `./start.sh` re-creates the symlink if missing) that blocks new hardcoded trading-threshold anti-patterns in `acd.js`/`caseEngine.js`, plus 3 more broadly-scoped patterns across `server/`/`src/`/`scripts/` (wrong $/pt constants, `SUM(pnl)`-style CumPL-diff violations, the JS-`toISOString()`-for-a-trading-day-date bug) — 7 patterns total, defined once in `scripts/hardcoded-threshold-patterns.sh` and shared with the Stop hook's advisory version. Deliberate exception: `ALLOW_HARDCODED=1 git commit ...`.
- Server only: `npm run server` (now `scripts/dev-server-guard.sh` — stops systemd, kills any stale nodemon/node instance, hands the port back on exit; no longer a bare `nodemon` with no cleanup) — Client only: `npm run client`
- DB: `server/schema.sql` is a full `pg_dump --schema-only` snapshot (122 tables/views, regenerated 2026-06-30 after a dead-table cleanup) — there's still no tracked migration history, so it drifts again the moment a table is added/altered live without regenerating. See the regen command in [ARCHITECTURE.md](ARCHITECTURE.md#schema-source-of-truth). When in doubt, query `information_schema.tables`/`information_schema.columns` against the live DB rather than trusting the file is current.

## Hard rules

- **Anything that drives or anticipates a decision — a data point, a computed signal, a classifier, a backtest finding, not just a "script" — must be built so the system keeps learning from it and can revisit it. No dead ends.** Before considering anything of this kind done, check all four:
  1. **Persisted somewhere queryable** (`performance_audit` or a real table) — not a script-local variable, a one-off `scratch/*.json` file, or a hardcoded literal in the serving code.
  2. **Has a recheck/recalibration path** — a scheduled script (`run_weekly_backtests.sh`), a `RESEARCH_CLAIM` 30-day cycle, or at minimum a documented manual recheck trigger. Not compute-once-and-forget.
  3. **Actually wired to a consumer, or explicitly flagged if not** via a `flag_decision.mjs` `OPEN_DECISION` — a signal nobody reads is a dead end whether or not it's persisted.
  4. **Discoverable** — an entry in `ARCHITECTURE.md`'s inventory or this file's "Where to look" section, not an orphan only findable by grepping and hoping.
  This is the reason `RESEARCH_CLAIM`, `OPEN_DECISION`, "export the real function," and the no-static-thresholds rule below already exist — treat it as the reason those mechanisms exist, not a fifth one to build. See [docs/HARD_RULES_DETAIL.md#no-dead-ends-the-four-part-checklist](docs/HARD_RULES_DETAIL.md#no-dead-ends-the-four-part-checklist) for the incident that prompted this and the recurring hand-typed-stat failure case.
- **Any new setup, signal, or calibration finding must be evaluated for BOTH the RTH window (9:30am-4pm ET) and the Globex/overnight window (6PM-8:30AM ET) before being considered complete — never default to RTH-only and call it done.** A tested-and-recorded "doesn't apply overnight" is fine; an untested gap is not. The RTH level-fade engine (`acd.js` ~3830-4430) and `detectGlobexSetup()` are structurally different — if their live-wiring mechanisms differ (e.g. only one has a `sizeMultiplier` stack), wire into whichever supports it and flag (`OPEN_DECISION`) whichever doesn't, rather than silently leaving a finding RTH-only. See [docs/HARD_RULES_DETAIL.md#rth-globex-both-required](docs/HARD_RULES_DETAIL.md#rth-globex-both-required).
- **No static thresholds.** Every threshold in this codebase must be derived from a rolling distribution (σ from a rolling mean/std), never a hardcoded number. This applies to entries, stops, targets, signal triggers — everything. If you're about to write a literal number as a cutoff, stop and compute it from historical data instead. **This rule stops new hardcoded numbers — it does not, by itself, catch existing ones that already slipped through and have since decayed.** Found 2026-09-07: `sizeMultiplier`'s NL30/loss-streak/win-streak/overnight-alignment branches were each calibrated once from a real backtest (dated 2026-07-05 to 2026-07-08 in their own code comments) and never rechecked — over the following ~2 months, multiple branches decayed to near-zero edge or fully inverted (a boost landing on what's now a negative-EV bucket, a penalty landing on the wrong bucket entirely), invisible because nothing ever re-validated the comment against current data. **A hardcoded WR/EV/z-score backtest comment justifying a live branch, with no corresponding scheduled recalibration script, is a standing audit target, not a one-time-verified fact** — when investigating "is this signal still good" (prompted by the user or found incidentally), grep the relevant IIFE/function for dated backtest comments lacking a scheduled recalibration path and re-verify each against current real data before trusting it, the same way `sizemultiplier_loss_win_streak_overnight_stale_20260907`/`nl30_regime_conditioning_stale_boost_inverted_20260907` did.
- **P&L must use the CumPL diff method**, not `SUM(pnl)` or `SUM(FlatToFlat)` — both overcount. See the SQL pattern in [ARCHITECTURE.md](ARCHITECTURE.md#pnl-calculation-cumpl-diff--critical-dont-regress-this). `/api/daily-logs` and `/api/stats/daily` both implement this — keep them in sync if you touch either. **A raw, account-type-unscoped CumPL-diff is a DIFFERENT and WORSE bug than the `SUM(pnl)` it replaces — always scope to `custom_fields->>'account' LIKE '%-PRO%'`**, matching `stats.js`'s `/stats/capture-ratio`. When there's no PRO-account CumPL data for a given day, fall back to raw `SUM(pnl)` for that day only — imprecise but not corrupted. See [docs/HARD_RULES_DETAIL.md#cumpl-diff-pl-method](docs/HARD_RULES_DETAIL.md#cumpl-diff-pl-method) for the practice-account balance-reset incident this guards against.
- **This journal trades MNQ (Micro E-mini Nasdaq): $2/point, $2 round-trip commission ($1 per side).** Ground truth lives in `server/config/instruments.js`'s `LIVE_INSTRUMENT.commissionPerRoundTrip` (imported by `server/routes/acd.js` and `scripts/update_optimal_stops.mjs`), mirrored in `src/constants/contract.js` for the frontend. **Never copy a `$/pt` or commission constant from another script without checking it matches this** — and don't trust an in-code "verified" comment either, see the standing rule on that below; check it against `acd.js`'s live values first. See [docs/HARD_RULES_DETAIL.md#mnq-commission-constant](docs/HARD_RULES_DETAIL.md#mnq-commission-constant) for the 2026-08-11 commission correction and a prior $/pt-copy incident.
- **~80% of `active_setups` is synthetic backfill data, not live-fired trades — check `origin_status` before treating any figure derived from this table as realized/historical performance.** `active_setups.origin_status` (immutable at insert) distinguishes `ACTIVE` (real, live-visible) / `SHADOW` (suppressed, background-only) / `BACKFILL` (synthetic) / `UNKNOWN` (pre-2026-07-09, unrecoverable). **A `SUM(actual_pnl)` or backtest built from `active_setups` without filtering by `origin_status` is a strategy simulation, not an account record.** Before presenting any `active_setups`-derived dollar figure as "what happened," check what fraction of its population is `BACKFILL`. See [docs/HARD_RULES_DETAIL.md#activesetups-backfill-vs-originstatus](docs/HARD_RULES_DETAIL.md#activesetups-backfill-vs-originstatus).
- **`active_setups.is_cluster_primary` (added 2026-09-07, cluster touch credit Phase 2) is a SECOND, independent filter every real-N/EV aggregation needs, on top of `origin_status` — a query that filters `origin_status` correctly but not this will still overcount.** A confluence-cluster touch now writes one primary row (the winning candidate) plus `is_cluster_primary=false` sibling rows for the other cluster members (`suppression_reason IN ('CLUSTER_SIBLING_TOUCH_CREDIT','CLUSTER_SIBLING_TOUCH_CREDIT_BACKFILL')`) so the losing siblings get real N credit for setup-level stats — but a naive `COUNT(*)`/`SUM(actual_pnl)` grouped by `setup_type` without `AND (is_cluster_primary IS NULL OR is_cluster_primary = true)` double-counts the same touch event once per cluster member. Caught 2026-09-07 via self-audit: an ad hoc verification query missing this filter overcounted real decisive N by ~43% (3,803 vs. the correct 2,653) — DeepSeek's own independently-run query had it right, Claude's didn't. `is_cluster_primary` defaults `true` (rows predating the column, and any non-clustered touch, are correctly primary) — the risk is a *new* aggregation query written without the exclusion, not existing calibration scripts (`backtest_setup_status.mjs` and the pooled/real trade filters already apply it). See `scripts/backtest_setup_status.mjs`'s `REAL_TRADE_FILTER`/`POOLED_TRADE_FILTER` for the canonical filter pair to reuse rather than hand-rolling a new one. **This risk is NOT backend-only** — found live 2026-09-08: `server/public/quick-check.html`'s own displayed Net P&L (`computeRangeStats()`/`renderEquityCurve()`) summed `actual_pnl` with no `is_cluster_primary` filter at all, inflating a real -$1,908 into a displayed +$738 in one day. **There are 4 real insert sites that can produce a cluster-tagged row, not 3** — the 4th, `EARLY_TOUCH_BACKFILL` (a level already touched by the time the poller catches up, commonly right at the RTH open), had zero cluster-tagging until fixed the same day; see the "Cluster touch credit" entry below for the full incident and both fixes.
- **Never fabricate a stat.** Any win-rate/hit-rate claim needs N≥20 in the sample before it's reported as decisive (see `engineReadHitRates.js` convention). Below that, say so explicitly rather than rounding to a confident-sounding number.
- **Never hand-type a WR%/N/$ literal into a live-rendered card, label, or description string.** This has recurred 7+ times, several silently drifting to *directionally wrong* (e.g. a hardcoded "88% WR" vs. a real 66% WR at negative EV). Either derive the text from a `row` field already fetched from `performance_audit` in the same function, or write a dedicated `scripts/backtest_<name>.mjs` that populates one — never hand-type the number, even as a placeholder "for now." **When touching ANY file that renders a WR%/N/$ figure for any reason, even in passing, check whether that figure is a live query result or a literal before moving on.** See [docs/HARD_RULES_DETAIL.md#never-hand-type-a-wrn-literal](docs/HARD_RULES_DETAIL.md#never-hand-type-a-wrn-literal) for all 7 instances.
- **No lookahead in backtests/replays.** Case engine, day-type reassessment, and all backtest scripts must only use information that would have been available at that point in time. This is a frequent source of subtle bugs — when writing a new backtest script, explicitly check that no future bar/level data leaks into a decision made earlier in the session.
- **Volume-profile computations (POC/VAH/VAL) must spread each bar's volume across its own high-low tick range, never bucket 100% of it at one price.** `server/services/developingValueService.js`'s `computeProfile(bars)` is the single correct reference implementation (spreads volume evenly across each bar's H-L range, then extends VAH/VAL alternately toward whichever side has more volume next until 70% total is enclosed) — for any period-range VA (week/month/quarter/2-day), call its exported `computeVolumeProfileForRange(queryFn, { startDate, endDate, ... })` wrapper rather than writing a new SQL aggregation. See [docs/HARD_RULES_DETAIL.md#volume-profile-spreading-pocvahval](docs/HARD_RULES_DETAIL.md#volume-profile-spreading-pocvahval) for the multi-location bucket-by-low bug this replaced.
- **Export the real function, never reimplement live-derived classification logic inline in a backtest script.** If a backtest/mining script needs a value that a live service already computes (a regime, a day-type, a level, any derived classifier — not a raw DB column), it must `import`/reuse that service's real exported function, or read a persisted canonical table populated by it, never re-code the math by hand. When adding a new derived/live classifier, export its core function(s) at creation time rather than waiting for a second script to need it and reimplement instead. See [docs/HARD_RULES_DETAIL.md#export-the-real-function-never-reimplement](docs/HARD_RULES_DETAIL.md#export-the-real-function-never-reimplement) for the two-independent-reimplementations-disagreeing incident.
- **Every tested research/exploratory claim — not just setup-calibration ones — gets recorded via `scripts/record_claim.mjs`, positive or negative.** `recordClaim({slug, claimText, sourceFile, sourceDate, sampleSize, winRate, evPerTrade, rigorStatus, status})` writes/updates a `signal_type='RESEARCH_CLAIM'` row in `performance_audit` with a 30-day `next_recheck_due`; `node scripts/record_claim.mjs --list` shows every claim and flags overdue ones. `status` is `CONFIRMED` (independently re-verified), `PROVISIONAL` (real but thin/unstable/unchecked-for-rigor), or `STALE` (needs a fresh recheck). This is separate from the setup-calibration tables (`SETUP_STATUS`/`OPTIMAL_STOP`/etc.), which have their own lifecycle. **Always import and call `recordClaim()` — never hand-write the INSERT**, even for a one-off script. See [docs/HARD_RULES_DETAIL.md#every-tested-claim-gets-recorded-via-recordclaimmjs](docs/HARD_RULES_DETAIL.md#every-tested-claim-gets-recorded-via-recordclaimmjs) for the hand-rolled-INSERT/same-day-recheck-due bug this caught.
- **A `next_recheck_due` on a `RESEARCH_CLAIM` row is a FLAG, not an auto-rerun** — if the finding is still open (not a settled/rejected/confirmed one-time study), wire its source script into `run_weekly_backtests.sh` or `run_daily_calibration.sh` too, or it never actually recomputes itself. Unlike `SETUP_STATUS` (a single generic `GROUP BY setup_type` scan that auto-covers every setup_type), each `RESEARCH_CLAIM` is backed by a bespoke script with no generic scanner — the promotion/wiring decision stays a human call, only the *recompute* becomes automatic once scheduled. `test_invariants.mjs` check `[10]` WARNs (aggregate count only) on unscheduled claims; per-claim detail is in `scratch/research_claim_unscheduled.txt` for triage. See [docs/HARD_RULES_DETAIL.md#researchclaim-recheck-flag-vs-recurring-cron](docs/HARD_RULES_DETAIL.md#researchclaim-recheck-flag-vs-recurring-cron).
- **Resolve every `isolation: worktree` Agent dispatch before the spawning session ends — never leave one for a future session to discover by accident.** The harness only auto-deletes an isolated worktree if the agent made *no* changes; if it made any, the path/branch are handed back and a human/session must explicitly commit+merge or deliberately discard (`git worktree remove` + delete the branch). `.claude/hooks/session-start.sh` flags any stray worktree at session start as a backstop, but the real fix is not leaving one dangling. See [docs/HARD_RULES_DETAIL.md#resolve-every-isolation-worktree-dispatch](docs/HARD_RULES_DETAIL.md#resolve-every-isolation-worktree-dispatch).
- **Do not guess on third-party tool behavior** (especially Sierra Chart specifics) — fetch documentation or ask before asserting how an external tool behaves.
- **Never write a file >400 lines in a single Write call.** Use Write for the first section, then `cat >>` via Bash to append the rest. This avoids the 32k output token limit that silently truncates large writes.
- **Use Gemini for mining/analysis, Claude for implementation.** When a task is combinatorial exploration or pattern-mining over `performance_audit`/trade data, dispatch it to Gemini via the `scratch/claude_request.md` protocol rather than grinding through it directly — Gemini has read-only DB access and doesn't burn Claude's context on large query output. Claude's job is building the systems and interpreting/applying what Gemini finds. Reserve direct work for small, deterministic, single-query aggregations, and for anything Gemini has already gotten wrong twice (see the 2-strikes rule below). Does not relax **Audit all Gemini output** below.
- **For higher-stakes work — anything that could end up gating/sizing a live trade, or any live-wiring change to production code — use a 3-phase Gemini workflow, not just mine-and-run.** (0) Before writing any code, send Gemini the intended approach/design (not code) and ask it to critique the plan itself. (1) Full AUTONOMOUS mine-and-run for the actual analysis. (2) A separate review-only pass on the resulting code (explicit "do not execute, do not edit" header) before trusting any number or shipping any live change. Use this specifically for anything touching risk management or live execution behavior, not just entry-signal research. See [docs/HARD_RULES_DETAIL.md#three-phase-gemini-workflow-for-higher-stakes-work](docs/HARD_RULES_DETAIL.md#three-phase-gemini-workflow-for-higher-stakes-work) for the incident that prompted adopting this.
- **"Claude looks to Gemini for critique and heavy lifting"** — the default mode for substantive research/analysis work generally, not just higher-stakes work. Gemini's job spans the full loop: (1) review the idea/design before code is written, (2) do the heavy-lifting mine-and-run analysis, (3) review code before it's implemented/shipped, including real implementation Claude is about to wire live. Claude's job is an explicit, stated review-and-approve gate on every piece of Gemini output — never a silent pass-through of a number, conclusion, or script — then synthesize/decide/report; this is not a request to consult the user before each dispatch. Reserve solo direct work for small single-query lookups or a direct factual question, not for anything that involves forming a conclusion from an analysis. See [docs/HARD_RULES_DETAIL.md#gemini-critique-and-heavy-lifting-broadened-default](docs/HARD_RULES_DETAIL.md#gemini-critique-and-heavy-lifting-broadened-default) for worked examples.
- **Gemini gets 2 correction attempts, then Claude takes over.** When Gemini's output is wrong (wrong numbers, wrong methodology, SQL error loops — not a partial/incomplete response), send a correction prompt and re-invoke. If the 2nd attempt is also wrong, stop correcting and do the task directly instead of retrying a 3rd time.
- **Audit all Gemini output before acting on it.** Gemini produces plausible-looking but sometimes wrong — occasionally fabricated — analysis. After reading `scratch/antigravity_response.md`, verify 3–5 key claims by re-querying the DB directly before promoting any finding to `performance_audit` or changing live parameters: (1) 100%/0% WR with N≥10 → check `COUNT(DISTINCT log_date)` for single-session clustering; (2) grid output → compare "current params" cell to existing `performance_audit` baseline; (3) N counts → verify with a direct `COUNT(*)`; (4) any TRADE recommendation → re-run the WR/EV query yourself. **Always read the actual script/query, never just the writeup, before trusting any "successfully executed" claim** — if a dispatch produces a script, re-run it locally before trusting its findings; never treat the response file's prose summary as the source of truth on its own. See [docs/HARD_RULES_DETAIL.md#audit-all-gemini-output-before-acting-on-it](docs/HARD_RULES_DETAIL.md#audit-all-gemini-output-before-acting-on-it) for the confirmed-fabrication incidents.
- **A correct diagnostic finding from DeepSeek/Gemini and its recommended remedy are two separate things — auditing the finding as accurate does not obligate following the recommendation.** Found 2026-09-07: a DeepSeek design critique correctly proved 2 of 4 proposed live-safety gates are structurally unreachable for the 3 service-poller detectors being wired (verified independently before accepting), then recommended wiring all 4 anyway with the 2 dead ones "documented as inert, for consistency." That recommendation conflicts with this codebase's own stronger, already-demonstrated standard (dead code gets deleted, not commented-and-kept — see the `lfPriorStop`/`lfPriorWin`/`sessionBiasMatch` removals the same session). Shipped with only the 2 live gates wired, explicitly deviating from the critique's own bottom line while fully accepting its diagnosis. **How to apply**: when a review pass nails the diagnosis but its suggested fix runs softer than an existing house convention, take the diagnosis, apply the house convention, and document the deviation and why — don't default to the reviewer's literal final answer just because the analysis leading up to it checked out.
- **Every pending product/architecture decision — "wire this in or delete it," "merge this branch," "change this cadence" — gets recorded via `scripts/flag_decision.mjs`, not left as prose in `docs/OPEN_THREADS.md`.** `flagDecision({slug, decisionText, sourceFile, sourceDate})` writes a `signal_type='OPEN_DECISION'` row; `decisionText` must include real context — what's being decided, why it matters, what resolving it looks like. `node scripts/flag_decision.mjs --list` shows every pending decision; `--resolve <slug> '<resolution text>'` closes one out. Every pending decision prints unconditionally at the start of every session (`.claude/hooks/session-start.sh`'s `OPEN_DECISIONS` section) — nothing here depends on a future session remembering to re-read prose elsewhere. See [docs/HARD_RULES_DETAIL.md#every-pending-decision-gets-recorded-via-flagdecisionmjs](docs/HARD_RULES_DETAIL.md#every-pending-decision-gets-recorded-via-flagdecisionmjs). **A slug's name can go stale even while its content stays accurate — when re-flagging an existing decision with `flagDecision()` grows its scope well past what the slug originally named** (found 2026-09-07: `nl30_regime_conditioning_needs_recalibration_20260907` was updated 3 times as more stale sizeMultiplier factors were found, until the slug — still NL30-specific — was actively misleading about a thread that had grown to cover 9 unrelated factors; NL30 itself had been done for hours by the time this was caught, only because the user said "I thought nl30 was done"), **resolve the old slug with a note explaining the rename and flag a fresh one that names the current scope**, rather than let an accumulated decision keep growing under its original narrow name. The session-start hook only prints the slug plus decision text, not a diff of how the scope evolved — a drifted name is invisible until someone opens the full text or gets confused by the slug alone.
- **A code comment claiming a number was "verified" or "cross-checked" is not itself verification** — this is a more dangerous risk than a one-off wrong Gemini response, because it sits trusted in committed production code instead of a scratch file someone might still double-check. Treat "verified" language in an existing comment as a claim to re-check when you're about to depend on the value it's attached to, not as a substitute for checking it yourself. See [docs/HARD_RULES_DETAIL.md#a-verified-code-comment-is-not-itself-verification](docs/HARD_RULES_DETAIL.md#a-verified-code-comment-is-not-itself-verification) for the `DEFAULT_DPP=5` incident (25 setup_types calibrated wrong before it was caught).
- **When a script both computes a derived value (a sweep, a classifier, an optimizer) and separately stores/prints a number labeled as describing it, that pairing needs a standing check** — a feature added on top of an existing field can silently break the field's own meaning without anyone touching the field's line. When you add a smarter computation on top of a value a field already claims to describe, either rewire that field in the same commit or explicitly rename/re-scope it — and if the value is trusted enough to sort/display/gate anything, add an invariant check that re-derives it via the real function, not a plausibility check a bug could trivially satisfy. See [docs/HARD_RULES_DETAIL.md#derived-value-field-drift-when-a-smarter-computation-is-added](docs/HARD_RULES_DETAIL.md#derived-value-field-drift-when-a-smarter-computation-is-added) for the `ev_per_trade` incident this names.
- **Auditing a backtest's simulation logic is not the same as auditing where its baseline comes from — a comparison is only as trustworthy as its least-audited side.** When building or auditing any script that claims "config X beats baseline Y," verify baseline Y was computed by the exact same method as X in the same pass — a stored/precomputed number read from elsewhere, even one that looks authoritative because it's "the live value," needs the same scrutiny as the thing being tested against it. See [docs/HARD_RULES_DETAIL.md#baseline-must-be-computed-the-same-way-as-the-candidate](docs/HARD_RULES_DETAIL.md#baseline-must-be-computed-the-same-way-as-the-candidate) for the incident this reversed a finding on.
- **A backtest comparing two conditions against a shared, unchanging exit needs to rule out a structural/algebraic advantage before crediting the "smarter" condition with the win.** When a new backtest's two arms differ in WHEN they enter but share the same exit levels, add an entry-price-only control arm (no "smart" filter, just the later/different entry) before trusting that a filter/pattern/signal explains an EV gap — the gap may be entirely explained by the entry price itself. See [docs/HARD_RULES_DETAIL.md#structural-advantage-control-in-comparison-backtests](docs/HARD_RULES_DETAIL.md#structural-advantage-control-in-comparison-backtests) for the overshoot-entry incident (~$77/trade of an apparent edge was actually ~$6).
- **When a backtest script's overfitting guardrails get tightened and re-run, anything that no longer survives needs its OLD `performance_audit` row explicitly deleted, not just left unrefreshed by the new run.** When re-running a script with stricter/different guardrails, explicitly query which prior rows for that `signal_type` are NOT in this run's fresh output and delete them (or diff on a marker field the new version writes that the old one didn't) — don't assume `ON CONFLICT DO UPDATE` alone keeps the table consistent, since it only touches rows the new run actually produces. See [docs/HARD_RULES_DETAIL.md#delete-stale-rows-when-guardrails-tighten](docs/HARD_RULES_DETAIL.md#delete-stale-rows-when-guardrails-tighten).
- **A backtest candidate grid needs a floor derived from the underlying DATA'S OWN RESOLUTION, not just statistical overfitting guardrails** (thin-tail/plateau/OOS/rigor) — those catch overfitting to a few trades, not overfitting to a precision the data cannot actually resolve. Before trusting ANY backtest whose candidates are themselves a distance/time/price granularity (a trail width, a tick-level threshold, a sub-bar timing window), check that granularity against the data's own real resolution — a candidate finer than what the underlying data can distinguish will pass statistical guardrails by construction, since the "noise" it's exploiting is measurement noise, not market behavior. See [docs/HARD_RULES_DETAIL.md#backtest-candidate-grid-floor-from-data-resolution](docs/HARD_RULES_DETAIL.md#backtest-candidate-grid-floor-from-data-resolution) for the sub-bar-noise trailing-stop incident.
- **The cell a grid search picks as "best" needs its OWN day-clustering/mean-median/chronological-split check, at its exact population and its exact real-dollar simulation — checks run on the raw forward-return population, a different threshold/population, or only one dimension of a multi-dimension grid do NOT transfer.** Found 2026-09-10 (DeepSeek design review): a "35/35 cells beat placebo" climactic-acceleration-reversal result looked decisive, but the winning cell (picked from a 7-hold x 5-target grid) had never itself been day-clustering-checked or chronologically half-split — only the raw Phase 1 forward-return population had been. Applying the same lens retroactively to `STALL_DEFENDED_LEVEL` (shipped live the same session) found the identical gap: after a mid-session threshold correction changed the population, only the WINNING hold value (210min) was re-swept against targets — the full hold grid was never re-run on the corrected population, so there's no way to tell if 210min is a smooth, robust choice or a lucky spike in a jagged neighborhood (the majors' own threshold-sensitivity sweep is the model to match — a 5-point sweep across the parameter, not a single re-verified point). **A related trap in the same family**: an EV that resolves mostly via stop-out or time-exit (not the target) means the "target" in "target/hold grid" is barely being tested at all — check the stop%/target%/time% breakdown at the winning cell before treating the target choice as validated.
- **A "catch rate" / "capture rate" claim built on a loose time-window match (a signal counts as catching an event if it fired anywhere in a wide window before or after) can be a matching artifact, not a real capture, whenever the underlying strategy has a high stop-out rate or resolves faster than that window.** Found 2026-09-10: a "climactic acceleration reversal caught 53.6% of real Globex 300+pt moves" claim used a 24h-before/1h-after match window, but the strategy itself stops out 67-89% of the time and resolves in 120-960min — so a signal could count as "catching" a move while the position it would have opened had already been stopped out hours before the move even started. The real question ("was a position still open, correctly-directioned, and alive when the event happened") was never asked. Before trusting any X%-catch-rate number, check whether the match window is wide enough relative to the strategy's own resolution time that a stopped-out or already-closed signal could still count as a "catch."
- **Check for frontend render errors before calling any frontend change done.** Run `npm run lint:frontend` (ESLint `no-undef`, catches missing-import/undefined-variable `ReferenceError`s) and `npm run build` (catches broken import/export paths and esbuild-level errors lint won't). Also check `scratch/gemini_alerts.txt` / `scratch/server_errors.jsonl` for `[CLIENT_ERROR]` entries. If real browser/Playwright tooling isn't available, say so explicitly rather than claiming an interactive check was done — static checks confirm nothing throws on load, not that a specific interaction behaves correctly. `.claude/hooks/post-edit-lint.sh` runs single-file lint on every touched file as a fast real-time layer underneath this — it does NOT replace the full `npm run build` + browser-check gate (single-file lint can't see cross-file import/export breaks or interaction bugs). See [docs/HARD_RULES_DETAIL.md#check-for-frontend-render-errors-before-calling-a-change-done](docs/HARD_RULES_DETAIL.md#check-for-frontend-render-errors-before-calling-a-change-done).
- **Whenever a change touches data-fetching — a new component with its own `fetch`/poll, a new or modified API endpoint, or edits to an existing one on a hot path — verify actual loading performance before calling it done, not just that it renders without errors.** "No console errors" and "actually fast" are different claims this codebase has repeatedly conflated. Using Playwright:
  1. **Count and list actual network requests** (`page.on('request', ...)`, filter to `/api/`) — group by endpoint and flag anything firing more than once per load. `grep` the exact URL string across `src/` before adding a new fetch of something that sounds "already available elsewhere."
  2. **Time the actual settle point**, not just first paint — watch the page's loading indicator until it hits zero, not just `domcontentloaded`.
  3. **For any new/modified endpoint, `curl` it standalone** (`-w "%{time_total}s size=%{size_download}\n"`) and compare against its cost *inside a real concurrent page load* — a large gap means connection contention or a pile-up, not a slow query.
  4. **If a response is unexpectedly large, check the query is actually bounded** — this codebase has repeatedly found genuinely unbounded historical queries hiding behind code that looked bounded at a glance.
  Don't run this full checklist for every one-line change, but any change adding a fetch, a component on Morning Prep/Trades, or touching a known-hot endpoint gets it. See [docs/HARD_RULES_DETAIL.md#verify-data-fetching-performance-not-just-no-errors](docs/HARD_RULES_DETAIL.md#verify-data-fetching-performance-not-just-no-errors) for the concrete incidents (51MB unbounded response, N+1 firing 18x, a 16x request multiplication) this checklist was built from.
- **Before proposing or building anything new, `grep ARCHITECTURE.md` for the relevant capability first** — the doc is more complete than it gets credit for, and re-deriving via `grep`/exploration when the answer is already written down wastes real time. When a task starts with "can we build X" or "is Y already tracked somewhere," `grep -i` the relevant keyword against `ARCHITECTURE.md` before writing a single line of new code or running an exploratory grep across `server/`/`scripts/`/`src/`. See [docs/HARD_RULES_DETAIL.md#check-architecturemd-before-building-or-exploring](docs/HARD_RULES_DETAIL.md#check-architecturemd-before-building-or-exploring).
- **Verifying a statistical split is real is not the same as verifying the classifier that produced the split is valid.** A backtest that conditions setup EV on a newly-invented regime/classification (not an existing, already-computed field) can pass every rigor check this codebase uses while the classifier itself is still an arbitrary construction — window lengths/cutoffs picked semi-informally, never sensitivity-tested, never checked for independent predictive power, never adjusted for the multiple-comparisons surface of testing several regime schemes against dozens of setup_types at once. A plausible economic narrative for why a split makes sense is not validation either. Before trusting a *newly-built* classifier's conditioning effects, validate the classifier's own parameter robustness and independent predictive power first. See [docs/HARD_RULES_DETAIL.md#validating-a-classifier-is-not-the-same-as-validating-its-split](docs/HARD_RULES_DETAIL.md#validating-a-classifier-is-not-the-same-as-validating-its-split) for the worked example where 3 EV splits passed rigor and all 3 classifiers behind them then failed independent validation.
- **A `PENDING` `OPEN_DECISION` describes the state of the codebase *as of whenever it was last checked*, not necessarily now — verify its underlying claim before relying on it if you're doing related work**, especially anything touching a file it names. The mechanism guarantees a decision won't be silently forgotten; it does not guarantee the decision's factual claim is still accurate, and nothing currently re-checks a claim automatically. Before citing a pending decision's claim as current fact, a cheap `grep`/direct check costs little and prevents compounding a stale finding into new work. See [docs/HARD_RULES_DETAIL.md#a-pending-opendecision-can-be-stale](docs/HARD_RULES_DETAIL.md#a-pending-opendecision-can-be-stale) for the incident where a decision described already-deleted code.
- **A prior "confirmed empirically" claim — including Claude's own, including from earlier in the same session — is not evidence once it has crossed a context-compaction boundary; the actual verification script/query is gone and only the conclusion survives.** A compaction summary preserves *what was concluded*, not *how it was checked*, and a wrong methodology survives the summary just as confidently-worded as a correct one. Before resolving an `OPEN_DECISION`, or otherwise treating any pre-compaction "confirmed via a standalone script" claim as load-bearing, redo the actual check against current live data rather than trusting the conclusion. See [docs/HARD_RULES_DETAIL.md#pre-compaction-claims-are-not-evidence](docs/HARD_RULES_DETAIL.md#pre-compaction-claims-are-not-evidence) for the incident this was named from (a "fix" committed against a false premise, later corrected).
- **A hypothesis about market behavior gets tested against raw bar/price history first; only route a hypothesis through `active_setups` trade-level N when the question is specifically about how this system's OWN setups perform.** If the question is "does market condition X predict market outcome Y" (a claim about NQ price behavior in general), build it as a bar-level scan over `price_bars_primary` first — cheap, dense, and not selection-biased by which setups fired. Only route through `active_setups`/real trade N once the question becomes "does this system's OWN setup capture that edge" (a claim about execution, not the market). Getting a real zero from a trade-level test doesn't mean the market phenomenon doesn't exist — it may just mean the question was asked through too thin a lens. See [docs/HARD_RULES_DETAIL.md#market-behavior-hypotheses-go-through-bar-history-first](docs/HARD_RULES_DETAIL.md#market-behavior-hypotheses-go-through-bar-history-first) for three threads that were structurally forced toward a false zero this way.

## Conventions

- **A `localStorage`-persisted drag-to-reorder column order must be MERGED against the current column set at load time, never just filtered against it.** The obvious-looking pattern (`(savedOrder || defaultKeys).map(k => COLS.find(c => c.key === k)).filter(Boolean)`) only ever drops a key that's no longer valid — it can never re-add a key that's new since the user's last drag. Reconcile any current-columns key absent from the saved order back in (appended at the end) in the `useState` initializer itself. See [docs/CONVENTIONS_DETAIL.md#localstorage-drag-reorder-must-be-merged-not-filtered](docs/CONVENTIONS_DETAIL.md#localstorage-drag-reorder-must-be-merged-not-filtered) for the incident (a brand-new column silently missing for any user with an already-saved order).
- **Any new script querying `price_bars_primary` with a lower-bound-only `ts >= X` filter must add an explicit upper bound.** `price_bars_primary` is a VIEW (join + `GROUP BY` aggregation, confirmed via `information_schema.tables.table_type='VIEW'`) — it can never have an index, so an unbounded query forces the aggregation across everything from `X` through the present before it can sort/`LIMIT`. See [docs/CONVENTIONS_DETAIL.md#pricebarsprimary-needs-an-explicit-upper-bound-too](docs/CONVENTIONS_DETAIL.md#pricebarsprimary-needs-an-explicit-upper-bound-too) for the 15-20x speedup this produced in one script and the broader materialize-historical-bars decision it points at.
- **Never look up a bar by an exact epoch/timestamp match against a real trade's `fired_at`** — real trades carry genuine sub-minute precision (recorded live via `NOW()`), but bars are always exactly on the minute, so an exact match silently fails and the trade gets dropped, not flagged. Floor the timestamp to the minute before the lookup. See [docs/CONVENTIONS_DETAIL.md#floor-timestamps-to-the-minute-before-matching-bars-to-firedat](docs/CONVENTIONS_DETAIL.md#floor-timestamps-to-the-minute-before-matching-bars-to-firedat) for the incident (31 real trades silently erased, producing a false "zero occurrences" finding).
- **A `level_prices`-style "prior day" lookup (`WHERE trade_date <= X ORDER BY trade_date DESC LIMIT 1`) silently goes one extra session stale across any 2+ day market closure (a weekend, a holiday) — search `>=`/`ASC` (the nearest FOLLOWING-OR-EQUAL checkpoint), not `<=`/`DESC`.** `level_prices.trade_date` is the day a row APPLIES TO (it describes the session immediately BEFORE that date), so on a normal weekday `X` always has its own exact-match row and either search direction returns the same thing — but on a Saturday/Sunday `X` (a real calendar date some callers legitimately compute, e.g. `resolveDepartureDay()` during a Sunday-evening Globex reopen), there's no row for `X` at all, so `<=`/DESC falls back to the LAST row that exists — which, under the same convention, describes the session BEFORE the one you actually wanted. Found 2026-09-14 in `globexFlushDetector.js`'s `findDeparture()`: for a Sunday `departureDay`, `<=`/DESC picked Friday's own `trade_date` row (describing Thursday's close) instead of the row under `trade_date=Monday` (describing Friday's actual close, needed for a Sunday-evening departure check) — fixed by flipping to `>=`/ASC, verified byte-identical to the old query on all 44 real trading days checked, differing only on the weekend gap it was silently wrong on. Any new `level_prices` lookup using this "closest prior applicable row" pattern needs the same `>=`/ASC direction, not `<=`/DESC, or it will re-introduce this same one-session staleness the next time it crosses a closure.
- **An OVERNIGHT-suffixed level-fade trade's reported `mfe_points`/`mae_points` (and any "captured vs. available" / "left on the table" stat derived from them) can be almost entirely an artifact of an untradeable price GAP, not real captured price action — check whether the touch was a gap-through before trusting the number.** `mfe_points`/`mae_points` are computed by walking bars from `fired_at` to `resolved_at` and measuring excursion against the row's own static `entry_zone` (`resolveSetups.js` ~line 518-523) — if that entry is a level whose price sat far from where the market actually reopened (a weekend/holiday gap crossed the level without any bar ever trading through it), the "MFE" mostly measures the size of the gap itself, not anything happening during the trade's real, short bar-by-bar life. Found 2026-09-14: a real `10D_IB_MID_FADE_SHORT_OVERNIGHT` trade reported "43% of a 315pt MFE captured" — ~280pt of that 315pt was the Friday-close-to-Sunday-reopen gap itself (no continuous price path for any detector to trade through), and the market's real post-gap behavior over the next several hours was a contained ~120pt range, not a missed trend. Before treating a level-fade OVERNIGHT trade's MFE-based capture stat as a real missed-opportunity signal, check the bar immediately before `fired_at` against `entry_zone` — if they're far apart, the level was crossed by a gap, not touched continuously, and the MFE number is measuring the gap, not a tradeable move.
- **A number that survives being computed twice by the same script is not verified — it only proves the script is consistent, bugs included.** Independent re-derivation (by hand with a different method, or dispatched to Gemini without showing it the original methodology) is what actually catches a deterministic bug a repeated run cannot. Audit a disagreement in both directions, don't assume whichever answer already existed is correct. See [docs/CONVENTIONS_DETAIL.md#a-number-computed-twice-by-the-same-script-isnt-verified](docs/CONVENTIONS_DETAIL.md#a-number-computed-twice-by-the-same-script-isnt-verified).
- **A trade-simulation "target" set to a self-referential statistic of the SAME population being tested (its own median/percentile MFE, its own mean, etc.) makes the resulting win rate a tautology, not a finding.** If ~half a population's outcomes are, by definition, above that population's own median, then "win = reached the median" will always land near 50% regardless of any real edge — the win rate measures nothing about the underlying signal's quality. Found 2026-09-10 (`scratch/zigzag_magnitude_filter_backtest.mjs`): a fade-trade backtest set each threshold bucket's target to that bucket's own p50 MFE, producing an eerily flat ~50.0-51.4% win rate across 6 wildly different thresholds — initially suspected as a too-tight-stop artifact, but widening the stop 10x (0.02x ATR → 0.30x ATR) left the win rate essentially unchanged, which is what confirmed the target itself, not the stop, was the tautology. Any backtest computing its own target/threshold FROM the same outcome population it then measures win rate against needs a target derived independently (a fixed value tested identically across every arm, or a value derived from a DIFFERENT population/period) before the win rate means anything. **Same trap, different shape, found the same day**: asking "does this ZigZag leg continue or reverse the immediately-prior leg's direction" is ALSO a tautology, not a finding — consecutive accepted ZigZag legs always alternate direction by construction (a LOW's leg goes up to the next HIGH, down to the next LOW), so that comparison is guaranteed to say "reversal" almost every time regardless of real market behavior. Caught after being reported out loud with the result misread in the bargain (an 99%-reversal number described as "99% continuation"). Always compare a leg's direction against an INDEPENDENT trend measure (real net price change over a genuine time window, a moving average) — never against its own immediately-adjacent leg.
- **Never key a per-trade state object (a `Map`, a lookup keyed by timestamp) by `fired_at`/`fired_at_ms` in any event-driven simulation over `active_setups`.** 12.6% of real trades share an exact `fired_at` with another trade (confluence-cluster siblings fire within milliseconds of each other) — a Map keyed this way silently lets a later trade's state overwrite an earlier trade's entry under the same key, corrupting which trade's outcome gates which. Found 2026-09-08: a loss-streak backtest script keyed its suppression-state Map by `fired_at_ms` and produced a 75%-inflated result (RTH delta $3615.22 vs. the correct $1900.72), caught only by disagreement against an earlier, correctly-keyed script. Key by a genuine per-row identifier instead (array index, or the row's own `id`) — see `scripts/backtest_morning_weakness_deepdive_20260908.mjs`'s fix for the pattern.
- **Confound checklist for any new comparison-style backtest (two arms compared against each other).** Before recording or promoting any "config/condition A beats B" finding, check all four:
  1. **Do the two compared arms differ ONLY in the hypothesis variable**, or does one arm have a structural/algebraic edge (a later entry against the same fixed exit, a smaller remaining risk distance)? Ask whether the "smarter" arm would still win with the intelligence stripped out and only the structural difference left.
  2. **Is the baseline computed by the exact same method/window as the thing being compared** — resimulated in the same pass, never read from a stored column computed under different historical conditions.
  3. **Does the signal select a sub-population** (overshot / made-a-new-extreme / pattern-present / any filter)? If so, is there a same-selection-minus-the-signal-condition control bucket — the `SIGNAL` / `SAME_SELECTION_NO_SIGNAL` / `NEVER_SELECTED` 3-way template in `scripts/pilot_cvd_divergence.mjs` is the pattern to reuse, not reinvent.
  4. **Was this the largest of K effects pulled from a sweep** (top-N setup_types, best candidate in a grid)? If so, run `computeReplication()` (`server/services/rigorDiagnostics.js`) — a held-out/permutation check that pools the selected subset against everything NOT selected and reports whether the direction and magnitude survive.
  Items 1-3 are domain-specific reasoning each time, not a generic detector (deliberately not built — would create false confidence); item 4 has a real, reusable function. See [docs/CONVENTIONS_DETAIL.md#confound-checklist-for-comparison-style-backtests](docs/CONVENTIONS_DETAIL.md#confound-checklist-for-comparison-style-backtests) for the 5+ times this exact failure shape recurred before the checklist was built.
- **Any backtest with an asymmetric stop/target distance (target closer than stop, or vice versa) combined with a long or no time limit needs a placebo test — randomize the direction/signal label, re-simulate the identical structure, compare — before trusting ANY resulting win rate, even one that looks plausible rather than obviously too good.** A driftless random walk hits the near barrier before the far one with probability `farDist/(nearDist+farDist)` (gambler's ruin) — a target at 0.5x ATR against a stop at 1.5x ATR alone predicts a ~75% win rate with ZERO real directional skill. Found 2026-09-10 (`scratch/defended_breakout_phase3_notimeout_confluence.mjs`): removing a time cap on a defended-level-breakout backtest produced a spectacular-looking WR=82.2%/EV=$114.79 that was correctly not trusted on sight (extreme sweep result → hand-trace) and was proven purely geometric via a same-structure random-direction placebo (WR=81.5%/EV=$95.76, nearly identical). **The dangerous part: the SAME placebo method, applied retroactively to an earlier, much more modest-looking capped-at-720min result from the same thread (WR=57.5%, EV=$16.96) that had NOT been flagged as suspicious, also failed** (real WR=58.1%/EV=$22.82 landed inside the noise range of 5 independent placebo trials). A result that looks reasonable is not automatically exempt from the same scrutiny that catches an implausible one — if the stop/target geometry is asymmetric, placebo-test by default, not just when the number looks too good.
- **Never pass claim/decision text containing a literal `$` through `node -e "..."` (or any inline double-quoted shell string) — write it to a real file and execute that instead.** Bash interpolates `$<digit>` as a positional shell parameter (usually empty) before Node ever sees the string, silently corrupting the text with no error at write time — `$0.96` becomes `/bin/bash.96`, `$16.96` becomes `6.96` (the `$1` vanishes). Found 2026-09-10: a `recordClaim()` update sent via `node -e "..."` corrupted every dollar figure in a persisted `RESEARCH_CLAIM` row; caught only by chance, re-reading the row back during an unrelated verification check — the write itself reported success. `scripts/record_claim.mjs`/`flag_decision.mjs` calls with any `$` in the text must go through a `.mjs` file (`node scratch/some_file.mjs`), never an inline `-e` string.
- **A pooled verdict — positive OR negative — is a question, not an answer.** Before trusting a pooled positive, or declaring an idea dead from a pooled negative (including a walk-forward failure or a full-sample reversal after a bug fix), split by the natural structural variable (mode, family, session type, day-type) and check for opposite-signed subgroups cancelling inside the average. Recurred 3 times in one session (2026-08-28): Globex flush mode-pooling hid a $99.93/trade effect; RTH vs. Globex needed opposite pooling strategies for the same volume-building signal; a roster-wide filter's pooled reversal (after 2 real bugs were fixed) was genuine, but re-splitting by level family showed 3 families untouched by the fix while one reversed hard — the pooled reversal did not mean the whole idea was dead, only that opposite-signed families were cancelling. See [docs/CONVENTIONS_DETAIL.md#pooled-verdict-hides-opposite-signed-subgroups](docs/CONVENTIONS_DETAIL.md#pooled-verdict-hides-opposite-signed-subgroups).
- **A negative verdict on a runner/winner-extension-class finding must cite a specific TESTED mechanism failure with a number — "negative by analogy/inference to a different pipeline's result" is not a valid closure.** Run the adversarial stress-test battery (exclude dominant family → exclude top day → exclude cluster → verify P&L → check for bugs) by default before recording any POSITIVE runner finding too, not only when challenged after the fact. See [docs/CONVENTIONS_DETAIL.md#negative-trace-rule-for-runner-findings](docs/CONVENTIONS_DETAIL.md#negative-trace-rule-for-runner-findings) for the incident this closes.
- **Benchmarking bundle size across commits (git worktrees): never symlink `node_modules` between a worktree and the main repo** — Vite's dependency-optimizer cache (`node_modules/.vite`) is shared through the symlink and can silently contaminate build output from a different commit's module graph, even after deleting `node_modules/.vite` between runs. Give each worktree its own real `npm install`, and build each commit in its own fully separate Bash/tool call. See [docs/CONVENTIONS_DETAIL.md#benchmarking-bundle-size-across-worktrees](docs/CONVENTIONS_DETAIL.md#benchmarking-bundle-size-across-worktrees) for the self-audit this bit before catching it.
- Backtest scripts live in `scripts/`, named `backtest_<hypothesis>.js`, run manually via `node`, and typically write findings to the `performance_audit` table. They are not imported by the running app.
- **Frontend API calls fetch the backend directly (`:3002`), never the relative `/api` path.** `src/constants/api.js` exports the one `API_URL` constant every view/component imports — never redeclare `const API_URL = '/api'` locally. Routing API traffic through Vite's `/api` proxy puts it on the same origin as the app's own module-serving, and Chrome's per-origin connection cap can starve a newly-navigated-to view's module fetch behind background polling. See [docs/CONVENTIONS_DETAIL.md#frontend-api-calls-go-directly-to-3002-never-through-api-proxy](docs/CONVENTIONS_DETAIL.md#frontend-api-calls-go-directly-to-3002-never-through-api-proxy) for the root-cause investigation.
- **Day-stable backend queries (bounded strictly `< date`, or otherwise fixed for a whole trading day) should be cached on `server/lib/cache.js`'s `cacheGet`/`cacheSet`, not recomputed on every poll.** Already used by `acd.js`/`auctionRead.js`/`keyLevels.js`/`weekly.js`/`stats.js`/`morningBrief.js`/`sessionForecastService.js` — check this file before hand-rolling a new module-level cache object; `cacheSet` returns its `val`, so `return cacheSet(key, result, ttl)` works inline. Use a shorter TTL (minutes, not hours) for anything reading the *target date's own same-day* data. See [docs/CONVENTIONS_DETAIL.md#cache-day-stable-backend-queries](docs/CONVENTIONS_DETAIL.md#cache-day-stable-backend-queries) for the two-convention comparison this is based on.
- **`useSharedPollData` (`src/utils/useSharedPollData.js`) dedupes the recurring poll interval automatically, but NOT independently-added consumers of a new URL, and NOT (before 2026-07-15) the very first load.** Before wiring a new component to an existing polled endpoint, `grep` for the exact URL string first — this hook has already silently gained duplicate non-shared fetchers of the same endpoint multiple times. See [docs/CONVENTIONS_DETAIL.md#usesharedpolldata-dedup-gaps](docs/CONVENTIONS_DETAIL.md#usesharedpolldata-dedup-gaps) for the in-flight-guard fix (16 requests for one URL on a single page load, down to 1).
- **A genuinely expensive endpoint that's also polled frequently or fetched by several components on mount needs a request-coalescing lock, not just a data cache.** If a response takes longer than its own poll interval, or several components independently fetch it at once on page load, concurrent/overlapping requests pile up and compete for the same DB connections — a self-inflicted DoS against your own database. Pattern (see `acd.js`'s `setupDetectionInFlight` / `confluence.js`'s `confluenceTodayInFlight`): a module-level in-flight promise; the first caller runs the real handler, concurrent callers `await` the same promise. Don't add this reflexively — only where polling frequency or multi-component fan-out can realistically overlap with the endpoint's own response time. See [docs/CONVENTIONS_DETAIL.md#request-coalescing-lock-for-expensive-polled-endpoints](docs/CONVENTIONS_DETAIL.md#request-coalescing-lock-for-expensive-polled-endpoints) for the 13-24s+ measured pile-up this fixed.
- Sierra Chart TAL data is stored as JSONB under `custom_fields->'sierra_data'` rather than typed columns — this lets new TAL columns appear without a migration and lets old Activity Log format rows coexist.
- Account filter state is lifted to `App.jsx` and shared between Calendar and Dashboard — don't duplicate it locally in a component.
- **Views stay mounted across tab switches — never add a new top-level view back to the old conditional-unmount pattern.** `App.jsx` tracks a `visitedViews` Set and CSS-hides (`display:none`) inactive views instead of removing them from the tree. Consequence: any `setInterval`/polling effect in a view now keeps running while hidden unless it's gated — new polling code inside a view must read `useViewActive()` (`src/utils/useViewActive.js`) and add it to the effect's dependency array with an early `if (!isViewActive) return;`. Bounded, user-triggered job-status polls (e.g. `ACDBacktestRunner`) are the deliberate exception. See [docs/CONVENTIONS_DETAIL.md#views-stay-mounted-across-tab-switches](docs/CONVENTIONS_DETAIL.md#views-stay-mounted-across-tab-switches).
- **Don't keep adding to `App.jsx`.** When a change needs new UI, extract it into its own file (`src/components/dashboard/` or `src/views/`) instead of inlining it. If a constant or helper needs to be shared across view files, put it in `src/constants/` or `src/utils/` — don't leave it as an unexported local `const`/`function` in App.jsx; that exact mistake caused several `ReferenceError`s across a file boundary after the 2026-07-13 code-split. See [docs/CONVENTIONS_DETAIL.md#dont-keep-adding-to-appjsx](docs/CONVENTIONS_DETAIL.md#dont-keep-adding-to-appjsx).
- **Default new `acd.js` logic to `server/services/`, not inline — same rule as App.jsx, applied to the backend.** User directive 2026-09-05, after `acd.js` grew from ~13,990 to ~14,030+ lines across one session's worth of small, individually-reasonable additions (a new calibration reader, a new shadow-tagger, several explanatory comments) — no single edit was ever big enough to trip the monolith-file nudge (`.claude/hooks/post-edit-filesize.sh` only checked growth *since the last commit* against the cohort median, and this codebase's own commit discipline keeps every commit well under that). **Before adding new logic to `acd.js`, default to `server/services/*.js` instead** (matching the existing precedent — `stepTrailWalker.js`, `pitchCatchWalker.js`, `rigorDiagnostics.js`, `developingValueService.js`, `globexSetupDetector`-shaped extractions) **unless it's genuinely coupled to the file's own block-scoped request-handler state** — reads/writes `liveStats` or `allRthBarsRow.rows` directly (see this file's own block-scoping footgun entry below), or is a `router.get/post` handler itself. A standalone calibration reader, a SHADOW-only tagging function, a lifecycle/expiry pass, a detection function for a structurally separate session (Globex vs RTH) — none of these need `acd.js`'s own scope and should be written directly into `server/services/` from the start, not written inline and extracted later. The hook now also tracks *cumulative* growth per chronically-oversized file (`.claude/hooks/.filesize_baselines.json`, gitignored — resets after each nudge) so slow creep across many small commits gets caught too, not just one big single addition. This doesn't retroactively shrink `acd.js`'s existing size (that's the separate, deliberately-paced `OPEN_DECISION acdjs_deferred_cleanup_from_deepseek_audit_20260905` extraction work) — it's specifically about not adding to the problem going forward.
- **Unnecessary monolith files get an automatic, non-blocking nudge — not a fixed line-count limit.** `.claude/hooks/post-edit-filesize.sh` compares a touched file's line count against its own cohort (sibling files in the same directory/extension) using median + MAD, firing only when the file is BOTH a statistical outlier for its cohort AND grew by at least a cohort-median's-worth of lines since the last commit. Advisory only — splitting isn't always right, cohesion sometimes matters more than line count. See [docs/CONVENTIONS_DETAIL.md#monolith-file-size-nudge-is-statistical-not-a-fixed-limit](docs/CONVENTIONS_DETAIL.md#monolith-file-size-nudge-is-statistical-not-a-fixed-limit).
- **WEEKLY_OPEN no-trade window (Sun 6PM ET level-set → Mon 9:30AM ET RTH open):** `detectGlobexSetup()` (`server/routes/acd.js`, right after `widerLevelPrices` is populated) nulls out `widerLevelPrices.WEEKLY_OPEN` for the whole Sun-evening-through-Monday-morning window — user rule (2026-09-08): firing a fade against the Weekly Open right after it's set is "too close to when the level was set" to be meaningful. This is a full skip (no row at all, not even SHADOW), matching the same formation-gate idiom already used for IB/OR levels before they exist — not a SHADOW-eligibility gate. Covers both `WEEKLY_OPEN_FADE_SHORT_OVERNIGHT` and `WEEKLY_OPEN_SWEEP_REVERSAL_LONG/SHORT_OVERNIGHT` (both read the same nulled price). Scoped to `WEEKLY_OPEN` only, not generalized to `MONTHLY_OPEN`/`PY_*`/other period-open levels.
- **Globex daily no-trade-at-open window (6:00-6:29:59 PM ET, every day):** `detectGlobexSetup()` (`server/routes/acd.js`, top of the function) returns `null` immediately if the current ET clock time is within the first 30 minutes after the 6PM Globex open — a full early return before any candidate is evaluated, not a per-candidate SHADOW gate. User rule (2026-09-08, tested first at 5min, then extended to 30min same day after a follow-up check showed the negative effect wasn't confined to the first 5 minutes — the 30min cumulative window is actually deeper/better-supported than the original 5min one, N=34/13 days/EV=-$29.36 vs N=24/12 days/EV=-$27.93). Effect persists fairly evenly out to ~45-60min before fading toward the session baseline around 60-90min. Honest caveat, applies at both window sizes: day-concentration sits ~60-67% from the top 5 days through the 45min mark — can't fully rule out a handful of bad sessions driving it, though the direction is stable across a chronological half-split. Separate from and in addition to the `WEEKLY_OPEN` window above (that one only applies Sun evening → Mon RTH open; this one applies to every single Globex session's first 30 minutes, every day).
- **No-new-entries dead zone (4:00-6:00 PM ET):** `runSetupDetection()` (`server/routes/acd.js`) forces any new RTH candidate that would otherwise fire `ACTIVE` in this window to `SHADOW` instead (`suppression_reason='POST_RTH_DEAD_ZONE'`). Does NOT force-close already-open positions — only NEW entries are blocked. Overlaps with, but is broader than, the pre-existing 5-6PM ET hard-close gap. See [docs/CONVENTIONS_DETAIL.md#no-new-entries-dead-zone-4-6pm-et](docs/CONVENTIONS_DETAIL.md#no-new-entries-dead-zone-4-6pm-et).
- **Server-autonomous detection:** `server/index.js` runs a `setInterval` every 15s calling `GET /api/acd/setup-detection`, across the full CME Globex week (Sun 6PM–Fri 5PM ET, with the daily 5–6PM ET maintenance gap excluded) — not just RTH. This is intentional — it makes level fade detection independent of browser clients. Do not remove it. The INSERT in `acd.js` is `ON CONFLICT DO NOTHING` so concurrent client polls don't double-write. **A hard rule describing this poller's schedule/endpoint is exactly the kind of fact that silently drifts when the poller itself changes** — if you touch its schedule or endpoint again, grep for "9:30" / "RTH" / "market hours" across `CLAUDE.md`, `src/components/dashboard/`, and `src/views/` in the same session, not just the code. See [docs/CONVENTIONS_DETAIL.md#server-autonomous-detection-schedule](docs/CONVENTIONS_DETAIL.md#server-autonomous-detection-schedule) for how stale this entry itself went undetected for 7 weeks.
- **Level fade detection gate:** `acd.js` line ~3838 checks `allRthBarsRow.rows.length >= 3` (not 60). IB-specific levels (`ibHighToday`/`ibLowToday`, `IB_MID_SCALP_FADE`, `OR_MID_AFTER_IB_FADE`) self-gate via `etMinNow >= 630` — do not restore the 60-bar gate.
- **sizeMultiplier is an IIFE** inside the `levelScalpSetup` object in `acd.js`. All sizing adjustments live there. When adding a new factor: (a) research backs it (N≥20), (b) add to the IIFE, (c) update `AlphaEngineOverview.jsx`, (d) document in OPEN_THREADS. **Confirmed rigid, not distribution-derived** (2026-08-30): ~25 factors combined via a fixed-order additive stack of mostly-hardcoded flat step sizes, only 2 of 25 self-recalibrate from live data. A composite-score redesign is scoped (design-only, not built) at `docs/SIZE_MULTIPLIER_COMPOSITE_REDESIGN_SPEC.md` — see the "Where to look" entry below before touching this mechanism again.
- **DAY_TYPE_MANAGED types need their bucket breakdown re-read, not just their headline recommendation.** A `DAY_TYPE_CONDITIONAL` type (currently `IB_BULLISH`/`IB_BEARISH`) staying off `SUPPRESS` can mean "genuinely strong on its good day-type" or "one thin/borderline bucket happens to clear the bar this week" — the recommendation string alone doesn't distinguish these. `.claude/hooks/session-start.sh`'s `DAY_TYPE_MANAGED WATCH` section prints every such type's full bucket breakdown at the start of every session — read it, don't skip past it as boilerplate. See [docs/CONVENTIONS_DETAIL.md#daytypemanaged-bucket-breakdown-re-read](docs/CONVENTIONS_DETAIL.md#daytypemanaged-bucket-breakdown-re-read) for the regression this caught.
- **`active.sizeMultiplier` overwrite footgun:** the level-fade IIFE sets a real, ~20-factor `sizeMultiplier` (0.10-1.5x) on `levelScalpSetup`. Later in the same handler, the "Build full trade brief" block sets `active.sizeMultiplier` again for a different reason (post-loss "Death Sequence" protection) — applied as an additional ceiling (`Math.min(existing, 0.5)`), never a replacement. **Any future code that sets a field a candidate object already sets in its own construction must merge/cap, never blindly overwrite** — check what the candidate already computed first. See [docs/CONVENTIONS_DETAIL.md#activesizemultiplier-overwrite-footgun](docs/CONVENTIONS_DETAIL.md#activesizemultiplier-overwrite-footgun) for the two-layer incident (an unconditional overwrite, then an unscoped `hasLossToday`) this was fixed in.
- **`sizeMultiplier` is a continuous 0.10-1.5x score — discretizing it into an actual contract count needs a real floor at zero, not a `Math.max(1, ...)` clamp.** The user trades exactly 1 MNQ micro contract at a time (see `user_contract_size.md` memory) — the ONLY realistic base size to design and test against, not a hypothetical multi-contract account. Found 2026-09-07: `SizeChip` (`MarketPulseBar.jsx`) and its duplicate in `quick-check.html` both computed the recommended contract count as `Math.max(1, Math.round(base * mult))` and used a literal `mult === 0` check for the SKIP badge — but the sizeMultiplier IIFE's own documented range never actually reaches exactly 0, so SKIP was dead code, and the `Math.max(1, ...)` floor forced EVERY non-zero score up to "1 ct" at base=1, destroying the score's real information (a 0.10x near-suppressed setup and a 1.1x high-conviction setup both rendered identically). Fixed: `rec = Math.round(base * mult)` (no floor — can legitimately be 0), SKIP derived from `rec === 0`, not `mult === 0`. **Any future feature that turns a continuous score into a discrete recommendation must be designed and hand-checked against base=1 specifically** — a floor/clamp that "works" at a larger hypothetical base can silently collapse to a single constant value at the user's real size, and this is easy to miss without explicitly testing the base=1 case. This formula is duplicated in 2 places (`MarketPulseBar.jsx`'s `SizeChip` and `quick-check.html`) — any future change to it must touch both or they will drift.
- **New setup type checklist (anti-hardcode gate) — also the checklist for auditing *existing* live setups, not just gating new ones.** Before any new `setup_type` goes live in `acd.js`, ALL of the following must be satisfied:
  1. Run `node scripts/backtest_setup_status.mjs` → generates/updates `SETUP_STATUS` row in `performance_audit`.
  2. Run `node scripts/update_optimal_stops.mjs` → generates/updates `OPTIMAL_STOP` row with data-derived stop/target.
  3. If N<20 resolved trades, do NOT fire live — insert as `status='SHADOW'` only until N≥20.
  4. Never write a stop, target, or WR claim as a literal number in `acd.js` — always read from `liveStats._opt[type]` or `performance_audit`.
  4a. **Run a signal-level forward-return pre-test before building any trade machinery** — the cheapest, most decisive screen in this checklist. No stop, no target, no exit rule: at each signal event, measure the raw forward price change at several bar horizons and compare the CONDITIONAL mean against the UNCONDITIONAL mean over the same horizon (never against zero — a multi-year NQ sample has real underlying drift). Enrich with volume/order-flow using `touchQuality.js`'s `getVolumeBaseline()` and `acd.js`'s `volZ`/`oneSidedRatio`, and derive any bucket cutoff from the pooled data (e.g. median volZ), not a threshold borrowed from a different purpose.
  5. **Simulate the real stop/target as an actual bar-by-bar trade before ever calling a signal "validated."** A statistically stable *sign* on a raw point value is necessary but not sufficient — it has no drawdown tolerance and is not a tradeable strategy.
  6. **Verify integration, don't assume it.** Check `dropToTimeline()` is called from the actual insert path, check `SETUP_DISPLAY_LABELS` has an entry, check `isLongSetup()`/`inferDirection()` resolve correctly for the exact string being inserted.
  7. **If the setup lives outside the level-fade candidates array (its own poller), it must replicate the dynamic SHADOW→ACTIVE promotion check manually** — the candidates-array path already does this via `liveStats._suppressedSetups`; a standalone poller has no such built-in mechanism and will sit in SHADOW forever without a `getLiveStatus()`-style live re-check.
  8. **If the calibration script writes a new `recommendation` value, register it in `/api/performance-audit/unified`'s status-mapping** or the setup type is silently invisible in the Unified Signal Table.
  9. **If a setup's fire gate is earlier than the data it depends on for suppression, its suppression logic is a silent no-op** — verify the fire-gate timing (`etMin >= ...`) against when the suppression dependency actually becomes available, not just that a suppression check exists in the code.
  10. **A backtest/backfill's detection population must match what the live poller actually fires on — window, touch-definition, and all.** A narrower/stricter historical scan describes a different, more selective strategy than what's actually live. Same-day-forming levels (Opening Range, Initial Balance) need their own formation gate or a re-test introduces lookahead. **This bit the live early-touch backfill loop too, not just backtests** — found 2026-09-08 (user-caught live): `acd.js`'s early-touch-backfill scan (~line 7925) had no formation-gate check at all, so the bar that SETS an OR5/10/15/30 or IB high/low trivially "touched" it before the window even closed (30 setup_types affected). Fixed via `getLevelFadeDefinition()`'s existing `formationGate` (already used correctly by `scripts/backtest_rth_calibration_genuine_holdout.mjs`, just never wired into this one live loop).
  11. **A level with zero real touches ever is not automatically SHADOW-safe** — `backtest_setup_status.mjs` groups by `setup_type`, so a type with 0 rows never gets even a `THIN_N` row, and the live insert gate treats "absent from `_suppressedSetups`" as "not suppressed." Before considering a new level "covered," check `SELECT 1 FROM performance_audit WHERE signal_type='SETUP_STATUS' AND signal_name=<type>` — seed a manual `THIN_N` placeholder if absent.
  The `Stop` hook (`.claude/hooks/check-docs-drift.sh`) scans for hardcoded anti-patterns and will warn if any of these are violated. The `SessionStart` hook checks for setup_types missing a fresh SETUP_STATUS row. See [docs/CONVENTIONS_DETAIL.md#new-setup-type-checklist-anti-hardcode-gate](docs/CONVENTIONS_DETAIL.md#new-setup-type-checklist-anti-hardcode-gate) for the found-2026-07-14 incident behind every numbered item.
- **`active_setups`' `existingSetup` dedup check only prevents two *simultaneously open* rows — it does NOT stop the same exact touch instant from being inserted twice once the first one resolves.** Fixed with an unconditional second unique index `idx_as_unique_touch_instant` on `(trade_date, setup_type, fired_at)` (alongside the existing partial index on ACTIVE/SHADOW only), and **every `ON CONFLICT` clause against `active_setups` is now bare `ON CONFLICT DO NOTHING`** (no explicit target) so it catches a violation against *either* index — a targeted clause only suppresses a violation against the index it names. See [docs/CONVENTIONS_DETAIL.md#activesetups-touch-instant-dedup-gap](docs/CONVENTIONS_DETAIL.md#activesetups-touch-instant-dedup-gap) for the root cause (a stale 1PM hard cap) and the 13 repaired duplicate pairs.
- **`scripts/backtest_unified.js`'s `detectLevelFades()` restricts most level families to a blanket 10:30 AM-noon scan window that doesn't match live firing (first RTH touch anytime 9:34 AM-4PM), and had zero walk-forward stability checking.** Fixed: only the 6 genuinely same-day-forming levels (`OR_HIGH`/`OR_LOW`/`IB_HIGH`/`IB_LOW`/`IB_MID_SCALP`/`OR_MID_AFTER_IB`) keep their formation gate; the other ~59 now scan the full RTH session, and `computeRigor()` chronological-stability checking was added. **Practical live impact is bounded** — `_suppressedSetups` (what gates live firing) reads `SETUP_STATUS`, never `UNIFIED_BACKTEST`; this fix corrects fallback sizing/tier data only. See [docs/CONVENTIONS_DETAIL.md#backtestunifiedjs-detectlevelfades-window-mismatch-bug](docs/CONVENTIONS_DETAIL.md#backtestunifiedjs-detectlevelfades-window-mismatch-bug) for the numbers (76% of corrected recommendations fail the stability check).
- **A `backtest_unified.js` detector reconciled to match live's trigger/geometry exactly can still show a materially different (even sign-flipped) WR/EV than real `SETUP_STATUS` — check whether the setup goes through `acd.js`'s priority-selection candidates array before treating a residual gap as an incomplete fix.** ~15 RTH setups (including `VALUE_AREA_RESPONSIVE_*`) compete in `buildAllCandidates()`'s array (take the first eligible one per poll) — live only fires a setup when it's ALSO the highest-priority eligible candidate that poll, not merely when its own trigger fires, and a per-setup-type detector can't reproduce that without re-simulating the whole array. Not a live-firing risk either way — `SETUP_STATUS` (real fired trades) is what actually gates live, never `UNIFIED_BACKTEST`. See [docs/CONVENTIONS_DETAIL.md#backtestunifiedjs-detectors-cant-reproduce-lives-candidate-priority-selection](docs/CONVENTIONS_DETAIL.md#backtestunifiedjs-detectors-cant-reproduce-lives-candidate-priority-selection) for the `detectVAResp` investigation this was found in.
- **Any code path that can terminally close an `active_setups` row must compute a real `actual_pnl` in the same transaction** — `resolveSetupsByPrice()`'s general branch used to fall through with `resolution=null` on running out of bars, relying on a separate `expireStaleSetups()` that force-closed with `resolution='TIME_EXPIRED'` and never touched `actual_pnl`, a permanent null. Both now mark-to-market against the last available bar's close (`method='MARK_TO_MARKET'`) instead of leaving `actual_pnl` null — only rows with no entry price recorded, or where price data never arrived, stay genuinely null (`method='NO_PRICE_DATA'`). `INVALIDATED`/`POST_ENTRY` structural invalidations get the same treatment; `PRE_ENTRY` invalidations and `SESSION_CLOSED` remain deliberately null (no real entry occurred / separate open question). See [docs/CONVENTIONS_DETAIL.md#resolvesetupsbyprice-timeexpired-null-pnl-bug](docs/CONVENTIONS_DETAIL.md#resolvesetupsbyprice-timeexpired-null-pnl-bug) for the 387-row backfill this required.
- **Any string literal written into a short `VARCHAR(N)` column** (`resolution_method`/`resolution`/`actual_outcome`/`replay_resolution` are `VARCHAR(20)`; `status` is `VARCHAR(10)`; `origin_status` is `VARCHAR(12)`; `performance_audit.signal_type` is `VARCHAR(30)`, `.recommendation` is `VARCHAR(20)` — corrected 2026-09-02, this entry itself previously claimed both were VARCHAR(20), caught when a 37-char signal_type literal overflowed against the real 30-char column) **throws a raw, uncaught Postgres error, not a graceful truncation — and if written from inside a shared per-row loop, ONE bad literal crashes resolution for every OTHER row in that same poll too.** Check any new hardcoded status/method/label string against its target column's actual declared width in `server/schema.sql` before assuming it fits — a string that reads as "short enough" by eye is not the same as verifying the number. See [docs/CONVENTIONS_DETAIL.md#varchar-n-literal-overflow-risk](docs/CONVENTIONS_DETAIL.md#varchar-n-literal-overflow-risk) for two live-but-untriggered overflows this caught, plus a near-miss in `flag_decision.mjs`/`record_claim.mjs` now guarded with a fail-fast length check.
- **A `SHADOW_SETUP_TYPES`-style hardcoded classification list is the same anti-pattern as a hardcoded suppression list, even when it's "just" a display filter.** `/api/setups/history` now reads `getShadowSetupTypes()` (fresh `performance_audit` SETUP_STATUS query) instead, falling back to a short, `test_invariants.mjs`-checked `UNCALIBRATED_SHADOW_TYPES` list (`server/config/setupTypes.js`) only for setup_types with no SETUP_STATUS row yet. **A hardcoded fallback added to patch a gap in a dynamic query needs the same standing staleness check as the thing it was patching** — "much shorter" is not the same as "self-maintaining." See [docs/CONVENTIONS_DETAIL.md#shadowsetuptypes-hardcoded-list-anti-pattern](docs/CONVENTIONS_DETAIL.md#shadowsetuptypes-hardcoded-list-anti-pattern) for both the original bug (3 real, promoted setups permanently hidden) and the fallback list's own quiet re-drift.
- **Trading-day dates: always derive from SQL `CURRENT_DATE`, never JS `new Date().toISOString()`.** The DB server runs in `America/New_York`; JS's `toISOString()` is UTC, and the two disagree once past 8PM ET — a full calendar day earlier in JS-UTC-land than the DB's own ET-based "today." Fetch it with `SELECT CURRENT_DATE::text as today` in every new backtest script. See [docs/CONVENTIONS_DETAIL.md#trading-day-dates-from-currentdate-never-toisostring](docs/CONVENTIONS_DETAIL.md#trading-day-dates-from-currentdate-never-toisostring) for the 3 same-day bugs this caused.
- **Never parse a naive (no-timezone) timestamp string with `new Date(str).toISOString()` — it silently depends on the ambient process/system timezone, not on any timezone you actually intend.** The pattern to reuse for any future naive-local-timestamp parsing: guess a UTC instant from the naive components, ask `Intl.DateTimeFormat` what that guess actually looks like in the target zone, then correct by the wall-clock difference — this correctly handles both EDT and EST per-date, unlike a hardcoded offset (`etNaiveStringToUtcIso()` in `sierraParser.js`). **Process lesson: don't trust "recent data looks right" as proof a timestamp pipeline is healthy** — verify via the raw source string against the stored column directly, not the rendered UI. See [docs/CONVENTIONS_DETAIL.md#naive-timestamp-parsing-depends-on-ambient-timezone](docs/CONVENTIONS_DETAIL.md#naive-timestamp-parsing-depends-on-ambient-timezone) for the multi-month timezone-flip incident and the two duplicate-import cleanups it led to. **A second, distinct failure mode in the same family, found by DeepSeek code review 2026-09-10**: `new Date(naiveTsStr.replace(' ','T')+'Z').getTime()` compared directly against `Date.now()` is a live-breaking bug, not just an imprecise one — the naive string gets mislabeled as UTC (running ~4-5h behind its true epoch), so `Date.now() - thatValue` computes a genuinely fresh (5-real-minutes-old) signal as ~245 minutes old. Both `majorPivotDefendedBreakDetector.js` and `stallDefendedLevelDetector.js` had this exact bug in their "is this signal recent enough to insert" check (30/60min windows), meaning **neither live detector could ever have actually inserted a real setup** — confirmed by direct calculation before fixing, caught before either had fired live (real N=0 for both at the time). Fixed via a new shared `etNaiveTimestampToMs()` (`acdShared.js`, wraps the already-existing, now-exported `etNaiveStringToUtcIso()`) — use this for ANY comparison between a naive bar/entry timestamp and a genuinely-UTC anchor (`Date.now()`, `NOW()`-derived values). **Naive-vs-naive arithmetic does NOT need this fix** — computing `expires_at` by adding minutes to the same mislabeled value is self-consistent and was never actually wrong, since the mislabeling only matters once something touches a true-UTC value.
- **Rigor diagnostics are standing, not one-off.** Day-clustering (top-5-dates % of N) + 3-way chronological sign-stability run on every weekly mining/calibration pass, across all 4 statistical pipelines (`backtest_setup_status.mjs`, `mine_minutebar_conditions.mjs`, `patternScannerService.js`'s `mineLevelFades()`, `mine_tod_patterns.mjs`). These are informational — they do NOT feed SUPPRESS/PROMOTE logic, because "unstable" doesn't mean "losing." Classify each flag (degrading vs. improving vs. noisy-but-stable vs. too-thin-to-tell) before acting. Aggregate coverage: `GET /api/acd/rigor-stability-coverage` + `RigorStabilityPanel` in `AlphaEngineOverview.jsx`. **One deliberate, narrow exception**: `backtest_setup_status.mjs`'s per-day-of-week sub-pass (`SETUP_STATUS_DOW`) DOES gate live SUPPRESS/ACTIVE directly on `computeRigor`'s `clean` bit — a false SUPPRESS there directly blocks real trading opportunities, a materially different cost than the other pipelines' informational badge. Do not extend rigor-gating to the other pipelines without the same deliberate call. See [docs/CONVENTIONS_DETAIL.md#rigor-diagnostics-are-standing-not-one-off](docs/CONVENTIONS_DETAIL.md#rigor-diagnostics-are-standing-not-one-off) for the full snapshot numbers and the DOW-gating audit.
- **Share modules when the same logic would otherwise be reimplemented.** The rigor/replication diagnostics above were each independently hand-written multiple times before being centralized into `server/services/rigorDiagnostics.js` (`computeRigor()`/`computeReplication()`) — import the shared version, don't write a new copy. **A sharper version of the same mistake: check whether an existing COLUMN already answers the question before adding a new one** — before adding a new persisted field to answer "what was true at the moment X happened, immutably," grep the target table's own INSERT sites for an existing column already carrying that answer (`origin_status` already covered a case a new `fired_status` column was built to solve, reverted the same session it was caught). **A third instance, found 2026-09-07 via a user-prompted duplication audit of `acd.js`**: the runner-trail-width lookup (a `performance_audit`/`BREAKEVEN_TRAIL_TEST` query + JSON-parse) existed as 4 near-identical inline copies across the file's insert sites — and the code's OWN comments were self-aware of the duplication ("Same lookup, third copy") without ever catching that a 4th, undocumented copy existed, and that copy was strictly worse than the ones the comments were tracking: it was missing the `.catch(() => ({rows: []}))` safety net the other 3 had, on the highest-traffic of the 4 sites. **Duplicated logic doesn't just cost maintenance effort — it lets a fix or safety-net silently diverge between copies, and the newest, least-noticed copy is the one most likely to be missing something the earlier copies already learned the hard way.** When extracting N copies of duplicated logic into one shared function, first check whether all N sites actually resolve their inputs identically before assuming the whole block is interchangeable — one of the 4 sites here pre-resolved its lookup key via a different function (`resolveUnconditionalTrailVariant()`) than the other 3, so only the truly-identical tail (the query + parse) got extracted, not the resolution step, preserving each site's real behavior exactly. See [docs/CONVENTIONS_DETAIL.md#share-modules-instead-of-reimplementing](docs/CONVENTIONS_DETAIL.md#share-modules-instead-of-reimplementing).
- **Proactive self-improvement — extracting reusable functions and fixing duplication on sight — is a standing practice, not something that needs to be requested each time.** User's own words, 2026-09-07, after this session's runner-trail-width/RSI(14)/session-end-cap dedup fixes landed in one sitting: "Self improvement should be a rule. Reusable functions are welcome." When duplicated logic turns up — in the file already being touched, or noticed while investigating something else — extract and fix it in the same session rather than just flagging it for later, the same proactive instinct the user already brings (see the "user-workstyle takeaway" in [[feedback_single_source_of_truth_no_deadends]]) now stated as a green light to act on it directly rather than wait to be asked. **Speed and accuracy of the live detection/app-firing path are the tantamount priority** — a refactor is only worth doing if it doesn't regress either, so it needs the same verification discipline every extraction in this file already documents (byte-diff old vs new output over real data, `node scripts/test_invariants.mjs` against baseline, live server restart + health check) before being considered done, not just a syntax check. **When extracting a function boundary out of `acd.js`'s tangled `runSetupDetection`, free-variable/return-completeness must be checked by exhaustively grepping every candidate name against the ENTIRE downstream body — never by eyeballing a read-through, even a careful one.** Confirmed twice now: the `buildAllCandidates()` (P2) extraction missed `etMin` this way (caught only by ESLint's `no-undef`, a lucky second net); the `computeLevelFadeFactors()` (P3) extraction would have missed `_lfOvOpen` and `_pulseVolSigma` the same way — both looked like pure intermediates on a read-through but were genuinely referenced downstream (one in a sizing decision, one in output serialization), only surfaced by grepping each candidate name's count against the rest of the function body before finalizing the return list. Treat `no-undef` lint as a necessary-but-not-sufficient second check, not the primary one — grep first, lint to confirm.
- **When a static, non-bundled page (`server/public/*.html`) and a React component duplicate the same NAMED color/threshold token because they genuinely can't share one JS module, verify the token NAME resolves to the SAME value on both sides — a coincidental hex match under swapped/mismatched names is worse than an obvious value mismatch, because it silently breaks the moment either side's palette is edited.** Found 2026-09-07: `quick-check.html`'s `--muted`/`--dim` CSS variables were swapped relative to `MarketPulseBar.jsx`'s `C.muted`/`C.dim` (and `--green` was a genuinely different hue), so every shared threshold-color function (`sigmaColor()`, range/delta/rvol color mapping) only agreed by accident — each file had independently picked the oppositely-named token that happened to resolve to the matching hex. Fixing the token VALUES alone is not sufficient — every usage site that was relying on the old (wrong) name-to-value mapping to coincidentally agree must also have its token-name reference corrected, or the value fix silently breaks sites that used to agree by luck. Verify parity by tracing the literal token-name sequence at each duplicated branch in the real, current file content on both sides — not by re-typing the logic from memory into a throwaway test script, which only proves the memory is self-consistent, not that the actual files match (the same "a number computed twice by the same script isn't verified" trap, applied to a parity check instead of a calculation). Add a "KEEP IN SYNC" cross-reference comment at every such site in both files, matching the precedent set by the `sizeMultiplier` fix.
- **Unified suppression pipeline (the only suppression source):** All suppression of level fade setups flows through `scripts/backtest_setup_status.mjs` → `performance_audit` SETUP_STATUS/SETUP_STATUS_DOW → `liveStats._suppressedSetups` / `liveStats._dowSuppressToday`. Never add a hardcoded suppression list or day-of-week skip list to `acd.js` — the pipeline handles it automatically and re-evaluates weekly.
- **The `PROMOTE` recovery-detection check never checked whether the "recovery" trades were real, and its N≥15 floor was already below this codebase's own N≥20 significance floor.** Fixed by adding `PROMOTE_MIN_REAL_N = 5` requiring at least that many `ACTIVE`/`SHADOW`-origin (non-`BACKFILL`) trades within the recovery window, and by requiring the main all-time check's `real_n` (not just blended `n`) to also clear `SUPPRESS_MIN_N=20`, else `THIN_N`. Effect: 6 of 7 then-`PROMOTE` setups reverted to `SUPPRESS` (one had zero real trades behind a synthetic-only "recovery"), and 64 more setup_types system-wide flipped `ACTIVE`→`THIN_N` — every flip is honest, not punitive, since `THIN_N` still fires `SHADOW` and self-clears at real N≥20. See [docs/CONVENTIONS_DETAIL.md#promote-recovery-detection-needed-a-real-n-floor](docs/CONVENTIONS_DETAIL.md#promote-recovery-detection-needed-a-real-n-floor).
- **A shared classification function that infers its answer from `setup_type` NAME ALONE can silently return the wrong answer when the SAME name is inserted from two different live code paths under two different true contexts.** `getBetClass()` inferred purely from the setup_type string and had no way to know which engine (RTH vs. Globex) actually fired a given row, silently mistagging every Globex fire of 4 shared-name types (plus 12 more) as the RTH bucket — and that contamination then corrupted a DIFFERENT bet_class's own analysis. **When the SAME identifier can legitimately mean two different things depending on which code path produced it, a name-only classifier is structurally unable to get it right for both — either give the two producers distinct identifiers, or have the producer that already knows the true context assert it directly** instead of routing through the shared inference. See [docs/CONVENTIONS_DETAIL.md#setuptype-name-only-classification-is-structurally-ambiguous](docs/CONVENTIONS_DETAIL.md#setuptype-name-only-classification-is-structurally-ambiguous).
- **An UNCONDITIONAL `resolveSetupType()` diversion (a variant that captures 100% of a base type's future touches, not just some) permanently freezes the base type's own population for any script that groups by raw `setup_type`.** After a variant's `addedDate`, the base type can never accumulate a new real trade again — real, growing data lands under the variant name instead, where a naive `byType[t.setup_type]` grouping never looks. Before grouping/aggregating any table by `setup_type`, check whether `CONDITIONAL_VARIANTS` has an unconditional entry pointing at it, and pool base+variant trades before sweeping. See [docs/CONVENTIONS_DETAIL.md#unconditional-resolvesetuptype-diversion-freezes-base-type-population](docs/CONVENTIONS_DETAIL.md#unconditional-resolvesetuptype-diversion-freezes-base-type-population) for the 6 `_TRAIL` variants this was found in.
- **Conditional variant setup checklist** — when a single level name generates two setup types based on entry condition (e.g., `WPP_FADE_SHORT` vs `WPP_FADE_SHORT_GAP_UP`):
  1. Add the override to `resolveSetupType()` in `acd.js` (defined just above the `nearLevels` filter) — the single source of truth for both the live path and early-touch backfill.
  2. Write a dedicated backtest script (`scripts/backtest_<variant>.mjs`) that persists SETUP_STATUS + OPTIMAL_STOP rows for the new type. Add it to `run_weekly_backtests.sh`.
  3. Add an entry to `CONDITIONAL_VARIANTS` in `server/config/setupTypes.js` with `baseType`, `direction`, `condition`, `backtestScript`. If the type name doesn't encode direction via a standard suffix, also add it to `CONTEXTUAL_DIRECTION_TYPES`.
  4. Update `AlphaEngineOverview.jsx`'s `suppressed` array — add entries for both the suppressed base type and the active variant.
  5. Add a note to `docs/OPEN_THREADS.md` to monitor the variant at N=50.
  6. **Run `node scripts/test_invariants.mjs`** — catches OPTIMAL_STOP coverage, direction inference, backtest script presence, base-type suppression, and stored-EV-matches-fresh-sweep all automatically. If it passes, the pipeline is complete.
  See [docs/CONVENTIONS_DETAIL.md#conditional-variant-setup-checklist](docs/CONVENTIONS_DETAIL.md#conditional-variant-setup-checklist) for the `test_invariants.mjs` `[6]` check history.
- **A trade-brief's `targetLabel`/`description` text is a claim about execution, not documentation of intent — verify it against the actual wiring before trusting it, and whenever you touch one for any reason.** Recurred 3 times in one day (2026-09-07, `backtest_unified_detectors_systemic_divergence_20260907`): `VWAP_MAGNET` and `C_STANDALONE_UP`/`C_STANDALONE_DOWN` all advertised "half off at T1 · Runner: Npt" / "breakeven after T1" in their live `acd.js` trade brief, but none of them had a `runner_trail_width`/`extend_target_level`/`CONDITIONAL_VARIANTS` entry — every real trade actually resolved flat via the standard `resolveSetupsByPrice()` path, so the scale-out was never mechanically enforced. **Before trusting any `targetLabel`/`description` string that mentions a scale-out, runner, or breakeven-after-T1 behavior, `grep` the setup_type against `runner_trail_width`/`extend_target_level`/`CONDITIONAL_VARIANTS` — if none exist, the text is describing a plan that was never wired, not what actually happens.** Fix by rewriting the text to describe the real flat-exit behavior, not by wiring the scale-out (that's a separate, much bigger decision needing its own backtest per the checklist above).
- **`antigravityEdges.js` Promise.all ordering footgun:** `getLiveEdgesContext()` fires all DB queries in a single `Promise.all` wave (20 entries). The destructured variable names are **positionally bound** — adding or removing a query shifts every entry below it to a wrong variable with no runtime error. Always update the destructuring list and the array body together, and check the index comments (0–19) when inserting. Cache-miss-only entries (indices 0–3) use `isCached ? Promise.resolve(null) : query(...)` — do not move them to a later position without updating the cache skip logic.
- **`acd.js` block-scoping footgun:** `liveStats` is declared with `let` inside the level-fade block (`if (currentPrice && allRthBarsRow.rows.length >= 3)` at line ~4065, inner `if (last5.length >= 3...)` at ~4108). Anything outside those blocks — including `const candidates`, the `for (const cand of candidates)` loop, `shadowCandidates`, and the INSERT body — is outside `liveStats`'s scope and will throw a ReferenceError if it references `liveStats` directly. The fix pattern is `getCached(todayET, 'levelFadeStats')` at the outer scope. **When adding any new reference to `liveStats` in `acd.js`, check whether the call site is inside or outside the level-fade block.** Similarly, `ls()` and `lsMon()` are only valid inside the inner block.
- **`getCached()`'s null-vs-undefined contract (`acd.js`) — a miss returns `null`, NEVER `undefined`. Never check `cached !== undefined` against its return value; always `cached != null` (or `??`).** Found 2026-09-05 via a DeepSeek dead-code audit, independently re-verified before acting: at least 9 calibration readers across ~15 inline blocks used `cached !== undefined ? cached : await query(...)` — since `null !== undefined` is always `true`, every one of them treated a genuine cache miss as a hit and the real query was unreachable dead code, permanently. Confirmed real, not theoretical, live impact — `ENTRY_PRESSURE_SHORT` (a validated, positive-EV live sizeMultiplier boost with real calibration data since 2026-08-24) and `WIDER_TARGET_PRESSURE_GATE` had both silently run in their null/fail-safe state for ~12 days despite real calibration rows sitting unused in `performance_audit`; a same-session SHADOW-tagging ship was dead on arrival from the same mistake. Fixed by consolidating the `_global`-scoped readers into one correct shared helper, `getGlobalCalib(key, fetchFn)` (~line 115, right after `setCached`) — use it for any new `_global`-scoped calibration read instead of hand-rolling the cache-check again. See [docs/CONVENTIONS_DETAIL.md#getcacheds-null-vs-undefined-contract](docs/CONVENTIONS_DETAIL.md#getcacheds-null-vs-undefined-contract) for the full audit, every affected site, and the verification steps. **Independently re-verified via a second, review-only DeepSeek pass same day** (all 14 originally-broken sites confirmed fixed, zero remaining `!== undefined` against `getCached()` anywhere in the file) — the review also caught and corrected one inaccurate explanatory comment Claude had written about why a since-fixed instance was harmless (it wasn't harmless for the stated reason; two independent copies of the same bug had coincidentally canceled out). Two trivial, zero-risk items from the same audit's deferred-cleanup list (`OPEN_DECISION acdjs_deferred_cleanup_from_deepseek_audit_20260905`) were also knocked out same day — `postWinDirOf` (dead, deleted) and 6 dead imports (removed) — the 5 larger extraction candidates remain deliberately deferred, not urgent.
- **Caching an async lookup keyed by date/pivot under `Promise.all` concurrency must cache the in-flight PROMISE, not the awaited value — a naive `if (!cache.has(k)) cache.set(k, await fetch())` has a real race.** When many candidates on the same key (e.g. 25 stacked pivots sharing a date) are processed via `Promise.all`, they all call the cache-check before any of them finishes awaiting and writing back — so they all miss and all fire the same expensive query anyway, silently defeating the whole point of caching. Found 2026-09-09 (`server/services/pivotConfluenceAnalysis.js`'s `getCachedBaseline`/`getCachedATR`/`getCachedLevels`): this made a 25-deep pivot stack spanning 2 distinct dates take ~7s instead of the expected ~1s, caught only by timing it, not by reading the code. Fix: `if (!cache.has(k)) cache.set(k, fetchFn(...))` — no `await` before the `.set()` call — so every concurrent caller for the same key awaits the SAME promise. Same request-coalescing idea as `setupDetectionInFlight`/`confluenceTodayInFlight`, applied per-key instead of globally. Any new per-key async cache introduced under concurrent processing needs this pattern from the start, not discovered by a slow benchmark later.
- **A "zone," "streak," or any other stateful tracker that's supposed to persist across time must be walked over ONE continuous bar sequence — never rebuilt inside a per-calendar-day (or per-`trade_date`) outer loop.** A per-day loop that reinitializes its own state each iteration (`for (const day of days) { const zones = []; ... }`) silently caps how far back any accumulated history (a touch count, a denial streak, "has this broken yet") can reach — at most one day's worth, no matter how long the real underlying event actually lasted. Found 2026-09-09 (`scratch/denial_streak_breakout_backtest.mjs`, a same-day research script): a real, verified ~32-hour, 30-touch overnight battle at one price zone (2026-09-07 evening through 2026-09-09 02:30am) got silently split across the `trade_date` boundary into two separate zones, each restarting its own touch/streak count at zero — confirmed because the max streak observed anywhere in the entire 1-year backtest was 13, roughly half the real example, and could never be more since no zone's window ever spanned more than ~24h. This is the same root shape as the older `allRthBarsRow.rows`-freezes-at-4pm footgun above, one level more general: any concept meant to span days needs its own continuous, persistent state, refreshed (not reset) as new daily inputs (e.g. a fresh `level_prices` cluster) arrive, with a reset happening only on a real domain event (the zone actually breaking) or genuine staleness (untouched for a long, explicit window) — never on a calendar/loop boundary that has nothing to do with the concept itself.
- **`allRthBarsRow.rows` freezes at 4PM RTH close — never reuse it for anything needing "bars through right now."** This array is scoped to the RTH window (9:30am-4pm), so its last element stops advancing the moment RTH closes, even though the poller keeps running through the whole 4-6PM dead zone and beyond. Found 2026-08-28: a new volume-building signal reused this array assuming its last bar always represented "the current moment," which silently froze the measurement for every dead-zone SHADOW fire — 17 fires spread across 37 minutes all showed byte-identical values. The fix pattern is `getSessionBarsSinceOpen(boundaryMod)` (`acd.js`, bounded `ts<=NOW()` query from the most recent session-open bar through now, boundaryMod=570 for RTH/1080 for Globex) — use this for anything computing a live, moment-relative measure outside the strict RTH candidate-detection path, not `allRthBarsRow.rows` directly. See [docs/CONVENTIONS_DETAIL.md#allrthbarsrowrows-freezes-at-4pm-rth-close](docs/CONVENTIONS_DETAIL.md#allrthbarsrowrows-freezes-at-4pm-rth-close).
- **The Stop hook's hardcoded-threshold-pattern alert (patterns A-E in `scripts/hardcoded-threshold-patterns.sh`) is a whole-file scan, not diff-only — a pre-existing false positive re-alerts on EVERY turn forever, not just sessions that touch the flagged file.** Found 2026-09-05: the user reported the same alert firing after every message; all flagged lines turned out to be legitimate, reviewed code from 2026-06-02 through 2026-08-16 (confirmed via `git blame`), not new violations. Don't just reassure the user it's a false positive — that doesn't stop it recurring. Fix it in the shared pattern file (add a precise, `git blame`-verified exclude token, then verify a synthetic true positive still triggers before and after) so it actually stops. See [docs/CONVENTIONS_DETAIL.md#stop-hook-whole-file-false-positives-must-be-fixed-in-the-pattern-file](docs/CONVENTIONS_DETAIL.md#stop-hook-whole-file-false-positives-must-be-fixed-in-the-pattern-file) for the fix procedure and the 6 specific instances this was first done for.

## Collaboration

- **Never analyze or reference the contents of the `trades` table as evidence of whether this system's signals are being followed, or as a basis for diagnosing execution/behavioral gaps, unless the user explicitly asks for that specific analysis.** `/api/stats/capture-ratio` (and any future metric comparing model P&L to real account P&L) only means something if the real trades were actually taken based on this system's signals — that has not been established, and the metric's *existence* doesn't imply its premise holds. If a future session is tempted to use `trades` data to diagnose "why doesn't the model match reality," ask first rather than assume the comparison is valid. See [docs/COLLABORATION_DETAIL.md#never-use-the-trades-table-to-diagnose-signal-following-behavior](docs/COLLABORATION_DETAIL.md#never-use-the-trades-table-to-diagnose-signal-following-behavior) for the retracted capture-ratio finding this rule came from.
- **During a multi-threaded investigative session — several items in flight, one branching into another — proactively state what's currently pending on the user, don't wait to be asked.** Recording a decision correctly via `flag_decision.mjs` is necessary but not sufficient: the mechanism guarantees nothing gets buried *forever*, but does nothing to keep the user informed *in the moment*. Whenever a new `OPEN_DECISION` gets flagged mid-session, or multiple threads are open at once and one becomes genuinely blocked on user input, say so explicitly in the next reply — a short, direct status line. See [docs/COLLABORATION_DETAIL.md#proactively-state-pending-items-during-multi-threaded-sessions](docs/COLLABORATION_DETAIL.md#proactively-state-pending-items-during-multi-threaded-sessions).
- **Don't give up on a thread without vetting it thoroughly — persist to a real, root-caused answer, not the first plausible-looking stopping point.** A result that looks like a dead end is exactly as suspect as a result that looks too good — the audit discipline already applied to suspiciously positive numbers applies equally to suspiciously conclusive negative ones. When something comes back surprising, too clean, or flatly debunking, treat it as a lead to root-cause mechanically, not a stopping point — only stop once the result is stable under continued scrutiny. See [docs/COLLABORATION_DETAIL.md#persist-to-a-root-caused-answer-dont-stop-at-a-plausible-negative](docs/COLLABORATION_DETAIL.md#persist-to-a-root-caused-answer-dont-stop-at-a-plausible-negative) for the GARCH investigation this was named from (three compounding bugs, each looking like a valid stopping point).
- **"Self check" (user says "have you done a self check" or similar) means running this full list, not a vibe-based "yes, looks good":**
  1. `bash .claude/hooks/check-docs-drift.sh` — the persistence self-check (CLAUDE.md/ARCHITECTURE.md drift vs structural code changes) and the hardcoded-threshold-pattern scan. Distinguish pre-existing hits (`git blame`/`git log`) from genuinely new ones.
  2. `node scripts/test_invariants.mjs` — reviewed line by line, not just checked for a nonzero exit code; note any WARNING even if unrelated to this session's work, confirmed via `git stash` whether pre-existing.
  3. `npm run lint:frontend`, `npm run lint`, `npm run build` — all three, not a subset, even if only one layer was touched.
  4. Tail `scratch/server_errors.jsonl` and `scratch/gemini_alerts.txt` for any `CLIENT_ERROR`/`SERVER_ERROR`/`SERVER_DOWN` entries newer than session start.
  5. `node scripts/flag_decision.mjs --list` — confirm nothing new needs flagging, and nothing relied on this session is a stale `PENDING` claim.
  6. `git status` — confirm the working tree is clean and every change is committed with a real message.
  7. If anything resolved this session originated from a summarized/pre-compaction claim, confirm it was actually re-derived from live data this session, not just re-asserted.
  8. `node scripts/audit_pipeline_freshness.mjs` — cross-references every `signal_type` in `performance_audit` against whether a writer is still scheduled and whether anything live reads it back.
  Report results honestly, including anything that DOESN'T pass — a self check that always comes back clean isn't being run for real.
- **Standing permission to run commands in this repo without asking first.** This covers all normal tool use — Bash commands, file edits/writes, running scripts/backtests, git commits, schema/doc regeneration, dropping confirmed-dead tables after backup, etc. Don't pause to confirm routine actions; just do the work.
  The one carve-out: genuinely high-blast-radius or hard-to-reverse actions outside normal project workflow still warrant a heads-up — force-push, `git reset --hard`, dropping data without a backup, anything touching production secrets, or actions visible outside this repo (sending messages, posting externally). This carve-out is narrow on purpose; don't expand it to cover ordinary commands.
- **Gemini timeout/fallback:** After invoking Gemini and waiting: (1) 2 min with no `scratch/antigravity_response.md` update → nudge by re-invoking with the same task; (2) another 2 min still nothing (4 min total) → Gemini's context ran out, Claude does the task directly. Don't block on Gemini indefinitely. Check elapsed time with `stat --format='%Y' scratch/antigravity_response.md` vs invocation time.
- **Gemini rabbit-holing / going off-task:** If Gemini's step-by-step trace shows it chasing something well outside the task (process/PID tracing, hunting for log files, systemd/OS-level forensics) instead of running the requested queries, this is a standing property of the default Flash-tier model config, not necessarily a recent regression — check `/home/mmoniz/.gemini/antigravity-cli/settings.json`'s `"model"` field, but verify the full session-transcript history before diagnosing it as "something changed lately." Current default is `Gemini 3.1 Pro (Low)` (a deliberate 2026-07-14 upgrade, user-confirmed). See [docs/COLLABORATION_DETAIL.md#gemini-rabbit-holing-default-model-history](docs/COLLABORATION_DETAIL.md#gemini-rabbit-holing-default-model-history) for the transcript audit this conclusion is based on.
- **Gemini quota exhaustion:** If `invoke_gemini.sh` exits in under 10 seconds with 0 lines written, diagnose immediately: `agy --log-file /tmp/agy_diag.log --print "test" 2>&1; grep -i "RESOURCE_EXHAUSTED\|quota" /tmp/agy_diag.log`. If it shows `RESOURCE_EXHAUSTED (429)`, quota is exhausted — **do not wait, do not pause work.** Claude does the task directly right now. Note the reset time for future Gemini use, but keep moving immediately.
- **`invoke_deepseek.sh` timeout ≠ failure — read the partial `scratch/deepseek_response.md` before re-dispatching.** The script uses `set -e`, so when `cline` hits its own `-t` timeout, the script exits 1 and any background-task notification wrapping it reports "failed" — but `cline`'s streamed output up to that point is still written to `scratch/deepseek_response.md` in full, and is often substantively complete (e.g. a review that finished 3 of 4 checks correctly before running out of time on the wrap-up). Found 2026-09-06: a `buildAllCandidates()` review dispatched at 600s timed out mid-answer; the partial file already contained a correct, fully-reasoned byte-diff/free-variable/return-completeness verdict. Always `cp` the partial to a preserved filename (same discipline as any other DeepSeek response) before re-dispatching with a longer timeout — don't discard it as a failed run. Same underlying lesson this codebase already has for Gemini (`feedback-gemini-cli-timeout-script-still-valid`), now true for DeepSeek's script too.
- **A THIRD, distinct `invoke_deepseek.sh` failure mode, found 2026-09-14: "Insufficient Balance" — an immediate exit (seconds, not the full timeout), zero usable content in `scratch/deepseek_response.md` (just the error line itself), not a partial-but-salvageable response.** Do not treat this like the timeout case above (there is no partial content worth reading) and do not retry the same dispatch — a balance error doesn't self-resolve by waiting or re-invoking, it needs the account topped up. Detect via `grep -i "insufficient balance\|error:" scratch/deepseek_response.md` when a dispatch reports failed but the file is tiny (a real timeout's partial file is typically large). When blocked this way on a higher-stakes-work review pass (the 3-phase rule), don't leave the work unresolved or silently skip the review step — ask the user how they want to proceed (wait for a top-up vs. Claude self-reviewing in its place), and if they choose self-review, explicitly document in the resulting `OPEN_DECISION`/commit that the review was self-done, not independent, so a future session doesn't mistake it for a real second opinion.
- **`invoke_gemini.sh`'s exit code/status has NO reliable correlation with content quality in either direction — never gate on it, always read the file and re-run the saved scripts yourself.** Found 2026-09-06: twice in one session, `agy --print`'s output landed genuinely corrupted/truncated (opening section missing, started mid-sentence e.g. `t Classifier)`) while the wrapper still reported a clean success (exit 0, normal line count) — it only measures wall-clock/line-count, not content integrity. Then, on the very next dispatch, the OPPOSITE happened: the wrapper reported `failed` (agy itself returned non-zero, no "Done" line ever printed) while the response file it left behind was actually complete, coherent, and independently verified correct in every particular. So a clean exit proves nothing and a reported failure proves nothing — the only real signal is reading the file directly and re-running whatever scripts it saved. This is a DIFFERENT failure mode from the DeepSeek timeout-partial-output issue above — no timeout occurred either time. Separately, this same audit found a real bug INSIDE a saved script (not a file-corruption issue) — `computeReplication()`'s `selectedIds` had no minimum-N floor, so it silently selected two 5-trade-total setup types as the "dominant driver" and then misreported which types it had actually picked in its own prose; a correctly N-floored re-selection reversed the practical conclusion. Script-level bugs and file-level truncation are separate risks that both need independent checking — neither one implies the other is fine. **A third, distinct failure mode found 2026-09-07 (DeepSeek, not Gemini): a "successful" response file can have the model's own raw `[thinking]`/tool-call trace (ANSI codes, tool-status echoes, occasionally hundreds of KB) appended after a complete, clean answer** — not truncation, not corruption of the real content, just debris mixed into the same file. Diagnose with `awk '{print length, NR}' <file> | sort -rn | head` — one absurdly long outlier line (hundreds of KB in a single line) is the signature; the real answer is usually intact earlier in the file, and if the tool delivered a final text-only summary in its own chat output, that's a free independent cross-check that it matches. Don't reject a response just because the file looks huge or `wc -l`/`ls -la` looks alarming — read it and separate signal from debris before deciding it's unusable.
- **Gemini vs DeepSeek division of labor.** Gemini owns heavy DB mining/analysis (combinatorial sweeps, backtests over real trade/bar data, anything where the output is a real N/WR/EV claim) — it has `gemini_readonly` DB access, and this is still subject to the standing "audit all Gemini output" rule and the 2-corrections-then-Claude-takes-over rule. DeepSeek owns design critique + code review, via `cline` (`scripts/invoke_deepseek.sh`) — grounded in real code, willing to disagree with the framing it's handed. **Do NOT dispatch full backtest/mining-script authorship to DeepSeek** — that overlap already went wrong once (a "$61,960/29x-return" claim that blew the account once real bugs were fixed). DeepSeek gets the same 2-correction-attempts discipline as Gemini, and its own self-audit of its own work is NOT a substitute for independent review even when explicitly prompted to be skeptical. For anything high-stakes — a live-code change, or a design that will gate/size a real trade — get the OTHER model's genuinely independent (blind) read before trusting either one's solo output. `deepseek_readonly` DB access is proposed but blocked on Postgres superuser setup; revisit if DeepSeek design work keeps hitting "can't verify this without real data" gaps. See [docs/COLLABORATION_DETAIL.md#gemini-vs-deepseek-division-of-labor](docs/COLLABORATION_DETAIL.md#gemini-vs-deepseek-division-of-labor) for the incidents behind each clause (the Grind Detector self-audit miss, the walk-forward script that blew the account).
- **Never combine a trailing `&` with the Bash tool's own `run_in_background: true`** when calling `invoke_gemini.sh` (or any long-running script) — stacking both loses harness tracking of the real process, so a "completed" notification fires while `agy` is still working and `antigravity_response.md` looks like a stall or failure. Just call `./scripts/invoke_gemini.sh <timeout>` directly with `run_in_background: true` and no `&`. `invoke_gemini.sh` takes a `flock` on `scratch/.invoke_gemini.lock` and refuses to start a second run. See [docs/COLLABORATION_DETAIL.md#never-combine-trailing-ampersand-with-runinbackground-for-gemini](docs/COLLABORATION_DETAIL.md#never-combine-trailing-ampersand-with-runinbackground-for-gemini) for the interleaved-write corruption this caused.
- **`invoke_gemini.sh` now `cd`s into the repo and passes `--add-dir` before invoking `agy`.** Without this, `agy`'s working directory isn't pinned to the project, so a prompt that says "read docs/X.md" makes it fall back to a full `find /` filesystem scan (~10-15s wasted). If Gemini's output is full of "I am listing the contents of... I am running find to locate..." lines instead of actual task progress, check that `invoke_gemini.sh` still has the `cd "$REPO"` / `--add-dir "$REPO"` lines before re-diagnosing from scratch.
- **A background `./start.sh` Bash call left running when a Claude Code session ends becomes an orphaned dev-server supervisor**, invisible to `systemctl status` and capable of silently taking the app down hours or days later. `start.sh` has an EXIT/INT/TERM trap that hands port 3002 back to `trading-journal-server.service` when the top-level dev session ends — but this does NOT cover `nodemon`'s spawned child crashing independently while `start.sh`/`nodemon`/`vite` all stay alive (verified live: a health-monitor now polls port 3002 directly and self-heals if it goes dark, and the systemd unit's restart-burst limits were widened so a real failure surfaces instead of looping silently). If you run `./start.sh` as a background Bash call, either run `./stop.sh` before the session ends or verify the trap fired (`systemctl --user is-active trading-journal-server.service`) — don't let a background dev session dangle and assume it's harmless. See [docs/COLLABORATION_DETAIL.md#orphaned-startsh-dev-server-supervisor-incident](docs/COLLABORATION_DETAIL.md#orphaned-startsh-dev-server-supervisor-incident) for the 4-day, 114,824-attempt crash loop this was found from.

## Where to look

- **IB_BULLISH/IB_BEARISH audit + redesign — RESOLVED NEGATIVE, both setup types suppressed** (spec'd 2026-08-31, tested and killed the same day; corrected here 2026-09-04 after Opus Audit #12 found this entry stale): `docs/IB_BULLISH_BEARISH_AUDIT_AND_REDESIGN_SPEC.md`. The live `computeIbBullBear()` never implemented the setups' actual intended thesis (break-and-retest of the 60-min Initial Balance boundary, then drive until the move exhausts) — zero break-of-boundary check, zero retest logic, no drive confirmation. Idea 1 (PRIMARY, build the real break/retest/drive detector) was run through its own cheapest kill-gate — a placebo/level-swap control — same day: the real IB boundary underperformed an economically meaningless midpoint (`RESEARCH_CLAIM ib_break_retest_drive_placebo_test_negative`, CONFIRMED), i.e. retest+drive added nothing over trading the raw break. User's call, verbatim: **"dump them both."** Both `IB_BULLISH`/`IB_BEARISH` now carry a deliberate `MANUAL_SUPPRESS_OVERRIDE` in `scripts/backtest_setup_status.mjs` (do not remove on a routine recovery signal — the override's own text says so). `OPEN_DECISION ib_bullish_bearish_audit_and_redesign_scoped` is RESOLVED; the live successor thread is `OPEN_DECISION capitalize_on_big_breaks_continuation_idea` (MEDIUM) — a fresh continuation/breakout idea, explicitly NOT this IB-specific one, which is now the 3rd of what's grown to 4 independent real negatives for this general shape (`OPEN_TEST_DRIVE`, `STRUCTURAL_BREAKOUT_RETEST_SPEC` 0/8, this placebo test, and a 2026-09-04 ADX-for-`BRACKET_BREAKOUT` finding separately corrected the same day — see `docs/OPUS_AUDIT_PROMPT_12.md`/`scratch/opus_audit_12_results.md`). Read `capitalize_on_big_breaks_continuation_idea`'s full text before proposing anything in this space again — it already carries all 4 priors.
- **Pattern Memory / condition_memory** (`server/services/patternMemoryUpdate.js`, tables `daily_performance_log`/`condition_memory`/`pattern_stats`): nightly pipeline (`runNightlyUpdate()`, cron `5 16 * * 1-5` + a same-day and, as of 2026-08-31, historical (90-day lookback) catch-up in `server/index.js`'s 30-min self-healing cron) that logs each session's outcome and rolls it into `condition_memory`'s per-condition-combination occurrences/win_rate/avg_pnl, read live (informational only, not gating) by `acd.js`'s `getHistory()` and `GET /api/pattern/combinations`+`/today-combination`. **2026-08-31 incident**: `daily_performance_log`'s catch-up only ever checked "today," so 2 real trading days landed permanently unprocessed for a month once their data arrived late (server instability) — fixed with a genuine historical scan. `condition_memory` was separately ~3.2x inflated (a since-removed redundant `setInterval` double/triple-counted days before a 2026-08-19 idempotency-guard fix) and was rebuilt clean via `scripts/rebuild_condition_memory_20260831.mjs` (backup: `condition_memory_backup_20260831`). Full writeup: `docs/OPEN_THREADS.md`'s 2026-08-31 entry.
- **2-lot scale-out with a breakeven-minus-5 runner** (2026-08-31, backtest-only, no live/SHADOW code exists): `docs/TWOLOT_SCALEOUT_BREAKEVEN_MINUS5_SPEC.md` — Lot 1 takes a quick target, Lot 2's stop arms to entry-minus-5 (a deliberate small-capped-loss, not exact breakeven) the instant Lot 1 fills and runs to a real structural level (`level_prices`, prior-period-only categories, no lookahead). Second-pass result on the real OR-short fade family: best T1=12pt, +$7.63/trade vs exit-all-no-runner, train/test OOS nearly identical ($7.57/$7.75), rigor-clean, bootstrap 98.9% positive — read the "outcome composition" breakdown before trusting the headline mean, it's not a typical-trade effect. `RESEARCH_CLAIM twolot_scaleout_be_minus5_orshort_structural`, `PROVISIONAL`. Open: independent re-verification, comparison against the user's actual current strategy as the primary baseline (not yet done), and `OPEN_DECISION twolot_scaleout_breakeven_minus5_runner_scoped_20260831` (remaining build/verification work) + `OPEN_DECISION twolot_scaleout_generalize_to_other_setups` (LOW, deferred question of applying this to other struggling setup_types — screen via Setup Reference's "Left on Table" metric, don't test pooled).
- **sizeMultiplier composite redesign spec** (2026-08-30, design-only, not built): `docs/SIZE_MULTIPLIER_COMPOSITE_REDESIGN_SPEC.md` — scopes replacing the ~25-factor hardcoded additive-bump stack (acd.js's sizeMultiplier IIFE) with a continuous, self-recalibrating composite score, in the same style as the volume-building work. Phased rollout (DeepSeek design critique → Gemini mine-and-run comparison of old vs new on real `active_setups` history → DeepSeek code review → shadow-parallel logging → promote only if it beats the hand-stack on real N). `OPEN_DECISION sizemultiplier_composite_redesign_scoped_pending_review`. Read the spec before touching the sizeMultiplier IIFE or starting this build.
- **Roster-rebuild roadmap** (Opus-authored, 2026-08-10, `scratch/MASTER_OPUS_ROSTER_REBUILD_ROADMAP.md`): plan to replace the ~130-type fade roster with 6 genuinely distinct bets (Setup A Value Fade consolidation through F Globex Level Test), gated by a strict validation protocol (Stage 0 pre-registration → Stage 1 bar-history backtest → Stage 2 shadow-live → Stage 3 reconciliation → Stage 4 live-at-1-contract → Stage 5 size). All 9 phases shipped 2026-08-10/11, including a live demotion of `VALUE_FADE` (real N=1274, EV=-$1.26/trade). Roster status: A(`VALUE_FADE`) bet-class-suppressed unless a constituent proves itself independently; B(`FAILED_SWEEP_REVERSAL`)/D(`OPENING_DRIVE_15MIN`) genuinely new, SHADOW, zero real fires yet; C/E killed by Phase 5's discriminator; F(`GLOBEX_LEVEL`) legacy-live, thin. Full detail, all bet_class resweeps, and the live-demotion writeup: `docs/OPEN_THREADS.md`'s 2026-08-10/11 entries — read those before touching anything VALUE_FADE-related again.
- **OR-length family** (`OR5`/`OR10`/`OR15`/`OR30`) + the IB 30-vs-60min window bug (2026-08-12): `computeIbBullBear()` was being fed a stale 30-min window instead of the real 60-min Initial Balance; fixed, and the two windows disagree on bullish/bearish/neither 51% of the time (`RESEARCH_CLAIM ib_bullbear_30min_vs_60min_window_test`). `IB_BULLISH`/`IB_BEARISH`/`STOP_SWEEP_LONG/SHORT` recalibration under the corrected window is still open (`OPEN_DECISION ib_bullbear_window_fix_recalibration_needed`). Legacy `OR_HIGH`/`OR_LOW`/`OR_MID_AFTER_IB` renamed to `OR5_HIGH`/`OR5_LOW`/`OR5_MID`; `OR10`/`OR15`/`OR30` HIGH/LOW/MID built and wired live SHADOW-only (all 18 new setup_types THIN_N-seeded before wiring). Phase 1 bar-history backtest found real blended edge (N=2945, WR=63.2%, EV=$7.47/trade, `RESEARCH_CLAIM or_length_phase1_fade_sweep`). Full design spec: `docs/OR_LENGTH_SEASONALITY_SPEC.md`.
- **Structural breakout/retest research + the dtClass live-gating bug** (2026-08-03, design done, build not started): `docs/STRUCTURAL_BREAKOUT_RETEST_SPEC.md` — Phase 0 test spec (build-ready, 4-arm design) plus the discovery that `acd.js`'s `isTrendCounterFade()` and 3 other live decisions gate on a column (`dtClass`) that's structurally null the entire trading day (`OPEN_DECISION dtclass_null_all_day_neuters_multiple_live_gates`, HIGH). Two Gemini build attempts failed; read this doc before touching either thread.
- **Standalone "quick-check" page + external (Home Assistant) access** — `server/public/quick-check.html`, served at `GET /quick-check`. Self-contained vanilla JS/CSS page (pulse bar, active setup card, Performance section) sourced from `active_setups`, not the real `trades` table. Exposed via a named, persistent Cloudflare Tunnel (`~/.cloudflared/config.yml`, not in this repo) at `tj.6claire.page`, locked down with exact-path ingress allowlisting plus Cloudflare Access (Google OAuth) — the rest of the app is deliberately not exposed. Full tunnel/Access setup: Claude's own memory `reference_ha_cloudflare_tunnel.md` (not in this repo — check there first for anything tunnel/Access-related). **A second standalone page, `server/public/setup-performance.html` (`GET /setup-performance`, linked from quick-check's nav menu), added 2026-08-29**: a real (`origin_status IN ACTIVE,SHADOW` only, no synthetic BACKFILL) per-setup-type N/WR/EV/rigor-trend table plus a day-by-day cumulative $ P&L chart (dollars only, deliberately NOT normalized to a % of account size — considered, rejected, see `docs/OPEN_THREADS.md`'s 2026-08-29 entry for why), backed by `GET /api/setups/performance-summary` (`server/routes/setups.js`). **Any new standalone page under `server/public/` needs its route added to BOTH `server/index.js` (an explicit `app.get()`, not generic static serving) AND `~/.cloudflared/config.yml`'s exact-path ingress allowlist (one line per new page AND per new API endpoint it calls) before it's reachable through the tunnel** — confirmed live 2026-08-29 (302 redirect to Cloudflare Access on the tunnel = working correctly, not a bug; a 404 there would mean the ingress rule is missing). Restart `cloudflared-trading-journal.service` (a user systemd service) after editing the tunnel config for it to take effect. **A third standalone page, `server/public/loss-prevention.html` (`GET /loss-prevention`, linked from quick-check's nav menu), added 2026-09-08**: Today/This-Week rollup of how much realized loss each observation-only shadow tag (DirGate/MomFade/StepTrail/PitchCatch/RangeSlope/VolRoll) would have prevented, backed by `GET /api/setups/loss-prevention-summary` (`server/routes/setups.js`) — that endpoint's `summary_text` field is pre-formatted for a Home Assistant REST sensor, same convention as `/api/setups/today-summary`. Both the page and the API path are already added to `~/.cloudflared/config.yml`'s allowlist per the rule above.
- Conviction/session read logic: `server/services/caseEngine.js`
- Opening range / day-type: `server/services/acdService.js` + `server/routes/acd.js`
- **Level fade alpha engine** (size multiplier stack, suppression logic, keepLevels, sizeMultiplier IIFE): `server/routes/acd.js` lines ~3830–4430. Gate is 3 bars (~9:34 AM). Server polls every 15s via `setInterval` in `server/index.js`.
- **Cluster touch credit** (a confluence-cluster's losing levels now get real N credit, not just the winner): `docs/CLUSTER_TOUCH_CREDIT_SPEC.md`. Live-forward insert (`acd.js`, search `CLUSTER_SIBLING_TOUCH_CREDIT`), a general distinct-day promotion floor (`scripts/backtest_setup_status.mjs`, reuses `computeRigor().clustered`), and the RTH-only historical backfill (`scripts/backfill_cluster_touch_credit_20260904.mjs`, 1,279 rows) all **shipped 2026-09-04** — the backfill took 3 Gemini dry-run rounds + 3 DeepSeek code-review rounds, catching 5 real bugs total before the real write (naming-convention gaps, wrong resolution vocabulary, a live-vs-backfill WR/EV-inflating bar-walk cap, ~93 spurious rows from a misidentified non-fade engine, a per-type expiry-window bug). Post-write recalibration produced 27 real SETUP_STATUS changes, including **3 live `ACTIVE`→`SUPPRESS` demotions** (`ONL_FADE_LONG`, `OR5_HIGH_FADE_LONG`, `OR5_LOW_FADE_SHORT`) — these had been trading on a winner-only-biased sample; adding the previously-invisible losing sibling touches flipped their true EV negative. **Globex-origin siblings shipped 2026-09-07** (`RESEARCH_CLAIM globex_cluster_touch_credit_shipped_20260907`, PROVISIONAL): `detectGlobexSetup()` had no winner-selection to port (unlike RTH's EV-ranked `sortedCandidates`+`break` — it already fires every eligible candidate as its own row), so only pooled-dedup tagging was added, via a post-insert UPDATE (DeepSeek corrected the original splice-into-INSERT plan). **`is_cluster_primary=true` means something different here than in RTH — array-order-first-to-insert-this-poll, NOT highest-EV** (Globex's `candidates` array is a fixed enumeration, never EV-sorted). Do not read a Globex `is_cluster_primary=true` row as "the best representative" by analogy with RTH's convention. Scoped to `detectGlobexSetup()`'s own candidates only — does not cover `globexFlushDetector.js` or a theoretical cross-detector co-fire between the two. Honest gap: verified via SQL dry-run + static checks, not yet observed firing on a real multi-candidate touch. Read the spec's Phase 3b section for the RTH history before touching any of this again. **2 more real gaps found+fixed 2026-09-08** (user-caught live, pushed past a first "looks fixed" answer): (1) RTH's `sortedCandidates` winner never got tagged with its siblings' `cluster_touch_id` when the winner itself ALSO failed a later eligibility check and fell into the separate "suppressed near-level audit" INSERT branch — that branch's column list simply never included the column; fixed by adding it. (2) A 4th, entirely separate insert path — `EARLY_TOUCH_BACKFILL` (a level already touched by the time the poller catches up, most commonly right at the RTH open, where several levels can be already-touched at once) — had ZERO cluster-tagging at all; fixed live (generates a real `cluster_touch_id` when 2+ levels backfill in the same poll, first-in-array-order tagged primary, matching Globex's own no-EV-ranking precedent) and historically repaired (43 rows / 13 batches back to 2026-07-02, see `docs/DB_BACKUP_CATALOG.md`'s `active_setups_early_touch_backfill_cluster_repair_backup_20260908` entry). Gap #2 is also what was silently inflating `quick-check.html`'s displayed Net P&L (see the hard rule above) — fixing it corrected a further $227 on top of the frontend filter fix. **Same day, still not done — user kept pushing past each "looks fixed" answer and kept being right to**: (3) round 1's repair marker (`historical_avg_pnl IS NOT NULL`) under-covered the true population — that field comes from the touched level's own calibrated EV, so a THIN_N type with none yet (`OR15_MID_FADE_SHORT`/`OR30_MID_FADE_SHORT`, caught live sharing an identical price+timestamp) slipped through untagged; the complete marker is `t1_label LIKE '%(backfilled early touch)%'` (set unconditionally by this path) — round 2 caught 14 more rows/6 more batches back to 2026-07-08 (`scripts/repair_early_touch_backfill_cluster_tagging_round2_20260908.mjs`). (4) A 5th real insert path, `shadowCandidates` (this file's own-documented "5th active_setups INSERT" — where `STOP_SWEEP`/`IB_BEARISH`/`C_PAIRED`/`VWAP_MAGNET`/`TRT`/`BRACKET_BREAKOUT` etc. actually fire from, not the main candidates array), also had zero cluster-tagging — fixed, though it turned out NOT to be what caused the OR15/OR30 example (that was #3) — found independently while auditing coverage generally. (5) **A real bug in fix #4's first draft**, caught by a self-check ("did you make the fix cleanly"): the backfill loop's own fix (from #2) generated ONE shared id for the ENTIRE batch unconditionally, without checking the entries shared a touch moment — different levels can each have their own earliest-touch discovered in the same poll, which would have wrongly linked unrelated touches. Fixed by extracting the now-duplicated "tag a same-poll batch" logic into one shared `tagClusterBatch(items, keyFn)` in `server/services/acdShared.js` (backfill groups by each touch's own `etMin`, `shadowCandidates` by entry price) — this also shrank `acd.js`'s net growth from +69 to +53 lines. **CORRECTED again same day (7th bug, found via code review before committing, not live data)**: that extraction still pre-assigned "primary" by raw array position, computed BEFORE either loop's own async eligibility gates (risk check, refire cooldown, `isLiveEligible`, the backfill loop's `existing`-row dedup) had run — a `continue` between the pre-assignment and the `INSERT` could gate out array position 0 and leave a group with real siblings but no primary ever written. Not observed live (RTH closed before a fresh cluster could form under the fix), confirmed via code reading + a synthetic no-DB unit test. Fixed properly: `tagClusterBatch()` now only returns per-key group membership + a `touchId`; a new `claimClusterRole(assignedKeys, key)` decides primary-vs-sibling lazily, called only AFTER each row's own `INSERT ... RETURNING id` actually succeeds — mirroring the already-shipped Globex (`globexPrimaryAssigned`) and RTH-winner (`active.clusterTouchId`) post-insert-`UPDATE` pattern instead of a pre-gate scheme. (6) **Still not fully clean**: a 4th, structurally separate, never-audited insert path was found live the same session — the older "case engine" family (`C_PAIRED`/`C_REVERSAL`/`TRT`/`A_UP_STRONG`/`A_DOWN_WEAK`/`ZONE_EDGE_FADE`/`FAILED_AUCTION`/`FAILED_SWEEP_REVERSAL`, `acd.js` ~line 2400-2600+, builds its own candidates independently of `nearLevels`/`sortedCandidates`). Confirmed real (3 same-day instances of 3 types touching within 0.25pt at the identical instant, unlinked), all SHADOW-origin. **Not fixed** — `OPEN_DECISION case_engine_family_cluster_tagging_gap_20260908` tracks it; read that first if quick-check.html's SHADOW display looks wrong again before re-deriving any of this.
- **1-year Globex-inclusive "prop challenge" walkthrough** — the whole-system combined P&L simulation across RTH+Globex at different DLL levels. Flagship script: `scripts/backtest_1yr_globex_inclusive_prop_challenge_20260720.mjs` (3 scenarios: `CURRENT_VALIDATED_ROSTER` / `LEGACY_ROLLING` / `ALL_WIRED_NO_FILTER`, the last an explicit ceiling not a recommendation). Latest numbers: `scratch/backtest_1yr_globex_inclusive_prop_challenge_RESULTS.md`, refreshed 2026-09-02 (not the standalone `docs/GLOBEX_INCLUSIVE_1YR_PROP_RESULTS_20260722.md`, stale, and not a `docs/OPEN_THREADS.md` entry — that one was pruned once resolved, per this file's own archival convention). **Headline reversed since the prior 2026-07-23 refresh**: the `CURRENT_VALIDATED_ROSTER` RTH leg alone is now solidly positive ($7,395–$7,781 across DLL 200/400/600, Globex excluded) — Globex only adds real value at the tightest DLL ($200: +$115) and actively *drags the total down* at looser DLL ($400: -$1,362; $600: -$2,457), the opposite of the old "Globex is what turns it positive" framing. Don't cite the old framing going forward. `RESEARCH_CLAIM GLOBEX_INCLUSIVE_1YR_PROP`, does not auto-refresh — re-run manually for a current number.
- **Confluence persistence + live pair-bonus lookup, with per-pair calibrated proximity** (2026-07-22): `active_setups.confluence_score_at_detection`/`confluence_levels_at_detection` written on every live INSERT. The old hardcoded 5-pair bonus map was replaced with a live lookup against `backtest_confluence.js`'s real `VALIDATED_PAIR` rows (per-pair calibrated proximity threshold, not a flat cutoff) — 71 pairs now clear the bar (N 301-1291, EV $4-$20/trade). Pairs involving a *developing* level (session VWAP, DEV_POC) are excluded from calibration by design (a confirmed confound). See `confluence_pair_proximity_2yr_backtest` `RESEARCH_CLAIM` for the full writeup.
- **Value-area regime measurement layer** (2026-07-31, tagging only, no gating): every `active_setups` INSERT is stamped with `regime_pos_Nd`/`regime_label_Nd` for 7 lookbacks via `computeRegimeStamp()`, position relative to the true volume-weighted value area. Built after `docs/REGIME_INTELLIGENCE_SPEC.md`'s full gating/routing engine was audited and rejected — zero suppression/authorization logic wired to this. Do not read anything into these columns as validated; they exist so real forward data can be judged in a few months.
- **Overnight/Globex level-fade detector** (6PM–8:30AM ET window, separate from the RTH engine): `detectGlobexSetup()`, `server/routes/acd.js` ~line 582. 8 `_OVERNIGHT` setup_types beyond the original PD_VAH/PD_VAL/PD_POC, each dynamically SHADOW-gated via `getOvernightLevelLiveStatus()` until real forward data clears N≥20/EV≥-$5 — check `widerWindowNew` or the setup_type suffix before assuming any candidate fires live. `backtest_setup_status.mjs`'s generic scan already reassesses these (and everything else) automatically; no special wiring was needed. See `docs/OPEN_THREADS.md`'s 2026-07-22 entry for the scoping/verification writeup.
- **Alpha Engine overview page**: Edge → Alpha Engine tab (`src/components/dashboard/AlphaEngineOverview.jsx`) — human-readable summary of every system component, size multiplier factor, suppressed setup, and pending road map.
- **Opus audit prompts**: `docs/OPUS_AUDIT_PROMPT.md` through `_5.md` — 5 strategic-review audits (code/bugs forensics, sizeMultiplier discrimination/objective alignment, closed-loop learning, order-flow research pattern, overall-architecture review). Audit 5 (2026-07-28) covers the "only 5 of 182 setup_types are live" finding and prioritization of the open backlog. Run manually via Claude Opus 4.8 when strategic review is needed, not automated.
- **Database migration protocol**: [docs/DB_MIGRATION_PROTOCOL.md](docs/DB_MIGRATION_PROTOCOL.md) — standing checklist for any schema/data change. There is no tracked migration-file system in this codebase; this protocol replaces one. Read before writing any `ALTER TABLE`, bulk `UPDATE`, or backfill script.
- **Database backup table catalog**: [docs/DB_BACKUP_CATALOG.md](docs/DB_BACKUP_CATALOG.md) — every `*_backup_*` table in the live DB, what it's a snapshot of, and why. Any script that creates a backup table must add an entry here in the same commit.
- **Standing data-sanity audit**: `node scripts/data_sanity_audit.mjs` (weekly cron) — catches impossible MAE/MFE values, non-uniform `$/pt` constants, unexpected symbols in `price_bars_primary`, impossible `win_rate`/`sample_size` bounds. Exit code 1 is currently expected (one standing flag: `ES` symbol data still present 2023-11-15 to 2023-12-15, queries reading it are fixed to filter by symbol but the source rows remain). **This audit only flags rows in `price_bars_primary` itself — it cannot verify every ad hoc script querying that table has its own `symbol='NQ'` filter.** When writing or auditing a new script that reads `price_bars_primary` directly, check for that filter explicitly.
- Shared query helpers (NL30/NL10, gap drift, prior-week range): `server/services/queries.js` — check here before writing a new one
- Point-in-time regime classification (directional-trend/price-stretch/persistence z-scores, post-rotation-day flag, vol-regime lookup, conditioned sizing multiplier): `server/services/regimeClassificationService.js`. **Deduplicates the code only — the methodology was tested the same day and FAILED.** Do not build live sizing/suppression logic on Regime A/B/C as currently constructed (see the regime detection entry below).
- **Compression → tail-MFE spec** (not yet built, 2026-08-04): `docs/COMPRESSION_TAIL_MFE_SPEC.md` — a fresh angle on whether pre-trade compression predicts tail-MFE probability (not mean EV), analyzed separately within mean-reversion vs continuation strategy families rather than pooled. Written to be read standalone.
- **Regime detection/anticipation research spec + validation framework** — `regime_detection_methodology_needs_validation` RESOLVED (negative): `docs/REGIME_DETECTION_SPEC.md` (2026-07-18) surveys the methodology space and ran a 5-stage validation framework against Regime A/B/C. **All three now have a real, tested negative result** — an independent PELT-changepoint ground truth found Regime A's label transitions align with real structural breaks *worse than chance*. Doesn't rule out regime detection generally, rules out today's specific z-score/tercile construction.
- **External audit + remediation plan** (2026-08-05): `docs/audit_findings.md`/`docs/remediation_plan.md` (Opus, verbatim) + `docs/DECISIONS_LOG.md` (what was actually verified/corrected — read this one first). Headline: RTH `OPTIMAL_STOP`'s genuine-holdout status is still an open question; 6 of 7 live-calibration-mismatch flags were timing artifacts, 1 (`STOP_SWEEP_LONG/SHORT`) was a real bug, fixed.
- **Compression → session-range research** (2026-08-04/05, resolved MIXED — not ready to wire): `docs/COMPRESSION_TAIL_MFE_SPEC.md` — found a real volatility-clustering effect, and that wide-IB days predict TURBULENT (whipsaw), not TREND (clean continuation) — so "hold longer on wide-IB days" as originally framed is likely wrong, not just unvalidated. **Not wired, not ready to design** — `OPEN_DECISION wire_intraday_ib_range_exit_signal` (HIGH) now requires day-type as a required second condition. Trade-level version of these metrics: `active_setups.va_width_pctile_60d`/`va_overlap_streak`/`ib_range_pctile_60d`.
- **Candlestick & order-flow entry-confirmation research** (2026-07-21): `docs/CANDLE_ORDERFLOW_RESEARCH_SPEC.md` — consolidated index of every test in this thread. Headline: every candle-pattern variant failed once properly checked, including one that looked real and was retracted after failing a held-out replication test. Read this spec before starting any related work.
- **Volume-building strength as a non-directional expansion precursor** (2026-08-29, tested, NOT wired): `docs/VOLUME_BUILDING_EXPANSION_SIGNAL_SPEC.md` — confirmed, confound-checked (RTH+Globex both independently) finding that composite volume-building strength predicts a bigger swing coming soon, in EITHER direction (top decile 44% larger 20min excursion than bottom decile); direction itself is a clean negative by two independent tests (price momentum, order-flow imbalance); "momentum feeds momentum" beats the coiled-spring intuition; an initial "37min early warning" finding was self-corrected the same session — flicker-frequency decomposition showed it's mostly a base-rate coincidence, not real lead time — treat this as a real-time gauge, not a predictive alert; persistence is short regardless (median 4min episodes). A connective test onto the existing fade roster (does high building predict worse fade outcomes) was rejected as a blanket rule — opposite-signed within-family results. Read this before extending or re-testing anything volume-building-related.
- **`momentum_ctx_sameday` (SAME_DAY_FORMING momentum-context sizing) — RESOLVED NEGATIVE 2026-09-13, do not re-wire without new data.** The underlying research (`RESEARCH_CLAIM momentum_ctx_sameday_corrected_after_deepseek_audit`, CONFIRMED 2026-08-30) claimed a real +$14.40/trade EV gap between `momentumContext='ACTIVE'` vs `'QUIET'` for SAME_DAY_FORMING (IB/OR-boundary) fades, N=276, walk-forward stable — a genuinely well-vetted-looking finding that sat fully computed (`classifyLevelFormation()` + `computeLiveVolumeBuildingSignal()`'s `momentumContext`, already run and persisted to `active_setups.vol_building_signal` on every real SAME_DAY_FORMING fire) but never wired into anything live, found while auditing for unexploited positive findings. A DeepSeek design critique (before any code was written) found the plan needed real fixes — `sizeDelta` must be z-score-derived like `DAY_TYPE_ALPHA`, not a raw dollar gap; needs the same co-primary breakeven/EV-bootstrap tests as the 2026-09-13 `DAY_TYPE_ALPHA` fix; must reuse existing `VOLUME_BUILDING_CALIBRATION` state rather than duplicate it — and explicitly warned "do not assume $14.40 survives a properly-filtered fresh measurement." It didn't survive: `scripts/calibrate_momentum_ctx_sizing.mjs` (inert, `performance_audit`-only, built following the corrected methodology — stored fire-time `momentumContext`, not re-derived; `is_cluster_primary` filter applied; per-trade real breakeven from each trade's own recorded stop/target via `resolveDirection()`) measured ACTIVE n=49 WR=38.8% EV=-$3.68/trade vs QUIET n=63 WR=41.3% EV=-$0.70/trade — the SIGN REVERSED from the original claim, EV difference z=-0.20 (indistinguishable from noise), day-blocked bootstrap CI=[-$42.64,$60.65] (crosses zero massively), both groups clustered at only 8-9 distinct real dates. `RESEARCH_CLAIM momentum_ctx_sizing_fresh_measurement_negative_20260913`, CONFIRMED. `OPEN_DECISION momentum_ctx_sameday_wiring_untested_20260913` RESOLVED, not wired — no `sizeMultiplier` factor was built. Same failure shape as NL30/win-streak/overnight-alignment: real live N was far thinner than the backtest's re-derived population implied. The original 2026-08-30 claim is left unmodified in the ledger as an honest record, not deleted — this entry is the one to cite going forward. Two reusable extractions came out of this thread regardless of the negative result: `breakevenWr()`/`hashSeed()`/`mulberry32()`/`dayBlockedBootstrapCI()` moved from `backtest_day_type_alpha.js` into `server/services/rigorDiagnostics.js` (shared, now a 2nd consumer) so a future real-EV-gap calibration script doesn't reimplement the day-blocked bootstrap a third time.
- **Touch-quality signal ideas** (2026-08-25, DeepSeek brainstorm, NOT tested): `docs/TOUCH_QUALITY_SIGNAL_IDEAS_SPEC.md` — 6 candidate pre-decision quality-filter ideas for level touches (volatility-normalized proximity, level liquidity depletion, signed structural runway, approach path geometry, tonight's fade-friendliness, touch-in-expansion vs. rotation), each with a sanity-check plan and kill criteria, grown out of the same session's confirmation-entry and breakout-decisiveness threads (both retracted/negative — see the `price_bars`/`price_bars_primary` multi-contract-collision fix this session also shipped). `OPEN_DECISION touch_quality_ideas_pending_test` (MEDIUM) — nothing here is built or wired; read the spec before starting any related work.
- **Confluence + volume/delta exhaustion interaction monitor** (2026-07-23): `scripts/backtest_confluence_exhaustion_interaction.mjs` (weekly cron) — tests whether the pooled-negative exhaustion signature behaves differently at a validated confluence zone. Persists `active_setups.exhaustion_signal_at_detection` (informational only, never wired into sizing/suppression) and `RESEARCH_CLAIM confluence_exhaustion_interaction` (`PROVISIONAL` — a real signature survives a leak fix but fails the stability bar both train and test).
- **Engagement/tussle entry-TIMING test** (2026-07-23, one-off, not scheduled): `scripts/backtest_engagement_confirmation_entry.mjs` — asks whether waiting for engagement to resolve before entering beats immediate entry. Result: negative — neither of two candidate triggers beat both the baseline and a blind-delay control. `RESEARCH_CLAIM engagement_confirmation_entry_timing`.
- **Bar-6 "worst point passed" checkpoint** — real finding, wired LIVE informational-only (2026-07-23), escalated to an "EXIT NOW" alert (2026-07-26): `active_setups.bar6_checkpoint` cleanly separates real outcomes (63-67% WR/+$25 EV vs 43-47% WR/-$34 EV, independently reproduced). **Deliberately does not gate/delay the original entry** — and still cannot auto-close a position, since this system has no order/broker execution capability. `active_setups.bar6_exit_recommended` (the frozen `targetDistFraction<0.873` rule) cleared its N≥20 real-data validation bar and is now surfaced as a more assertive recommendation. Full detail: `docs/OPEN_THREADS.md`.
- **Big-move-day signal (`BIGMOVE_LIVE_SIGNAL`) and sigma-continuation signal** — both wired LIVE informational-only (2026-07-24/26), both tested as EXIT triggers and found not ready that way: sigma-continuation failed a train/test check; big-move-day's "fades against the day's trend" hypothesis was **refuted** (the signal helps trend-following more than fades). A blind, signal-free fast-exit-when-fading-against-trend rule showed a much larger effect (+$14,320/N=258) — `OPEN_DECISION test_blind_fast_exit_fading_against_trend`. **Recurring lesson**: a numeric-threshold exit signal is prone to a "freshness" confound (the condition may already be true at entry) — always gate on the condition being NEW since entry, and always compare against a blind/structural control. The runner-extension idea built from this thread was RESOLVED NEGATIVE (the trigger condition occurred zero times in real, non-`BACKFILL` history) — full account `docs/BIGMOVE_SIGNAL_RUNNER_EXTENSION_SPEC.md`.
- **Flush post-entry exit signals (`post_entry_exit_signals`)** — wired LIVE, informational-only (hypothetical-only, never touches real `actual_pnl`/`resolution`), 2026-09-02: on every open real `GLOBEX_FLUSH_*` position, `acd.js`'s `resolveSetupsByPrice()` calls `detectPostEntryExitSignals()` (`scripts/pilot_exits_extended.mjs`) each 15s poll and persists `active_setups.post_entry_exit_signals` (JSONB) once per mechanism if it fires: `range_slope` (both Globex modes) or `vol_rollover` (Reversal-mode only — Continuation fails rigor). `scripts/backtest_flush_post_entry_exit_signals_promotion.mjs` (weekly, `run_weekly_backtests.sh`) is the real promotion/retirement trigger — no-ops below N=20 real fires per mechanism/mode, then writes a final `RESEARCH_CLAIM` verdict (paired against the trade's own real `actual_pnl`, segmented ALL vs BIG-MOVE-ONLY by top-tercile `mfe_points`) and, if positive, flags a new `OPEN_DECISION` proposing to actually change `globexFlushDetector.js`'s live target logic. Monitoring: `GET /api/setups/flush-exit-signals-summary` feeds both `quick-check.html`'s RangeSlope/VolRoll row tags (tap — not hover, touchscreen-safe — opens a popup with the per-trade hypothetical + running cumulative) and a new ledger card on `setup-performance.html`. Full build writeup: `docs/OPEN_THREADS.md`'s 2026-09-02 entry.
- **Stack-break + volume/delta confirmation signal (`STACK_VOL_BREAK_LIVE`)** — wired LIVE informational-only (2026-07-27), the fastest-firing member of this informational-signal family: fires on a price close through a proximity cluster after being clearly on the other side within the last 30 bars. RTH threshold (v2, superseded v1): `clusterSize>=1 + volZ>=0.5 + OSR>=0.55 + pace(paceZ>=1)` at stop40/target40 → EV +$6.70/trade, N=481, rigor-clean but not yet clearing the strict replication bar (`PROVISIONAL`, `RESEARCH_CLAIM loose_confluence_pace_rth_promising_not_confirmed`). Globex kept on the v1 threshold. A real "dead during Globex hours" bug (an early-return in the route physically preceded the signal block) was found and fixed by extracting `computeStackVolSignal()` into a real function called from both branches. VWAP has no structured `active_setups` tracking yet (`OPEN_DECISION vwap_not_structurally_persisted_like_other_levels`). Full narrative and all 4 build versions: `docs/OPEN_THREADS.md`'s 2026-07-27 "missed-continuation-move research arc" entry.
- **Single-firing / directional-conflict gate** (2026-09-03, BUILT, wired to all 4 real insert sites): `docs/SINGLE_FIRING_DIRECTIONAL_CONFLICT_SPEC.md`. `isOppositeDirectionOpen()` (`server/routes/acd.js`) forces SHADOW when a new real candidate's direction conflicts with a currently-open real (`origin_status='ACTIVE'`) position — same-direction stacking stays allowed. Live on the RTH main active-slot path, the Globex path (`detectGlobexSetup()`), `STACK_VOL_BREAK_LIVE`, and the `shadowCandidates` loop (added one at a time per user request, each tested before the next). The other 3 raw `INSERT INTO active_setups` sites in this file are hardcoded always-SHADOW/EXPIRED audit inserts that can never produce a real ACTIVE row, so they correctly don't need this gate (checked — matches where the cross-direction/sibling-reversal gates also aren't wired). Tested pre-ship at every site: live smoke test + an 8-case synthetic unit test of the classification logic (all passed, no DB writes used), plus `test_invariants.mjs` after each (no new failures). Empirically backed: holding beats switching by $3.09/case when a conflict occurs (`RESEARCH_CLAIM opposite_direction_hold_vs_switch_20260903`, CONFIRMED, N=1316 — a first-pass bug 85x-counted one long-lived trade, fixed); the gate's own account-level P&L impact is negligible (-$41 on N=20 real conflicts, PROVISIONAL) — **frame as risk-discipline, not a return improvement**. `OPEN_DECISION single_firing_directional_conflict_gate_not_built` (MEDIUM) now just tracks the design/code-review pass the full rollout should still get before being considered fully shipped.
- **Same-setup-type "refire gate"** (2026-09-03 calibration, LIVE-WIRED 2026-09-13/14): `scripts/backtest_same_setup_refire_gate.mjs` (daily, `run_daily_calibration.sh`), `performance_audit` `signal_type='SAME_TYPE_REFIRE_GATE_CALIB'`, per `(setup_type, RTH/GLOBEX session)` plus `_POOLED_ALL_RTH`/`_POOLED_ALL_GLOBEX` fallback rows. Mirrors the live sibling-reversal gate's event-based design (blocked until a DIFFERENT setup fires, never a timer) applied to same-exact-setup_type re-entry instead of opposite-direction. Corrected a real bug found in an initial Gemini pass: a strict boundary check against only the nearest same-type predecessor silently misclassified the single most common refire pattern (an exact-tie resolve-then-immediately-refire, since timestamps here are minute-quantized) as "allowed," undercounting the true effect by ~2.6x. `_POOLED_ALL_RTH` cleared `GATE` for the first time 2026-09-10 and stayed stable through 9 days of nightly reruns (N=335 blocked real trades, blockedEv=-$8.54/trade vs allowedEv=-$0.19/trade, distinctDates=41, not day-clustered) — no individual `(setup_type, session)` row nor `_POOLED_ALL_GLOBEX` has cleared `GATE` yet. `RESEARCH_CLAIM same_type_refire_gate_calibration_20260903` (`PROVISIONAL`), self-recalibrates nightly. **`isSameSetupRefireBlocked(tradeDate, setupType, session)`** (`acd.js`, exported, right after `isOppositeDirectionOpen()`) is now wired to all 4 real insert sites (Globex candidates loop, `STACK_VOL_BREAK_LIVE`, RTH main active-slot path, `shadowCandidates` loop), forcing SHADOW not skip, same convention as the other 3 force-SHADOW gates. Deliberately unscoped by `trade_date` in the block-check itself (matching the calibration's own unscoped chain-walk — only the calibration-row lookup is day-cached). Known limitation, documented not fixed: at the RTH main path only, `skipRedundantShadowInsert` will silently swallow many of this gate's forced-SHADOW candidates within 5min of a same-type resolution (its own trigger condition), thinning real-time outcome data at that one site — not special-cased around, since bypassing it would reintroduce the 2026-08-20 duplicate-row-flood bug. **Phase 2 (independent DeepSeek code review) was NOT completed** — the dispatch failed on a DeepSeek account "Insufficient Balance" error, not a timeout; Claude self-reviewed in its place per explicit user direction 2026-09-14. `OPEN_DECISION same_type_refire_gate_live_wiring_pending` is RESOLVED with this caveat — a real independent code-review pass is still recommended once the DeepSeek account balance is restored.
- Risk guardrails (DLL, profit-lock, cooldown): `server/routes/dll.js`, `profitLock.js`, `cooldown.js`
- **Optimal stops/targets** (data-derived, weekly recompute): `performance_audit` rows with `signal_type='OPTIMAL_STOP'`; computed by `scripts/update_optimal_stops.mjs`; read in `acd.js` as `liveStats._opt[setup_type]`. **Always read the LATEST row per `signal_name` via `DISTINCT ON (signal_name) ... ORDER BY signal_name, run_date DESC`** — a `GROUP BY signal_type` join can silently exclude, not just stale-serve, individual rows (found live in `/api/performance-audit/unified`, fixed). Target selection has two paths: the base EV-sweep, and `computeCorrectedTarget()` (chronological bar-walk, guardrailed) which overrides when it clears every guardrail — check `notes.method` for `"corrected-resim"`. **A circuit breaker (`applyCircuitBreaker()`) now rejects any stop/target move >35% off its prior value unless real N has grown meaningfully** — shipped 2026-08-04 after `computeCorrectedTarget()`'s guardrail stack was found to have been cleared largely by *synthetic* (`BACKFILL`-origin) data, not real trades, for most of its life. **`sweepOptimalStopAndTarget()` (the STOP side) is now `origin_status`-filtered too** — `rawByTypeReal`, `origin_status IN ('ACTIVE','SHADOW')`, feeding `computeStopTargetForType()` as the primary decision-tree input (verified live 2026-08-20: all 4 previously-synthetic-heavy setup_types below show `method: 'EV-sweep-real'`/`'p75mae-real-fallback'`, not a synthetic-derived method). Shipped 2026-08-09 as the deliberate one-time re-baseline this entry used to describe as still-needed (`rawByType_origin_status_filter`, resolved 2026-08-10) — the circuit breaker was bypassed for that one run only per its own design, then re-armed normally. Day-type-conditioned sub-calibration exists for `IB_BULLISH`/`IB_BEARISH` (`{setup_type}_{day_type}`-keyed rows). Full derivation, every bug in this chain, and current numbers: `docs/TARGET_CALIBRATION_SPEC.md` + `docs/OPEN_THREADS.md`.
- **The 4 highest-volume live setup_types (`GLOBEX_VWAP_MAGNET_LONG/SHORT`, `VWAP_MAGNET_LONG/SHORT`) had their BACKFILL history built from a bug distinct from the STOP-filtering one above** — `fired_at`/`resolved_at` on their BACKFILL rows are stored 4-5h early (DST-dependent, a naive ET-as-UTC timestamp round-trip; `entry_zone_low`/`stop_level`/`t1_level`/`resolution`/`actual_pnl` are all correct). Since `rawByTypeReal` above already excludes BACKFILL entirely, this has **no live stop/target consequence today** — it only matters to a script that pools BACKFILL-origin data and reads timing (bars-to-resolution, time-of-day). Full writeup: `docs/VWAP_MAGNET_BACKFILL_REPAIR_SPEC.md`, `OPEN_DECISION vwap_magnet_backfill_entry_price_trade_date_mismatch` (MEDIUM, optional).
- **Setup Reference page** (Edge → Setup Reference): what each setup_type IS — criteria, detection window, family — joined against live N/WR/EV/status/rigor. Static definitions in `server/config/setupDefinitions.js`, served by `GET /api/setups/reference`, frontend `SetupReferenceView.jsx`. 16 setup_types remain genuinely undocumented (shown, flagged, not hidden) — add real definitions before claiming full coverage. Rebuilt as a sortable table 2026-07-28 with MFE/MAE/Avg-P&L/Left-on-Table/touch-freshness columns, preferring real (`origin_status IN ('ACTIVE','SHADOW')`) trades once N≥5.
- **Research Ledger page** (Edge → Research Ledger): every `RESEARCH_CLAIM`/`OPEN_DECISION` row in one browsable table. `GET /api/research/ledger` reuses `listClaims()`/`listDecisions()` directly. **Wired Live column** greps each claim's slug against `server/routes`/`server/services`/`src` to show whether it's actually affecting live output — 17/152 claims currently show as wired.
- **Overnight research** (past-year walkthroughs, stop/target recalibration, taper-timing, big-move diagnostics): `docs/OVERNIGHT_RESEARCH_SPEC.md` — line-by-line mapping of every RTH lesson to its overnight analog. Two open decisions: `overnight_bigmove_target_truncation_needs_sequence_fix` (HIGH) and `overnight_bigmove_trailing_mechanism_test` (MEDIUM).
- Wider-window (24hr / valid-until-superseded) level-fade backtest findings: `docs/WIDER_WINDOW_BACKTEST_20260720.md` — `OPEN_DECISION wider_window_level_fade_backtest_findings_20260720` (not implemented live). **The 12-row same-day-forming-levels half is confirmed unreliable, not just unverified** — `detectLevelFades()` hardcodes an RTH-only bar filter, so no wider-window test for those 12 rows can call the real function; do not use them for a live decision. The separate 59-level prior-period-levels table is unaffected.
- Monthly VWAP + Prior-Year value area (`MONTHLY_VWAP`, `PY_VAH`/`PY_VAL`/`PY_POC`): computed by `scripts/compute_levels.js`, wired into `keepLevelsAll` — all 4 are `THIN_N` (SHADOW-only). Real finding, not a bug: most touches of a month/year-scale magnet don't resolve within a single RTH session (`PY_VAL` has 0 touches over ~2.3 years). Continues accumulating real SHADOW data via the already-scheduled generic pipeline.
- **Step-trail runner extension (Phase 1: SHADOW-parallel logging, shipped 2026-09-04, not promoted)**: `server/services/stepTrailWalker.js` (`stepStepTrail()`) — once a real trade reaches the existing live 1.5x wider target (`widerTargetWalker.js`), instead of banking, ratchets the stop forward in fixed-size steps and keeps riding. Composed on top of the real `stepWiderTarget()` (never reimplemented). Step fraction + base-distance floor derived weekly by `scripts/calibrate_step_trail_fraction.mjs` (`performance_audit` `STEP_TRAIL_FRACTION`), selected via plateau + `computeRigor().clean` + a genuine big-win-presence subgroup-symmetry check (LONG/SHORT, ACTIVE/SHADOW, FADE/NONFADE) — not max-EV. Writes `active_setups.step_trail_shadow` (JSONB, observation-only, never touches a real trade's `resolution`/`actual_pnl`/`stop_level`) via two cooperating pieces in `acd.js`'s `resolveSetupsByPrice()`: inline tracking inside the `widerTargetMult` branch, plus a follow-up `completeStepTrailShadows()` pass (called every poll) for rows whose real path already resolved before the shadow finished walking — both try/catch-isolated per-row so a bug here can never block a real resolution. `RESEARCH_CLAIM step_trail_runner_shadow_parallel_20260904` (PROVISIONAL). `OPEN_DECISION step_trail_phase2_promotion_pending` (MEDIUM) — Phase 2 (flipping the live branch to actually resolve on the trail) needs 6 weeks/N≥20 real armed rows and the full pre-registered promote/kill criteria (`scratch/opus_audit_12_results.md` §7) before it's even considered, then the full 3-phase DeepSeek/Gemini review per the higher-stakes-work rule. Full context: `docs/OPUS_AUDIT_PROMPT_12.md` / `scratch/opus_audit_12_results.md` (Opus Audit #12).
- **Pitch and Catch (forward-tracking only, shipped 2026-09-04, UNVALIDATED — 6 independent negative tests)**: `server/services/pitchCatchWalker.js` (`stepPitchCatch()`) — bank the first leg at the existing 1.5x wider target, then watch for a genuinely confirmed pullback (3 consecutive closes retracing ≥15% of the leg), gate it on a calibrated filter (RVol of the settle window vs the first leg, settle speed in bars, daily ADX regime — all percentile-derived, `scripts/calibrate_pitch_catch_filter.mjs`, `performance_audit` `PITCH_CATCH_FILTER`), then simulate a fresh re-entry. **Every test run so far is negative or failed to replicate**: full population, wide/structural stop sweep, momentum snapshot, a 9:45-11am window (day-clustering artifact), CVD trend into the pause (opposite-signed from the hypothesis), and a combined RVol+settle+ADX filter that looked real on the small setup-gated sample (N=60 LONG, +$24.11) but was fragile under day-exclusion and did NOT replicate on a much larger bar-level backtest (N=748, both directions negative). Shipped as SHADOW-parallel observation-only logging anyway, at the user's explicit request ("if it's not good then the N will stay low") — writes `active_setups.pitch_catch_shadow`, same inline+follow-up (`completePitchCatchShadows()`) architecture as the step-trail mechanism above, same try/catch isolation guarantees. Quick-check.html's "PnC" tag (tap for the per-trade hypothetical + running LONG/SHORT totals via `GET /api/setups/pitch-catch-shadow-summary`). `RESEARCH_CLAIM pitch_and_catch_forward_tracking_20260904` (PROVISIONAL, full 6-test evidence trail in the claim text). `OPEN_DECISION pitch_and_catch_recheck_pending` (LOW) — do not treat a future positive at low N as validation without the same day-clustering/top-day-exclusion scrutiny that killed the small-sample version once already.
- **Breakeven-then-trail single-position exit mechanism**: `performance_audit` rows with `signal_type='BREAKEVEN_TRAIL_TEST'`, computed by `scripts/backtest_breakeven_trail.mjs` (weekly cron). Mechanism: stay on the normal stop until target is reached, then snap to breakeven and trail forward instead of a fixed second target — 1-contract-compatible. 6 blended (non-day-type) survivors wired live SHADOW-only via `CONDITIONAL_VARIANTS`. **5 of these 6 confirmed non-functional 2026-08-04** — no fresh calibration row, and 100% real resolutions are plain fixed-stop/target; the trail has never once engaged for any of them. **Root cause found 2026-09-08** (user-caught live: `PD_POC_FADE_LONG_TRAIL` took an 89pt stop vs. the base type's correctly-calibrated 24pt): 4 `liveStats._opt[type]` lookups in `acd.js` used the POST-`resolveSetupType()` type name directly, but `update_optimal_stops.mjs` only ever calibrates/stores `OPTIMAL_STOP` under a setup_type's BASE name — every one of these 6 variants' real trades was silently falling through to the crude `mae_p75`/`STOP` fallback instead of a validated stop, explaining why the trail never had a real chance to engage. Fixed via `getOptStopForType()` (`server/services/acdShared.js`), which unwraps to `CONDITIONAL_VARIANTS[type].baseType` before the lookup. This mechanism is the leading candidate remedy for this project's standing risk-management priority — `OPEN_DECISION breakeven_trail_4_more_variants_lost_calibration_row` (HIGH), now genuinely worth re-evaluating with a correct stop. The `IB_BULLISH`/`IB_BEARISH` day-type-conditioned extension found **0 of 5 buckets survive** the full IS/OOS/plateau/rigor pipeline — a real, honest negative, kept scheduled so it self-recalibrates as real IB volume grows.
- GARCH(1,1) walk-forward daily volatility scale (canonical, persisted, do not refit inline): `performance_audit` rows with `signal_type='GARCH_VOL_SCALE'`, computed by `scripts/backfill_garch_vol_scale_history.py` (requires `symbol='NQ'` filter). Real but modest, minority effect against `OPTIMAL_STOP` (28/103 setup_types improve) — **not wired into live `sizeMultiplier`**. Took 4 compounding bugs to get a trustworthy number (frozen-anchor confound, ES contamination, near-unit-root numerical degeneracy, downstream `OPTIMAL_STOP` staleness) — see `scripts/backtest_garch_scaled_stop_all_setups.mjs`'s own comments before touching this again. **A dual-barrier extension (both stop AND target scaled together, matching the actual triple-barrier method) was tested 2026-09-08 and REJECTED** — pooled result looked positive on 3 live setup_types, but a subgroup-symmetry check (real `ACTIVE`-origin trades vs `SHADOW`-origin) reversed all 3; the pooled improvement was an artifact of `SHADOW` trades that never fired live (`RESEARCH_CLAIM garch_dual_barrier_subgroup_symmetry_negative_20260908`). Two real infra fixes shipped the same day regardless of that result and are worth keeping: the `ON CONFLICT` key was normalized from script-run-date to trade-date (was silently duplicate-prone on any re-run), and a one-time catch-up backfill closed a ~7-week gap where the series had gone stale (last computed mid-July, never scheduled).
- **Standalone volatility-regime monitor** (2026-09-08, informational only, deliberately isolated from every setup-related module): `server/services/volatilityRegime.js` + `server/routes/volatilityRegime.js` (`GET /api/volatility/regime`, `GET /api/volatility/regime/history`), a small tappable card on `quick-check.html` that opens a historical-scale line-chart modal (reuses the existing `#modal`/mRow/mSection popup convention, chart drawn with the same canvas-sizing pattern as the Performance section's equity curve). **Any new endpoint a `server/public/*.html` page calls must also be added to `~/.cloudflared/config.yml`'s ingress allowlist** (existing rule, restated here because it was actually missed building this feature) — the base `/api/volatility/regime` endpoint was live and correct on localhost for hours before anyone noticed the card was silently 404ing through the Cloudflare Tunnel (`tj.6claire.page`), since a failed fetch here just hides the card rather than throwing a visible error. Caught only because the user checked the tunnel URL directly and reported an empty box. Reads the single freshest `GARCH_VOL_SCALE` row (`signal_name='LATEST'`) — a forward-looking forecast for the *next* session, computed nightly (`run_daily_calibration.sh`, 8:20 PM ET, ~7s) using the **full expanding all-history window** through that night's close, deliberately NOT the walk-forward's own backward-looking historical rows (those would already be stale by the next morning) and NOT a rolling window (see below). User's explicit direction: reads volatility conditions for their own sake, not tailored to or wired into any setup's stop/target/sizing — lives in its own service/route file, never folded into `acdShared.js`/`setups.js`.
  - **Model spec cross-checked against NYU Stern's V-Lab GARCH documentation** — conditional-variance formula, `α+β<1` stability constraint, unconditional-variance formula, Normal-innovation assumption, and MLE estimation all match the field's own canonical GARCH(1,1) baseline exactly.
  - **Window choice (expanding vs. rolling) flip-flopped FOUR times on 2026-09-08 before landing on expanding for good** — full blow-by-blow in `scripts/backfill_garch_vol_scale_history.py`'s own header comment. Short version: (1) started expanding on an initial empirical test; (2) a DeepSeek review found that test diluted and statistically meaningless, switched thinking toward rolling; (3) a properly-scoped, scipy-validated Wilcoxon re-test found rolling-250 significantly more accurate (one-sided p=0.0054) — shipped rolling on that evidence; (4) investigating "how has this forecast the biggest real moves" (a direct user question) surfaced two real DATA bugs contaminating the whole comparison — `price_bars_primary` has 6 multi-month calendar gaps from NQ's quarterly contract roll not being stitched into a continuous series, plus several roll-week price discontinuities (front/back-month blending) that don't show up as gaps at all. Once both are excluded (see `nq_roll_week_dates()` and the calendar-gap check in the script), re-running the SAME comparison on clean data REVERSES it: expanding is now significantly better (one-sided p=0.018, N=169, median delta flips from -0.037 favoring rolling to +0.0056 favoring expanding) — a short rolling window turned out to be far more vulnerable to a single extreme fake data point than an expanding window with years of real history diluting it. Reverted to expanding on that final, doubly-cleaned evidence. **Two remaining large moves (2026-06-23, 2026-06-29) sit just outside the precise roll-week window and remain unexplained** — flagged as a genuinely open question (`flag_decision.mjs`), not silently absorbed into a wider guess.
  - **No qualitative HOT/WARM/NORMAL/COOL/COLD label** — shipped with one, removed the same day after directly testing whether it predicted next-day realized moves (`scratch/test_garch_label_calibration.py`): it didn't, for either window choice (non-monotonic bucket ordering, weak ~0.14-0.15 correlation between the continuous scale and realized move size). Shows the raw scale only now — an honest description of the reading, not a predictive claim.
  - This whole thread is a worked example of "a number computed once isn't verified," taken further than usual — even the SECOND, properly-controlled statistical test (real library, correct methodology, real significance) was itself still only as trustworthy as the data underneath it, and that data turned out to be contaminated. A clean methodology on dirty data can look exactly as decisive as a clean methodology on clean data.
  - **Does the forecast scale actually predict big moves?** (direct user question, answered on the final clean/expanding data, 2026-09-08): yes, a real but modest lift, not a reliable alarm. Spearman correlation(scale, |next-day move|) = 0.26 (p<0.0001, N=298), direction stable across a 3-way chronological split. Baseline elevated-scale rate (scale>1.0) is 41.6% of all days, rising monotonically to 48.4%/57.1%/65.4% at 200+/300+/400+pt move days (Mann-Whitney p<0.001 at every threshold). Honest limit: 35% of 400+pt days still show a NOT-elevated forecast the day before, including 2 of the single 10 biggest moves in the dataset — this is what a GARCH(1,1) model is built to do (near-term variance from recent clustering), not a magnitude-specific predictor of any one outsized move. `RESEARCH_CLAIM garch_forecast_scale_predicts_big_move_size_20260908`, PROVISIONAL.
  - **Does a LOW reading predict an imminent "vol pop"?** (direct user question, 2026-09-09) — tested, no support, weak evidence points the OPPOSITE way. Bottom-quintile scale days were followed by LOWER forward realized vol at 5/10/20-day horizons, not higher (5-day max-move Mann-Whitney p=0.03); pooled Spearman(scale today, forward 10d realized vol) = +0.24 (persistence, not mean-reversion-into-a-spike). Weaker footing than the big-move finding above though — a 3-way chronological split of that correlation is NOT stable (signs: -0.04, +0.13, +0.06, none individually significant). Net: no evidence for "a pop is coming soon," net lean is toward continued calm, but treat that second part as provisional, not settled. `RESEARCH_CLAIM garch_low_scale_predicts_vol_pop_negative_20260909`, PROVISIONAL.
  - **Does a LOW reading predict BALANCE-type (range-bound) conditions over TREND/TURBULENT (explosive)?** (direct user question, 2026-09-09) — real, correctly-directioned lean, not statistically decisive. Joined the scale series against `acd_daily_log.day_type` (the ground-truth BALANCE/TREND/TURBULENT classifier, N=312 no-lookahead pairs): bottom-tercile scale days are 62.5% BALANCE/15.4% TURBULENT vs top-tercile 50.0% BALANCE/23.1% TURBULENT, median scale ordered correctly (BALANCE 0.961 < TREND 0.978 < TURBULENT 1.009) — but pooled significance doesn't clear conventional thresholds (Kruskal-Wallis p=0.27, BALANCE-vs-rest Mann-Whitney p=0.13), likely underpowered at N=312 split 3 ways, and likely substantially overlapping information with the big-move finding above (TURBULENT is itself defined by `range_ratio>=1.25`, not an independent signal). `RESEARCH_CLAIM garch_scale_predicts_balance_vs_turbulent_daytype_20260909`, PROVISIONAL.
- **Momentum-chase live setup (`MOMENTUM_CHASE_MEDIUM_LONG`/`_SHORT`, shipped 2026-09-09, SHADOW-only, real N=0)**: the one survivor of a 5-round generic-setup backtest arc this same session (`RESEARCH_CLAIM setup6_momentum_chase_medium_regime_positive_20260909` — N=128, win rate 58.6%, mean +$13.23/trade, stable across both chronological halves). Mechanism: `server/services/momentumChaseDetector.js` — 5-min RTH bar closes beyond prior day's RTH high/low, enter immediately (no retest), symmetric stop/target sized to `scripts/calibrate_momentum_chase_width.mjs`'s nightly-recalibrated 75th-percentile-of-bar-range width (NOT the backtest's original fixed 46pt — a real, self-recalibrating value per this codebase's own no-static-thresholds rule). **Only fires when `getCurrentGarchRegime()` (`server/services/volatilityRegime.js`) reads MEDIUM** — LOW and HIGH were both confirmed real losers, not just thin, in the same backtest. This is the first live setup that reads GARCH regime as an actual gate rather than a display number — a deliberate, narrow exception to the volatility monitor's own "isolated, informational only" header comment, composed at the ROUTE level (`GET /api/volatility/regime`'s `favorableSetups` field, via `server/services/validatedSetupRegistry.js` — a small, explicit, growable list of "setup X validated positive in regime Y," not a hardcoded display string) rather than inside `volatilityRegime.js` itself, which stays setup-agnostic. Real N=0 as of shipping — the standing weekly `SETUP_STATUS` scan will pick this setup_type up automatically once it fires; flag a fresh `OPEN_DECISION` once real N approaches 20, not before. `GET /api/volatility/regime`'s `favorableSetups` field only shows a line on quick-check.html's volatility card when something IS favorable that day (matches `#sweep-watch`'s own "stay hidden unless there's something to say" convention).
- **Live pulse (volume-acceleration) reading** (`server/services/pulseReading.js`, `GET /api/pulse/reading`, shipped 2026-09-09): raw, live "is 5-min-bar volume accelerating (PICKING_UP) or decelerating (DROPPING_OFF) right now" read — the SAME `volZ` definition (current bar's volume z-scored against its own trailing 20-bar mean/std, compared to 3 bars ago) validated in a same-session backtest that tested it as a directional signal and found it does NOT reliably predict forward direction, even combined with VWAP slope and price-vs-VWAP (`RESEARCH_CLAIM pulse_vwap_slope_direction_negative_20260909`). Shown purely for its own sake on quick-check.html, honestly captioned "informational only, not a signal" — same restraint as the volatility card's own raw-scale-no-label design. Own standalone service/route file, not folded into acd.js.
- **Cross-session pivot/confluence/dormancy research thread** (2026-09-09, informational-only live endpoints shipped, core trading hypothesis still unresolved): triggered by investigating why no live signal caught a real ~300pt overnight NQ flush. Shipped: `server/services/swingPivots.js` (canonical fractal swing-pivot detector), `server/services/pivotConfluenceAnalysis.js` + `server/routes/pivotAnalysis.js` (`GET /api/pivots/live-analysis`/`/api/pivots/session-volume` — cross-session support/resistance with cascade-past-broken, confluence, dormancy, volume; see ARCHITECTURE.md's Services table for the full description). **Tested and largely REJECTED, in order**: (1) elevated volume at/after a pivot predicting a better hold — confirmed BACKWARDS, elevated volume predicts a worse hold (`RESEARCH_CLAIM pivot_volume_predicts_worse_hold_20260909`, `pivot_break_volume_window_sweep_20260909`); (2) confluence>=2 or a composite score predicting bigger moves or better trade quality — no stable effect, several dead-end variants (`pivot_compound_signature_9_9_not_confirmed_20260909`, `reliable_level_break_mfe_negative_20260909`); (3) a bar-by-bar simulation of fading a directly defended (4+ bar hold-and-progress), confluence-validated pivot — genuinely unprofitable, N=166-572/bucket, not day-clustered, verified independently. **The one cleanly stable finding**: a pivot with ZERO nearby confluence reliably gets run through on retest (`RESEARCH_CLAIM pivot_zero_confluence_predicts_breakthrough_20260909`) — a negative/avoidance signal, not a positive trade trigger. **Two real methodology bugs found and fixed mid-thread, both now standing Conventions entries above**: an async per-key cache race under `Promise.all` concurrency, and a stateful tracker (a "zone," a denial streak) silently reset by an outer per-`trade_date` loop instead of persisting across the full continuous timeline — both cost a full wasted backtest cycle before being caught by timing/sanity-checking the output rather than trusting it. **Denial-streak hypothesis — RESOLVED NEGATIVE (2026-09-09/10)**: does a level tested and denied many times predict a bigger/more reliable move on the touch that finally breaks through? Real-world anchor: a confluence zone (~29629-29631, PD_VAH/CAM_R3/OR5_MID) was touched 30 times over ~32 hours before the 30th touch broke into the real flush. `scratch/denial_streak_breakout_backtest.mjs` (1-year, continuous cross-day zone tracking, the async-cache and per-day-reset bugs below both fixed) came back flat/non-monotonic: pooled EV by streak bucket was 0-0 denials $-4.97, 1-2 $+3.11, 3-4 $-3.51, 5+ $-2.86 (N=2337-4385/bucket, day-clustering 5.9%, not an artifact). A longer fight does not predict a better breakout — the real 30-touch example was a real event, not a generalizable pattern. **Follow-up angle, 2026-09-10, also RESOLVED NEGATIVE**: a genuinely different question than the above — does a zone's CURRENT touch density (touches in a trailing 24h window, measured live at each touch, non-directional) predict a BIGGER upcoming move, a coiled-tension/release framing modeled on the validated `VOLUME_BUILDING_EXPANSION_SIGNAL_SPEC.md` non-directional-expansion pattern? `scratch/denial_density_precursor_phase0.mjs` (Phase 0 forward-return pre-test, N=48,747 touch events, same continuous zone-tracking) found the OPPOSITE: mean |forward move|/ATR falls monotonically as density rises (0.141 at first-touch down to 0.103 at 13+ touches, same shape at +60/+120min) — a heavily-contested zone behaves like an absorbing range, not a coiled spring. Day-clustering low and stable across all buckets, not an artifact. **Both tested angles on the denial-streak/touch-count family (cumulative streak-at-break → EV, and live touch density → forward move) are now negative** (`RESEARCH_CLAIM denial_touch_density_precursor_negative_20260910`) — no detector was built. The real 9/8-9/9 example remains a genuine, non-generalizable event; catching that event-shape (if pursued further) needs a signal family other than touch count/density.

**Raw fractal pivot detector is far too noisy to be useful directly — found and partially addressed 2026-09-09/10**: `findSwingPoints(bars, 5)` (the live default) placed 64 confirmed pivots over just 2 days (2026-09-08/09) — roughly one every 40-45 minutes, all day. Simply raising `swingWidth` only adds confirmation lag without adding real significance (even swingWidth=100, 8+ hours of lag, still produced pivots describing the same handful of real moves). A ZigZag-style magnitude filter (keep the fast swingWidth=5 fractal detector, but resequence so an opposite-type pivot is only accepted as a new structural turn if it moved ≥`threshold*ATR20` from the last accepted one — `scratch/zigzag_magnitude_filter_backtest.mjs`) cuts this dramatically (5848/yr survivors at threshold=0 down to 59/yr at threshold=2.0x ATR) and, tested against the real 2026-09-07/09 flush, correctly flagged a confirmed reversal 28 hours before the eventual low. But two real limits found: (1) it improves REWARD, not RELIABILITY — HOLD rate drifts down as threshold rises (79.1%→69.8%), while realized MFE-when-held grows sharply and consistently across two independent stop widths (0.79-1.26 ATR at threshold=0 up to 2.25-2.86 ATR at threshold=2.0x) — bigger payoff, not better odds; (2) it fires ONCE per swing — a continuation move (like the flush's own 9/9 05:30-08:20 acceleration phase) just silently extends the already-accepted pivot, generating no second alert. **Methodology trap found mid-thread**: the original $EV/WR simulation table used each threshold's own median (p50) MFE as the trade target, which makes ~50% win rate a tautology by construction regardless of real edge — confirmed by widening the stop 10x (0.02x ATR → 0.30x ATR) with the win rate barely moving. `RESEARCH_CLAIM zigzag_magnitude_filter_bigger_reward_not_higher_wr_20260910`, PROVISIONAL. Whether entering and holding a position off a magnitude-filtered reversal would actually capture a meaningful fraction of a real flush's P&L is untested — proposed and explicitly declined by the user 2026-09-10, not rejected — `OPEN_DECISION magnitude_filter_flush_capture_untested_20260910` tracks it as genuinely open, not urgent.

**Volume-piling-on-at-the-break — tested POSITIVE, 2026-09-10** (a different, narrower question than the pivot_volume_predicts_worse_hold finding above — that one asked "does volume at a touch predict whether the level holds," this one asks "does the market's own big move show a volume signature at its own initiation," and the two are consistent with each other, not in tension): `scratch/big_globex_move_volume_piling.mjs` found 35 Globex-origin moves >=300pt (ZigZag-ATR-filtered, 2025-09-28 to present), located each move's own 30-min steepest-acceleration window (Globex-hours-only search — an early version let an intervening RTH session win the search on multi-day legs, which is why the 9/9 flush's own accel point first came back wrong; fixed), and compared per-move volZ in the 60min before vs the 30min at/after that point: 30/35 moves (85.7%) positive delta, mean +1.03, median +0.50, and a clean aggregate 10-min-bucket profile (flat ~0.2-1.0 pre-break, peaking at 0.86/1.24/2.35 at +0/+10/+20min, decaying back to ~0 by +170min). The motivating concrete example replicates individually: the 9/7-9/9 flush's defended zone (~29629-29631) held ~4.5h at near-baseline volume (mostly -0.7 to +0.8 z), then spiked to +5.14 z in the exact 10-min bucket price finally broke. Honest caveat: price range and matched volume aren't fully independent at short timescales, so this is a real correlation about market structure (quiet absorption vs. a volume-backed break), not a from-scratch independent-variable test — no formal significance test run, sign-count/median only. `RESEARCH_CLAIM globex_big_move_volume_piling_at_accel_20260910`, PROVISIONAL. Informational only, not wired to anything live — `OPEN_DECISION globex_volume_piling_live_wiring_untested_20260910` tracks whether a live "is volume piling on right now" card (matching `pulseReading.js`/`sessionVolumeMonitor.js`'s existing informational-only pattern) is worth building; not proposed or declined yet, genuinely open. **Follow-up 1, same day: is the peak at the BEGINNING or END of the move?** By price distance, clearly the beginning. By clock time (measured from the zigzag origin, which includes any prior consolidation) it's messier — see the corrected numbers below. The 9/9 motivating example is itself one of the later-peaking cases (84% price / 92% time from origin) because ~28 of its ~29 total hours were the quiet defended-zone battle — but within the sharp acceleration leg alone, the spike is still at the front (peaks ~20-25min after the real break, decays over the next 2h+). **Follow-up 2, same day, caught a real data bug: "was there a DOW/time-of-day/news pattern to the 9/35 moves that never showed a volume pile-on?"** Yes — but it's a data artifact, not a market pattern. All 9 originated inside 2025-09-28 through 2025-11-19 16:14 ET, a previously-undocumented 4th instance of the known recurring quarterly-rollover gap (`docs/KNOWN_ISSUES.md` item 16) where `price_bars_primary` NQ has degraded/sparse density and near-flat tiny bid/ask volume instead of a literal void — recovers at exactly 2025-11-19 16:15 ET, the same signature timestamp as the other 3 documented gaps. Re-run on the corrected clean window (`DATA_START='2025-11-20'`, N=26): **zero moves show no pile-on** (was 9/35) — 24/26 (92.3%) positive delta, mean +1.36, median +0.85 (was 85.7%/+1.03/+0.50); price-fraction median 0.15 (mean 0.30), time-fraction median 0.30 (mean 0.37, EARLY=15/MID=3/LATE=8 of 26). The 2 remaining negative-delta moves aren't counter-examples — both already had high volZ *before* the accel window (an already-hot session), so the marginal increase reads negative even though absolute volume stayed elevated throughout. A suspiciously clean calendar cluster (all exceptions landing in one 7-week window) was the tell that the original "26% never pile on" caveat needed checking before trusting, not reporting as-is. **Follow-up 3, same day: user proposed firing a trade on the volume spike after a defended level breaks — tested, RESOLVED NEGATIVE for the volume-trigger part, real positive for a different part.** `scratch/volume_confirmed_breakout_backtest.mjs`, Phase 1 forward-return pre-test only (no stop/target), reused `denial_streak_breakout_backtest.mjs`'s confirmed-breakout population (N=11,233, clean-volume window only): HIGH_VOL breaks (volZ above median) continue WORSE than LOW_VOL breaks at every horizon (+240min: -3.48 vs +1.88) — volume-at-the-break does NOT predict continuation, consistent with the already-existing `pivot_volume_predicts_worse_hold_20260909` finding. But splitting the SAME population by genuine prior defense (streak>=1, i.e. tested and held at least once before breaking) vs. undefended (streak==0, broken on first touch) with volume held constant showed a large real gap: defended+high-vol breaks hold up (+240min -0.26) while undefended+high-vol breaks are a steadily worsening fakeout (+240min -11.44). **Important reconciliation, since this looks like it contradicts the big-move volume-piling finding above but doesn't**: that finding is retrospective (selects moves already known to have gotten big, then looks at volume during their fastest stretch — close to true by construction, since covering more points fast mechanically involves more matched volume) while this one is forward-predictive (takes every break with no foreknowledge of outcome, asks if volume forecasts continuation). Both are true; only the second one tells you anything usable in real time — "big moves show volume in hindsight" does NOT mean "a volume spike right now predicts a big move," and the forward test says the opposite: a volume spike more often marks a fakeout than a continuation. Do not read the big-move volume-piling finding as a live trading signal — it isn't one, and this test is the direct evidence why. Phase 2 (real bar-by-bar stop/target simulation of the defended-vs-undefended angle) not yet built — negative Phase 1 means no trade machinery gets built for the volume-trigger version; the defended-vs-undefended angle passed the cheap screen and would need Phase 2 before it means anything tradeable. **Correction, same day (user asked whether stops would have killed any of these): the first pass never checked the CSV's own stopPrice (median dist 74.8pt) against the walk-forward — `scratch/volume_confirmed_breakout_stop_check.mjs` did, and it materially changes 2 of the 4 group readings.** By +240min ~51% of ALL events would have been stopped somewhere along the way. With the stop honored (frozen at -stopDist once touched): UNDEFENDED+HIGH_VOL's dramatic clean -11.44 was overstated, real is -4.42 (still bad, less extreme); LOW_VOL's clean +1.88 (looked like the better continuation group) REVERSES to -1.42 (LOW_VOL gets stopped MORE often than HIGH_VOL, 54.3% vs 48.3% — the no-stop read doesn't survive real risk management); DEFENDED+HIGH_VOL flips from slightly negative (-0.26 clean) to modestly POSITIVE (+0.89 with-stop) — looked at the time like the one group that improved.

**FINAL, same day: RESOLVED NEGATIVE after placebo-testing — nothing in this sub-thread survives scrutiny.** `RESEARCH_CLAIM volume_confirmed_defended_breakout_continuation_20260910`, CONFIRMED negative. User pushed the parameter search further (real bar-by-bar sim with a real, non-tautological ATR-multiple target grid — `defended_breakout_phase2*.mjs` — then a session split after correctly pointing out 120min is too short for a ~14.5h Globex session, then a no-timeout version plus a confluence-cluster-size filter after correctly pointing out 11,233 events/294 days is ~38 signals/day, far too loose to be a "known market level"). The no-timeout + confluence-filter version (`defended_breakout_phase3_notimeout_confluence.mjs`) produced dramatically good-looking numbers (WR up to 82.2%, EV up to $114.79) — correctly NOT trusted on sight per the standing math-self-audit rule (extreme sweep result = hand-trace before reporting), and root-caused via a placebo test (`defended_breakout_phase3b_placebo.mjs`: same population/stop/target/structure, trade direction randomly shuffled instead of the real breakout direction) — placebo WR=81.5%/EV=$95.76 nearly matched real WR=82.2%/EV=$115.48, and both were close to the analytic driftless-random-walk baseline (gambler's ruin: farDist/(near+far)=75.0%, since target=0.5xATR sits much closer than stop=1-1.5xATR with no time limit). **The same placebo method was then applied RETROACTIVELY to the earlier, more modest 720min-capped GLOBEX result** (`defended_breakout_phase2d_placebo.mjs`, 5 independent placebo trials for a noise floor) that had NOT originally looked suspicious (WR=57.5%, EV=$16.96) — real WR=58.1%/EV=$22.82 fell inside the placebo trials' own range (WR 56.7-57.3%, EV $6.84-$22.63) — also geometric, not real signal. **No version tested at any hold window, defense/confluence threshold, or volume filter shows a real edge once placebo-tested.** Worked example of why the placebo-testing/math-self-audit rules exist: the obviously-too-good result (Phase 3) got caught immediately, but the earlier, plausible-looking result (Phase 2d) would NOT have been caught without deliberately re-applying the same check retroactively — a result that looks reasonable is not automatically exempt from the same scrutiny that catches an implausible one.

**Redesigned angle, same day, genuinely promising so far: `major_swing_pivot_defended_break_continuation_20260910`, PROVISIONAL.** The user clarified the level should be a real PRICE-ACTION swing pivot (a level the market itself turned from before), not a `level_prices` indicator overlap — matching how they described the original 9/8-9/9 flush discovery by hand. Rebuilt on `swingPivots.js` + the already-validated ZigZag-ATR filter (1.5x ATR — the exact threshold that already flagged the real 9/8-9/9 reversal 28h early), tracked with the same touch/denial/break state machine, filtered to streak>=1 (genuinely defended). Phase 1: N=59, median +30.75pt at +240min, 62.7% positive, spread across 49 distinct dates (checked directly, not clustering-driven). A prior-week-mid directional filter was tested as a side check and found redundant (93% of the population was already naturally aligned with it). **A real methodology bug was caught mid-Phase-2**: the CSV export never included `stopPrice`, so every stop comparison silently checked against `NaN` and could never fire (100% of trades effectively had no stop) — stop%=0.0 across every cell was the tell. Fixed, then re-tested with a proper WITH_STOP variant and, deliberately, a NO_STOP (target-or-timeout only) variant, both against a placebo WIDENED to 10 independent random-direction trials per cell (not the earlier 1-3). **Real direction beat the placebo average in all 12 of 12 cells tested** (6 target/hold combos x 2 stop variants) — WITH_STOP real EV $47.62-$88.72/trade vs placebo averages of -$12.77 to $20.46 (a realistic near-zero/negative no-skill baseline); NO_STOP real EV $54.67-$106.13 vs placebo averages $1.37-$28.87 (placebo itself elevated from general NQ drift over the window, noisier evidence but still favorable). Best cell: WITH_STOP, target=1.5x ATR, hold=240min — WR=52.7%, EV=$88.72, placebo avg $8.27 (10-trial range -$24.21 to $51.02, real sits clearly outside it). Most consistent, most rigorously placebo-tested positive result of the whole session. Still backtest-only (N=55, above the N>=20 floor but below full comfort, no SHADOW wiring, no real live N) — next steps before any live consideration: widen the placebo further, sensitivity-check the 1.5x ATR threshold choice, only then consider the standard new-setup-type checklist.

**RTH-vs-Globex smoothness + RTH chop-handling, same day.** User's own hand-read ("RTH takes longer to decide but moves smoother") tested directly and came back OPPOSITE on both counts: GLOBEX takes ~3x longer to decide (median 800min vs RTH's 285min from first touch to break) and GLOBEX is smoother (mean MAE 0.15 ATR vs RTH's 0.25 ATR) — RTH is the faster-but-choppier session. Session split at the 1.5x threshold also showed RTH carrying roughly double Globex's per-trade EV ($112.77 vs $57.66) despite the extra chop. Tested handling the RTH chop by widening the stop to a multiple of the pivot's own real structural distance: 1.5x is the sweet spot (stop-out rate 38.7%->19.4%, WR 48.4%->58.1%, EV $112.77->$133.15) — concrete risk goes from median 111.2pt/$222 to 166.7pt/$333. Per-trade check confirmed this reduces whipsaw, not misses: of 6 trades saved from a 1.0x stop-out, zero reached the actual target, all resolved via time-exit. Follow-up (does a bigger TARGET catch bigger moves?): no — target-hit rate stays flat at 6.5% from 1.5x to 2.5x ATR (the same 2 trades are the only real big winners regardless of target width), dropping to 0% at 3.0x. The real lever is HOLD TIME, not target size: 2.5x ATR/720min hold reaches WR=61.3%, EV=$244.38, total $7,575.78 (nearly double the 240min baseline) by giving the same underlying move more time to develop via the time-exit, not by reaching a bigger target more often. **Threshold sensitivity check, same day — CONFIRMED, not a cherry-picked threshold.** `scratch/major_pivot_threshold_sensitivity.mjs`, requested after the user explicitly said not to declare this dead from one weak cell: swept the ZigZag threshold itself (1.0x/1.25x/1.5x/1.75x/2.0x ATR, fixed at target=1.5x ATR/hold=240min/real stop, 10-trial placebo each). Real beat placebo average at **all 5 thresholds**, not just 1.5x — deltas $27.93/$31.41/$53.71/$72.77/$42.44 respectively, with win rate climbing steadily from 42.3% to 54.5% as the pivot definition gets more selective (exactly the shape a genuine effect should have — a more major level is a cleaner signal). 1.75x is actually the single best cell (WR=54.3%, EV=$95.08, delta $72.77), not the original 1.5x pick. The ATR-normalization itself does not look like a fragile design choice — the monotonic WR-vs-threshold relationship is itself evidence it's capturing something real, so a fixed-point-distance alternative (considered per user request) wasn't warranted. This materially strengthens the finding beyond the original single-threshold result. Still backtest-only.

**Both pivot ideas SHIPPED LIVE, SHADOW-only, same day** — `server/services/majorPivotDefendedBreakDetector.js` (`MAJOR_PIVOT_DEFENDED_BREAK_LONG`/`SHORT`, the finding two paragraphs up) and `server/services/stallDefendedLevelDetector.js` (`STALL_DEFENDED_LEVEL_LONG`/`SHORT`, RTH-only, below) — both wired into `acd.js`'s `runSetupDetection` as a single function call each, both real N=0 as of shipping. **A real, live-breaking bug was found (DeepSeek code review) and fixed the same day, before either had fired live** — see the naive-timestamp-parsing Convention entry above for the fix; the "is this recent" check was silently rejecting every real signal, so this catch mattered. **Distinct designs, worth not conflating: majors START from a confirmed pivot and wait for a retest; the stall detector START from a live price stall (4 consecutive 5-min bars, tight range) and only THEN checks whether that zone was defended before** — the opposite sequencing, closer to how the user originally hand-spotted the 9/8-9/9 zone.

**MAJOR_PIVOT_DEFENDED_BREAK's Globex arm (config "A") suppressed 2026-09-10, RESOLVED.** A DeepSeek-recommended stricter standard (day-blocked bootstrap CI must exclude both zero AND the full 30-trial placebo range, `scratch/major_pivot_globex_regrid_robust.mjs`) found 0/64 stop/target/hold cells pass at the then-current N=24 Globex sample (`RESEARCH_CLAIM major_pivot_globex_regrid_0of64_20260910`) — flagged as `OPEN_DECISION major_pivot_globex_extend_history_or_suppress_20260910` to either extend the backtest window (price_bars_primary nominally has NQ back to 2022-12-14) or suppress cleanly. **Extending was checked and found NOT viable**: pre-2025 NQ data in this DB is almost entirely single daily placeholder rows (~1.0 bars/day for most of 2022-2024, only scattered incomplete months have real intraday density — see `docs/KNOWN_ISSUES.md` item 17, a much broader gap than the known quarterly-rollover pattern) — pooling it would corrupt the backtest, not strengthen it (`RESEARCH_CLAIM major_pivot_globex_extend_history_not_viable_20260910`). Resolved via the decision's own option (b): `majorPivotDefendedBreakDetector.js` now skips firing entirely on a Globex-session defended break rather than using the disproven 1.0x/1.5x/240min config — RTH arm (config "B") is unaffected and continues SHADOW-only.

**Stall-defended-level detector build, same day.** `SWING_WIDTH=40` (RTH) confirms a "prior defense" raw fractal pivot (any magnitude, unfiltered — not ZigZag-magnitude-filtered like majors); a stall = 4 consecutive 5-min bars whose combined range stays under a **data-derived** "quiet" cutoff. This replaced an earlier arbitrary `0.08x ATR` literal (never calculated, just guessed) — measuring the real distribution found 0.08x was only the ~30th percentile of all 4-bar windows (not rare/special), and RTH/Globex have genuinely different baseline noise (RTH's median 4-bar range is ~1.75x Globex's), so one flat number was treating both sessions inconsistently. Replaced with each session's own real p10 (RTH=0.0702x ATR, Globex=0.0447x ATR) — this alone roughly **doubled** RTH's real-stop EV ($13.75→$28.55 at the best cell). A second refinement, CLEAN vs COILED (a stall window tight by price but hiding elevated volume underneath), was tested twice with opposite results: on the ORIGINAL, uncorrected quiet-threshold population using raw forward-return only, COILED looked like it collapsed toward flat and CLEAN beat the pooled average — but **once the quiet threshold was fixed to the real p10 value above AND a real stop/target was applied, a proper 0.5/0.75/1.0/1.25/1.5x volZ sweep reversed this: COILED actually BEATS CLEAN at every threshold except a too-thin N=4 one.** The original finding did not survive being re-checked properly — **the CLEAN/COILED gate was REMOVED same-day; the live detector fires on every qualifying stall regardless of volume** (`maxVolZ` still computed and returned for future analysis, not yet persisted to the `active_setups` row). Real stop (the stall's own tight-range extreme), target=0.5x ATR20 (the cell with the best real-vs-placebo delta, not the biggest raw number), hold=210min **uncapped** at the RTH session close (untested capped, so left uncapped to match what was validated, same precedent as the majors' own uncapped design). CLEAN-only backtest population: N=98, WR=30.6%, EV=$28.55/trade, 10-trial placebo avg=$4.64, delta=$23.91 — the LIVE population (CLEAN+COILED combined, post-gate-removal) is N=109. `RESEARCH_CLAIM stall_defended_level_rth_shipped_20260910`, PROVISIONAL. **Robustness check RESOLVED 2026-09-10** (`OPEN_DECISION stall_defended_level_phase2_robustness_gap_20260910`): at the real live population (N=109), both the actually-shipped hold=210min cell (CI=[$0.14,$60.02]) and the best-by-delta hold=360min cell (CI=[$6.98,$88.09]) have a positive bootstrap-CI lower bound but fail to exclude the full placebo range — genuinely marginal, not a clean pass. Top-5 trades = 75% of total EV at 210min. Still more favorable than the other two ideas tested the same day (both had negative CI lower bounds) — consistent with DeepSeek's original "most robust of the three" call, just not decisive. No wiring change (real N=0, zero capital risk); `RESEARCH_CLAIM stall_defended_level_rth_robustness_marginal_20260910`. **Globex was tested too (own p10, SWING_WIDTH=50) and looked comparable on raw forward-return, but collapsed to ~$2-4/trade once a real stop was applied — NOT wired, RTH-only by design.**

**Does NOT catch the motivating 9/8-9/9 flush example — confirmed directly, twice.** Neither detector fires on 9/9 (verified live via `computeDefendedBreaks()`/`computeStallDefendedLevelSignals()` against real data, not just backtest). Root cause for the stall detector, traced precisely: the real 29629-29631 zone was actively re-tested ~12 times over the battle and stayed in that band only ONE time long enough to register as quiet (right at the very start, before any prior-defense evidence existed) — the STALL trigger structurally can't recognize a zone that's constantly being fought over rather than sitting still, regardless of hold time, `SWING_WIDTH`, or volume-based calibration (both tested directly: volume during a pivot's formation doesn't predict how fast the next rotation happens, Spearman≈0.02-0.09 across two independent tests; the real 9/9 zone's own volume during its retests was almost exactly average, not elevated, ruling out an RVol-based fix too). Catching that shape of event needs a separate denial-streak/touch-count-based trigger (closer to the majors' own design) — scoped but not built, tracked as a genuinely open, separate idea, not a gap in this detector.

**Follow-on thread, same day: "how many big moves do these 4 setups actually miss?" — led to the most promising new idea of the session, still NOT wired.** Measured the miss rate directly (duration-bounded, since an unbounded scan wrongly counts multi-week trend legs as the same kind of event as a fast flush): a consistent **~87-90% of real 300-400+pt moves are missed** by the 2 detectors above, across every cut tested (`RESEARCH_CLAIM four_live_setups_miss_87pct_of_big_moves_20260910`). Digging into the missed population surfaced a real market pattern — a "climactic acceleration reversal": missed moves are overwhelmingly (89.6%) reversals of the prevailing 24h trend, NOT preceded by a quiet stall (only 21.9%), but preceded by the prior trend's own final 4h *accelerating* sharply (93.4% show 3x+ the leg's average pace) rather than decelerating — a blow-off right before the snap, independently confirming the earlier `VOLUME_BUILDING_EXPANSION_SIGNAL_SPEC.md` "momentum feeds momentum" finding in a new context. **A real self-caught methodology bug lives in this thread's own history**: an early "continuation vs reversal" check compared each move against its own immediately-adjacent ZigZag leg, which ALWAYS alternates direction by construction — a tautology, not a finding (misread out loud once too, as "99% continuation" when the number was 99% reversal) — corrected by comparing against the real net 24h price change instead. **New Convention, applies generally**: never compare a ZigZag leg's direction against its own immediately-adjacent leg to ask "continuation or reversal" — always compare against an independent trend measure (net price change over a real time window), since consecutive legs are guaranteed to alternate. Built and Phase-2-tested (real stop = the trailing window's own extreme, target/hold grid, 10-trial placebo) a Globex-only fade-the-acceleration idea: **35 of 35 cells beat placebo** (N=132), best practical cell target=1.0x ATR/hold=240min, EV=$26.40/trade ($6.60/hr of exposure — more efficient than the 16-hour cells despite their bigger raw EV). Caught 53.6% of real Globex 300+pt moves in the Phase 1 pre-test — dramatically better coverage than either shipped detector. RTH confirmed NEGATIVE for this idea (fading a climactic acceleration loses money at every horizon in RTH) — Globex-only by design. **Full spec, all methodology corrections, and the exact Phase 2/3 scope still needed**: `docs/CLIMACTIC_ACCELERATION_REVERSAL_SPEC.md`. A further "adds"/pyramiding extension was scoped but explicitly NOT tested, given the base strategy's own 67-89% single-shot stop rate makes blind scaling-in a real risk, not an obvious win: `docs/CLIMACTIC_ACCEL_REVERSAL_ADDS_SCOPE.md`. **DeepSeek design review, same day, substantially tempers the headline numbers — read `docs/CLIMACTIC_ACCELERATION_REVERSAL_SPEC.md`'s own review section before citing "35/35" or "53.6%" again.** Code itself confirmed clean (plus the timezone bug above, caught by the same review). But: the chosen cell's EV is 97% non-target-driven (3.0% target-hit, rest stop/time-exit); the hold-time surface is non-monotonic in an overfitting-shaped way ($26.40→$8.46→$31.60 across nearby holds); the cell is an unvalidated winner's-curse max of a 35-cell grid; and — most importantly — **the 53.6% "catch rate" is likely a matching artifact**, since "caught" only required a signal firing sometime in the prior 24h, not that a position was still open and correctly-directioned when the move actually started, and the base strategy already stops out 67-89% of the time. `OPEN_DECISION climactic_acceleration_reversal_phase2_needed_20260910` tracked the corrected next step: one robustness script (median P&L, day-clustered bootstrap, a real half-split AT the chosen cell, plus the still-needed p80-p95 threshold sweep) on the existing population — no new detector, no wiring. **RESOLVED NEGATIVE, same day**: that script found the day-blocked bootstrap 95% CI on the chosen cell is [-$27.88, $76.57] (does not exclude zero — fails the strict standard) and the top 5 trades alone account for 107.1% of total EV (the other 127 net negative) — confirming the winner's-curse concern directly. Combined with the already-confirmed RTH negative, this closes the idea across both sessions (`RESEARCH_CLAIM climactic_accel_reversal_globex_resolved_negative_20260910`). No detector was ever built/wired — nothing to pull. The "adds" extension (`docs/CLIMACTIC_ACCEL_REVERSAL_ADDS_SCOPE.md`) is now moot.

Read this entry and the backing `pivotConfluenceAnalysis.js`/`swingPivots.js` file headers before extending or re-testing anything in this space — several plausible-sounding ideas (break-then-retest-of-the-break, a fixed touch-count factor, a composite quintile score, denial-streak length) were tried and explicitly ruled out already.
- Day-type per-setup edge (SIZE_UP/SUPPRESS flags): `performance_audit` rows with `signal_type='DAY_TYPE_ALPHA'`; computed by `scripts/backtest_day_type_alpha.js` Sunday 9:10 PM; read in acd.js as `liveStats._dta[setup_type-day_type]`. **Real gap found 2026-09-13, fix designed and dry-run-validated, NOT yet wired**: the recommendation logic gates on a win-rate-vs-blended-average z-score only (never EV) and reads BLENDED `sample_size`/`ev_per_trade` (never the `real_n`/`real_ev` it already computes and stores in `notes`) — the same blended-vs-real gap the 2026-07-28/08-12 IB-specific fixes closed, never extended roster-wide. A corrected mechanism (breakeven-WR test against `OPTIMAL_STOP`-derived economic breakeven, co-primary with a day-blocked-bootstrap real-EV-vs-$-5-floor test) was designed with a DeepSeek (deepseek-v4-pro) critique and dry-run tested against real data — see `RESEARCH_CLAIM daytype_alpha_wr_only_gate_blind_to_ev_20260913` for the full account, including a retracted flagship example (`IB_LOW_FADE_LONG-TREND` turned out to be 100% synthetic BACKFILL, zero real trades) and the more important systemic finding that most real per-day-type cells roster-wide are day-clustered per `computeRigor()` even at N=50-170 — a data-maturity ceiling on day-type conditioning generally, not a bug in this one script. `OPEN_DECISION daytype_alpha_realdata_breakeven_fix_pending_review` tracks the remaining work (DeepSeek code review of the actual diff, an explicit decision on whether to extend `computeRigor()`'s clustering check into a hard gate here — a new deliberate exception alongside `SETUP_STATUS_DOW`, not a silent third instance) before this touches live `sizeMultiplier`.
- **Live-roster backtest stability/trend** (does the currently-firing setup roster's backtested edge hold up chronologically): computed weekly by `backtest_setup_status.mjs`'s `computeRigor()`/`classifyTrend()`, stored in `SETUP_STATUS`'s `notes.rigor`. Aggregate view: `GET /api/acd/rigor-stability-coverage` + `RigorStabilityPanel` in `AlphaEngineOverview.jsx`.
- Nightly latency audit results: `performance_audit` rows with `signal_type='LATENCY_AUDIT'`; alerts in `scratch/gemini_alerts.txt`
- Daily order-flow (CVD — cumulative volume delta, overnight Globex + RTH split): `performance_audit` rows with `signal_type='CVD_DAILY'`; computed by `scripts/backfill_cvd_daily_history.mjs` (`symbol='NQ'` filtered). Overnight CVD extremity has no directional predictive power for the next RTH session; a weak, real range-elevation effect exists. Not wired into anything live.
- Globex/overnight large-move-vs-known-level proximity: `performance_audit` rows with `signal_type='GLOBEX_LEVEL_PROX'` (also `RESEARCH_CLAIM globex_large_moves_start_near_pit_safe_levels`); computed by `scripts/backtest_globex_move_levels.mjs`. Modest, real effect confirmed via a 5,000-draw permutation test (a single random-draw placebo was insufficient — always permutation-test a "near a level" claim). Not wired into anything live.
- **Pipeline freshness/consumption report**: `node scripts/audit_pipeline_freshness.mjs` — cross-references every `signal_type` in `performance_audit` against whether a writer is still scheduled and whether anything live reads it back. Static-grep based (misses parameterized `signal_type = $1` reads) — treat a flag as "worth a look," not proof. **Scheduled weekly since 2026-09-07** (`run_weekly_backtests.sh`) and persists a `PIPELINE_FRESHNESS_AUDIT` row (`notes` JSON has the full flagged list); the session-start hook prints just the flagged/total count each session, not the full list (grep-based, ~half the roster flags by design — informational-only `RESEARCH_CLAIM` signals correctly have no live consumer). This is the tool that catches "a calibration script runs on schedule but nothing ever reads its output live" — e.g. it's what flags `SAME_TYPE_REFIRE_GATE_CALIB` (`OPEN_DECISION same_type_refire_gate_live_wiring_pending`).
- **Execution-efficiency audit** (realized outcome vs a trade's own MAE/MFE, MFE left on the table for winners, standing sanity check on `bar6_checkpoint`): `scripts/analyze_execution_efficiency.mjs`, weekly, persists `signal_type='EXECUTION_EFFICIENCY_AUDIT'` rows (currently just `IB_BEARISH`, self-expands as real N grows elsewhere). See `RESEARCH_CLAIM ib_bearish_mfe_left_on_table_20260727`.
- Known bugs / tech debt: [docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md)
- Pending decisions, unconfirmed proposals, stale-stats findings, unfinished multi-session work: [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md) — **check this at the start of any session**, especially after a context clear.

## Documentation maintenance — keep this perpetually current

This file and [ARCHITECTURE.md](ARCHITECTURE.md) are the handoff point for the next session (human or Claude) — treat letting them go stale as a real cost, the same as introducing a bug. A `Stop` hook (`.claude/settings.json`) checks at the end of every session whether structural files changed without a matching doc update and prints a reminder — if you see that reminder, act on it before ending the turn rather than dismissing it.

Concretely, whenever a session does any of the following, update the relevant doc **in that same session**, not as a follow-up:
- Add/remove a route file, service file, or DB table → update [ARCHITECTURE.md](ARCHITECTURE.md)'s route/service/table tables, and if a table was added/dropped/altered, regenerate `server/schema.sql` (command is in ARCHITECTURE.md)
- Add/remove a dashboard component → update the frontend structure table in [ARCHITECTURE.md](ARCHITECTURE.md)
- Establish a new hard rule or convention (the user corrects an approach, or confirms a non-obvious one) → add it to the **Hard rules** or **Conventions** section above
- Fix something listed in [docs/KNOWN_ISSUES.md](docs/KNOWN_ISSUES.md) → remove that entry; discover a new one → add it
- The user confirms a fix/decision but it isn't fully implemented in the same session, proposes a follow-up that isn't confirmed yet, or you find a stat/display that's stale or disconnected from its source of truth → add it to [docs/OPEN_THREADS.md](docs/OPEN_THREADS.md) **before the session ends** — don't rely on conversation memory alone to carry pending work forward. This is on you to maintain proactively; the user has delegated it rather than asking for it each time.
- `docs/` also holds point-in-time specs (`MONTE_CARLO_SPEC*.md`, `BACKTESTING_PLAYBOOK.md`) and parked design docs (`*_parked.md`) — those describe proposals, not live state, and aren't required to track the running app. Don't confuse them with ARCHITECTURE.md.
- **Any change that supersedes existing code, data, or documentation must clean up what it replaces in the same session** — don't leave stale duplicate descriptions, dead constants/variables, orphaned scratch/temp files, or superseded DB rows sitting alongside the replacement. Concretely: rewriting a script's methodology → grep for every other doc that describes the old methodology (not just the primary one) and update all of them, and remove now-unused constants/params from the code itself; reverting a bad live change → delete the temp files used to make and revert it; a finding that invalidates an old proposal → replace/remove the old note, don't just append a new one next to it (OPEN_THREADS.md especially — resolved items get struck through with a "Resolved <date>" note per its own convention, not left dangling as if still open; see that file's own header — the older "delete when fixed" wording was changed 2026-07-14). Restated 2026-07-13 after a session left stale/duplicate descriptions in ARCHITECTURE.md and HARDCODED_CONSTANTS.md that had to be caught separately after the code change was already done.

There is exactly one canonical architecture doc — `/ARCHITECTURE.md` at the repo root. If you're ever tempted to write a second one (e.g. inside `docs/`), update the existing one instead; a prior session already made that mistake once (`docs/ARCHITECTURE_2026-06-07_superseded.md`) and it silently went stale because nothing pointed back to it.
